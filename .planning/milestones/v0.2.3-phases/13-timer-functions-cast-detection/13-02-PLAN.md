---
phase: 13-timer-functions-cast-detection
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - BuffEngine.lua
autonomous: false
requirements: [CDM-01, CDM-02]

must_haves:
  truths:
    - "The CDM tab Suggested section renders three entries: Lust, Trinket, Damage Pot — no Lua errors"
    - "Right-click > Add to Bars on the Trinket suggested entry creates ns.db.trackedBuffs['trinket'] with section='bars'"
    - "Right-click > Add to Buffs on the Pot suggested entry creates ns.db.trackedBuffs['pot'] with section='buffs'"
    - "In-game casting of at least 2 trinket spellIDs and 2 pot spellIDs produces correctly timed bars/icons with the proper spell name as label"
    - "Shared-slot overwrite behaves correctly under real cast sequencing — cast trinket A then B, only B's timer remains"
    - "All tested spell IDs in TRINKET_SPELLS/POT_SPELLS match actual on-use ability IDs (debug log confirms UNIT_SPELLCAST_SUCCEEDED fires with those IDs)"
    - "Shipping BuffEngine.lua contains no temporary debug instrumentation after Task 3"
  artifacts:
    - path: "BuffEngine.lua"
      provides: "Temporary debug log during verification; clean production file after Task 3"
      contains: "debugLogging"
  key_links:
    - from: "CDMTab Suggested section render"
      to: "ns.db.trackedBuffs['trinket'|'pot']"
      via: "right-click context menu Add to Bars/Buffs"
      pattern: "ns.db.trackedBuffs\\[key\\]"
    - from: "in-game cast event"
      to: "visible countdown bar/icon"
      via: "UNIT_SPELLCAST_SUCCEEDED -> OnSpellCastSucceeded -> activeTimers -> UpdateDisplay"
      pattern: "metaSlot"
---

<objective>
Verify Phase 13 end-to-end in-game: CDM-01 (trinket/pot entries render in Suggested section), CDM-02 (copy-on-drag creates the DB entry), and the spell-ID accuracy pitfall flagged in research (PITFALLS.md — on-use ability ID vs proc buff ID can differ for the 13 spellIDs sourced from CSVs).

Purpose: Guard against shipping a timer that never fires because the spellID in the table doesn't match the cast event's spellID. User verifies with real trinkets/pots equipped.
Output: Human-verified green light, any spell-ID corrections applied, debug log removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-timer-functions-cast-detection/13-CONTEXT.md
@.planning/phases/13-timer-functions-cast-detection/13-01-SUMMARY.md
@.planning/research/PITFALLS.md
@BuffEngine.lua
@CDMTab.lua
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add temporary cast-ID debug log to OnSpellCastSucceeded</name>
  <files>BuffEngine.lua</files>
  <read_first>
    Read BuffEngine.lua lines 1-10 (ns.debugLogging flag) and OnSpellCastSucceeded after Plan 01 edits.
  </read_first>
  <action>
    Add a single debug-logged print at the very top of `ns:OnSpellCastSucceeded(spellID)` (before the trinket fan-out) that prints the cast spellID and its name when `ns.debugLogging` is true.

    Insert at the start of the function:

    ```lua
    if ns.debugLogging then
        local info = C_Spell.GetSpellInfo(spellID)
        local name = (info and info.name) or "?"
        print("|cff00ccffTBT Debug|r: cast " .. tostring(spellID) .. " (" .. name .. ")")
    end
    ```

    Also temporarily enable the flag at load. Immediately after the existing `ns.debugLogging = false` line (around line 5), add a comment-flagged override:

    ```lua
    ns.debugLogging = true -- TEMP Phase 13 verification, remove in Task 3
    ```

    This avoids needing an in-game slash command to toggle.

    Run stylua: `~/.cargo/bin/stylua BuffEngine.lua`
    Deploy: `./scripts/install.bat`
  </action>
  <acceptance_criteria>
    - grep finds the line `"TBT Debug|r: cast "` in BuffEngine.lua
    - grep finds the comment `TEMP Phase 13`
    - stylua check passes
    - install.bat exits 0
  </acceptance_criteria>
  <verify>
    <automated>~/.cargo/bin/stylua --check BuffEngine.lua &amp;&amp; grep -q 'TBT Debug|r: cast ' BuffEngine.lua &amp;&amp; grep -q 'TEMP Phase 13' BuffEngine.lua &amp;&amp; cmd.exe //c scripts\\install.bat</automated>
  </verify>
  <done>Temporary debug log deployed; ready for in-game verification in Task 2.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-game verification of CDM-01, CDM-02, and tested spell IDs</name>
  <files>BuffEngine.lua</files>
  <action>
    USER PERFORMS THIS TASK IN-GAME. Claude cannot automate WoW client interaction.
  </action>
  <what-built>
    - Phase 13 Plan 01: OnSpellCastSucceeded fan-out for trinket/pot spellIDs with shared-slot overwrite via metaSlot
    - Phase 13 Plan 02 Task 1: Temporary debug log printing every cast's spellID + name, debug flag forced on
    - Phase 12 already shipped: Trinket + Pot entries in CDM Suggested section
  </what-built>
  <how-to-verify>
    1. Launch WoW retail. Log in on a character that can equip at least one tracked trinket and carry at least one tracked damage pot. `/reload` if already logged in.

    2. **CDM-01 verification:** Type `/tbt`. Open the TBT CDM tab. Confirm the Suggested section shows THREE entries:
       - Lust / Heroism (existing)
       - Trinket (question-mark icon placeholder — Phase 14 will wire real icon)
       - Damage Pot (question-mark icon placeholder)
       Check chat for Lua errors during render.

    3. **CDM-02 verification:** Right-click the Trinket suggested entry -> "Add to Bars". Then type `/dump TerribleBuffTrackerDB.trackedBuffs["trinket"]` and confirm the result contains `key="trinket"`, `section="bars"`, `layoutOrder=<number>`, `metaBuff=true`, `label="Trinket"`, `duration=0`.

       Repeat for Pot -> "Add to Buffs". Confirm `/dump TerribleBuffTrackerDB.trackedBuffs["pot"]` returns a valid table with `section="buffs"`.

    4. **TMR-01 / TMR-02 spell-ID accuracy (CRITICAL pitfall check):** For each tracked trinket and pot you can use (aim for at least 2 trinkets + 2 pots):
       - Equip/carry the item
       - Use it in-game
       - Observe chat: should print `TBT Debug: cast <spellID> (<name>)`
       - Confirm the spellID printed matches one of the keys in BuffEngine.lua TRINKET_SPELLS or POT_SPELLS
       - Confirm a timer bar/icon appears with correct duration (e.g., 20s for Algeth'ar Puzzle Box spellID 383781, 30s for Light's Potential spellID 1236616, 12s for Void-Shrouded Tincture spellID 1236551)
       - Note any mismatches: "Item X cast event shows spellID Y, table has spellID Z"

    5. **TMR-03 shared-slot overwrite:** If possible, cast trinket A, then cast trinket B before A's timer expires. Confirm only B's timer is visible afterward. If cooldowns prevent this live test, dump activeTimers to inspect state: `/dump ns` is not accessible — instead use `/reload` to reset state and cast twice in sequence if cooldowns allow. Document "not tested live" if unavoidable.

    6. **TMR-04 aura-scan compat:** After a trinket cast that grants a visible player aura, let the aura drop naturally or remove it. Confirm the timer disappears when the aura drops (via existing ScanActiveTimersForCancellation path).

    7. **Report:**
       (a) "approved — all tested spell IDs match, CDM Suggested renders 3 entries, copy-on-drag works"
       (b) OR a list of corrections: "TRINKET_SPELLS[X] should be Y — cast event printed Y when using item Z"
       Also report which spell IDs were untested (for future follow-up).
  </how-to-verify>
  <verify>
    <automated>MISSING — human verification required; no automated substitute for in-game cast behavior</automated>
  </verify>
  <done>User reports approval or provides a list of spell-ID corrections. Task 3 applies any corrections and strips the debug instrumentation.</done>
  <resume-signal>Type "approved" to close the phase, or paste the list of spell ID corrections / issues.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Remove temporary debug log and apply any spellID corrections</name>
  <files>BuffEngine.lua</files>
  <read_first>
    Read the Task 2 verification report from the user. Read BuffEngine.lua TRINKET_SPELLS and POT_SPELLS blocks (lines 52-84).
  </read_first>
  <action>
    1. If the user reported spell-ID mismatches, update the affected entries in `TRINKET_SPELLS` or `POT_SPELLS` by replacing the wrong spellID key with the correct one from the cast event. Preserve the `{ duration, itemID }` value. The derived `TRINKET_ITEM_IDS` / `POT_ITEM_IDS` sets rebuild automatically since they iterate the parent tables at load.

    2. Remove the temporary debug log block added in Task 1 (the `if ns.debugLogging then ... "cast " .. tostring(spellID) ...` block at the top of OnSpellCastSucceeded).

    3. Remove the TEMP override line `ns.debugLogging = true -- TEMP Phase 13 verification, remove in Task 3`. Leave the original `ns.debugLogging = false` line intact.

    4. Run stylua: `~/.cargo/bin/stylua BuffEngine.lua`

    5. Deploy: `./scripts/install.bat`
  </action>
  <acceptance_criteria>
    - grep for `'TBT Debug|r: cast '` returns 0 matches (debug log removed)
    - grep for `TEMP Phase 13` returns 0 matches (override removed)
    - grep for `ns.debugLogging = false` still returns at least 1 match (original flag preserved)
    - stylua check passes
    - install.bat exits 0
    - Any spell-ID corrections from verification are reflected in TRINKET_SPELLS/POT_SPELLS
  </acceptance_criteria>
  <verify>
    <automated>~/.cargo/bin/stylua --check BuffEngine.lua &amp;&amp; ! grep -q 'TBT Debug|r: cast ' BuffEngine.lua &amp;&amp; ! grep -q 'TEMP Phase 13' BuffEngine.lua &amp;&amp; grep -q 'ns.debugLogging = false' BuffEngine.lua</automated>
  </verify>
  <done>Phase 13 production code is clean. Any user-reported spell ID corrections have been applied. TMR-01 through TMR-04, CDM-01, and CDM-02 are all green.</done>
</task>

</tasks>

<verification>
- CDM Suggested section shows Lust + Trinket + Damage Pot (CDM-01).
- Copy-on-drag creates correct `ns.db.trackedBuffs["trinket"]` / `["pot"]` entry (CDM-02).
- At least 4 tracked spellIDs (2 trinket + 2 pot) cast in-game produce correct timers (TMR-01, TMR-02).
- Shared-slot overwrite observed OR logically confirmed (TMR-03).
- Aura-scan cancellation still fires for meta-timers when aura drops (TMR-04).
- Debug log removed from shipping code.
- stylua clean.
</verification>

<success_criteria>
- Phase 13 end-to-end: casting any usable tracked trinket or pot shows a running timer with correct duration and spell name label.
- CDM-01 and CDM-02 verified live.
- All tested spell IDs match real cast events; mismatches corrected.
- No debug instrumentation in shipping code.
</success_criteria>

<output>
After completion, create `.planning/phases/13-timer-functions-cast-detection/13-02-SUMMARY.md` listing:
- Spell IDs tested in-game
- Corrections applied to TRINKET_SPELLS/POT_SPELLS (if any)
- Untested spell IDs (future follow-up)
</output>
