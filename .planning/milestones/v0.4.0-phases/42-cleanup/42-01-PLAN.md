---
phase: 42-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .pkgmeta
  - Providers.lua
autonomous: true
requirements: []
must_haves:
  truths:
    - "A released zip contains only the addon's runtime files — no .planning, no .github, no .gitattributes"
    - "Every top-level tracked path is either a runtime file or named in .pkgmeta's ignore list"
    - "No namespace export added by this milestone is left with zero readers"
  artifacts:
    - path: ".pkgmeta"
      provides: "Packager ignore list covering every non-runtime top-level path"
      contains: ".planning"
    - path: "Providers.lua"
      provides: "Provider mixins with no zero-reader milestone exports"
  key_links:
    - from: ".pkgmeta"
      to: "BigWigs Packager"
      via: "ignore list consumed at package time"
      pattern: "^ignore:"
---

<objective>
Close the two findings that affect something outside this repository's source tidiness: what a
player actually downloads, and one dead namespace export this milestone added.

This plan lands first because every task in it is zero-risk and unambiguous — the `.pkgmeta` change
touches no Lua at all, and the Providers.lua change is a single-line deletion with grep evidence.

Purpose: Success criterion 4 (release tooling reviewed) and criterion 3 (grep-gated dead-code
sweep). The `.pkgmeta` half is the highest-value item in the whole phase: it is the only finding
that changes shipped output rather than source readability.

Output: a corrected `.pkgmeta` ignore list and one deleted export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/42-cleanup/42-CONTEXT.md
@.pkgmeta
</context>

<constraints>
Binding for every task in this plan:

- **NO BEHAVIOUR CHANGES.** There are no automated tests and nothing can be exercised in game until
  Phase 43. Every change must be justifiable as behaviour-preserving BY READING alone.
- **NEVER touch `CHANGELOG.md`.** `CLAUDE.md` forbids it outside an explicit user request.
- **Scope fence.** Only what this milestone (Phases 31-42) created or rewrote is in scope.
- **Do not create, rename or switch a git branch.** Work on the branch already checked out.
- After any Lua edit, run `stylua` from the repo root with **no flags** (the repo-root
  `stylua.toml` pins `line_endings = "Windows"`), then confirm `git diff` shows no
  line-ending-only churn.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Stop shipping the planning directory to players</name>
  <files>.pkgmeta</files>
  <action>
Add three entries to the `ignore:` list in `.pkgmeta`: `.planning`, `.github` and `.gitattributes`.

Why this matters: `.pkgmeta`'s ignore list currently holds only `.gitignore`, `.pkgmeta`,
`CHANGELOG.md`, `CLAUDE.md`, `README.md`, `LICENSE`, `scripts`, `tools`, `*.png`,
`RELEASE_NOTES.md` and `stylua.toml`. BigWigs Packager ships everything else in the repository, so
every release zip currently carries 334 tracked files of roadmaps, requirements, phase plans,
contexts and verification reports straight into players' AddOns folders, plus the CI workflow
directory and the git attributes file. This is in scope: Phase 32 wrote this `.pkgmeta` and Phase
33 was the tooling pass — both belong to this milestone.

Do NOT add an entry for `Assets/`. It holds three README screenshots which the existing `*.png`
glob already covers; a directory entry would be redundant and would drift if a non-png asset is
ever added there deliberately.

Do NOT reorder or reformat the existing entries, and do not touch `package-as:` or
`manual-changelog:`. Append the three new entries to the end of the list so the diff is three
added lines and nothing else.

The release tooling (`scripts/install.bat`, `scripts/release.bat`,
`.github/workflows/release.yml`) has already been read end to end and needs no change — criterion
4's "read end to end" clause is satisfied by that read plus this fix. Do not re-read them.
  </action>
  <verify>
    <automated>grep -c '^  - \.planning$' .pkgmeta; grep -c '^  - \.github$' .pkgmeta; grep -c '^  - \.gitattributes$' .pkgmeta</automated>
Each of the three greps must print `1`.

Then run the coverage read, which is the real check:

    git ls-files | sed 's|/.*||' | sort -u

Read the resulting list against `.pkgmeta`'s `ignore:` block by eye and confirm every entry is
either (a) a runtime file the addon needs at load — the six `.lua` files, `CDMTab.xml`,
`TerribleBuffTracker.toc`, `tbt_icon_64x64.blp` — or (b) named in the ignore list, directly or via
the `*.png` glob. `Assets` is covered by `*.png`.

This fix CANNOT be verified locally. BigWigs Packager only runs in CI against a real tag, so the
zip's contents are not observable from the working tree. Real confirmation belongs to the first
release and is already tracked in `STATE.md`'s "two things to watch at that first release" — do not
invent a local packaging run to fake it.
  </verify>
  <done>`.pkgmeta`'s ignore list names `.planning`, `.github` and `.gitattributes`; the top-level coverage read leaves no tracked path unaccounted for; no other line in the file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the zero-reader RacialProviderMixin export</name>
  <files>Providers.lua</files>
  <action>
Delete the line `ns.RacialProviderMixin = RacialProviderMixin` from `Providers.lua` (around line
906, immediately above `local RacialProvider = CreateFromMixins(SpellProviderBaseMixin,
RacialProviderMixin)`).

The mixin table itself and the `CreateFromMixins` call below it STAY. Only the namespace export
goes: nothing outside the file's own `CreateFromMixins` call ever reads `ns.RacialProviderMixin`,
so the export is dead. It was added by Phase 41 (RACE-02/RACE-03), which puts it inside the
milestone fence.

If the deleted line carried a trailing or leading comment describing the export, delete that with
it; if the comment describes the mixin rather than the export, leave it attached to the mixin.

**Do NOT touch the other nine unreferenced `ns.*` exports in this file** —
`ns.SpellProviderBaseMixin`, `ns.TrinketProviderMixin`, `ns.PotProviderMixin`,
`ns.LustProviderMixin`, `ns.UserSpellProviderMixin`, `ns.TRINKET_SPELLS`, `ns.POT_SPELLS`,
`ns.TRINKET_ITEM_IDS`, `ns.POT_ITEM_IDS`. Every one of them predates v0.4.0 (Phases 17-22), so
`PROJECT.md`'s "No refactors during cleanup phases" protects them. They are recorded as an
out-of-scope finding in this phase's hot-path/duplication audit (plan 42-06), not deleted here.
  </action>
  <verify>
    <automated>test "$(cat *.lua *.xml | grep -c 'ns[.:]RacialProviderMixin\b')" -eq 0 && test "$(grep -c 'RacialProviderMixin' Providers.lua)" -ge 4 && stylua --check .</automated>
The first clause proves the export has no readers left anywhere in the addon; the second proves
the mixin table and its methods survived the deletion; `stylua --check .` must exit 0.

Then confirm the diff is exactly one deleted line (plus at most an attached comment):

    git diff --stat Providers.lua

and that `git diff Providers.lua` contains no line whose only change is its line ending.
  </verify>
  <done>`ns.RacialProviderMixin` appears nowhere in the addon; `RacialProviderMixin` still exists as a local table with its methods and is still passed to `CreateFromMixins`; `stylua --check .` exits 0; the nine pre-v0.4.0 exports are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → released zip | Everything on the addon side of this boundary is distributed to every player who installs TBT. The `.pkgmeta` ignore list is the only thing that decides what crosses it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-01 | Information Disclosure | `.pkgmeta` ignore list | mitigate | Task 1 adds `.planning`, `.github` and `.gitattributes`, stopping 334 internal planning files (roadmaps, verification reports, unshipped decisions) from being distributed to players. |
| T-42-02 | Tampering | npm/pip/cargo installs | accept | This phase installs no packages of any kind. No package-manager command appears in any task; the only tool invoked is `stylua`, already on the machine. |
</threat_model>

<verification>
1. `stylua --check .` exits 0 from the repo root.
2. `git diff` shows only the intended lines — three added to `.pkgmeta`, one removed from
   `Providers.lua`. No whitespace-only or line-ending-only hunks.
3. `git ls-files | sed 's|/.*||' | sort -u` has every entry accounted for by the ignore list or by
   the addon's runtime file set.
4. `CHANGELOG.md` is untouched: `git diff --name-only` must not list it.
</verification>

<success_criteria>
- `.pkgmeta` ignores `.planning`, `.github` and `.gitattributes`.
- `ns.RacialProviderMixin` has zero occurrences in the addon.
- `stylua --check .` clean, no line-ending churn.
- Two atomic commits, one per task.
</success_criteria>

<commits>
One commit per task, imperative subject under ~72 characters, body in prose explaining WHY.

Task 1 subject: `build: stop shipping the planning directory in the release zip`
Body should say what was crossing the boundary and why it was invisible — the ignore list was
written before `.planning` had grown, nothing local reveals the zip's contents, and the first real
tag push is where it would have surfaced.

Task 2 subject: `refactor(42): drop the unread RacialProviderMixin namespace export`
Body should note that the export had exactly one writer and no readers, that the mixin itself is
unchanged, and that the nine older exports beside it are left alone on purpose.

Both commits end with:

    Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
    Claude-Session: https://claude.ai/code/session_01UBnJrcKyhWYjBUQ8LxffBd
</commits>

<output>
Create `.planning/phases/42-cleanup/42-01-SUMMARY.md` when done.
</output>
