---
phase: 35.1-config-panel
verified: 2026-09-21T00:00:00Z
status: passed
closed_at_milestone: v0.4.0 (2026-09-23)
closure_evidence:
  - ".planning/testing/43-FOREVER-E2E-PASS.md"
  - ".planning/testing/44-RETAIL-PASS.md"
closure_note: >-
  Closed in bulk at milestone close, not item by item. The human_verification list below was
  written when this phase's code landed and records what still needed a live client at that
  moment. Those observations were carried out in Phase 43 (Forever beta, build 1.60.1.69913,
  continuous play-testing 2026-09-21 to 22) and Phase 44 (retail, Mythic+ and a raid encounter,
  2026-09-23). The user signed both off as a whole -- "everything on Forever is tested and
  acceptable" and "no lua errors so far" -- rather than ticking each row, so read the list below
  as covered by those two passes collectively, not as individually attested. It is kept intact
  because it is the best record of what this phase could not prove statically.
score: 9/9 must-haves verified (code-level); 6 items require in-game confirmation
overrides_applied: 0
human_verification:
  - test: "/tbt → check the gear square next to the + in Suggested"
    expected: "A grey cog square sits immediately right of the green + in the Suggested section; hovering shows the 'TBT Settings' tooltip; holding the mouse down shifts the cog 1px down-right and releasing restores it. The icon renders as a recognisable gear, not a blank square."
    why_human: "Rendered appearance and hover/pressed state feedback cannot be confirmed by static reading — this is exactly the atlas-vs-file-texture risk the plan called out."
  - test: "Collapse a section, scroll the tracker list, then click the gear"
    expected: "The tracker list is replaced in place by the config page (title, Back button, CDM Steal Mode checkbox) on the same tab page. The CDM window does not resize or move, the TBT tab stays lit, and the tab stays anchored under the bottom-most Blizzard tab."
    why_human: "Frame show/hide swap and layout result are only observable when GridLayoutFrame/anchors are actually resolved in a running client."
  - test: "On the config page, confirm no blank section appears below the checkbox"
    expected: "Only the title, Back button, and CDM Steal Mode checkbox+description are visible; the Phase 36 seam (ns.tbtConfigContainerSection) stays hidden and produces no visible gap or empty box."
    why_human: "Hidden-frame layout correctness needs the actual rendered panel."
  - test: "Click Back"
    expected: "The tracker list returns with the same section collapsed and at the same scroll offset as when the gear was clicked."
    why_human: "Scroll-restore timing (C_Timer.After nested behind the OnShow hook's own deferred refresh) is a runtime race that can only be observed live."
  - test: "Tick CDM Steal Mode, click Back, /reload, /tbt, click gear"
    expected: "The checkbox is still ticked, the page is intact, and the list behind it still renders. (This tests panel rebuild only — not SavedVariables persistence, which /reload cannot exercise; that belongs to Phase 44's retail logout→login pass.)"
    why_human: "Panel rebuild after ADDON_LOADED re-fires can only be observed with an actual /reload."
  - test: "Open the tab, swap both directions, and /reload with the config page open"
    expected: "No Lua error at any point, including with `/console scriptErrors 1` enabled."
    why_human: "Runtime error absence cannot be proven by static analysis alone."
---

# Phase 35.1: Config Panel Verification Report

**Phase Goal:** TBT gains a home for addon-wide settings on its own CDM tab, without opening a second window.
**Verified:** 2026-09-21
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | CFG-01: A gear square sits beside the `+`, built from the shared chrome helper, in the Suggested grid, without shifting/breaking the `+` | VERIFIED | `CDMTab.lua:972-992` defines `CreateActionSquare(parent, r, g, b, a)`; both `addSquare` (`:1015`) and `gearSquare` (`:1046`) are built from it (`grep -c 'CreateActionSquare'` = 3: definition + 2 call sites). `SUGGESTED_RESERVED_SLOTS = 2` (`:21`) is the single source for the gear's `layoutIndex` (`:1048`) and `RefreshTBTSections`'s catalog seed (`:727`, `local suggestedSlot = SUGGESTED_RESERVED_SLOTS`; the old `= 1` form is gone). `addSquare.layoutIndex = 1` is unchanged. |
| 2 | CFG-02/CFG-04: page swap is two children of the same tab page, no second window/tab | VERIFIED | `ns.tbtConfigPanel` (`TBTConfigPanel`, `CDMTab.lua:902`) is parented to `CooldownViewerSettings` with the identical two `SetPoint` calls and frame level as `ns.tbtPanel` — a sibling frame, not a new window. `ns:ShowTBTConfigPage`/`ns:ShowTBTTrackerList` (`:1311-1334`) toggle `Hide()`/`Show()` on the two siblings. No `CreateFrame(... , "Button", ...)` template registers a new CDM tab; `TBTSettingsTab` (the one tab) is untouched by this phase's diff. |
| 3 | CFG-02/CFG-04: `ns:ShowTBTTrackerList` has no render logic of its own; redraw comes from the existing `OnShow` hook | VERIFIED | `ns:ShowTBTTrackerList` (`CDMTab.lua:1322-1334`) contains `Hide`/`Show`/`SetFrameLevel`/`C_Timer.After` only — no call to `RefreshTBTSections`, `BuildAllSections`, or `BuildTBTSection`. `grep -c 'ns:RefreshTBTSections()'` = 12, unchanged from pre-phase baseline (11 call sites + definition), proving no new call was added. The redraw is `ns.tbtPanel:Show()` (`:1325`) firing the pre-existing `OnShow` hook (`:1288-1296`). |
| 4 | CFG-02/CFG-04: scroll restore is deferred and clamped | VERIFIED | `SetVerticalScroll` appears exactly once (`:1332`), inside `C_Timer.After(0, function() ... end)` (`:1331-1333`), clamped via `math.min(savedListScroll, ns.tbtPanel:GetVerticalScrollRange())`. |
| 5 | CFG-03: `ns.db.stealMode` seeded false-by-default, no schema bump | VERIFIED | `Core.lua:89-91`: `if ns.db.stealMode == nil then ns.db.stealMode = false end`, placed after the `tbtVisible` default and before `containerSettings`, matching the `tbtVisible` idiom exactly. `BuffEngine.lua:79` still reads `CURRENT_SCHEMA_VERSION = 4`; `BuffEngine.lua` does not appear in this phase's diff. |
| 6 | CFG-03: steal mode is persisted but has zero behavioural readers | VERIFIED | `grep -ln 'stealMode' *.lua` returns exactly `CDMTab.lua` and `Core.lua`. All 5 occurrences are: the `Core.lua` seed (comment + nil-check + assignment), the checkbox `OnClick` write (`CDMTab.lua:946`, `ns.db.stealMode = checked`), and the `SetChecked` re-sync (`CDMTab.lua:1316`). No third file, no conditional branch anywhere reads it to change addon behaviour. |
| 7 | Scope fence: no container create/delete, debug toggle, reset-all-positions, global scale/opacity, or per-container settings were added; Phase 36 seam exists, hidden, empty, nothing anchored below it | VERIFIED | `grep -n 'CreateContainer\|DeleteContainer\|RemoveContainer' *.lua` returns nothing. No debug-toggle/reset-position/scale-opacity control exists in `CreateConfigPanel`'s body (read in full, `CDMTab.lua:901-967`) — the only controls are the title, Back button, and steal checkbox. `ns.tbtConfigContainerSection` (`:961-964`) is created, `Hide()`d, marked with a `-- SEAM: Phase 36 (CONT-04/CONT-05)` comment (`:957`), and is the last statement before `return panel` (`:967`) — nothing anchors below it. |
| 8 | Taint discipline: no `parentArray`, no field written on a CDM frame, no CDM mixin call, no `C_CooldownViewer.SetLayoutData` | VERIFIED | `grep -n 'parentArray' CDMTab.lua CDMTab.xml` returns only a pre-existing comment (`CDMTab.xml:6`) describing the rule, not a usage. `grep -n 'SetLayoutData' CDMTab.lua Core.lua` returns nothing. Every `CooldownViewerSettings.` hit in `CDMTab.lua` is either a comment, a read (`.CooldownScroll`, `.GroupBuffFilter`, `.displayMode`, `.ShowUIPanel`), or `Hide()`/`SetShown()` on those two children — confirmed via `git diff 4c27c5f~1..6e25a4c` that none of those lines were touched by this phase (the diff shows zero `CooldownViewerSettings.` hits changed, only the unrelated `GroupBuffFilter:Hide()` line shifted by line-number, unchanged in content). `CooldownViewerSettings:GetFrameLevel()` (a plain widget getter) appears 6 times, up from the pre-existing baseline of 2 — accepted per verification instructions as a pre-existing pattern, not a new taint risk. |
| 9 | No flavour checks; gear uses file texture, not an atlas | VERIFIED | `grep -n 'GetBuildInfo\|WOW_PROJECT\|tocversion\|IsTestBuild' CDMTab.lua Core.lua` returns nothing. `gearSquare.Icon:SetTexture("Interface\\ICONS\\Trade_Engineering")` (`:1047`) — a file texture; `addSquare.Icon:SetAtlas(...)` is the only `SetAtlas` call touching either action square and it belongs to the pre-existing `+`, not the gear. |

**Score:** 9/9 truths verified at the code level.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `CDMTab.lua` | `CreateActionSquare`, gear square, `TBTConfigPanel`, `ns:ShowTBTConfigPage`/`ns:ShowTBTTrackerList` | VERIFIED | All present and substantive — read in full, not stubs (see truths 1-4, 7-9). |
| `CDMTab.lua` | `TBTStealModeCheckbox` and `ns.tbtConfigContainerSection` seam | VERIFIED | `:928` (`CreateFrame("CheckButton", "TBTStealModeCheckbox", ...)`), `:961` (seam). Both wired (truths 5-7). |
| `Core.lua` | `ns.db.stealMode` default, sibling of `tbtVisible` | VERIFIED | `:85-91` (truth 5). |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| gear square `OnMouseUp` | `ns:ShowTBTConfigPage` | direct call, `LeftButton` + `upInside` | WIRED | `CDMTab.lua:1065-1071` |
| `ns:ShowTBTTrackerList` | `ns:RefreshTBTSections` | `ns.tbtPanel:Show()` firing the existing `OnShow` hook | WIRED | `CDMTab.lua:1325` → `:1288-1296` |
| `TBTStealModeCheckbox OnClick` | `ns.db.stealMode` | direct boolean write, no consumer | WIRED | `CDMTab.lua:944-952`; confirmed inert (truth 6) |
| `ns:ShowTBTConfigPage` | `ns.tbtStealCheckbox` | `SetChecked(ns.db.stealMode == true)` on every show | WIRED | `CDMTab.lua:1316` |

### Data-Flow Trace (Level 4)

Not applicable in the strict sense — the config page renders a checkbox reflecting a persisted boolean flag, not a dynamic dataset. The relevant trace (checkbox state ← `ns.db.stealMode` ← `Core.lua` seed / checkbox write) is already covered by the key-link table above; there is no separate fetch/query layer to trace.

### Behavioral Spot-Checks

SKIPPED (no runnable entry points). This is a WoW addon with no headless harness, test runner, or CLI entry point. This is the project's permanent condition, not a phase-specific gap.

### Probe Execution

No probes declared or found. `find scripts -path '*/tests/probe-*.sh' -type f` returns nothing, and neither PLAN nor SUMMARY file in this phase references a probe script. Section not applicable.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|--------------|------------|-------------|--------|----------|
| CFG-01 | 35.1-01 | Gear/settings square next to `+` | SATISFIED | Truth 1 |
| CFG-02 | 35.1-01 | Clicking it turns the tab page into a config panel | SATISFIED (code-level); visual swap and Lua-error-free behaviour remain human checks | Truths 2-4 |
| CFG-03 | 35.1-02 | Config panel holds the steal-mode toggle | SATISFIED (code-level); `/reload` panel-rebuild and retail persistence remain human/Phase-44 checks | Truths 5-6 |
| CFG-04 | 35.1-01 | User can return from the config panel to the tracker list | SATISFIED (code-level); scroll/collapse-state fidelity is a human check | Truths 2-4 |

No orphaned requirements — `.planning/REQUIREMENTS.md` maps only CFG-01..04 to Phase 35.1, and all four appear in one of the two plans' `requirements:` frontmatter.

### Anti-Patterns Found

None. `TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER` (case-insensitive) and "coming soon"/"not yet implemented"/"not available" greps against `CDMTab.lua`, `Core.lua`, `CDMTab.xml` return only one pre-existing, unrelated hit (`CDMTab.lua:1212`, `-- placeholder until OnShow fires`, a comment on the pre-existing scroll-child width line, not touched by this phase's diff and not describing incomplete work in the phase's own code). No empty-implementation stubs, no hardcoded-empty render props, no console.log-only handlers.

`stylua --check .` exits 0 from the repo root with no output. `git diff 4c27c5f~1..6e25a4c --name-only` lists only `CDMTab.lua` and `Core.lua`. `CHANGELOG.md` last touched by an unrelated v0.3.0 commit (`9cdfc1d`), zero lines changed across the four phase commits.

### Human Verification Required

See YAML frontmatter `human_verification` for the full list (6 items — gear rendering/hover/pressed state, the page swap in place, the hidden seam producing no visible gap, Back restoring scroll/collapse state, the checkbox surviving a panel rebuild via `/reload`, and the absence of any Lua error across all transitions). All six were already flagged as not-performed by both SUMMARY.md files, which explicitly deferred them to Phase 42's Forever end-to-end pass rather than blocking this autonomous run — consistent with this project's `Implement, do not probe` preference and Phase 35's own precedent.

### Gaps Summary

No code-level gaps found. Every truth, artifact, and key link traced from the roadmap goal backward to the shipped code holds up under direct reading: the gear square shares the `+`'s exact chrome helper and did not shift the `+`'s slot or the catalog's start slot; the page swap is strictly two sibling frames on the same tab page with no new window/tab; the return path reuses the existing `OnShow` redraw with zero new `RefreshTBTSections` calls; the scroll restore is deferred and clamped; `ns.db.stealMode` is seeded exactly like `tbtVisible`, with no schema bump, and has exactly three write/read sites — all inert or UI-sync, zero behavioural readers; the scope fence held (no container CRUD, no debug/reset/scale controls); the Phase 36 seam is hidden, empty, and terminal in the content stack; taint discipline holds (no `parentArray` usage, no CDM frame field writes, no CDM mixin calls, no `SetLayoutData`); the gear deliberately uses a file texture instead of an atlas; no flavour-check was introduced; and `stylua --check .` is clean with `CHANGELOG.md` untouched.

The only open items are the six in-game observations both SUMMARY.md files already flagged as un-performed, deliberately deferred to Phase 42's Forever end-to-end pass per this phase's own plan text ("fold the six observations into Phase 42's ... pass ... and mark this phase code complete rather than blocking the run"). None of these are contradicted by the code; they are simply unobservable outside a running client.

---

*Verified: 2026-09-21*
*Verifier: Claude (gsd-verifier)*
