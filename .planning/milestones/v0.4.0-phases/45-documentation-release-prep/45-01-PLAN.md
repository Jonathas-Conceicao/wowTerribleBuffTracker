---
phase: 45-documentation-release-prep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
autonomous: true
requirements: [DOC-02, DOC-03, DOC-05]

must_haves:
  truths:
    - "A reader of README.md learns TBT is ONE download that runs on both Midnight retail and the WoW Forever beta"
    - "README.md names both interface versions, 120100 and 16001, and they agree with TerribleBuffTracker.toc"
    - "README.md describes cooldown trackers, the four built-in containers, user containers, Merge Mode, Centered growth and the Options > AddOns settings panel"
    - "A Forever user reading README.md learns that settings not persisting is a beta-client bug, not a TBT bug"
    - "README.md states the Trinket and Damage Potion meta-trackers are kept, and makes no claim about what Blizzard's Cooldown Manager tracks natively"
    - "README.md names gnome, troll and orc racial trackers -- not Eureka! alone"
    - "The Showcase, AI Usage and License sections are byte-identical to their pre-plan state"
  artifacts:
    - path: "README.md"
      provides: "The public product copy, and the single source the user pastes into CurseForge and Wago by hand"
      contains: "16001"
  key_links:
    - from: "README.md"
      to: "TerribleBuffTracker.toc"
      via: "the interface version list must agree"
      pattern: "120100"
    - from: "README.md"
      to: ".planning/testing/43-FOREVER-E2E-PASS.md"
      via: "the Forever SavedVariables caveat is the client bug recorded there"
      pattern: "Forever"
---

<objective>
Rewrite `README.md` so it describes the addon v0.4.0 actually ships, not the v0.2-era addon the
current text describes. Features, Usage and `## Lust / Heroism Tracking` are fully rewritten (D-05);
Known Issues is reduced to the exactly three entries the user approved for public copy (D-10, D-11);
the intro gains the one-download / two-interface-versions statement (D-07).

`README.md` is also **the single source for the CurseForge and Wago descriptions** (D-08). The user
pastes its Markdown into both sites by hand. So DOC-03 is closed by the README itself carrying the
Forever SavedVariables caveat -- **no store-description file is created by this plan, and no store
copy is handed over in chat.**

`## Showcase`, `## AI Usage` and `## License` are NOT touched (D-06). The Showcase images are
knowingly stale and the user has chosen not to block the release on new captures.

Purpose: closes DOC-02, DOC-03 and DOC-05, and makes the public copy stop repeating PROJECT.md's
stale "Eureka! alone" racial claim (D-12).
Output: a rewritten `README.md`, committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/45-documentation-release-prep/45-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the intro and the Features section</name>
  <files>README.md</files>
  <read_first>
    - `README.md` -- the current text, in full. Lines 1-18 are the intro, 20-28 `## AI Usage`,
      30-34 `## Showcase`, 36-46 `## Features`.
    - `TerribleBuffTracker.toc` -- read-only. Line 1 is `## Interface: 120100, 16001`. The README
      must agree with it exactly.
    - `.planning/testing/43-FOREVER-E2E-PASS.md` -- the "Features exercised" table is the list of
      what is confirmed working on Forever.
    - `.planning/testing/44-RETAIL-PASS.md` -- the Merge Mode rework section and the
      "Still on a fallback path" list.
    - `.planning/phases/45-documentation-release-prep/45-CONTEXT.md` -- D-05, D-06, D-07, D-12 and
      the "Claude's Discretion" block on comparative claims.
  </read_first>
  <action>
Rewrite the intro paragraphs (everything between the `# TerribleBuffTracker` heading and the
`## AI Usage` heading) and replace the whole `## Features` bullet list.

Do NOT modify `## AI Usage`, `## Showcase` or `## License` -- not one character (D-06).

**Intro must say, in the user's plain register:**
- TBT tracks buff and cooldown timers manually, because `COMBAT_LOG_EVENT_UNFILTERED` is disabled in
  Midnight and aura data is largely Secret Values. It uses `UNIT_SPELLCAST_SUCCEEDED` plus known
  durations instead.
- It configures through a tab inside Blizzard's Cooldown Manager settings window, and its containers
  are positioned with Edit Mode, so it sits inside the game's own UI rather than beside it.
- **One download, two clients (D-07, DOC-02):** the same download runs on Midnight retail
  (interface `120100`) and on the WoW Forever beta (interface `16001`). Say explicitly that there
  is no separate retail build and no separate Forever build. Write both five-digit numbers
  literally.

**DOC-05 / no comparative claims.** The current first paragraph says TBT tracks timers "that
Blizzard's Cooldown Manager doesn't support". **Delete that claim and write nothing equivalent.**
Describe what TBT does and stop. The finished README must contain none of the strings
`doesn't support`, `does not support`, `natively`, `already tracks`, `unlike the Cooldown Manager`.
Referring to the Cooldown Manager as a thing TBT integrates with, or whose entries Merge Mode draws,
is fine -- what is banned is any statement about what it does or does not track.

**Replace `## Features` with a bullet list covering, at minimum:**
- Buff trackers -- any spell by ID with a duration you type; bar or icon display
- Cooldown trackers -- track a spell's cooldown as well as its buff; icon-only, with charge counts
  where the client reports them. State plainly that a cooldown tracker counts down the duration you
  typed from the moment of the cast, so haste and cooldown reduction are not reflected
- The same spell can be tracked as a buff AND as a cooldown at once, as two separate trackers
- Four built-in containers: **Tracked Bars**, **Tracked Buffs**, **Essential Cooldowns**,
  **Utility Cooldowns** (these are the exact titles in the addon)
- User-created containers -- made as a Buffs container or a Cooldowns container, chosen at creation
  and fixed afterwards; Cooldowns containers are icon-only; each carries its own settings
- **Merge Mode** -- optional and all-or-nothing: Blizzard's Cooldown Manager containers move off
  screen and their entries are drawn inside TBT's matching containers alongside your own trackers.
  Note that a merged buff icon appears and disappears correctly but draws no cooldown sweep
- Edit Mode integration -- every container drags, and clicking one opens a settings popup
  (icon size, padding, bar width, opacity, items per row, growth direction, visibility)
- Centered growth on buff icon containers -- the row re-centres on its anchor as buffs come and go
- A settings panel under **Options > AddOns**, opened by `/tbt` -- Merge Mode switch, container
  list, and a button through to the Cooldown Manager
- The CDM settings tab, with **Buffs** and **Cooldowns** tabs, drag-and-drop between sections, and
  right-click quick actions
- Meta-trackers: **Lust / Heroism**, **Trinket** and **Damage Potion**. DOC-05 requires the README
  to state these are KEPT -- say they remain as they are, with their own durations and bar display
- Racial trackers (D-12) -- **gnome (Eureka!), troll (Berserking) and orc (Blood Fury and a second
  orc racial)**, plus racial cooldown tiles on the Cooldowns tab. Other races show a
  "not supported yet" tile. State that these were verified on WoW Forever. Do NOT write that
  v0.4.0 ships Eureka! alone -- that claim is stale
- Rank grouping -- on WoW Forever, a "cover all ranks" checkbox makes one tracker cover every rank
  of a spell. Say it is a Forever-only control
- Aura-based timer cancellation, where the client lets the aura be read
- Mousing over a spell or aura shows its spell ID

Keep bullets one or two lines each. The user will rewrite the wording in their own voice, so
optimise for being easy to prune, not for polish.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c '120100' README.md && grep -c '16001' README.md && grep -c 'Essential Cooldowns' README.md && grep -c 'Merge Mode' README.md && grep -c 'Berserking' README.md && grep -c 'Blood Fury' README.md && grep -c 'Eureka' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '120100' README.md` is at least 1
    - `grep -c '16001' README.md` is at least 1
    - `grep -c 'Essential Cooldowns' README.md` is at least 1
    - `grep -c 'Utility Cooldowns' README.md` is at least 1
    - `grep -c 'Merge Mode' README.md` is at least 1
    - `grep -c 'Options > AddOns' README.md` is at least 1
    - `grep -c 'Berserking' README.md` is at least 1 and `grep -c 'Blood Fury' README.md` is at least 1
    - `grep -ci "doesn't support" README.md` is 0
    - `grep -ci 'does not support' README.md` is 0
    - `grep -ci 'natively' README.md` is 0
    - `grep -ci 'already tracks' README.md` is 0
    - `grep -c 'Eureka! only' README.md` is 0 and `grep -c 'Eureka! alone' README.md` is 0
    - `git diff README.md` shows ZERO changed lines inside the `## AI Usage`, `## Showcase` and
      `## License` sections. Check with
      `git diff -U0 README.md | grep -c 'Assets/'` equals 0 and
      `git diff -U0 README.md | grep -ci 'WTFPL'` equals 0 and
      `git diff -U0 README.md | grep -ci 'Claude AI'` equals 0
    - `grep -c '^## AI Usage' README.md`, `grep -c '^## Showcase' README.md` and
      `grep -c '^## License' README.md` each equal 1
  </acceptance_criteria>
  <done>The intro states one download covering interface 120100 and 16001, Features describes the v0.4.0 addon including cooldown trackers, the four named containers, user containers, Merge Mode, Centered growth, the Options > AddOns panel and the three racial races, and no sentence anywhere claims what Blizzard's Cooldown Manager tracks.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the Usage and Lust / Heroism Tracking sections</name>
  <files>README.md</files>
  <read_first>
    - `README.md` as left by Task 1 -- specifically `## Usage` and `## Lust / Heroism Tracking`.
    - `.planning/testing/43-FOREVER-E2E-PASS.md` -- "Reduced add dialog (no container, no type
      field)", "Duration parsing `30s` / `2m`", "Settings panel under Options > AddOns, `/tbt`",
      "Merge-mode slide switch, container list scroll, logo".
    - `.planning/phases/45-documentation-release-prep/45-CONTEXT.md` -- D-05 and the Specific Ideas
      block on store-page portability.
  </read_first>
  <action>
Replace the whole `## Usage` bullet list and rewrite the body of `## Lust / Heroism Tracking`.
Keep both section headings exactly as they are -- the `## Lust / Heroism Tracking` heading text is
unchanged, only its body is rewritten.

**`## Usage` must cover:**
- `/tbt` opens TBT's settings panel under **Options > AddOns**
- The Cooldown Manager settings window carries a TBT tab, itself split into **Buffs** and
  **Cooldowns** tabs
- Adding a tracker: the `+` square in the **Suggested** section. The dialog's type follows the
  active tab, and its title says which ("Add Buff Tracker" / "Add Cooldown Tracker"). Enter a spell
  ID and a duration written as `30s` or `2m`. A new tracker lands in **Not Displayed**, and you drag
  it to where you want it
- Organising: drag a tracker between any container's section, **Not Displayed** and **Suggested**;
  reorder within a section by dragging to a position. Dragging out of Suggested copies rather than
  moves
- Containers: create and delete your own from the settings panel. Buffs or Cooldowns is chosen at
  creation and cannot be changed afterwards
- **Merge Mode**: the switch in the settings panel. It is all-or-nothing across all four categories
- Edit Mode: click a TBT container to open its settings popup; **Copy Blizzard CDM Config** imports
  the Cooldown Manager's current settings into TBT in one click
- Right-click any tracker icon for Move, Hide or Remove
- One sentence of plain-language caution: a custom cooldown tracker counts down the duration you
  typed, from the cast, so a reset or a haste proc will not shorten it. Entries drawn by Merge Mode
  use the game's own cooldown instead

**`## Lust / Heroism Tracking` body** keeps its mechanism explanation, which is still accurate, and
is corrected only where the UI has moved on:
- Detection is via the Sated-family debuffs (Sated, Exhaustion, Temporal Displacement, Evoker
  Exhaustion, and Fatigued for Primal Rage / Harrier's Cry), which stay readable in combat and M+
- Drag the "Lust / Heroism" tile out of **Suggested** into whichever container you want it in -- do
  NOT keep the old "into Tracked Bars or Tracked Buffs" phrasing as the only option, since there are
  now four built-in containers plus any the user made
- A 40-second timer starts automatically when any lust goes out, cast by you, a party member or a
  drum
- The timer's icon and name match the lust actually used; the tile in the CDM tab shows your
  class-specific lust (Time Warp for mages, Fury of the Aspects for evokers, Bloodlust otherwise)
- The timer cancels early when the lust buff is readably gone -- which is not every context

**Store-page portability (D-08).** The user pastes these sections into CurseForge and Wago by hand.
Use no repo-relative links and nothing that only renders on GitHub in `## Features`, `## Usage`,
`## Lust / Heroism Tracking` or `## Known Issues and Limitations`. Plain Markdown headings, bullets,
bold, inline code. Absolute `https://` links are acceptable if one is genuinely needed. The
`## Showcase` images keep their `Assets/` paths and are not touched.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && awk '/^## Features/,/^## License/' README.md | grep -c ']([^h]' ; echo "exit=$? (grep exit 1 with printed 0 is the pass)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## Usage' README.md` equals 1 and `grep -c '^## Lust / Heroism Tracking' README.md` equals 1
    - `grep -c 'Not Displayed' README.md` is at least 1
    - `grep -c 'Suggested' README.md` is at least 1
    - `grep -c '30s' README.md` is at least 1 and `grep -c '2m' README.md` is at least 1
    - `grep -c 'Copy Blizzard CDM Config' README.md` is at least 1
    - `grep -c 'Sated' README.md` is at least 1
    - `awk '/^## Features/,/^## License/' README.md | grep -c ']([^h]'` prints `0` -- no
      repo-relative Markdown link exists anywhere between the Features heading and the License
      heading. (`grep -c` exits 1 when it prints 0; that non-zero exit is the pass condition.)
    - `git diff -U0 README.md | grep -c 'Assets/'` equals 0 -- the Showcase image lines are untouched
  </acceptance_criteria>
  <done>Usage describes the `/tbt` settings panel, the two-tab CDM tab, the tab-driven add dialog with `30s`/`2m` durations, section dragging, container creation, the Merge Mode switch, Edit Mode and right-click actions; the Lust section no longer names only two containers; and no section a store page would carry holds a repo-relative link.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce Known Issues to the three approved public entries</name>
  <files>README.md</files>
  <read_first>
    - `README.md` as left by Task 2 -- `## Known Issues and Limitations`, currently three bullets:
      the Forever SavedVariables bug, "Only Active Buffs can be tracked", and "Aura cancellation
      delay in restricted contexts".
    - `.planning/phases/45-documentation-release-prep/45-CONTEXT.md` -- **D-10 and D-11 are the
      authoritative list. Do not widen it and do not narrow it.**
    - `.planning/testing/44-RETAIL-PASS.md` -- the "Predicted, NOT bugs" table gives the real
      mechanism behind entries 2 and 3.
    - `.planning/testing/43-FOREVER-E2E-PASS.md` -- "Criterion 4 -- settled: Forever writes, but
      never reads back" is the evidence behind entry 1.
  </read_first>
  <action>
Rewrite `## Known Issues and Limitations` so it holds **exactly three** top-level bullets, in this
order:

1. **Settings do not persist between sessions on WoW Forever beta.** Keep the existing paragraph's
   substance and tone essentially as it stands -- it is already framed correctly as a beta-client
   bug with nothing to do from the addon side, and DOC-03 depends on a Forever user reading it that
   way rather than filing it against TBT. Light editing is fine; do not weaken the "this is the
   client, not the addon, and retail is unaffected" framing.
2. **In restricted content, a buff can keep showing until its typed duration runs out.** In Mythic+
   and other restricted contexts the client hides aura data, so TBT's cancellation scan is switched
   off there. Timers still start correctly -- a buff that falls off early simply stays on screen
   until its own timer runs down. This REPLACES the existing "Aura cancellation delay in restricted
   contexts" bullet, which describes an older, milder version of the same thing.
3. **Charge counts can be blank in restricted content.** The client will not report the number, so
   the icon shows no count rather than a wrong one.

**Delete the "Only Active Buffs can be tracked" bullet.** D-10 fixes the public list at exactly the
three above; what TBT does track is now covered in Features.

**Do not add anything from D-11.** Merged debuffs needing a hostile target, a potion cooldown not
resolving inside a key, and an equipped item's cooldown going unreadable under restriction are all
real and all recorded in the run sheets, but the user has ruled them out of the public copy -- they
read as noise to a player and would not prompt a bug report.

Use the same bullet shape as the existing section (a bold-ish lead line, then the explanation) so
the three entries look uniform.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && awk '/^## Known Issues and Limitations/,/^## License/' README.md | grep -c '^- '</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/^## Known Issues and Limitations/,/^## License/' README.md | grep -c '^- '` equals
      exactly `3`
    - `grep -c 'Only Active Buffs' README.md` equals 0
    - The Forever entry survives: `grep -ci 'forever beta' README.md` is at least 1 and
      `grep -ci 'retail is unaffected' README.md` equals 1
    - `grep -ci 'charge' README.md` is at least 1 -- entry 3 exists
    - None of D-11's excluded items appear: `grep -ci 'hostile target' README.md` equals 0,
      `grep -ci 'equipped item' README.md` equals 0
    - No `.lua` file was modified by this plan:
      `git status --porcelain -- '*.lua'` produces no output
    - `git status --porcelain` lists `README.md` and nothing else
  </acceptance_criteria>
  <done>The Known Issues section carries exactly three bullets -- Forever SavedVariables, restricted-content buff overhang, blank charge counts -- the "Only Active Buffs" bullet is gone, nothing from D-11's excluded list appears, and README.md is the only modified file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public (GitHub, CurseForge, Wago) | `README.md` is pasted verbatim onto two public store pages. Anything in it is published. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Information Disclosure | `README.md` published to CurseForge/Wago | mitigate | Copy is written from `.planning/testing/*-PASS.md` only. No account IDs, no SavedVariables paths, no local filesystem paths, no `X-Curse-Project-ID`/`X-Wago-ID` values are carried into the README. Task 3's acceptance criteria assert `README.md` is the only modified file. |
| T-45-02 | Repudiation | stale public claims about Blizzard's CDM | mitigate | DOC-05 / D-05: all comparative claims are removed and grep-asserted absent, so no published statement about the CDM can go stale or need re-verification against a live client. |

No package-manager installs occur in this phase, so no package legitimacy gate applies.
</threat_model>

<verification>
- `grep -c '120100' README.md` and `grep -c '16001' README.md` both at least 1, matching
  `TerribleBuffTracker.toc` line 1 exactly.
- `awk '/^## Known Issues and Limitations/,/^## License/' README.md | grep -c '^- '` equals 3.
- `git diff -U0 README.md | grep -c 'Assets/'` equals 0 and
  `git diff -U0 README.md | grep -ci 'WTFPL'` equals 0 -- Showcase and License untouched.
- `git status --porcelain -- '*.lua'` empty -- no addon behaviour changed.
- `git ls-files --eol README.md` still reports `i/lf w/crlf` (per STATE.md's warning that grep and
  awk silently strip CR in this repo, line endings are checked with git, never with grep).
</verification>

<success_criteria>
- ROADMAP Phase 45 criterion 1 is TRUE: `README.md` describes TBT as one download running on both
  Midnight retail and WoW Forever, and names 120100 and 16001.
- ROADMAP Phase 45 criterion 2 is TRUE by way of D-08: `README.md` is the store copy, and it carries
  the Forever SavedVariables caveat framed as a client bug.
- ROADMAP Phase 45 criterion 4 is half TRUE: the README states the Trinket and Damage Potion
  meta-trackers are kept, and asserts nothing about what Blizzard's CDM tracks natively.
- DOC-02, DOC-03 and DOC-05's README half are closed.
- No store-description file was created; no `.lua` file was modified; `.pkgmeta` is untouched (D-09).
</success_criteria>

<output>
Create `.planning/phases/45-documentation-release-prep/45-01-SUMMARY.md` when done.
</output>
