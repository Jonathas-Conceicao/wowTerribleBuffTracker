---
phase: 45-documentation-release-prep
plan: 02
type: execute
wave: 2
depends_on: ["45-01"]
files_modified:
  - CHANGELOG.md
  - .planning/PROJECT.md
autonomous: true
requirements: [DOC-04, DOC-05]

must_haves:
  truths:
    - "CHANGELOG.md carries a v0.4.0 entry titled exactly '## v0.4.0 — Cooldown Tracking and Full CDM View', sitting above the v0.3.0 entry"
    - "Every pre-existing line of CHANGELOG.md is byte-identical afterwards -- the diff contains zero deletions"
    - "The v0.4.0 entry uses the same '### New Features' / '### Fixes' / '### Known Issues' heading trio every prior entry uses"
    - "The v0.4.0 entry's public known-issues list is the same three items the README carries"
    - "PROJECT.md no longer claims v0.4.0 ships Eureka! alone"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "The v0.4.0 release entry, appended above v0.3.0"
      contains: "## v0.4.0"
    - path: ".planning/PROJECT.md"
      provides: "A corrected racial statement covering gnome, troll and orc"
      contains: "Berserking"
  key_links:
    - from: "CHANGELOG.md"
      to: ".planning/ROADMAP.md"
      via: "the v0.4.0 entry title matches the milestone name so public and planning docs agree"
      pattern: "Cooldown Tracking and Full CDM View"
    - from: "CHANGELOG.md"
      to: "README.md"
      via: "the three Known Issues bullets are the same three in both files"
      pattern: "Forever"
---

<objective>
Append the v0.4.0 entry to `CHANGELOG.md`, and correct `PROJECT.md`'s stale racial statement (D-12).

**This plan performs the single highest-risk operation in the milestone.** `CLAUDE.md` forbids
Claude from touching `CHANGELOG.md`, because on 2026-09-19 a whole v0.3.0 section was replaced by
script and the user's hand-written work was destroyed. **D-01 is an explicit, informed override
covering this ONE entry only** -- the user was shown that this is the exact operation the rule was
written about and granted it anyway, on the understanding that they review and rewrite the text
afterwards in their own voice. The rule stands untouched for everything else.

**D-02 still binds absolutely:** append-only. The new section goes above `## v0.3.0` and every
pre-existing line stays byte-identical. Task 1 opens with a pre-flight integrity check and **stops
and asks the user** if the file is not exactly as expected -- a shorter or reworded existing entry
is the user's deliberate edit, never a regression to fix.

Nothing is added to `.pkgmeta` (D-09) -- this plan creates no new tracked file.

Purpose: closes DOC-04 and the CHANGELOG half of DOC-05; clears the stale claim D-12 flags.
Output: a v0.4.0 CHANGELOG entry and a corrected PROJECT.md, both committed -- before the release
tag is cut, never after (`scripts/release.bat` tags and pushes; GitHub Actions packages).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/45-documentation-release-prep/45-CONTEXT.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight the CHANGELOG, then append the v0.4.0 entry above v0.3.0</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - `CLAUDE.md` -- the Workflow section's CHANGELOG rule IN FULL, including the 2026-09-19
      violation it records. Read it before editing, not after.
    - `CHANGELOG.md` -- the whole file. Note the shape of the `## v0.3.0 — WoW Forever Support`
      entry: a one-line summary paragraph, then `### New Features`, `### Fixes`, `### Known Issues`.
      That trio is consistent across every prior entry and is what the new entry must follow.
    - `README.md` as rewritten by plan 45-01 -- the Features list is the canonical naming for
      everything shipped, and the Known Issues section is the canonical public three. The CHANGELOG
      must agree with it, not invent parallel wording.
    - `.planning/testing/43-FOREVER-E2E-PASS.md` -- "Features exercised" and "Defects found and
      fixed during the pass" are the raw material for New Features and Fixes.
    - `.planning/testing/44-RETAIL-PASS.md` -- the Merge Mode rework, and the "Predicted, NOT bugs"
      table behind the two restricted-content known issues.
    - `.planning/ROADMAP.md` -- the milestone name `v0.4.0 Cooldown Tracking and Full CDM View`,
      which D-04 requires the entry title to match.
    - `.planning/phases/45-documentation-release-prep/45-CONTEXT.md` -- D-01, D-02, D-03, D-04,
      D-10, D-11.
  </read_first>
  <action>
**Step 1 -- pre-flight, before any edit (D-02).** Confirm all four:
- `git status --porcelain CHANGELOG.md` produces no output (no uncommitted changes)
- `wc -l < CHANGELOG.md` is `109`
- Line 1 is `# Changelog` and line 3 is `## v0.3.0 — WoW Forever Support`
- `git ls-files --eol CHANGELOG.md` reports `i/lf w/crlf`

**If ANY of these differ, STOP.** Do not edit, do not "fix", do not reconcile against this plan.
Report the difference to the user and wait. The file differing from expectation means the user
edited it, and their version is authoritative. This is the exact failure mode `CLAUDE.md` records.

**Step 2 -- insert.** Insert the new section so it begins on line 3, pushing the existing
`## v0.3.0` heading down. Insert only; change no existing line, including whitespace. Do not use a
script that rewrites the file wholesale -- edit in place.

**Title, exactly (D-04):** `## v0.4.0 — Cooldown Tracking and Full CDM View`
The dash is an em dash (U+2014), matching every other entry heading in the file.

**Shape (D-03):** a one-sentence summary line, then `### New Features`, `### Fixes`,
`### Known Issues`, in that order. Roughly 10-15 short bullets in total across the three -- aim for
12-15 and keep each bullet to one line. Only what a player would notice. The user will prune
further, so draft for prunability rather than polish.

**Summary line:** cooldown tracking arrives, and TBT can now draw Blizzard's Cooldown Manager
entries inside its own containers. Mention that the one download covers both Midnight retail and the
WoW Forever beta.

**`### New Features`** -- pick roughly eight from, in this priority order:
- Cooldown trackers -- track a spell's cooldown as well as its buff, shown as an icon with a charge
  count where the client reports one
- The same spell can be tracked as a buff and as a cooldown at the same time
- Two new built-in containers, **Essential Cooldowns** and **Utility Cooldowns**, beside Tracked
  Bars and Tracked Buffs
- Create your own containers, as Buffs or Cooldowns, each with its own settings
- **Merge Mode** -- draw the Cooldown Manager's entries inside TBT's containers alongside your own
  trackers
- Buff icon containers can grow from the centre, re-centring as buffs come and go
- A settings panel under **Options > AddOns**, opened by `/tbt`
- Racial trackers for **gnome, troll and orc**, plus racial cooldown tiles (D-12 -- do NOT write
  "Eureka! only"; that claim is stale)
- **Cover all ranks** on WoW Forever -- one tracker covers every rank of a spell
- Durations can be typed as `30s` or `2m`

**`### Fixes`** -- pick roughly four from:
- A custom cooldown tracker keeps the duration you typed instead of being overwritten by the game's
- Tracking the same spell as both a buff and a cooldown no longer replaces one with the other
- Preview timers no longer paint over a running cooldown, and no longer outlive the settings window
- Merged buffs no longer drift out of position inside a container
- `/tbt` no longer throws an error when opening settings
- The Cooldown Manager button opens the Cooldown Manager instead of Advanced Options

**`### Known Issues`** -- exactly the three the README carries (D-10). Nothing from D-11:
- Settings still do not persist between sessions on the WoW Forever beta -- a bug in the beta
  client, not in this addon; retail is unaffected
- In restricted content such as Mythic+, a buff can keep showing until its typed duration runs out,
  because the aura cancellation scan is switched off there
- Charge counts can be blank in restricted content

**No comparative claims about Blizzard's Cooldown Manager (DOC-05).** Describe what TBT does. Never
state what the CDM does or does not track natively. `doesn't support`, `does not support`,
`natively` and `already tracks` must not appear.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && git diff --numstat -- CHANGELOG.md && git diff -- CHANGELOG.md | grep -c '^-[^-]'</automated>
  </verify>
  <acceptance_criteria>
    - **Zero deletions.** `git diff --numstat -- CHANGELOG.md` prints a line whose SECOND field
      (deletions) is `0`. A modified line would appear as a delete plus an add, so a zero here is
      proof that no pre-existing line changed in any way.
    - `git diff -- CHANGELOG.md | grep -c '^-[^-]'` prints `0` -- the same assertion from the raw
      diff, excluding the `--- a/CHANGELOG.md` header line.
    - `grep -c '^## v0.3.0 — WoW Forever Support' CHANGELOG.md` equals 1
    - `grep -c '^## v0.2.6' CHANGELOG.md`, `grep -c '^## v0.2.0' CHANGELOG.md` and
      `grep -c '^## v0.1.0' CHANGELOG.md` each equal 1
    - `grep -n '^## v0.4.0 — Cooldown Tracking and Full CDM View' CHANGELOG.md` reports line `3`
    - `grep -c '^## v0.4.0' CHANGELOG.md` equals 1
    - The heading trio is present and in order:
      `awk '/^## v0.4.0/,/^## v0.3.0/' CHANGELOG.md | grep '^### '` prints exactly, in order,
      `### New Features`, `### Fixes`, `### Known Issues`
    - Bullet budget (D-03):
      `awk '/^## v0.4.0/,/^## v0.3.0/' CHANGELOG.md | grep -c '^- '` is between `12` and `15`
      inclusive
    - `awk '/^## v0.4.0/,/^## v0.3.0/' CHANGELOG.md | grep -ci 'Berserking\|orc\|troll\|gnome'` is
      at least 1, and `grep -c 'Eureka! only' CHANGELOG.md` equals 0
    - `grep -ci "doesn't support" CHANGELOG.md` equals 0, `grep -ci 'does not support' CHANGELOG.md`
      equals 0, `grep -ci 'natively' CHANGELOG.md` equals 0
    - `awk '/^## v0.4.0/,/^## v0.3.0/' CHANGELOG.md | grep -ci 'hostile target'` equals 0 and
      `... | grep -ci 'equipped item'` equals 0 -- nothing from D-11's excluded list leaked in
    - `git ls-files --eol CHANGELOG.md` still reports `i/lf w/crlf` -- unchanged, and checked with
      git rather than grep, because grep and awk silently strip CR in this repo
    - `git status --porcelain -- '*.lua'` produces no output
  </acceptance_criteria>
  <done>`CHANGELOG.md` opens with `# Changelog`, then the v0.4.0 entry at line 3 carrying the standard heading trio and 12-15 bullets, then the untouched v0.3.0 entry; `git diff --numstat` reports zero deletions, proving append-only.</done>
</task>

<task type="auto">
  <name>Task 2: Correct PROJECT.md's stale racial statement</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - `.planning/PROJECT.md` -- three places carry the stale claim:
      lines ~40-45 in the Current Milestone block ("Racial meta-tracker (Eureka! only) + rank
      grouping" / "This milestone implements Eureka! only"), lines ~73-75 under
      "Deferred Past v0.4.0" ("v0.4.0 ships Eureka! alone; every other racial is present but marked
      *not yet supported*"), and the Key Decisions table row "Racial meta-tracker ships with
      Eureka! only".
    - `.planning/phases/45-documentation-release-prep/45-CONTEXT.md` -- **D-12**, which is the
      authority for this correction.
    - `.planning/STATE.md` -- "Last activity: 2026-09-23 — Phase 44, plus a second racial tracker
      and racial cooldown tiles (Forever only, untested by user decision)".
    - `Providers.lua` around the `RACIAL_SPELLS` table -- READ ONLY, for the facts. It maps
      raceID 2 (Orc) to two racials, Blood Fury `20572` and `1299026`; raceID 7 (Gnome) to Eureka!
      `1259817`; raceID 8 (Troll) to Berserking `20554`. Two racial slots exist, `racial` and
      `racial2`. Do not modify this or any other `.lua` file.
  </read_first>
  <action>
Correct all three locations so PROJECT.md states what actually shipped as of 2026-09-23 (D-12):
**gnome (Eureka!), troll (Berserking) and orc (Blood Fury plus a second orc racial), verified on
WoW Forever**, tracked through two racial slots, plus **racial cooldown tiles on the Cooldowns tab**
which create ordinary `cd:<spellID>` trackers. Every other race still resolves to a
"not yet supported" tile.

1. **Current Milestone block.** Change the bullet heading `Racial meta-tracker (Eureka! only)` and
   the sentence `This milestone implements Eureka! only` to name the three races and the two racial
   slots, and to mention the racial cooldown tiles. Keep the surrounding facts that are still true:
   each racial is an explicitly-specified special case supplied by the user, the cast-driven stack
   consumption belongs to Eureka! specifically, and the rank-grouping half of the bullet is
   unchanged.
2. **Deferred Past v0.4.0.** Restate `v0.4.0 ships Eureka! alone; every other racial is present but
   marked *not yet supported*` as: v0.4.0 ships gnome, troll and orc; the follow-up milestone
   (`RACE-06` / `RACE-07`) fills in **the remaining races**. The item stays deferred -- only its
   description is corrected.
3. **Key Decisions table row.** PROJECT.md's Key Decisions are a historical record, so do NOT
   rewrite the Decision or Rationale cells -- the 2026-09-20 decision genuinely was Eureka!-only.
   Amend the **Status** cell instead, appending a note along the lines of
   `Widened 2026-09-23: troll (Berserking) and orc (Blood Fury + a second racial) shipped as well,
   verified on Forever; racial cooldown tiles added. The next minor milestone still completes the
   catalogue.` Leave the row's other cells alone.

Make no other change to PROJECT.md. Do not touch `.lua`, `.toc`, `.pkgmeta` or any script.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'Eureka! only\|Eureka! alone' .planning/PROJECT.md ; grep -c 'Berserking' .planning/PROJECT.md && grep -c 'Blood Fury' .planning/PROJECT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Eureka! only' .planning/PROJECT.md` equals 0 everywhere EXCEPT the Key Decisions
      table's Decision cell, which is a historical record and is deliberately preserved. Assert the
      narrow form: `grep -c 'implements Eureka! only' .planning/PROJECT.md` equals 0 and
      `grep -c 'Eureka! alone' .planning/PROJECT.md` equals 0
    - `grep -c 'Berserking' .planning/PROJECT.md` is at least 1
    - `grep -c 'Blood Fury' .planning/PROJECT.md` is at least 1
    - `grep -ci 'racial cooldown tile' .planning/PROJECT.md` is at least 1
    - `grep -c 'Widened 2026-09-23' .planning/PROJECT.md` equals 1
    - `grep -c 'RACE-06' .planning/PROJECT.md` is at least 1 -- the deferred follow-up survives
    - `git diff --numstat -- .planning/PROJECT.md` shows fewer than 15 changed lines in total
      (additions + deletions) -- this is a targeted correction, not a rewrite
    - `git status --porcelain -- '*.lua' '*.toc' '.pkgmeta'` produces no output
    - `git status --porcelain` lists only `CHANGELOG.md` and `.planning/PROJECT.md`
  </acceptance_criteria>
  <done>PROJECT.md states v0.4.0 ships gnome, troll and orc racials plus racial cooldown tiles, the deferred catalogue item refers to the remaining races, the Key Decisions row's history is preserved with a dated Status amendment, and no source file was touched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude -> `CHANGELOG.md` | A file `CLAUDE.md` normally forbids Claude to touch, opened by a one-entry exception. The user's prior hand-written content crosses this boundary and must survive it intact. |
| repo -> public release notes | The CHANGELOG ships with the release and is read by players. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-03 | Tampering | `CHANGELOG.md` pre-existing entries | mitigate | Pre-flight asserts 109 lines, clean git status, and `## v0.3.0` on line 3 before any edit; a mismatch halts the task and asks the user. Post-edit, `git diff --numstat` deletions must be `0` -- a modified line would show as delete+add, so zero deletions is proof of byte-identical pre-existing content. Wholesale-rewrite scripts are forbidden by the action text. |
| T-45-04 | Repudiation | stale public claims | mitigate | D-12's racial correction is applied to PROJECT.md and grep-asserted, and DOC-05's no-CDM-comparison rule is grep-asserted absent from the CHANGELOG. |
| T-45-05 | Information Disclosure | published release notes | accept | The entry is drafted only from `.planning/testing/*-PASS.md` and the README. No credentials, account IDs or local paths exist in that source material. |

No package-manager installs occur in this phase, so no package legitimacy gate applies.
</threat_model>

<verification>
- `git diff --numstat -- CHANGELOG.md` second field is `0`. This is the load-bearing check for
  ROADMAP criterion 3 and for D-02.
- `grep -n '^## v0.4.0 — Cooldown Tracking and Full CDM View' CHANGELOG.md` reports line 3, and the
  title matches `.planning/ROADMAP.md`'s milestone name.
- `awk '/^## v0.4.0/,/^## v0.3.0/' CHANGELOG.md | grep '^### '` prints the New Features / Fixes /
  Known Issues trio, in order.
- `grep -c 'Eureka! alone' .planning/PROJECT.md` equals 0.
- `git status --porcelain -- '*.lua'` empty -- no addon behaviour changed this phase, so `stylua` is
  not needed and must not be run as a cleanup step.
- `git ls-files --eol CHANGELOG.md .planning/PROJECT.md` both still `i/lf w/crlf`.
</verification>

<success_criteria>
- ROADMAP Phase 45 criterion 3 is TRUE: `CHANGELOG.md` gained a v0.4.0 entry by append only, and a
  diff shows every pre-existing line byte-identical.
- DOC-04 is closed. DOC-05's CHANGELOG half is closed -- the entry names the Trinket and Damage
  Potion meta-trackers as unchanged where relevant and asserts nothing about Blizzard's CDM.
- D-12's correction is applied: no planning or public document still claims v0.4.0 ships Eureka!
  alone.
- Both files are committed BEFORE any release tag is cut.
</success_criteria>

<output>
Create `.planning/phases/45-documentation-release-prep/45-02-SUMMARY.md` when done.
</output>
