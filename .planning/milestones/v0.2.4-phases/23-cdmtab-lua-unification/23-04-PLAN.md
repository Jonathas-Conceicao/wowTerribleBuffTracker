---
phase: 23-cdmtab-lua-unification
plan: 04
type: execute
wave: 4
depends_on: ["23-03"]
files_modified: []
autonomous: false
requirements: [DISP-02]
must_haves:
  truths:
    - "Addon deploys via scripts/install.bat without errors"
    - "CDM Suggested section shows 3 tiles: lust / trinket / pot with correct icons"
    - "Class-aware lust icon resolves correctly after Providers.lua demotion"
    - "Tile tooltips show: spell name + Spell ID line + TBT Duration line + description line for all 3 meta-buffs"
    - "Drag ghost icon exactly matches the dragged tile's icon"
    - "All 4 buff types (user / trinket / pot / lust) render correctly in active timer bars during preview"
    - "No Lua errors in /console scriptErrors during CDM open/close"
  artifacts: []
  key_links:
    - from: "User visual verification"
      to: "DISP-02 runtime confirmation"
      via: "CDM Settings → TBT tab inspection + drag interactions + tooltips"
      pattern: "human-verify"
---

<objective>
Deploy the Phase 23 changes to WoW via `scripts/install.bat` and perform in-game verification that DISP-02 is satisfied with zero visual regression vs Phase 22. Confirm all 5 phase success criteria pass live.

Purpose: Phase 23 is a refactor with zero intended user-visible behavioral change. Runtime verification is the final acceptance gate — static grep checks alone are insufficient for UI work per project convention (Phase 18/19/20/21/22 all used a final human-verify plan).

Output: Verification SUMMARY documenting pass/fail of each of the 5 phase success criteria. On pass → Phase 23 complete, unblocking Phase 24 cleanup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-cdmtab-lua-unification/23-CONTEXT.md
@.planning/phases/23-cdmtab-lua-unification/23-01-SUMMARY.md
@.planning/phases/23-cdmtab-lua-unification/23-02-SUMMARY.md
@.planning/phases/23-cdmtab-lua-unification/23-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deploy addon to WoW via install.bat</name>
  <read_first>
    - CLAUDE.md (deploy instruction: `./scripts/install.bat`)
    - scripts/install.bat (to confirm invocation on Windows bash)
  </read_first>
  <files>(no source edits — deploys built artifacts)</files>
  <action>
    Run `./scripts/install.bat` from the repo root to copy addon files to the WoW retail addons folder (per CLAUDE.md Install Path: `%PROGRAMFILES(x86)%\World of Warcraft\_retail_\Interface\AddOns\TerribleBuffTracker`).

    Pre-deploy sanity check — run stylua once across all modified Lua files to catch any drift:
    ```bash
    stylua --check Display.lua BuffEngine.lua CDMTab.lua Providers.lua
    ```
    If this fails, run `stylua Display.lua BuffEngine.lua CDMTab.lua Providers.lua` to fix and re-check.

    After install.bat succeeds, prompt the user to:
    1. Launch World of Warcraft (retail)
    2. Log in to any character (hunter/shaman/mage/evoker preferred for lust class-aware icon testing)
    3. Ready to begin verification in Task 2 (human-verify checkpoint)
  </action>
  <verify>
    <automated>stylua --check Display.lua BuffEngine.lua CDMTab.lua Providers.lua && ls "$PROGRAMFILES(X86)/World of Warcraft/_retail_/Interface/AddOns/TerribleBuffTracker/CDMTab.lua" 2>/dev/null || ls "/c/Program Files (x86)/World of Warcraft/_retail_/Interface/AddOns/TerribleBuffTracker/CDMTab.lua"</automated>
  </verify>
  <acceptance_criteria>
    - stylua --check on all 4 modified files exits 0
    - install.bat completes with exit code 0
    - CDMTab.lua exists in WoW AddOns folder
    - User confirms addon deployed
  </acceptance_criteria>
  <done>Addon deployed to WoW; stylua clean; ready for in-game verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-game verification checkpoint — Phase 23 success criteria</name>
  <read_first>
    - .planning/phases/23-cdmtab-lua-unification/23-CONTEXT.md
    - .planning/ROADMAP.md (Phase 23 Success Criteria list)
  </read_first>
  <what-built>
    Phase 23 CDMTab.lua unification is complete in code (static grep passes in Plans 23-01/23-02/23-03):
    - ns:ShowBuffTooltip extended with opts table (showSpellID, showDuration, extraLines)
    - ns.SUGGESTED_BUFFS → ns.SUGGESTED_KEYS ordered list
    - All 3 CDMTab resolution chains collapsed to single `ns:GetDisplayInfoForKey(key)` calls
    - META_DESCRIPTIONS promoted to CDMTab file-level local
    - CLASS_LUST_SPELL and GetHunterLustSpell demoted to Providers.lua module-local
    - Pending todo moved to done
  </what-built>
  <how-to-verify>
    Run through the following in-game verification matrix. Each item MUST pass for Phase 23 to be APPROVED.

    **Setup:**
    1. Launch WoW, log into a character
    2. Type `/tbt` to open CDM Settings with TBT tab selected (or open CDM Settings → click TBT tab)
    3. Open chat console: `/console scriptErrors 1`

    **Matrix (12 steps — mirrors Phase 22's 12-step matrix):**

    1. **[Suggested section renders]** TBT tab is visible; Suggested section shows exactly 4 tiles: Add square (green +) + Lust + Trinket + Pot in that order.

    2. **[Class-aware lust icon]** The Lust tile shows the correct icon for your class:
       - Shaman → Bloodlust icon (spellID 2825)
       - Mage → Time Warp icon (spellID 80353)
       - Evoker → Fury of the Aspects icon (spellID 390386)
       - Hunter (non-MM) → Primal Rage icon (spellID 264667)
       - Hunter (MM spec 254) → Harrier's Cry icon (spellID 466904)
       - Other classes → Bloodlust fallback (2825)

    3. **[Trinket icon at rest]** The Trinket tile shows either:
       - Currently equipped season trinket's spell icon (if you have one equipped and it's in TRINKET_SPELLS)
       - OR the first CSV fallback spell's icon (generic trinket glyph)

    4. **[Pot icon at rest]** The Pot tile shows either:
       - Your bag pot's spell icon (if you have a season pot in bags)
       - OR the first CSV fallback damage pot's icon

    5. **[Tile tooltip - Lust]** Hover the Lust tile. Tooltip shows:
       - Line 1: Spell name (e.g., "Bloodlust" / "Time Warp" / "Primal Rage")
       - Blank line
       - "Spell ID: NNNN"
       - "TBT Duration: 40s"
       - "Matches all Heroism/Bloodlust effects" (gray)

    6. **[Tile tooltip - Trinket]** Hover the Trinket tile. Tooltip shows:
       - Line 1: Resolved trinket spell name
       - Blank line
       - "Spell ID: NNNN"
       - "TBT Duration: Xs" (non-zero)
       - "Tracks all current season's on-use trinkets" (gray)

    7. **[Tile tooltip - Pot]** Hover the Pot tile. Tooltip shows:
       - Line 1: Resolved pot spell name
       - Blank line
       - "Spell ID: NNNN"
       - "TBT Duration: Xs" (non-zero)
       - "Tracks all current season's damage potions" (gray)

    8. **[Drag ghost icon matches tile]** Left-click-drag each of the 3 Suggested tiles. The cursor-following ghost icon must visually match the source tile's icon exactly (no 134400 ? placeholder).

    9. **[Drag copy-on-release]** Drag Lust from Suggested to Bars section; release. Result: Lust entry created in trackedBuffs; bar appears in preview with Lust icon and label. Ghost icon matched tile during drag.

    10. **[Right-click "Add to Bars" from Suggested]** Right-click Pot tile in Suggested → "Add to Bars". Result: Pot entry created in trackedBuffs (with provider's label/duration), appears in preview. No errors.

    11. **[Preview mode, all 4 buff types]** With entries added from Steps 9 and 10 (plus any pre-existing user-created buffs and Trinket if you added it), open CDM to trigger preview. All 4 buff types render with correct icons and labels — no question-mark placeholders, no 0-second durations for trinket/pot.

    12. **[No Lua errors]** Close and reopen CDM Settings at least 3 times. Check `/console scriptErrors 1` output — zero errors in the TBT namespace. Specifically test:
        - CDM open → StartPreview (RefreshMetaIcons + RefreshTBTSections + StartAllPreviewTimers)
        - Hover each tile (tooltip chain)
        - Drag a tile (BeginDrag ghost resolution)
        - CDM close → StopPreview (ClearAllTimers)

    Report PASS / FAIL for each step with brief notes. If any FAIL: capture /console errors and a screenshot if possible, describe discrepancy.
  </how-to-verify>
  <resume-signal>
    Reply "approved" if all 12 steps PASS. Otherwise, reply with step numbers that failed and details for gap closure (--gaps replan).
  </resume-signal>
  <files>(no source edits — manual verification only)</files>
  <action>See <how-to-verify> above — execute the 12-step in-game verification matrix. Halt on any failure and capture the step number + details for --gaps replan.</action>
  <verify>Manual verification by user in live WoW session. User reports PASS per step or FAILs with detail.</verify>
  <done>All 12 steps PASS and user replies "approved" (resume-signal). Any FAIL → halt, report to orchestrator for --gaps closure.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record verification results and write SUMMARY</name>
  <read_first>
    - .planning/phases/23-cdmtab-lua-unification/23-01-SUMMARY.md
    - .planning/phases/23-cdmtab-lua-unification/23-02-SUMMARY.md
    - .planning/phases/23-cdmtab-lua-unification/23-03-SUMMARY.md
  </read_first>
  <files>.planning/phases/23-cdmtab-lua-unification/23-04-SUMMARY.md</files>
  <action>
    Create `.planning/phases/23-cdmtab-lua-unification/23-04-SUMMARY.md` documenting:
    - Deploy status (install.bat exit code, stylua final result)
    - 12-step in-game verification matrix results (pass/fail per step, with notes)
    - Mapping from verification steps → Phase 23 success criteria:
      - SC1 (zero META_DESCRIPTIONS at function-closure level) → grep in Plan 23-02; verified by Step 5-7 tooltip correctness
      - SC2 (zero type(x) == "string" branches, zero shim calls) → grep in Plan 23-02
      - SC3 (all 4 buff types render correctly) → Steps 2, 3, 4, 11
      - SC4 (drag ghost uses ns:GetDisplayInfoForKey) → Step 8
      - SC5 (tile tooltip content preserved) → Steps 5, 6, 7
    - Runtime confirmation of DISP-02 (Phase 23 requirement)
    - User's APPROVED signal
    - Note that Phase 24 cleanup is now unblocked (shim removal, orphan deletion per DISP-04)
  </action>
  <verify>
    <automated>test -f .planning/phases/23-cdmtab-lua-unification/23-04-SUMMARY.md && grep -q "APPROVED" .planning/phases/23-cdmtab-lua-unification/23-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - 23-04-SUMMARY.md exists
    - Contains 12-step verification result table
    - Maps each SC (1-5) to verification step(s)
    - Contains user's APPROVED signal
    - Notes Phase 24 unblocked
  </acceptance_criteria>
  <done>Phase 23 verified in-game; SUMMARY written; Phase 24 unblocked.</done>
</task>

</tasks>

<verification>
- install.bat succeeds; stylua clean across all 4 modified files
- All 12 in-game verification steps pass (or gaps documented for --gaps replan)
- 23-04-SUMMARY.md captures results
- User APPROVED signal received
</verification>

<success_criteria>
- All 5 Phase 23 Success Criteria verified live:
  1. CDMTab.lua zero function-closure META_DESCRIPTIONS (file-level OK) — confirmed in Plan 23-02 grep + runtime tooltip correctness
  2. CDMTab.lua zero `type(x) == "string"` branches and zero GetAtRestMetaIcon/GetAtRestMetaInfo/ResolveSuggestedSpellID calls — confirmed in Plan 23-02 grep
  3. All CDM settings grid icons display correctly for all four buff types — in-game Steps 2, 3, 4, 11
  4. Drag-and-drop ghost icon uses ns:GetDisplayInfoForKey path — in-game Step 8
  5. Tile tooltip content preserved (spell name + Spell ID + TBT Duration + description) — in-game Steps 5, 6, 7
- DISP-02 runtime-confirmed
- No regressions in drag/drop/reorder/right-click/Add dialog
- Zero Lua errors during CDM lifecycle
- Phase 24 cleanup unblocked (DISP-04 shim removal is next phase)
</success_criteria>

<output>
On APPROVED signal: Phase 23 complete. Orchestrator advances STATE.md Phase 23 → complete; ROADMAP.md Phase 23 checkbox → [x]; Phase 24 becomes the active phase.

On failures: capture gaps in `.planning/phases/23-cdmtab-lua-unification/23-VERIFICATION.md` for `/gsd:plan-phase 23 --gaps` replan.
</output>
