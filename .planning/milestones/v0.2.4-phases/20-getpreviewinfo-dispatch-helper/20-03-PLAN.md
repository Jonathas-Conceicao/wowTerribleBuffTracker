---
phase: 20-getpreviewinfo-dispatch-helper
plan: 03
type: execute
wave: 3
depends_on: ["20-01", "20-02"]
files_modified: []
autonomous: false
requirements: [PROV-04]
must_haves:
  truths:
    - "Zero Lua errors in chat on /reload and during gameplay"
    - "Zero visual regression versus Phase 19 — bars/icons for user-spell, trinket, pot, lust look identical"
    - "CDM tab settings icons for trinket and pot match the equipped trinket / first available bag pot"
    - "Active cast timers for user spell / trinket / pot produce working countdown bars"
    - "Lust detection still triggers via Sated debuff (no-restart guard, secret-gate gate still work)"
    - "Preview mode still shows user-spell bars correctly; trinket/pot preview bars STILL show 0s (known, deferred to Phase 21)"
  artifacts:
    - path: ".planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md"
      provides: "In-game verification record of Phase 20 behavior parity (or diagnosed gaps)"
      min_lines: 20
  key_links:
    - from: "CDMTab.StartPreview"
      to: "ns:RefreshMetaIcons (wrapper) -> provider:RefreshAtRest"
      via: "unchanged call site; new internal dispatch"
      pattern: "RefreshMetaIcons"
    - from: "Display.lua / CDMTab.lua (14 call sites)"
      to: "ns:GetAtRestMetaIcon / ns:GetAtRestMetaInfo / ns:ResolveSuggestedSpellID shims"
      via: "unchanged call sites; shims now delegate through ns:GetDisplayInfoForKey"
      pattern: "GetAtRestMetaIcon|GetAtRestMetaInfo|ResolveSuggestedSpellID"
---

<objective>
Deploy the Phase 20 code via `scripts/install.bat`, launch WoW, and human-verify that behavior matches Phase 19 exactly. Since Display.lua and CDMTab.lua are UNTOUCHED in Phase 20, the user should see ZERO visual delta from Phase 19 — any regression indicates a shim is broken.

Purpose: Close PROV-04 with on-client confirmation. The three shims (ns:GetAtRestMetaIcon, ns:GetAtRestMetaInfo, ns:ResolveSuggestedSpellID) now internally route through the provider dispatch helper; Display/CDMTab still call them under the old names. If icons, tooltips, cast timers, lust detection, or preview all look/behave unchanged, the provider-side refactor is proven behaviour-parity and the milestone is ready for Phase 21 (StartAllPreviewTimers migration / LIFE-03).

Output: `.planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md` recording verification outcome (approved) or diagnosed gaps (for `/gsd:plan-phase --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-getpreviewinfo-dispatch-helper/20-CONTEXT.md
@.planning/phases/20-getpreviewinfo-dispatch-helper/20-01-PLAN.md
@.planning/phases/20-getpreviewinfo-dispatch-helper/20-02-PLAN.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- Expected state after Plans 20-01 and 20-02 complete: -->

**Providers.lua:**
- Base mixin has exactly 4 methods: GetEventInterests, OnTrigger, GetDisplayInfo, RefreshAtRest
- All 4 providers implement GetDisplayInfo returning { icon, label, duration, spellID }
- TrinketProvider.atRest and PotProvider.atRest caches exist with { spellID, duration }
- Trinket/Pot RefreshAtRest methods scan inventory and write their cache
- ns:GetDisplayInfoForKey(key) is exported; keyToProvider table is module-local

**BuffEngine.lua:**
- ns.metaAtRest deleted; FindSpellByItemID deleted
- ns:RefreshMetaIcons is a 10-line wrapper: combat-gate + iterate ns.providers calling RefreshAtRest
- ns:GetAtRestMetaIcon / ns:GetAtRestMetaInfo / ns:ResolveSuggestedSpellID delegate through ns:GetDisplayInfoForKey
- SUGGESTED_BUFFS structure unchanged

**Display.lua, CDMTab.lua, Core.lua, TerribleBuffTracker.toc:**
- UNTOUCHED
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deploy Phase 20 changes to WoW via install.bat and verify zero load errors</name>
  <files></files>
  <read_first>
    - CLAUDE.md (install workflow — `./scripts/install.bat`)
    - .planning/phases/20-getpreviewinfo-dispatch-helper/20-01-PLAN.md (Providers.lua changes)
    - .planning/phases/20-getpreviewinfo-dispatch-helper/20-02-PLAN.md (BuffEngine.lua changes)
  </read_first>
  <action>
    **Step 1 — Preflight: confirm all Plan 20-01 and Plan 20-02 acceptance checks pass.**

    Run the aggregated preflight on the two modified files:

    ```bash
    ~/.cargo/bin/stylua --check Providers.lua BuffEngine.lua
    ```

    Run the key structural greps. All MUST match expected values. If any fails, STOP and return to the failing plan:

    ```bash
    # Providers.lua — Plan 20-01 acceptance
    grep -c "function SpellProviderBaseMixin:GetDisplayInfo" Providers.lua   # expect 1
    grep -c "function SpellProviderBaseMixin:RefreshAtRest" Providers.lua    # expect 1
    grep -c "function SpellProviderBaseMixin:GetPreviewInfo" Providers.lua   # expect 0
    grep -c "function SpellProviderBaseMixin:GetAtRestInfo" Providers.lua    # expect 0
    grep -cE "function \w+ProviderMixin:GetDisplayInfo" Providers.lua        # expect 4
    grep -cE "function \w+ProviderMixin:RefreshAtRest" Providers.lua         # expect 2 (Trinket + Pot)
    grep -c "function ns:GetDisplayInfoForKey" Providers.lua                 # expect 1
    grep -c "^local keyToProvider" Providers.lua                             # expect 1
    grep -c "^local function FindSpellByItemID" Providers.lua                # expect 1

    # BuffEngine.lua — Plan 20-02 acceptance
    grep -c "ns\.metaAtRest" BuffEngine.lua                                   # expect 0
    grep -c "ns\.metaIcons" BuffEngine.lua                                    # expect 0
    grep -c "FindSpellByItemID" BuffEngine.lua                                # expect 0
    grep -c "GetInventoryItemID" BuffEngine.lua                               # expect 0
    grep -c "provider:RefreshAtRest" BuffEngine.lua                           # expect 1
    grep -c "ns:GetDisplayInfoForKey" BuffEngine.lua                          # expect >=3
    ```

    **Step 2 — Deploy.**

    Run the install script (Windows shell path — forward slashes OK under bash on Windows):

    ```bash
    ./scripts/install.bat
    ```

    Confirm the script reports the addon was copied into `_retail_\Interface\AddOns\TerribleBuffTracker`.

    **Step 3 — Launch WoW and observe initial load.**

    User launches WoW (or `/reload`s if already running) and reports back:
    - Chat contains ZERO Lua errors referencing `Providers.lua` or `BuffEngine.lua`
    - `/tbt` still opens CDM with the TBT tab visible
    - `print(ns:GetDisplayInfoForKey("trinket"))` from a Lua console (if available) returns a table with numeric spellID and non-zero duration (optional bonus check — only if user wants to run it via `/run`)

    If errors appear, copy the full error text into the verification doc so the gap-closure plan can target them.
  </action>
  <acceptance_criteria>
    - stylua --check passes on both Providers.lua and BuffEngine.lua
    - All structural grep counts match the expected values above
    - `./scripts/install.bat` runs without error and reports successful copy
    - User confirms zero Lua errors on `/reload` / login
    - CDM opens without error; TBT tab is visible
  </acceptance_criteria>
  <verify>
    <automated>~/.cargo/bin/stylua --check Providers.lua BuffEngine.lua &amp;&amp; grep -c "function ns:GetDisplayInfoForKey" Providers.lua | grep -q "^1$" &amp;&amp; grep -c "provider:RefreshAtRest" BuffEngine.lua | grep -q "^1$" &amp;&amp; grep -c "ns\.metaAtRest" BuffEngine.lua | grep -q "^0$"</automated>
  </verify>
  <done>
    Code is deployed; addon loads without Lua errors on `/reload`; CDM tab opens; all structural code checks pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — behavior parity with Phase 19 across all four buff types</name>
  <files></files>
  <read_first>
    - .planning/phases/19-lustprovider-unit-aura-dispatch/19-VERIFICATION.md (template for verification write-up)
    - .planning/phases/20-getpreviewinfo-dispatch-helper/20-CONTEXT.md (D-20/D-21 — Display.lua + CDMTab.lua untouched means zero visual delta expected)
    - .planning/research/PITFALLS.md (PITFALL-4 — trinket/pot 0-second preview is pre-existing; NOT a Phase 20 regression)
  </read_first>
  <what-built>
    **Phase 20 refactor of the provider display-info contract.**

    The code now:
    - Defines a single `GetDisplayInfo(key)` method on all 4 providers (replacing GetPreviewInfo + GetAtRestInfo)
    - Moves the trinket/pot at-rest cache and inventory scan from BuffEngine into the providers themselves (`provider.atRest = { spellID, duration }` + `provider:RefreshAtRest()`)
    - Exports `ns:GetDisplayInfoForKey(key)` as the new unified dispatch helper
    - Retains the three old entry points (`ns:GetAtRestMetaIcon`, `ns:GetAtRestMetaInfo`, `ns:ResolveSuggestedSpellID`) as thin shims that now internally call `ns:GetDisplayInfoForKey`

    **Critical expectation:** Display.lua and CDMTab.lua were NOT modified. They still call the three old entry points. Therefore the user should see ZERO visible change versus Phase 19. Any visual regression means a shim is returning wrong data.
  </what-built>
  <action>
    The executor does not write code in this task. The executor's job is to:

    1. Confirm Task 1 completed successfully (install.bat ran, zero load errors).
    2. Present the `<how-to-verify>` 8-test matrix below to the user verbatim.
    3. WAIT for the user's response. Do not make assumptions. Do not attempt to launch WoW from the command line (client is user-driven).
    4. On user reply, proceed to Task 3 to write the verification file.
  </action>
  <how-to-verify>
    **Environment:** Log in with the character that typically tests TBT. Ensure you have at least one equipped on-use trinket from `trinket_info.csv` and at least one damage pot from `pots_info.csv` in your bags.

    **Step 1 — Reload to baseline:**
    1. `/reload`
    2. Observe chat. Expected: no Lua errors. If errors appear, COPY them verbatim and report.

    **Step 2 — CDM tab open (triggers ns:RefreshMetaIcons -> provider:RefreshAtRest):**
    1. Press `Esc` -> `Options` -> `Edit Mode` -> enter Edit Mode -> click the Cooldown Manager -> click the `TBT` tab. (Alternatively: `/tbt`.)
    2. Observe the Suggested section icons:
       - Trinket icon: should show the equipped trinket's BUFF-SPELL icon (not the item icon). E.g. if you're wearing "Light Company Guidon" you should see spell 1259633's icon.
       - Pot icon: should show the buff-spell icon of the first damage pot found in your bags (or the first CSV-order pot if none present).
       - Lust icon: class-appropriate (Shaman=Bloodlust, Mage=Time Warp, etc.; Hunter MM=Harrier's Cry, Hunter BM/SV=Primal Rage).
    3. **Compare** to Phase 19 behavior — icons should be identical.

    **Step 3 — Trinket cast (exercises TrinketProvider:OnTrigger, unchanged by Phase 20):**
    1. Close CDM, exit Edit Mode.
    2. Cast your on-use trinket.
    3. Confirm a bar appears in the TBT bars container with the correct icon, correct duration (e.g. 15s for Light Company Guidon), counting down.
    4. If you have two on-use trinkets, cast the second one: the first bar should be REPLACED (not duplicated) by the new timer — `ns.activeTimers["trinket"]` is single-slot.

    **Step 4 — Pot cast:**
    1. Use a tracked damage pot (e.g. Light's Potential).
    2. Confirm bar appears with correct icon + duration, counts down.

    **Step 5 — User-spell cast:**
    1. Cast any tracked user buff.
    2. Confirm bar appears with correct icon + duration.

    **Step 6 — Lust detection:**
    1. Either receive lust from a real source OR via a party member casting it.
    2. If lust lands, confirm a 40s bar appears with class-appropriate label.
    3. Do not re-apply within 40s if possible — confirm no-restart guard leaves the first bar running.

    **Step 7 — Preview mode:**
    1. Open CDM -> TBT tab again.
    2. Preview timer bars appear:
       - User-spell bars: correct icon + duration (unchanged from Phase 19).
       - Trinket/pot preview bars: still show 0-second bars (KNOWN — Phase 21 / LIFE-03 fixes this).
       - Lust preview bar: correct icon + 40s duration.
    3. Close CDM -> active timers (if any mid-countdown) restore correctly.

    **Step 8 — Tooltip check (CDM open, hover an icon):**
    1. Hover over the trinket icon in CDM Suggested.
    2. Tooltip should show the trinket BUFF-SPELL tooltip (exercises `ns:GetAtRestMetaInfo(key)` + `ns:ResolveSuggestedSpellID(key)` shims).
    3. Hover over pot and lust icons — same expectation.

    **What to report:**
    - **APPROVED** if all 8 steps behave identically to Phase 19 (trinket/pot 0s preview is EXPECTED — Phase 21).
    - **REGRESSION** with exact step number, what was expected, what was observed, and any Lua error text.

    **Known expected behavior (NOT a regression):**
    - Trinket and pot preview bars show 0 seconds in preview mode — this is the pre-existing bug that Phase 21 (LIFE-03) fixes. Phase 20 does NOT touch StartAllPreviewTimers.
  </how-to-verify>
  <acceptance_criteria>
    - User has executed all 8 verification steps and reported either APPROVED or specific regressions
    - Regression reports include: step number, expected behavior, observed behavior, any Lua error text
    - Executor captures the user's response text verbatim for use in Task 3
  </acceptance_criteria>
  <verify>
    <automated>MISSING — checkpoint tasks are human-gated; verification automation is Task 3's verification file write</automated>
  </verify>
  <done>
    User has responded with either "approved" or a regression report. The response is captured and ready to write into 20-VERIFICATION.md in Task 3.
  </done>
  <resume-signal>Type "approved" or describe observed regressions with step number and error text.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 20-VERIFICATION.md recording the verification outcome</name>
  <files>.planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md</files>
  <read_first>
    - User's approval or regression report from Task 2
    - .planning/phases/19-lustprovider-unit-aura-dispatch/19-VERIFICATION.md (for format template)
    - .planning/phases/20-getpreviewinfo-dispatch-helper/20-CONTEXT.md (success criteria)
  </read_first>
  <action>
    Create `.planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md` recording Phase 20 verification outcome.

    **If APPROVED**, use this template:

    ```markdown
    # Phase 20 Verification: GetDisplayInfo + Dispatch Helper

    **Verified:** {today's date}
    **Status:** APPROVED

    ## Scope Verified

    - ns:GetDisplayInfoForKey dispatch helper (PROV-04 provider-side contract)
    - Provider-owned at-rest caches (PROV-F3 pulled forward)
    - Three backwards-compat shims delegating through ns:GetDisplayInfoForKey
    - Zero visual regression vs Phase 19 (Display.lua / CDMTab.lua untouched)

    ## Verification Steps Executed

    1. `/reload` — zero Lua errors observed
    2. CDM open — trinket/pot/lust icons match equipped/bag/class-appropriate spells
    3. Trinket cast — bar appears, correct icon/duration, reproc overwrites
    4. Pot cast — bar appears, correct icon/duration
    5. User-spell cast — bar appears correctly
    6. Lust detection — 40s bar, no-restart guard respected
    7. Preview mode — user-spell + lust preview correct; trinket/pot 0s (expected, Phase 21 scope)
    8. Tooltips — trinket/pot/lust icons in CDM show correct spell tooltips

    ## Known Deferred Behavior (NOT regressions)

    - Trinket and pot preview bars still show 0-second duration. PITFALL-4 / LIFE-03.
      Reason: StartAllPreviewTimers UNTOUCHED in Phase 20 per D-22. Fix lands in Phase 21.

    ## Success Criteria Met

    - [x] SC-1: ns:GetDisplayInfoForKey(key) returns correct { icon, label, duration, spellID } for any registered provider key
    - [x] SC-2: Trinket and pot GetDisplayInfo return non-zero durations (provider layer — consumer migration in Phase 21)
    - [x] SC-3: ns:GetDisplayInfoForKey exported to namespace, callable from Display.lua / CDMTab.lua
    - [x] SC-4: RefreshAtRest() on base mixin + meta-providers; ns:RefreshMetaIcons is a thin wrapper iterating ns.providers; CDMTab callsite unchanged

    ## Requirements Closed

    - PROV-04 — complete
    - PROV-F3 (Future) — pulled forward and closed

    ## Next Phase

    Phase 21: Preview Mode Migration (LIFE-03) — migrates StartAllPreviewTimers to use provider GetDisplayInfo, fixing the trinket/pot 0-second preview bars.
    ```

    **If REGRESSION**, use this template (status: diagnosed — so `/gsd:plan-phase --gaps` can pick it up):

    ```markdown
    # Phase 20 Verification: GetDisplayInfo + Dispatch Helper

    **Verified:** {today's date}
    **Status:** diagnosed
    **Blocking:** yes

    ## Gaps Detected

    ### Gap 1: {title}

    **Truth that failed:** {which success criterion or must_have}
    **Reason:** {root cause from user report / error text}
    **Artifacts involved:** {file paths}
    **Missing / To Fix:**
    - {specific thing that must be added or changed}
    - {if more than one, list each}

    ### Gap N: (repeat)

    ## Steps That Passed

    - {list the steps that did succeed, so gap-closure plan knows what NOT to redo}

    ## Next

    Run `/gsd:plan-phase 20 --gaps` to generate a targeted fix plan.
    ```

    Keep it concise — 20-40 lines is enough.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md` exists
    - File is at least 20 lines (min_lines in must_haves)
    - Status is either `APPROVED` or `diagnosed` — not a vague placeholder
    - File lists which of the 4 phase success criteria were met (if approved) or which gap fires (if diagnosed)
    - If diagnosed, each gap has truth + reason + artifacts + missing fields so `/gsd:plan-phase --gaps` can consume it
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md" &amp;&amp; [ "$(wc -l < .planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md)" -ge 20 ] &amp;&amp; grep -qE "Status:\s+(APPROVED|diagnosed)" .planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md</automated>
  </verify>
  <done>
    20-VERIFICATION.md is written reflecting user's actual verification outcome. Either PROV-04 is marked complete (APPROVED path) or diagnosed gaps are documented with enough detail for `/gsd:plan-phase 20 --gaps` to generate a targeted fix plan (diagnosed path).
  </done>
</task>

</tasks>

<verification>
- install.bat completes successfully and copies the addon into the WoW AddOns folder
- Zero Lua errors on /reload (user confirmation in Task 2)
- User confirms all 8 verification steps (or reports regressions)
- 20-VERIFICATION.md exists with either APPROVED or diagnosed status
- If APPROVED: all 4 Phase 20 success criteria marked complete; PROV-04 closed; PROV-F3 closed
- If diagnosed: gaps have enough detail (truth / reason / artifacts / missing) for gap-closure planning
</verification>

<success_criteria>
- Addon deploys via install.bat with zero errors
- User confirms CDM opens, all buff types display correctly, zero visual regression vs Phase 19 (trinket/pot preview 0s is expected/deferred)
- Cast timers work for user-spell, trinket, pot
- Lust detection and no-restart guard still work
- 20-VERIFICATION.md written with appropriate status
- PROV-04 (and pulled-in PROV-F3) closed if approved, or gap-closure path cleanly available if diagnosed
</success_criteria>

<output>
After completion, create `.planning/phases/20-getpreviewinfo-dispatch-helper/20-03-SUMMARY.md`
</output>
