---
phase: 24-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 24-01
  - 24-02
files_modified:
  - BuffEngine.lua
  - Providers.lua
  - CDMTab.lua
  - Display.lua
  - Core.lua
  - EditModeFrames.lua
  - CHANGELOG.md
  - .planning/todos/pending/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md
  - .planning/todos/pending/2026-04-22-mplus-lua-errors-secret-values-during-lust.md
  - .planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md
  - .planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md
autonomous: true
requirements:
  - DISP-04

must_haves:
  truths:
    - "All six Lua files pass stylua --check with exit code 0"
    - "CHANGELOG.md contains a '## v0.2.4' section above the existing '## v0.2.3' section"
    - "The v0.2.4 CHANGELOG section contains the line 'Internal file reworking — no user-visible changes.' per D-07"
    - "If 24-02-AUDIT.md note_text is not 'NONE', the CHANGELOG v0.2.4 section contains that exact single-sentence performance note (D-08)"
    - "BuffEngine.lua still has exactly one occurrence of 'local CURRENT_SCHEMA_VERSION = 3' — schema unchanged per D-12"
    - "The Evoker lust todo file is moved from .planning/todos/pending/ to .planning/todos/done/ with a closure note prepended"
    - "The M+ Lua errors todo file is moved from .planning/todos/pending/ to .planning/todos/done/ with a closure note prepended"
    - "No files remain in .planning/todos/pending/ after this plan completes"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v0.2.4 release notes entry"
      contains: "## v0.2.4"
    - path: ".planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md"
      provides: "Closed Evoker lust cancellation todo with resolution note"
      contains: "Closed:"
    - path: ".planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md"
      provides: "Closed M+ Lua errors todo with resolution note"
      contains: "Closed:"
  key_links:
    - from: "CHANGELOG.md"
      to: "v0.2.4 entry"
      via: "## v0.2.4 section header"
      pattern: "## v0\\.2\\.4"
    - from: ".planning/phases/24-cleanup/24-02-AUDIT.md"
      to: "CHANGELOG.md v0.2.4 entry"
      via: "note_text: value"
      pattern: "note_text:"
---

<objective>
Final release-prep for v0.2.4. Runs AFTER Plans 24-01 (code cleanup) and 24-02 (audit) complete. Executes the single stylua pass across all six Lua files per D-11, writes the minimal CHANGELOG v0.2.4 entry per D-07 with the optional performance note from the audit per D-08, verifies the schema hasn't changed per D-12, and moves both pending todos to done per D-05/D-06. Does NOT tag, push, squash, or run release.bat — that's post-phase work per D-13.

Purpose: Close DISP-04 in an audit-clean state and leave a working tree ready for `/gsd:complete-milestone` to squash-merge into main.
Output: Six stylua-clean Lua files; CHANGELOG.md with v0.2.4 entry; two todos moved from pending to done; schema version verified unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cleanup/24-CONTEXT.md
@.planning/phases/24-cleanup/24-01-PLAN.md
@.planning/phases/24-cleanup/24-02-PLAN.md
@CHANGELOG.md
</context>

<interfaces>
<!-- Inputs from Plans 24-01 and 24-02 that this plan consumes. -->

From Plan 24-02's audit output (.planning/phases/24-cleanup/24-02-AUDIT.md):
```
## CHANGELOG Input
note_text: "<string value — read this at Task 2 execution time>"
```
If `note_text == "NONE"`, omit the performance note. Otherwise paste the exact string verbatim.

From Plan 24-01's cleanup output (verified by grep gates — re-check in Task 4):
- Zero references to GetAtRestMetaIcon / GetAtRestMetaInfo / ResolveSuggestedSpellID / RefreshMetaIcons / ns.TRINKET_FALLBACK_ORDER / ns.POT_FALLBACK_ORDER in any Lua file
- ns:RefreshProvidersAtRest exists in BuffEngine.lua and is called from CDMTab.lua
- pots_info.csv and trinket_info.csv are deleted

CHANGELOG structure template (read CHANGELOG.md top-of-file to confirm current state — the top section should be `## v0.2.3`):
```markdown
# Changelog

## v0.2.4 — SpellProvider Refactor

Internal file reworking — no user-visible changes.
[optional: performance note from audit]

## v0.2.3 — Trinket & Pot Meta-Trackers
...
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Run stylua pass on all six Lua files (D-11)</name>
  <files>BuffEngine.lua, Providers.lua, CDMTab.lua, Display.lua, Core.lua, EditModeFrames.lua</files>
  <read_first>
    - CLAUDE.md (confirm stylua workflow: "Always run `stylua` on Lua files after finishing a task")
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-11 rule: standard stylua pass across all Lua files, zero formatting deltas on exit)
    - All six Lua files (to confirm they exist and are the expected post-Plan-24-01 state — at minimum, open BuffEngine.lua to confirm the renamed RefreshProvidersAtRest is present)
  </read_first>
  <action>
1. Run stylua to reformat all six Lua files:
   ```
   ~/.cargo/bin/stylua BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua
   ```
   Exit code must be 0. Any file modified by stylua is OK — that's expected after Plan 24-01's deletions may have left trailing whitespace or subtly misaligned blocks.

2. Run the verification check that confirms no further changes are needed:
   ```
   ~/.cargo/bin/stylua --check BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua
   ```
   Exit code must be 0.

3. Review the resulting diffs briefly:
   ```
   git diff --stat *.lua
   ```
   Small diffs (whitespace, indent tweaks) are expected. If any file shows a large diff (>20 lines changed), inspect whether Plan 24-01's deletions left unexpected state that needs manual attention.

No other code changes in this task. Next task handles CHANGELOG.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; ~/.cargo/bin/stylua --check BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua &amp;&amp; echo "stylua exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - `~/.cargo/bin/stylua --check BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua` exits 0
    - All six listed files exist in the repo root (sanity)
    - `grep -c "function ns:RefreshProvidersAtRest" BuffEngine.lua` returns 1 (sanity: Plan 24-01 rename landed)
    - `grep -c "ns:RefreshProvidersAtRest()" CDMTab.lua` returns 1 (sanity: Plan 24-01 call-site update landed)
  </acceptance_criteria>
  <done>All six Lua files pass `stylua --check` with exit 0. Plan 24-01's rename is confirmed present.</done>
</task>

<task type="auto">
  <name>Task 2: Write v0.2.4 CHANGELOG entry per D-07 + D-08</name>
  <files>CHANGELOG.md, .planning/phases/24-cleanup/24-02-AUDIT.md</files>
  <read_first>
    - CHANGELOG.md (confirm top entry is currently `## v0.2.3 — Trinket & Pot Meta-Trackers` — the new entry goes ABOVE it)
    - .planning/phases/24-cleanup/24-02-AUDIT.md (read the `## CHANGELOG Input` section; specifically the `note_text:` line)
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-07 one-liner rule; D-08 conditional performance note rule)
  </read_first>
  <action>
1. Open `.planning/phases/24-cleanup/24-02-AUDIT.md` and find the `note_text:` value inside the `## CHANGELOG Input` section. Capture the exact string. It will be either the literal word `NONE` (without quotes) or a complete single-sentence note (the content inside quotes).

2. Open `CHANGELOG.md`. Confirm the current top-of-file structure is:
   ```
   # Changelog

   ## v0.2.3 — Trinket & Pot Meta-Trackers
   ...
   ```
   (If it is not, STOP and flag — something unexpected is in the repo state.)

3. Prepend the new v0.2.4 section ABOVE `## v0.2.3`. Use this EXACT structure:

   **Case A — note_text == "NONE":**
   ```markdown
   ## v0.2.4 — SpellProvider Refactor

   Internal file reworking — no user-visible changes.

   ```

   **Case B — note_text is a concrete sentence:**
   ```markdown
   ## v0.2.4 — SpellProvider Refactor

   Internal file reworking — no user-visible changes.

   <paste exact note_text string here, preserving punctuation>

   ```

   (The blank line after each paragraph matches the existing CHANGELOG.md style — see the v0.2.3 entry for reference.)

4. Do NOT add feature bullets. Do NOT add "### New Features" / "### Fixes" / "### Improvements" subheadings. Do NOT reference internal decision IDs, phase numbers, or requirement IDs. Per D-07 quote: "No details, only say it's a internal file reworking, and mention performance change if we have any."

5. Save CHANGELOG.md.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; echo "=== v0.2.4 section present ===" &amp;&amp; grep -c "## v0.2.4" CHANGELOG.md &amp;&amp; echo "=== v0.2.4 appears above v0.2.3 ===" &amp;&amp; awk '/## v0\.2\.4/{found4=NR} /## v0\.2\.3/{if(found4&amp;&amp;NR&gt;found4)print "OK: v0.2.4 at line "found4", v0.2.3 at line "NR; exit}' CHANGELOG.md &amp;&amp; echo "=== D-07 one-liner present ===" &amp;&amp; grep -c "Internal file reworking — no user-visible changes" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## v0.2.4" CHANGELOG.md` returns 1
    - `grep -c "## v0.2.3" CHANGELOG.md` returns 1 (existing entry preserved)
    - The v0.2.4 section appears on a line BEFORE the v0.2.3 section (awk check in verify script)
    - `grep -c "Internal file reworking — no user-visible changes" CHANGELOG.md` returns exactly 1
    - `grep -cE "^### (New Features|Fixes|Improvements)" CHANGELOG.md` returns the same count as BEFORE this task (no new subheading added to v0.2.4)
    - If the audit's note_text value was literal `NONE`, the v0.2.4 entry contains ONLY the one-liner (no second sentence/paragraph in that section)
    - If the audit's note_text value was a concrete sentence, that exact sentence appears in CHANGELOG.md (`grep -F "<exact sentence>" CHANGELOG.md` returns 1)
  </acceptance_criteria>
  <done>CHANGELOG.md has a minimal v0.2.4 entry above the v0.2.3 entry. The entry contains the D-07 one-liner and, if the audit returned a concrete note, that verbatim sentence. No feature bullets or subheadings in the v0.2.4 section.</done>
</task>

<task type="auto">
  <name>Task 3: Close both pending todos as solved (D-05, D-06)</name>
  <files>.planning/todos/pending/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md, .planning/todos/pending/2026-04-22-mplus-lua-errors-secret-values-during-lust.md, .planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md, .planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md</files>
  <read_first>
    - .planning/todos/pending/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md (full current content — must be preserved in the moved file)
    - .planning/todos/pending/2026-04-22-mplus-lua-errors-secret-values-during-lust.md (full current content — must be preserved in the moved file)
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-05, D-06 closure instructions; quoted user rationale in `<specifics>`)
    - Existing done-todo for format reference: .planning/todos/done/2026-04-21-demote-remaining-lust-constants-to-provider-local.md (to see how prior closures were formatted)
  </read_first>
  <action>
**Todo 1 — Evoker Fury of the Aspects cancellation (D-05):**

1. Read the full content of `.planning/todos/pending/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md`.
2. Create `.planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md` with a closure note PREPENDED above the existing content. Specifically — after the YAML frontmatter block (the `---` ... `---` block at the top), insert a new section:

   ```markdown
   ## Closed: 2026-04-22 — Resolved

   User confirmed in-game on 2026-04-22 that Fury of the Aspects cancellation works correctly; the original bug is not reproducing. No code change required. Closed as solved per Phase 24 D-05 (CONTEXT.md). User quote: "we can ignore the need to fix evoker lust, it seems to be working correctly now".

   ---

   ```

   Then keep the ENTIRE original `## Problem` / `## Current data` / `## Possible causes` / `## Solution (for Phase 24 cleanup)` / `## Verification` sections intact below the closure note for historical reference.

3. Delete `.planning/todos/pending/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md`.

**Todo 2 — M+ Lua errors (D-06):**

4. Read the full content of `.planning/todos/pending/2026-04-22-mplus-lua-errors-secret-values-during-lust.md`.
5. Create `.planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md` with a closure note PREPENDED above the existing content (same pattern as Todo 1):

   ```markdown
   ## Closed: 2026-04-22 — Resolved (not a TBT bug)

   User reported on 2026-04-22 that the M+ Lua errors were caused by an integration issue with an unrelated addon, not TerribleBuffTracker. Closed as solved per Phase 24 D-06 (CONTEXT.md). User quote: M+ Lua errors were "related to integration with other addons, can be marked as solved".

   ---

   ```

6. Delete `.planning/todos/pending/2026-04-22-mplus-lua-errors-secret-values-during-lust.md`.

7. Verify the `.planning/todos/pending/` directory is empty after both moves.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; echo "=== done/ has both closure files ===" &amp;&amp; test -f .planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md &amp;&amp; test -f .planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md &amp;&amp; echo "both present" ; echo "=== pending/ has neither ===" &amp;&amp; test ! -f .planning/todos/pending/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md &amp;&amp; test ! -f .planning/todos/pending/2026-04-22-mplus-lua-errors-secret-values-during-lust.md &amp;&amp; echo "both deleted" ; echo "=== Closure notes present ===" &amp;&amp; grep -c "## Closed:" .planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md ; grep -c "## Closed:" .planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md` exits 0
    - `test -f .planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md` exits 0
    - `test ! -f .planning/todos/pending/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md` exits 0
    - `test ! -f .planning/todos/pending/2026-04-22-mplus-lua-errors-secret-values-during-lust.md` exits 0
    - `grep -c "## Closed:" .planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md` returns 1
    - `grep -c "## Closed:" .planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md` returns 1
    - `grep -c "## Problem" .planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md` returns 1 (original content preserved below closure note)
    - `grep -c "## Problem" .planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md` returns 1 (original content preserved below closure note)
    - `ls .planning/todos/pending/ | wc -l` returns 0
  </acceptance_criteria>
  <done>Both pending todos moved to .planning/todos/done/ with a prepended "## Closed: 2026-04-22 — Resolved" section citing the user's rationale. Original investigative content preserved in full below the closure note for history. Pending directory is empty.</done>
</task>

<task type="auto">
  <name>Task 4: Final verification sweep — DISP-04 closure gate + D-12 schema guard</name>
  <files>.planning/phases/24-cleanup/24-03-SUMMARY.md</files>
  <read_first>
    - BuffEngine.lua (confirm CURRENT_SCHEMA_VERSION unchanged per D-12)
    - All Lua files (for final DISP-04 grep sweep)
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-12 schema rule; overall exit criteria)
    - .planning/phases/24-cleanup/24-01-SUMMARY.md if exists (to cross-reference Plan 24-01's own grep results)
  </read_first>
  <action>
Final gate before Phase 24 is considered closed. This task produces the Plan 24-03 SUMMARY which also functions as the Phase 24 completion evidence.

1. Verify schema unchanged (D-12):
   ```
   grep -c "local CURRENT_SCHEMA_VERSION = 3" BuffEngine.lua
   ```
   Must return exactly 1. If it returns 0 or any other value, STOP — schema was accidentally bumped and must be reverted before Phase 24 can close.

2. Final DISP-04 dead-symbol grep sweep (re-verifies Plan 24-01's cleanup landed):
   ```
   for sym in "GetAtRestMetaIcon" "GetAtRestMetaInfo" "ResolveSuggestedSpellID" "RefreshMetaIcons" "ns\.TRINKET_FALLBACK_ORDER" "ns\.POT_FALLBACK_ORDER"; do
       echo -n "$sym: "
       grep -c "$sym" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2>/dev/null | awk -F: '{sum+=$2} END {print sum}'
   done
   ```
   Every count must be 0.

3. Final rename verification:
   ```
   grep -c "function ns:RefreshProvidersAtRest" BuffEngine.lua   # must be 1
   grep -c "ns:RefreshProvidersAtRest()" CDMTab.lua             # must be 1
   grep -c "RefreshProvidersAtRest" BuffEngine.lua CDMTab.lua Providers.lua Display.lua Core.lua EditModeFrames.lua | awk -F: '{sum+=$2} END {print sum}'
   # must be exactly 2 (one def + one call)
   ```

4. Final CSV-deletion verification:
   ```
   test ! -f pots_info.csv && test ! -f trinket_info.csv && echo "CSVs deleted"
   ```

5. Stylua final check:
   ```
   ~/.cargo/bin/stylua --check BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua
   ```
   Exit 0.

6. CHANGELOG sanity check:
   ```
   head -10 CHANGELOG.md
   ```
   First `##` entry must be `## v0.2.4`. Second `##` must be `## v0.2.3`.

7. Pending-todos sanity check:
   ```
   ls .planning/todos/pending/ | wc -l
   ```
   Must be 0.

8. Write `.planning/phases/24-cleanup/24-03-SUMMARY.md` following the standard summary template. Include a "Phase 24 Exit Evidence" section that captures the output of the above checks as the close-out proof for DISP-04. Also explicitly note in the summary that:
   - D-13 boundary was respected: no `release.bat` invocation, no `git tag`, no squash-merge.
   - Next step for the user is `/gsd:complete-milestone` to squash-merge the milestone branch, then `./scripts/release.bat 0.2.4` from main.

If ANY of the gates above fails, DO NOT write a success SUMMARY. Flag the failure and stop for user intervention.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; echo "=== D-12 schema unchanged ===" &amp;&amp; grep -c "local CURRENT_SCHEMA_VERSION = 3" BuffEngine.lua &amp;&amp; echo "=== DISP-04 dead-symbol sweep ===" &amp;&amp; for sym in GetAtRestMetaIcon GetAtRestMetaInfo ResolveSuggestedSpellID RefreshMetaIcons; do echo -n "$sym: " ; grep -l "$sym" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2&gt;/dev/null | wc -l ; done ; echo "=== RefreshProvidersAtRest ===" &amp;&amp; grep -c "RefreshProvidersAtRest" BuffEngine.lua CDMTab.lua &amp;&amp; echo "=== CSVs deleted ===" &amp;&amp; test ! -f pots_info.csv &amp;&amp; test ! -f trinket_info.csv &amp;&amp; echo "ok" ; echo "=== stylua ===" &amp;&amp; ~/.cargo/bin/stylua --check BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua &amp;&amp; echo "stylua ok" ; echo "=== CHANGELOG top ===" &amp;&amp; head -5 CHANGELOG.md | grep -c "## v0.2.4" ; echo "=== pending todos empty ===" &amp;&amp; test -z "$(ls .planning/todos/pending/ 2&gt;/dev/null)" &amp;&amp; echo "empty" ; echo "=== SUMMARY exists ===" &amp;&amp; test -f .planning/phases/24-cleanup/24-03-SUMMARY.md &amp;&amp; echo "ok"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "local CURRENT_SCHEMA_VERSION = 3" BuffEngine.lua` returns exactly 1
    - `grep -l "GetAtRestMetaIcon" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2>/dev/null | wc -l` returns 0
    - `grep -l "GetAtRestMetaInfo" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2>/dev/null | wc -l` returns 0
    - `grep -l "ResolveSuggestedSpellID" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2>/dev/null | wc -l` returns 0
    - `grep -l "RefreshMetaIcons" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2>/dev/null | wc -l` returns 0
    - `grep -c "function ns:RefreshProvidersAtRest" BuffEngine.lua` returns 1
    - `grep -c "ns:RefreshProvidersAtRest()" CDMTab.lua` returns 1
    - `test ! -f pots_info.csv && test ! -f trinket_info.csv` exits 0
    - `~/.cargo/bin/stylua --check BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua` exits 0
    - `head -5 CHANGELOG.md | grep -c "## v0.2.4"` returns 1 (v0.2.4 is at top of file)
    - `ls .planning/todos/pending/ | wc -l` returns 0
    - `test -f .planning/phases/24-cleanup/24-03-SUMMARY.md` exits 0
    - The SUMMARY contains a "Phase 24 Exit Evidence" section
    - The SUMMARY explicitly documents that no `release.bat`, no `git tag`, and no squash-merge was performed by Phase 24 (D-13 boundary respected)
  </acceptance_criteria>
  <done>All DISP-04 closure gates pass: schema unchanged, dead symbols gone, rename in place, CSVs deleted, stylua clean, CHANGELOG v0.2.4 entry at top, pending todos empty. 24-03-SUMMARY.md written with Phase 24 Exit Evidence and explicit D-13 boundary acknowledgment.</done>
</task>

</tasks>

<verification>
```bash
cd "C:/Users/jonat/Repositories/TerribleBuffTracker"

# Stylua clean
~/.cargo/bin/stylua --check BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua

# CHANGELOG v0.2.4 at top, D-07 one-liner present
grep -c "## v0.2.4" CHANGELOG.md
grep -c "Internal file reworking — no user-visible changes" CHANGELOG.md

# Schema unchanged (D-12)
grep -c "local CURRENT_SCHEMA_VERSION = 3" BuffEngine.lua

# DISP-04 dead-symbol gates (all 0)
for sym in GetAtRestMetaIcon GetAtRestMetaInfo ResolveSuggestedSpellID RefreshMetaIcons; do
    grep -l "$sym" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2>/dev/null | wc -l
done

# Todos closed
ls .planning/todos/pending/ | wc -l  # 0
test -f .planning/todos/done/2026-04-22-fury-of-the-aspects-cancellation-bug-evoker-lust.md
test -f .planning/todos/done/2026-04-22-mplus-lua-errors-secret-values-during-lust.md

# CSVs deleted (via Plan 24-01, reverified)
test ! -f pots_info.csv
test ! -f trinket_info.csv

# D-13 boundary: no tags created during Phase 24
git tag -l "v0.2.4"  # must return empty
```
</verification>

<success_criteria>
- All six Lua files pass `stylua --check` with exit 0
- `CHANGELOG.md` has `## v0.2.4 — SpellProvider Refactor` section above `## v0.2.3` with the D-07 one-liner (and optional D-08 performance note if audit returned one)
- `BuffEngine.lua` still has exactly one `local CURRENT_SCHEMA_VERSION = 3` (D-12 guard)
- All DISP-04 dead-symbol grep counts across every Lua file are 0
- `ns:RefreshProvidersAtRest` def + call = 2 total references across the codebase
- Both pending todos moved to `.planning/todos/done/` with prepended `## Closed: 2026-04-22` notes citing user rationale
- `.planning/todos/pending/` is empty
- No git tag `v0.2.4` has been created (D-13 boundary)
- No `release.bat` invocation (D-13 boundary)
- 24-03-SUMMARY.md written with Phase 24 Exit Evidence section
</success_criteria>

<output>
After completion, `.planning/phases/24-cleanup/24-03-SUMMARY.md` exists with:
- Standard summary template sections
- A "Phase 24 Exit Evidence" section showing each gate's grep/command output as close-out proof
- An explicit "D-13 Boundary Respected" block confirming no tag, no release.bat, no squash-merge was performed by Phase 24
- A "Next Steps for User" block pointing to `/gsd:complete-milestone` followed by `./scripts/release.bat 0.2.4`
</output>
