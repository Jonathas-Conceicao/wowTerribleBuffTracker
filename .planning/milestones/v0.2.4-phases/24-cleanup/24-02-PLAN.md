---
phase: 24-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/24-cleanup/24-02-AUDIT.md
autonomous: true
requirements:
  - DISP-04

must_haves:
  truths:
    - "A hot-path audit artifact file exists at .planning/phases/24-cleanup/24-02-AUDIT.md"
    - "The audit examines exactly three v0.2.4-touched hot paths: Display per-widget icon cache, OnUnitAura dispatch, ScanActiveTimersForCancellation"
    - "For each of the three paths the audit records either (a) 'no regression' finding or (b) specific file:line findings"
    - "The audit concludes with a CHANGELOG Input section providing the exact single-line performance-note string (or 'no note needed') for Plan 24-03 to copy verbatim"
    - "No Lua files are modified by this plan — audit is read-only"
  artifacts:
    - path: ".planning/phases/24-cleanup/24-02-AUDIT.md"
      provides: "Hot-path audit findings + CHANGELOG Input string for D-08"
      contains: "## CHANGELOG Input"
  key_links:
    - from: ".planning/phases/24-cleanup/24-02-AUDIT.md"
      to: "Plan 24-03 CHANGELOG task"
      via: "## CHANGELOG Input section"
      pattern: "## CHANGELOG Input"
---

<objective>
Targeted, read-only audit of the three v0.2.4-touched hot paths, per D-04. Verify no new per-frame allocations vs. the pre-refactor baseline and produce a single-line CHANGELOG input (per D-08) that Plan 24-03 copies verbatim into the v0.2.4 CHANGELOG entry. No code changes — this is analysis only.

Purpose: Decide whether the v0.2.4 CHANGELOG needs a performance note (D-07/D-08) and if so, what it says. Time-boxed to the three paths called out in CONTEXT.md; out of scope for any broader addon-wide audit (deferred list in CONTEXT.md).
Output: `.planning/phases/24-cleanup/24-02-AUDIT.md` with three findings sections and one "CHANGELOG Input" section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cleanup/24-CONTEXT.md
@BuffEngine.lua
@Providers.lua
@Display.lua
</context>

<interfaces>
<!-- Hot-path entry points being audited. Executor reads these directly from the live files. -->

Path 1 — Display per-widget icon cache (Phase 22):
```lua
-- Display.lua: cachedSpellID / cachedIcon fields on bar + icon frame widgets.
-- Change-detection pattern replaced the pre-Phase-22 metaIconsDirty flag.
-- Look for per-frame allocations in UpdateDisplay's bar/icon loops.
```

Path 2 — OnUnitAura dispatch (Phase 19):
```lua
-- BuffEngine.lua:367 OnUnitAura — unconditional ns:DispatchEventToProviders("UNIT_AURA", ...)
-- Providers.lua:592 DispatchEventToProviders — iterates eventToProviders["UNIT_AURA"]
-- Providers.lua:457 LustProviderMixin:OnTrigger — iterates updateInfo.addedAuras
```

Path 3 — ScanActiveTimersForCancellation (Phase 22 aliveBuffs iteration):
```lua
-- BuffEngine.lua:322 ScanActiveTimersForCancellation — iterates ns.activeTimers;
-- per-entry reads timer.aliveBuffs and calls C_UnitAuras.GetPlayerAuraBySpellID(buffID)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create audit artifact with Display per-widget icon cache findings (D-04 path 1)</name>
  <files>.planning/phases/24-cleanup/24-02-AUDIT.md</files>
  <read_first>
    - Display.lua (entire file — focus on UpdateDisplay, bar creation / icon creation helpers, the cachedSpellID / cachedIcon fields introduced Phase 22)
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-04 scope — "per-widget icon cache" is the specific concern)
  </read_first>
  <action>
Create `.planning/phases/24-cleanup/24-02-AUDIT.md` with a top-level heading and Task 1's section. The file will be appended to in Tasks 2-4.

1. Write the opening of the audit file:
   ```markdown
   # Phase 24 Hot-Path Audit

   **Scope:** Read-only audit of three v0.2.4-touched hot paths, per CONTEXT.md D-04.
   **Audit type:** Static code inspection. No runtime profiling (out of scope for v0.2.4 cleanup).
   **Output consumer:** Plan 24-03 reads the `## CHANGELOG Input` section at the bottom of this file and copies it verbatim into the CHANGELOG.md v0.2.4 entry.

   ---
   ```

2. Open Display.lua and find every `cachedSpellID` / `cachedIcon` read/write site:
   ```
   grep -n "cachedSpellID\|cachedIcon" Display.lua
   ```
   Record every line number in the audit file.

3. For each site, verify the change-detection pattern is:
   - Read current spellID / icon into a local
   - Compare to cached value
   - ONLY on mismatch: call SetTexture / apply new icon + update the cache
   - NO table allocation, NO closure creation, NO C_Spell.GetSpellInfo call on the cache-hit path

4. Check the UpdateDisplay accumulator pattern is preserved (per CLAUDE.md "wipe() accumulator" rule):
   - Look for `wipe(barTimers)`, `wipe(iconTimers)`, `wipe(activeBarBySpell)`, `wipe(barSlots)`, `wipe(buffSlots)`, `wipe(activeBySpell)` calls.
   - Confirm NO `local foo = {}` inside the per-buff UpdateDisplay hot loop bodies (those would allocate per frame).
   - Confirm NO `table.insert` into freshly-declared locals inside UpdateDisplay.

5. Scan for NEW per-frame allocations introduced by Phase 22:
   - `{...}` table constructor expressions inside UpdateDisplay.
   - `function()` inline closures inside UpdateDisplay.
   - Any occurrence inside the per-buff loop body is a FINDING — record file:line in the audit.

6. Append a `## Path 1: Display per-widget icon cache` section with this exact structure:
   ```markdown
   ## Path 1: Display per-widget icon cache

   **Sites found (Display.lua):**
   - Line NN: cachedSpellID [role — read/write/compare] in [function name]
   - Line MM: cachedIcon [role] in [function name]
   - ... (enumerate all)

   **Change-detection pattern verified:** yes | no
   - If no: [which site fails the pattern and why]

   **wipe() accumulator pattern preserved:** yes | no
   - Accumulators confirmed wiped each cycle: [list]
   - Any `local foo = {}` found inside hot loop body: [none | list with line numbers]

   **New per-frame allocations found:** none | [list with file:line]

   **Finding (Path 1):** [one-sentence conclusion — "no regression vs. pre-Phase-22 baseline" or "<specific issue>"]
   ```

DO NOT modify Display.lua. This is read-only audit.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; test -f .planning/phases/24-cleanup/24-02-AUDIT.md &amp;&amp; grep -c "# Phase 24 Hot-Path Audit" .planning/phases/24-cleanup/24-02-AUDIT.md &amp;&amp; grep -c "## Path 1: Display per-widget icon cache" .planning/phases/24-cleanup/24-02-AUDIT.md &amp;&amp; grep -c "Finding (Path 1):" .planning/phases/24-cleanup/24-02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - File `.planning/phases/24-cleanup/24-02-AUDIT.md` exists
    - `grep -c "# Phase 24 Hot-Path Audit" .planning/phases/24-cleanup/24-02-AUDIT.md` returns 1
    - `grep -c "## Path 1: Display per-widget icon cache" .planning/phases/24-cleanup/24-02-AUDIT.md` returns 1
    - `grep -c "Finding (Path 1):" .planning/phases/24-cleanup/24-02-AUDIT.md` returns 1
    - Path 1 section contains a "Sites found" list with at least one line-numbered entry from Display.lua (format `Line N:`)
    - `git diff --stat Display.lua` shows 0 changes
  </acceptance_criteria>
  <done>Audit file created with top-level heading and Path 1 section containing enumerated cachedSpellID/cachedIcon sites, wipe() pattern verification, per-frame allocation scan results, and a single-sentence finding.</done>
</task>

<task type="auto">
  <name>Task 2: Append Path 2 audit — UNIT_AURA dispatch flow (D-04 path 2)</name>
  <files>.planning/phases/24-cleanup/24-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/24-cleanup/24-02-AUDIT.md (current state from Task 1 — do not overwrite, APPEND)
    - BuffEngine.lua lines 367-403 (OnUnitAura dispatcher-first flow from Phase 19)
    - Providers.lua lines 9-10 (eventToProviders local declaration)
    - Providers.lua lines 571-578 (eventToProviders build loop at module load)
    - Providers.lua lines 592-609 (ns:DispatchEventToProviders implementation)
    - Providers.lua lines 457-511 (LustProviderMixin:OnTrigger implementation)
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-04 path 2 scope)
  </read_first>
  <action>
Audit the UNIT_AURA event flow for closure/table churn per event. UNIT_AURA fires frequently (every aura change on the player), so allocations here hurt most.

1. Verify eventToProviders is built ONCE at module load, never rebuilt per event:
   ```
   grep -n "eventToProviders" Providers.lua
   ```
   Expected line-number pattern:
   - One declaration `local eventToProviders = {}` near the top
   - One build loop at module load (lines ~571-578) — this is the ONLY write site
   - One read inside `ns:DispatchEventToProviders` (line ~593)
   - ZERO writes inside DispatchEventToProviders
   Record line numbers. If any write site exists outside the module-load build loop, that is a FINDING.

2. Audit `ns:DispatchEventToProviders` (Providers.lua:592-609) for per-call allocations. For each line in the function body, classify:
   - `local interested = eventToProviders[event]` — table lookup, no alloc. OK.
   - `local handled = 0` — scalar local, OK.
   - `for _, provider in ipairs(interested) do` — ipairs on existing table, OK.
   - `local proc = provider:OnTrigger(event, ...)` — allocation responsibility is on the provider itself (audited in step 3).
   - `ns.activeTimers[proc.key] = proc` — table assignment, OK.
   - `if ns.UpdateDisplay then ns:UpdateDisplay() end` — conditional function call, OK.
   FLAG any `{...}` constructors, any `function()` closures, any `string.format` / concat-heavy string builds inside the loop body.

3. Audit `LustProviderMixin:OnTrigger` (Providers.lua:457-511) for per-event allocations on the HOT path (the >99% case where no Sated debuff is in addedAuras):
   - Guards first (event name, unit, updateInfo/addedAuras nil, entry missing, entry.section=="hidden", no-restart guard) — these early-return before any allocation. CONFIRM by reading.
   - Loop `for _, aura in ipairs(updateInfo.addedAuras)` — iterates a Blizzard-provided table, no allocation.
   - Inside the loop: `issecretvalue(aura.spellId)` + `ns.SATED_DEBUFF_TO_LUST[aura.spellId]` lookup. Both non-allocating.
   - The `return { key=..., ... }` proc-table constructor only runs on a matching Sated debuff — this is ONCE per lust cast, not per frame. ACCEPTABLE.
   CONFIRM: no allocations on the no-match path (the hot path). FLAG any found.

4. Audit `BuffEngine.lua:OnUnitAura` (lines 367-403) for per-event allocations:
   - `ns:DispatchEventToProviders("UNIT_AURA", "player", updateInfo)` — passes the updateInfo reference, no new table.
   - `if C_Secrets.ShouldAurasBeSecret()` — API call returning boolean, OK.
   - `if not ns.secretGateLogged then ns.secretGateLogged = true ...` — scalar assignments + optional debug print.
   - `if updateInfo and updateInfo.isFullUpdate` — nil-safe boolean read, OK.
   - `ns:ScanActiveTimersForCancellation()` — delegated to Path 3.
   CONFIRM: no closures, no table constructors, no per-call allocations. FLAG any found.

5. APPEND (do not overwrite) to `.planning/phases/24-cleanup/24-02-AUDIT.md`:
   ```markdown
   ---

   ## Path 2: UNIT_AURA dispatch flow

   **eventToProviders built once (not per-event):** yes | no
   - Declaration site: Providers.lua:NN
   - Build-loop site (runs at module load): Providers.lua:NN-NN
   - Read sites (DispatchEventToProviders): Providers.lua:NN
   - Any write sites inside DispatchEventToProviders: none | [list]

   **DispatchEventToProviders per-call allocations:** none | [list with line numbers]

   **LustProvider:OnTrigger hot-path allocations (no-match path):** none | [list]
   - Hot path entered ~every UNIT_AURA event; proc-table allocation only on match (acceptable).

   **OnUnitAura per-event allocations:** none | [list]

   **Finding (Path 2):** [one-sentence conclusion]
   ```

DO NOT modify any Lua file. Read-only audit.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; grep -c "## Path 2: UNIT_AURA dispatch flow" .planning/phases/24-cleanup/24-02-AUDIT.md &amp;&amp; grep -c "eventToProviders built once" .planning/phases/24-cleanup/24-02-AUDIT.md &amp;&amp; grep -c "Finding (Path 2):" .planning/phases/24-cleanup/24-02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Path 2: UNIT_AURA dispatch flow" .planning/phases/24-cleanup/24-02-AUDIT.md` returns 1
    - `grep -c "eventToProviders built once" .planning/phases/24-cleanup/24-02-AUDIT.md` returns 1
    - `grep -c "Finding (Path 2):" .planning/phases/24-cleanup/24-02-AUDIT.md` returns 1
    - Path 2 section contains line-numbered references to Providers.lua for the eventToProviders declaration, build loop, and read site
    - `grep -c "## Path 1: Display per-widget icon cache" .planning/phases/24-cleanup/24-02-AUDIT.md` still returns 1 (Task 1 content preserved — APPEND not overwrite)
    - `git diff --stat BuffEngine.lua Providers.lua Display.lua` shows 0 changes
  </acceptance_criteria>
  <done>Audit file has Path 2 section appended (Task 1 content preserved), documenting eventToProviders single-build verification, per-call allocation findings for DispatchEventToProviders / LustProvider / OnUnitAura, and a Path 2 finding line.</done>
</task>

<task type="auto">
  <name>Task 3: Append Path 3 audit — ScanActiveTimersForCancellation (D-04 path 3) + CHANGELOG Input</name>
  <files>.planning/phases/24-cleanup/24-02-AUDIT.md</files>
  <read_first>
    - .planning/phases/24-cleanup/24-02-AUDIT.md (current state from Tasks 1-2 — do not overwrite, APPEND)
    - BuffEngine.lua lines 322-365 (ScanActiveTimersForCancellation post-Phase-22 aliveBuffs implementation)
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-04 path 3 scope, D-07/D-08 CHANGELOG rules)
  </read_first>
  <action>
Audit ScanActiveTimersForCancellation — called from OnUnitAura on every aura change (after the ShouldAurasBeSecret gate), so it runs frequently.

1. Audit `ns:ScanActiveTimersForCancellation` (BuffEngine.lua:322-365) line by line:
   - `local cancelledCount = 0` — scalar, OK.
   - `local cancelledLabels` — nil local (lazy-allocated only inside the `if ns.debugLogging` branch), OK.
   - `for key, timer in pairs(ns.activeTimers) do` — pairs iteration, allocates an iterator under the hood BUT this is unavoidable for pairs; not a Phase-22 regression.
   - `if timer.aliveBuffs and #timer.aliveBuffs > 0` — field read + length op, OK.
   - Inner loop `for _, buffID in ipairs(timer.aliveBuffs) do` — ipairs on an existing table, OK.
   - `C_UnitAuras.GetPlayerAuraBySpellID(buffID)` — API call; returns a table if aura present, nil if absent. NOTE: returns a NEW table on each call (Blizzard API behavior). Check whether the return value is STORED or DISCARDED:
     - Code does: `if C_UnitAuras.GetPlayerAuraBySpellID(buffID) then anyPresent = true; break end`
     - The returned table is discarded immediately (not bound to a local, not stored). WoW's GC handles it. This is Blizzard-API-induced allocation, not our regression — and pre-Phase-22 code had the same behavior on a different set of spellIDs.
   - `ns.activeTimers[key] = nil` — table assignment to nil (removal), OK.
   - The debug-log branch (`if ns.debugLogging`) lazy-allocates `cancelledLabels = {}` + `table.insert(cancelledLabels, ...)` — ONLY when debug logging is on (off by default). OK.
   - `table.concat(cancelledLabels, ", ")` — string build inside the debug-only branch. OK.

2. Compare to pre-Phase-22 baseline:
   - Pre-Phase-22 used branching on `timer.source == "lust"` / `timer.source == "trinket"` etc. Each branch performed similar aura lookups. Post-Phase-22 uses `timer.aliveBuffs` iteration — the SAME number of GetPlayerAuraBySpellID calls, just with cleaner dispatch.
   - Allocation-wise there is NO new per-frame allocation introduced by the aliveBuffs pattern.

3. Final synthesis: read all three Path findings (Path 1, Path 2, Path 3) and decide the CHANGELOG line per D-07/D-08:
   - If ALL THREE findings report "no regression" / "baseline maintained": D-08 says performance note is appended "only if a measurable improvement or regression." With zero findings either way, the correct D-08 action is NO PERFORMANCE NOTE. The CHANGELOG entry per D-07 stays a one-liner: "Internal file reworking — no user-visible changes."
   - If ANY finding reports a regression: the CHANGELOG Input section must flag the regression and Plan 24-03 will decide whether to address or document it.
   - If ANY finding reports a measurable improvement worth mentioning: the CHANGELOG Input section gives the one-sentence wording.

4. APPEND to the audit file:
   ```markdown
   ---

   ## Path 3: ScanActiveTimersForCancellation

   **Post-Phase-22 flow:** iterates ns.activeTimers; per-entry reads timer.aliveBuffs; calls C_UnitAuras.GetPlayerAuraBySpellID(buffID).

   **Per-call allocations in hot path:** none | [list with line numbers]
   - Note: C_UnitAuras.GetPlayerAuraBySpellID returns a Blizzard-allocated table on hit; we discard it immediately (no storage). Pre-Phase-22 had the same behavior; not a regression.
   - Note: The `if ns.debugLogging` branch lazy-allocates `cancelledLabels`; debug logging is off by default.

   **aliveBuffs iteration vs. pre-Phase-22 source branching:** [one-sentence comparison]

   **Finding (Path 3):** [one-sentence conclusion]

   ---

   ## CHANGELOG Input

   **Instruction to Plan 24-03:** Copy the `note_text` string below verbatim into the v0.2.4 CHANGELOG entry (after the one-liner from D-07), OR omit the note entirely if `note_text` is `"NONE"`.

   ```
   note_text: "<exact string to append to CHANGELOG.md v0.2.4 entry, or 'NONE' if no performance note is warranted>"
   ```

   **Rationale (for the planner, not for CHANGELOG):** [one-paragraph summary of the three Path findings that drove this decision]
   ```

DO NOT modify any Lua file. Read-only audit.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; grep -c "## Path 3: ScanActiveTimersForCancellation" .planning/phases/24-cleanup/24-02-AUDIT.md &amp;&amp; grep -c "Finding (Path 3):" .planning/phases/24-cleanup/24-02-AUDIT.md &amp;&amp; grep -c "## CHANGELOG Input" .planning/phases/24-cleanup/24-02-AUDIT.md &amp;&amp; grep -c "note_text:" .planning/phases/24-cleanup/24-02-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Path 3: ScanActiveTimersForCancellation" .planning/phases/24-cleanup/24-02-AUDIT.md` returns 1
    - `grep -c "Finding (Path 3):" .planning/phases/24-cleanup/24-02-AUDIT.md` returns 1
    - `grep -c "## CHANGELOG Input" .planning/phases/24-cleanup/24-02-AUDIT.md` returns 1
    - `grep -c "note_text:" .planning/phases/24-cleanup/24-02-AUDIT.md` returns exactly 1 (the single authoritative CHANGELOG-input line Plan 24-03 reads)
    - The value of `note_text` is either the literal string `"NONE"` or a single-sentence performance note — not a placeholder like "TBD" or "[fill in]"
    - Task 1 and Task 2 sections still present (`grep -c "## Path 1:" ...AUDIT.md` returns 1, `grep -c "## Path 2:" ...AUDIT.md` returns 1)
    - `git diff --stat BuffEngine.lua Providers.lua Display.lua CDMTab.lua Core.lua EditModeFrames.lua` shows 0 changes
  </acceptance_criteria>
  <done>Audit file has Path 3 section and the final CHANGELOG Input section appended. The `note_text` value is either `"NONE"` or a concrete single-sentence note that Plan 24-03 will paste verbatim. All three Path findings are captured. No Lua files were modified.</done>
</task>

</tasks>

<verification>
After all three tasks complete:

```bash
cd "C:/Users/jonat/Repositories/TerribleBuffTracker"

# Audit file exists and has all four required sections
test -f .planning/phases/24-cleanup/24-02-AUDIT.md
grep -c "# Phase 24 Hot-Path Audit" .planning/phases/24-cleanup/24-02-AUDIT.md  # 1
grep -c "## Path 1:" .planning/phases/24-cleanup/24-02-AUDIT.md                  # 1
grep -c "## Path 2:" .planning/phases/24-cleanup/24-02-AUDIT.md                  # 1
grep -c "## Path 3:" .planning/phases/24-cleanup/24-02-AUDIT.md                  # 1
grep -c "## CHANGELOG Input" .planning/phases/24-cleanup/24-02-AUDIT.md          # 1

# All three Path findings recorded
grep -c "Finding (Path 1):" .planning/phases/24-cleanup/24-02-AUDIT.md  # 1
grep -c "Finding (Path 2):" .planning/phases/24-cleanup/24-02-AUDIT.md  # 1
grep -c "Finding (Path 3):" .planning/phases/24-cleanup/24-02-AUDIT.md  # 1

# note_text for Plan 24-03
grep -c "note_text:" .planning/phases/24-cleanup/24-02-AUDIT.md  # 1

# Zero Lua modifications
git diff --stat *.lua  # empty
```
</verification>

<success_criteria>
- `.planning/phases/24-cleanup/24-02-AUDIT.md` exists with top-level heading + three Path sections + one CHANGELOG Input section
- Each Path section has an enumerated sites list with file:line references and a one-sentence Finding line
- CHANGELOG Input provides an unambiguous `note_text:` value — either `"NONE"` or a concrete single-sentence note
- Zero Lua files modified — audit is strictly read-only
- No stylua pass (not applicable — no Lua changes)
</success_criteria>

<output>
After completion, create `.planning/phases/24-cleanup/24-02-SUMMARY.md` following the standard summary template. The summary's "Key Outputs" section should highlight the `note_text:` value from the audit file so Plan 24-03's executor can pick it up without re-reading the full audit.
</output>
