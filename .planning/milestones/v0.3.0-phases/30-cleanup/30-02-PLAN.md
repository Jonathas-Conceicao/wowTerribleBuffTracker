---
phase: 30-cleanup
plan: 02
type: execute
wave: 2
depends_on: [30-01]
files_modified:
  - CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: []
# ROADMAP Phase 30: "Requirements: None (standing workflow phase per CLAUDE.md's GSD Workflow
# mandate)". Deliberately empty — no v0.3 REQ-ID is owned by this phase.

must_haves:
  truths:
    - "`CLAUDE.md`'s cleanup mandate now scopes 'unify repeated behavior' to duplication the milestone itself introduced, and says so in the rule text rather than in a phase document (D-01, D-03)"
    - "`PROJECT.md`'s 'No refactors during cleanup phases' Key Decision now says it protects pre-existing code, making the two rules readably complementary instead of contradictory (D-01, D-03)"
    - "`CLAUDE.md`'s stylua rule names the repo-root `stylua.toml` and states the bare invocation is now correct — the rule that caused the LF drift no longer causes it (D-09)"
    - "`CLAUDE.md` no longer describes `install.bat` as a retail-only copy, which Phase 26 made false"
    - "`PROJECT.md` no longer asserts v0.3 adds no user-facing features; TOOL-01 and META-01 are named as approved mid-milestone additions with their approval date (D-12)"
    - "v0.3's delivered work appears in `PROJECT.md`'s Validated list in the established per-milestone format, each entry naming its phase (D-13)"
    - "Every v0.3 Validated entry whose retail half is unproven says so in the entry — nothing outstanding is recorded as verified"
    - "`PROJECT.md` records the three substantive milestone findings: Forever ships no retail spell data, `GetScaledCursorPositionForFrame` is absent from Forever's engine, and SavedVariables persist on build `1.60.1.69913` contrary to the third-party report against `69893` (D-14)"
    - "No `.lua`, `.toc`, `.xml` or `scripts/` file is modified by this plan"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Scoped cleanup mandate, corrected stylua rule, corrected install.bat description, verified Forever build number"
      contains: "stylua.toml"
      min_lines: 59
    - path: ".planning/PROJECT.md"
      provides: "Corrected v0.3 scope statement, v0.3 Validated entries, scoped no-refactor decision, recorded milestone findings"
      contains: "1.60.1.69913"
      min_lines: 140
  key_links:
    - from: "CLAUDE.md cleanup mandate"
      to: "PROJECT.md no-refactor Key Decision"
      via: "complementary scoping clauses — milestone-introduced code vs pre-existing code"
      pattern: "pre-existing"
    - from: "CLAUDE.md stylua rule"
      to: "stylua.toml created by plan 30-01"
      via: "rule text naming the config as the reason the bare invocation is safe"
      pattern: "stylua\\.toml"
---

<objective>
Resolve the project's own rule conflict in the documents where it lives, and correct the two claims v0.3 falsified.

1. **The rule conflict (D-01, D-03)** — `CLAUDE.md` says "unify repeated behavior into shared functions" at every milestone cleanup; `PROJECT.md` says "No refactors during cleanup phases". Phase 30 is the first cleanup with real duplication to unify, so the collision finally bit. The user's decision is that the mandate covers code the milestone introduced and the prohibition protects everything older — complementary, once written down. **Writing it down is the deliverable.** D-03 is explicit that this is not optional tidying: without the amendment, the next milestone's cleanup phase rediscovers the contradiction from scratch.
2. **The stylua rule (D-09)** — "Always run `stylua`" as written is what silently converted two files to LF, because the bare invocation was the wrong one. Plan 30-01 made the bare invocation correct; this plan makes the rule say so.
3. **The false scope claim (D-12, D-13, D-14)** — `PROJECT.md` still states v0.3 adds no user-facing features. TOOL-01 (spell/aura ID tooltip) and META-01 (data-driven meta-tile hide) are both user-facing and both were added mid-milestone with explicit user approval on 2026-09-18. A constraint the shipped code violates is worse than no constraint. Alongside it: v0.3's work moves into Validated, and the milestone's three substantive findings get recorded where the next phase will read them.

Purpose: leave the project's standing rules and its state document describing what is actually true, so neither the rule conflict nor the parity claim recurs.
Output: three commits — `CLAUDE.md`, then `PROJECT.md` scope + Validated, then `PROJECT.md` decisions + findings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-cleanup/30-CONTEXT.md
@.planning/testing/FOREVER-TEST-PASS.md
@CLAUDE.md
</context>

<hard_constraints>
These bind every task in this plan. Violating any of them fails the plan.

- **Documentation only.** This plan modifies `CLAUDE.md` and `.planning/PROJECT.md` and nothing else. No `.lua`, no `.toc`, no `.xml`, no `scripts/`, no `.pkgmeta*`, no `CHANGELOG.md`, no `ROADMAP.md`, no `REQUIREMENTS.md`.
- **Do not touch the packaging-facing doc lines.** `CLAUDE.md`'s Architecture entry for `.pkgmeta` and `PROJECT.md`'s target-features bullet mentioning `.pkgmeta` both describe state that Phase 29 changes. Plan `30-04` owns them, because it is the plan gated on Phase 29 having landed. Editing them here would assert an outcome this plan cannot see.
- **Do not close v0.3's outstanding retail verification.** Two items remain provisional and are explicitly *not* this phase's to resolve (`30-CONTEXT.md` Deferred Question 2): the retail regression pass ran with a stale unsuffixed TOC still present in the deployed folder, and META-01's retail behaviour was never explicitly confirmed. Phase 25's retail gate (`TOC-01`/`VER-01`) and Phase 27's retail draggability half are both still open in `ROADMAP.md`. Every Validated entry touching them must say so in the entry itself. Do not write "verified on retail", do not mark those roadmap items complete, and do not change `ROADMAP.md` phase states.
- **No in-game claims of your own.** WoW cannot be launched from here. Everything recorded about Forever must be attributable to `.planning/testing/FOREVER-TEST-PASS.md` and dated 2026-09-18 against build `1.60.1.69913`. Quote the run-sheet's findings; do not extrapolate new ones.
- **No `git push`. No `git tag`. No `scripts/release.bat`. No `scripts/install.bat`.** Local commits only on branch `v0.3-wow-forever-compatibility`.
- **No questions.** D-01…D-21 in `30-CONTEXT.md` are locked. Anything unresolved goes under `<deferred_questions>` in the SUMMARY. Do not use AskUserQuestion.
- **No scope-reduction language anywhere in the edits.** No "v1", "for now", "placeholder", "future enhancement", "simplified". These documents are the project's memory; hedged text in them becomes next milestone's confusion.
- **Commit trailers.** Every commit ends with exactly these two lines:

  ```
  Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01UBnJrcKyhWYjBUQ8LxffBd
  ```
</hard_constraints>

<planner_findings>
Established by direct reading of this repo on 2026-09-18. Confirm each in the task that depends on it; carry all four into the SUMMARY.

1. **Both halves of the rule conflict are single lines, and they are quoted verbatim in `30-CONTEXT.md`.** `CLAUDE.md`'s is the last bullet of the `## GSD Workflow` section ("Always run a cleanup phase at the end of new milestones: clean up unused variables, definitions, unify repeated behavior into shared functions, review hot paths (especially game loop tick functions), and check release scripts"). `PROJECT.md`'s is a `## Key Decisions` table row whose Decision cell is "No refactors during cleanup phases", with outcome "✓ Good — Phase 16 + Phase 24 honored this". Amending a table row means editing the Rationale and Outcome cells; do not restructure the table, do not reorder rows, and do not touch the other rows in Task 1.

2. **Two further `CLAUDE.md` lines were falsified by v0.3 and are in scope as factual corrections, not as new rules.** The Architecture bullet reads "`scripts/install.bat` — copies addon to WoW retail addons folder", which Phase 26 replaced with an argument-free deploy to every client folder present including `_classic_beta_`. And the Key Constraints bullet pins the Forever build at `1.60.1.69893`, whereas the completed pass ran on `1.60.1.69913` at the same interface `16001` (`FOREVER-TEST-PASS.md`, "Build actually tested"). Correcting a stale fact in the same file, in the same commit class as D-09's rule fix, is planner discretion exercised in service of the phase goal ("docs corrected to match what v0.3 actually became") — record it as discretion in the SUMMARY so the widening is visible. The `.pkgmeta` Architecture bullet is *also* stale but belongs to plan `30-04`; leave it.

3. **The three findings D-14 wants recorded each have a specific, citable source — use it, do not paraphrase into vagueness.** (a) *Forever ships no retail spell data*: `FOREVER-TEST-PASS.md` "Record while you're in there" — the lust tile rendered the `134400` question-mark icon, which only happens when `C_Spell.GetSpellInfo(2825)` returns nil; that nil is the discriminator META-01 relies on. (b) *`GetScaledCursorPositionForFrame` is absent from Forever's engine*: the "Fixes landed mid-session" table — `CDMTab.lua:233` called it every frame of every drag, 53 errors in one session, fixed in `9e32f92` by reusing the file's existing `GetCursorPosition()`/`GetScale()` idiom; the defect was pre-existing since v0.2.0, not a v0.3 regression. (c) *SavedVariables persist*: Step 6 — Edit Mode positions survived `/reload` on build `1.60.1.69913`, so the third-party report of SavedVariables being written but never read back does not reproduce here, or was fixed between `69893` and `69913`. Record (c) with that hedge intact; it is a single-build observation, not a platform guarantee.

4. **The Validated list has a strict established format worth matching exactly.** Every entry is a single `- ` bullet: a description, then ` — `, then the milestone and phase (`— v0.2.4 Phase 24`, `— v0.2.1 Phase 10`, `— v0.2 Phase 3`). Entries for work shipped outside GSD use just the version (`— v0.2.5`). Longer entries pack decision IDs and mechanism detail inline. Follow that shape for v0.3 and append at the end of the list, after the `v0.2.6` entry, preserving chronological order. Note the list already contains "ns.TRINKET_FALLBACK_ORDER / ns.POT_FALLBACK_ORDER namespace exports ... all deleted" under v0.2.4 Phase 24 — that refers to the `ns.*` exports, not the file-locals; plan 30-01 removed the remaining trinket local. Do not edit that historical entry.
</planner_findings>

<tasks>

<task type="auto">
  <name>Task 1: Amend CLAUDE.md — scope the cleanup mandate, fix the stylua rule, correct two stale facts (D-01, D-03, D-09)</name>
  <files>CLAUDE.md</files>
  <action>
Confirm planner findings 1 and 2 by reading `CLAUDE.md` in full and `30-CONTEXT.md`'s `<the_rule_conflict_resolved>` section. If `CLAUDE.md`'s cleanup bullet or stylua rule does not match the text quoted in `30-CONTEXT.md`, stop and record the discrepancy under `<deferred_questions>` — the amendment has to land on the actual rule, not a remembered one.

Make four edits, all surgical. Nothing else in the file changes: not the Key Constraints about `COMBAT_LOG_EVENT_UNFILTERED` or secret values, not the Patterns section, not the Style Reference section, not the Testing section, and not the two `.pkgmeta`-facing lines reserved for plan `30-04`.

**Edit 1 — the cleanup mandate (D-01, D-03).** Rewrite the `## GSD Workflow` cleanup bullet so the unify clause carries its scope in its own text: unifying repeated behaviour applies to duplication **the milestone itself introduced**. Then add one bullet immediately after it stating the complement explicitly — that code predating the milestone is protected by `PROJECT.md`'s "No refactors during cleanup phases" decision, so the two rules are complementary rather than contradictory, and that this reading was settled by user decision on 2026-09-18. Name `PROJECT.md` in the text so a reader of either document finds the other. Keep the rest of the original bullet's content intact: unused variables and definitions, hot-path review with its "especially game loop tick functions" parenthetical, and checking release scripts.

**Edit 2 — the stylua rule (D-09).** Rewrite the `## Workflow` bullet "Always run `stylua` on Lua files after finishing a task" to state that the repo-root `stylua.toml` pins `line_endings = "Windows"`, so the bare `stylua <file>` invocation is now correct and no caller needs to remember the `--line-endings` flag, and that it must be run from the repo root so the config is discovered. Add one clause recording *why* the rule changed: the bare invocation previously reflowed files to LF, and because `core.autocrlf=true` with no `.gitattributes` makes the index `i/lf`, such a reflow produces an empty `git diff` and left no trace when it happened. A rule that does not carry its own reason gets "simplified" back to the broken form later. Do not add a `.gitattributes` recommendation — D-10 rules it out.

**Edit 3 — the `install.bat` Architecture bullet.** Replace "copies addon to WoW retail addons folder" with an accurate description of what Phase 26 shipped: an argument-free deploy of the shared file set and both flavor TOCs to every WoW client folder present on the machine, skipping absent ones and failing when none are found. Keep the bullet to one line in the style of its neighbours.

**Edit 4 — the pinned Forever build.** In the Key Constraints bullet describing the two flavor TOCs, replace the pinned build `1.60.1.69893` with the build the milestone actually verified, `1.60.1.69913`, noting the date 2026-09-18 and leaving interface `16001` unchanged (the interface did not move between those builds). Do not restate any other test result here — this is a fact correction, not a results summary.

Then commit `CLAUDE.md` alone. Suggested message: `docs(30): scope the cleanup mandate and correct the stylua rule (D-01/D-03/D-09)` with the two required trailer lines, and a body noting edits 3 and 4 as planner-discretion fact corrections.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
# D-01/D-03: the mandate now carries its scope, and points at the complementary rule.
grep -qi 'milestone' <(grep -i 'unify repeated behavior' CLAUDE.md)
grep -qi 'introduced' <(grep -i 'unify repeated behavior' CLAUDE.md)
test "$(grep -ci 'pre-existing' CLAUDE.md)" -ge 1
test "$(grep -c 'PROJECT.md' CLAUDE.md)" -ge 1
test "$(grep -ci 'No refactors during cleanup' CLAUDE.md)" -ge 1
# The original mandate content survived the rewrite.
test "$(grep -ci 'unused variables' CLAUDE.md)" -ge 1
test "$(grep -ci 'game loop tick' CLAUDE.md)" -ge 1
test "$(grep -ci 'release scripts' CLAUDE.md)" -ge 1
# D-09: the stylua rule names the config and the reason it changed.
test "$(grep -c 'stylua.toml' CLAUDE.md)" -ge 1
test "$(grep -c 'line_endings' CLAUDE.md)" -ge 1
test "$(grep -ci 'autocrlf' CLAUDE.md)" -ge 1
# D-10 respected: no .gitattributes recommendation crept in, and no such file exists.
test ! -f .gitattributes
# Edit 3: install.bat is no longer described as retail-only.
test "$(grep -c 'copies addon to WoW retail addons folder' CLAUDE.md)" -eq 0
grep -qi 'install.bat' <(grep -i 'every WoW client\|every client' CLAUDE.md)
# Edit 4: the pinned build is the one actually verified.
test "$(grep -c '1.60.1.69893' CLAUDE.md)" -eq 0
test "$(grep -c '1.60.1.69913' CLAUDE.md)" -ge 1
test "$(grep -c '16001' CLAUDE.md)" -ge 1
# Reserved for plan 30-04: the .pkgmeta Architecture bullet is untouched.
grep -q '`.pkgmeta` — BigWigs Packager config' CLAUDE.md
# No source, script or packaging file was touched, and no other doc moved.
test "$(git show --name-only --format= HEAD | grep -c .)" -eq 1
git show --name-only --format= HEAD | grep -qx 'CLAUDE.md'
test "$(git status --porcelain -- '*.lua' '*.toc' '*.xml' scripts .pkgmeta CHANGELOG.md | grep -c .)" -eq 0
# No scope-reduction language introduced.
test "$(git diff HEAD~1 HEAD -- CLAUDE.md | grep '^+' | grep -ciE '\bv1\b|for now|placeholder|future enhancement|simplified')" -eq 0
test "$(git tag --points-at HEAD | grep -c .)" -eq 0
echo "TASK 1 GATES PASS"
    </automated>
  </verify>
  <done>`CLAUDE.md`'s cleanup mandate scopes the unify clause to milestone-introduced duplication and names `PROJECT.md`'s complementary no-refactor rule; the stylua rule names `stylua.toml`, declares the bare invocation correct, and records the `autocrlf` reason it changed; `install.bat` is described as the multi-client deploy Phase 26 shipped; the pinned Forever build is `1.60.1.69913`; the `.pkgmeta` bullet is untouched; the commit contains `CLAUDE.md` alone.</done>
</task>

<task type="auto">
  <name>Task 2: PROJECT.md — retire the false parity claim and move v0.3's work into Validated (D-12, D-13)</name>
  <files>.planning/PROJECT.md</files>
  <action>
Read `.planning/PROJECT.md` in full, plus `.planning/testing/FOREVER-TEST-PASS.md` and `ROADMAP.md`'s v0.3 phase list. Confirm planner finding 4 — the Validated list's `description — milestone Phase N` shape — before writing a single entry.

**Edit 1 — the Current Milestone goal.** The `## Current Milestone` goal line currently ends "metadata and tooling only, zero new features". That is false. Rewrite it to state the milestone's actual outcome: one shared source set loading on both Midnight retail (Interface 120100) and WoW Forever beta (Interface 16001), with two user-facing additions approved mid-milestone. Keep the interface numbers.

**Edit 2 — the parity target-feature bullet.** The target-features bullet reading "Forever-specific breakage fixed to reach **parity only** — no new capability" is likewise false. Rewrite it to describe what actually happened: Forever-specific breakage was fixed with narrow defensive reads and no flavor branch, and two capabilities were added beyond parity on explicit user approval. **Do not touch the `.pkgmeta` / `release.yml` bullet in the same list** — plan `30-04` owns it.

**Edit 3 — the Constraints entry (D-12).** The `## Constraints` bullet "**Parity only**: v0.3 adds no user-facing features; Forever-specific fixes are allowed solely to reach Midnight parity" is the constraint the code violates. Replace it with one that states what v0.3 actually delivered and what still binds: Forever-specific fixes remain limited to narrow defensive reads with no flavor branch (that half held and is still load-bearing), and the parity-only scope was widened on 2026-09-18 with explicit user approval to admit TOOL-01 (spell and aura ID tooltip) and META-01 (data-driven meta-tile hide). Name both requirement IDs and the approval date. Leave the `**Cross-flavor**` constraint immediately above it untouched — one shared Lua/XML file set with no flavor-forked source is still true and still binding.

**Edit 4 — the Validated entries (D-13).** Append v0.3 entries at the end of the `### Validated` list, after the `v0.2.6` entry, in the established format. Cover, one bullet each:

- Split flavor TOCs (`_Mainline` at 120100, `_Camelot` at 16001) sharing one identical file list, with the `scripts/check-toc.ps1` pre-tag drift guard — **v0.3 Phase 25**. The entry must record that the Forever half was verified in-game on 2026-09-18 against build `1.60.1.69913` and that the retail load gate (`TOC-01`/`VER-01`) is **still open**.
- Argument-free multi-client `install.bat` deploying the shared file set and both TOCs to every client present, skipping absent ones, failing when none found — **v0.3 Phase 26**.
- Provider at-rest defensive fix: Trinket and Pot tiles never present an unresolved hardcoded fallback as real; a shared neutral placeholder with `duration = 0` keeps both tiles draggable rather than returning nil — **v0.3 Phase 27**. Record that the Forever half was confirmed in-game (all three meta tiles showed the question-mark placeholder) and the retail draggability half is **still open**.
- TOOL-01 spell and aura ID tooltip, one shared implementation behind a capability existence check on `TooltipDataProcessor.AddTooltipPostCall`, no flavor branch — **v0.3 Phase 27.1**. Note it is what unblocked Forever cast-detection testing at all.
- META-01 data-driven meta-tile hide via per-provider `HasResolvableCatalog` and the memoised `ns:IsSuggestedKeyResolvable`, plus the PAR-02 resolve-before-`SetSpellByID` guard — **v0.3 Phase 27.1**. Record that Forever hides all three tiles as designed, and that retail behaviour (tiles present and draggable) is **not explicitly confirmed**.
- Forever in-game verification pass on build `1.60.1.69913`, interface `16001`, 2026-09-18: `UNIT_SPELLCAST_SUCCEEDED` delivers a usable spellID, CDM attaches, the tab injects, bars and icons render on CDM's atlases, Edit Mode works end-to-end including position persistence across `/reload`, combat produces no uncaught aura-read error; zero failures, two Forever-only defects found and fixed (`9fde1eb`, `9e32f92`) — **v0.3 Phase 28**.

Do **not** add a Phase 29 entry — plan `30-04` owns it, because only that plan can see whether Phase 29 landed. Do not edit any pre-v0.3 entry, including the v0.2.4 Phase 24 entry that mentions the `ns.*FALLBACK_ORDER` exports.

Then commit `.planning/PROJECT.md` alone. Suggested message: `docs(30): correct v0.3's scope claim and record its delivered work (D-12/D-13)` with the two required trailer lines.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
P=.planning/PROJECT.md
# D-12: the false claims are gone in all three places.
test "$(grep -c 'zero new features' $P)" -eq 0
test "$(grep -c 'v0.3 adds no user-facing features' $P)" -eq 0
test "$(grep -c 'no new capability' $P)" -eq 0
# D-12: the replacement names both additions and the approval date.
test "$(grep -c 'TOOL-01' $P)" -ge 1
test "$(grep -c 'META-01' $P)" -ge 1
test "$(grep -c '2026-09-18' $P)" -ge 1
# The still-binding half of the old constraint survived.
grep -q 'Cross-flavor' $P
test "$(grep -ci 'no flavor branch\|no flavor-forked' $P)" -ge 1
# D-13: six new v0.3 Validated entries, in the established "— v0.3 Phase N" format.
test "$(grep -c '— v0.3 Phase ' $P)" -ge 6
grep -q '— v0.3 Phase 25' $P
grep -q '— v0.3 Phase 26' $P
grep -q '— v0.3 Phase 27' $P
grep -q '— v0.3 Phase 27.1' $P
grep -q '— v0.3 Phase 28' $P
# Phase 29 is reserved for plan 30-04.
test "$(grep -c '— v0.3 Phase 29' $P)" -eq 0
# Outstanding retail verification is NOT recorded as closed.
test "$(grep -ci 'still open' $P)" -ge 2
test "$(grep -ci 'verified on retail\|retail verified\|retail confirmed' $P)" -eq 0
# The verified build and interface are named.
test "$(grep -c '1.60.1.69913' $P)" -ge 1
test "$(grep -c '16001' $P)" -ge 1
# Reserved for plan 30-04: the .pkgmeta target-features bullet is untouched.
grep -q '`.pkgmeta` / `release.yml` produce artifacts' $P
# Historical entries untouched: the pre-v0.3 Validated list is byte-identical.
test "$(git diff HEAD~1 HEAD -- $P | grep -c '^-.*v0\.2')" -eq 0
# One doc in the commit; no source, script, packaging or roadmap file moved.
test "$(git show --name-only --format= HEAD | grep -c .)" -eq 1
git show --name-only --format= HEAD | grep -qx '.planning/PROJECT.md'
test "$(git status --porcelain -- '*.lua' '*.toc' '*.xml' scripts .pkgmeta CHANGELOG.md .planning/ROADMAP.md .planning/REQUIREMENTS.md | grep -c .)" -eq 0
test "$(git diff HEAD~1 HEAD -- $P | grep '^+' | grep -ciE '\bv1\b|for now|placeholder|future enhancement|simplified')" -eq 0
test "$(git tag --points-at HEAD | grep -c .)" -eq 0
echo "TASK 2 GATES PASS"
    </automated>
  </verify>
  <done>`PROJECT.md` no longer claims v0.3 adds no user-facing features in the milestone goal, the target-features list, or the Constraints section; the replacement constraint names TOOL-01, META-01 and the 2026-09-18 approval while preserving the still-binding no-flavor-branch and cross-flavor rules; six v0.3 Validated entries exist in the established `— v0.3 Phase N` format, with the retail-open items marked open rather than verified; no Phase 29 entry; no pre-v0.3 entry modified; the commit contains `PROJECT.md` alone.</done>
</task>

<task type="auto">
  <name>Task 3: PROJECT.md — scope the no-refactor decision and record the milestone's findings (D-01, D-03, D-14)</name>
  <files>.planning/PROJECT.md</files>
  <action>
**Edit 1 — the no-refactor Key Decision (D-01, D-03).** In the `## Key Decisions` table, amend the row whose Decision cell is "No refactors during cleanup phases". Leave the Decision cell text itself intact so the rule stays findable by its existing name. Extend the Rationale cell to state the scope settled on 2026-09-18: the rule protects code that **predates the milestone**, while `CLAUDE.md`'s cleanup mandate covers duplication the milestone itself introduced — complementary, not contradictory. Extend the Outcome cell to record that Phase 30 is where the two rules first collided and that the reading was settled by user decision, keeping the existing "Phase 16 + Phase 24 honored this" history. Do not restructure the table and do not reorder rows.

**Edit 2 — the v0.3 decision rows.** Three rows currently carry a bare `⏳ v0.3` outcome: the split-flavor-TOC decision, the `install.bat`-installs-to-every-client decision, and "v0.3 is metadata + tooling only — no Forever features". Update each Outcome cell to what is now known, and be precise about the seam:
  - split flavor TOCs — Forever half verified in-game 2026-09-18 on build `1.60.1.69913`; retail load gate still open. Keep it as `⏳`, not `✓`: the milestone has not shipped and the retail gate is unproven.
  - `install.bat` every-client deploy — shipped in Phase 26 and used to deploy every Forever test build; still `⏳` pending the milestone shipping.
  - "metadata + tooling only — no Forever features" — this decision was **superseded**. Rewrite its Outcome to record that it was widened on 2026-09-18 with explicit user approval to admit TOOL-01 and META-01, and why: TOOL-01 was a hard prerequisite for testing cast detection on Forever at all (no other way to discover Forever spell IDs), and META-01 removed three tiles that could never carry a value on that client. Then add one new row recording the widening as its own decision, with the rationale that the alternative was an untestable milestone.

**Edit 3 — the three findings (D-14).** Add the findings to the `## Context` section as bullets, each traceable to `.planning/testing/FOREVER-TEST-PASS.md` per planner finding 3. Use its evidence, not a paraphrase:
  - **Forever does not ship retail spell data.** The lust Suggested tile rendered the `134400` question-mark icon, which only occurs when `C_Spell.GetSpellInfo(2825)` returns nil. That nil is the exact discriminator META-01's data-driven hide relies on, which is why the mechanism needs no flavor check and why a client that later ships those spells shows the tiles again with no code change.
  - **`GetScaledCursorPositionForFrame` is absent from Forever's engine.** `CDMTab.lua`'s ghost-frame `OnUpdate` called it every frame of every drag — 53 errors in one session. Fixed in `9e32f92` by reusing the `GetCursorPosition()`/`GetScale()` idiom the file's three other cursor sites already used, removing the engine dependency instead of shimming it. Record that the defect was pre-existing since v0.2.0, not a v0.3 regression, and that the lesson generalises: an engine-side global present on Midnight is not guaranteed on Forever.
  - **SavedVariables do persist on build `1.60.1.69913`.** Edit Mode container positions survived `/reload`. The third-party report of addon SavedVariables being written on logout but never read back, filed against build `69893`, does not reproduce — either it was wrong or it was fixed between those builds. Keep that hedge: this is a single-build observation on a moving beta, not a platform guarantee.

**Edit 4 — Current State.** Add v0.3's status to `## Current State` without claiming a ship: the milestone is functional on Forever as of 2026-09-18 against build `1.60.1.69913`, and the two provisional retail items are named as provisional — the retail regression pass ran with a stale unsuffixed `TerribleBuffTracker.toc` still present in the deployed folder, and META-01's retail behaviour was never explicitly confirmed. Leave "Last shipped: v0.2.6" exactly as it is; v0.3 has not shipped. Update the document's trailing `*Last updated:*` line to 2026-09-18 naming Phase 30 cleanup.

Then commit `.planning/PROJECT.md` alone. Suggested message: `docs(30): scope the no-refactor rule and record v0.3's Forever findings (D-01/D-03/D-14)` with the two required trailer lines.
  </action>
  <verify>
    <automated>
set -e
cd "$(git rev-parse --show-toplevel)"
P=.planning/PROJECT.md
# D-01/D-03: the decision keeps its name and gains its scope.
test "$(grep -c 'No refactors during cleanup phases' $P)" -eq 1
grep -qi 'predate\|pre-existing' <(grep 'No refactors during cleanup phases' $P)
grep -qi 'CLAUDE.md' <(grep 'No refactors during cleanup phases' $P)
grep -q 'Phase 16 + Phase 24' $P
# Edit 2: no bare "⏳ v0.3" outcome cell survives unqualified, and the widening is a row of its own.
test "$(grep -c '| ⏳ v0.3 |' $P)" -eq 0
test "$(grep -ci 'superseded\|widened' $P)" -ge 1
test "$(grep -c 'metadata + tooling only' $P)" -ge 1
# D-14: all three findings present with their cited evidence.
test "$(grep -c '134400' $P)" -ge 1
test "$(grep -c 'C_Spell.GetSpellInfo(2825)' $P)" -ge 1
test "$(grep -c 'GetScaledCursorPositionForFrame' $P)" -ge 1
test "$(grep -c '9e32f92' $P)" -ge 1
test "$(grep -ci 'pre-existing since v0.2.0\|not a v0.3 regression' $P)" -ge 1
test "$(grep -c '69893' $P)" -ge 1
test "$(grep -c '1.60.1.69913' $P)" -ge 1
grep -qi 'savedvariables' <(grep -i 'persist' $P)
# Edit 4: v0.3 recorded as functional-but-unshipped, retail items still provisional.
grep -q 'Last shipped:\*\* v0.2.6' $P
test "$(grep -ci 'provisional' $P)" -ge 1
test "$(grep -c 'TerribleBuffTracker.toc' $P)" -ge 1
test "$(grep -ci 'verified on retail\|retail verified\|v0.3 shipped\|shipped 2026-09' $P)" -eq 0
grep -q 'Last updated: 2026-09-18' $P
# Table structure intact: same number of Key Decisions rows plus exactly one new one.
test "$(git diff HEAD~1 HEAD -- $P | grep -c '^-|')" -le 4
# One doc in the commit; nothing else moved anywhere in the repo.
test "$(git show --name-only --format= HEAD | grep -c .)" -eq 1
git show --name-only --format= HEAD | grep -qx '.planning/PROJECT.md'
test "$(git status --porcelain -- '*.lua' '*.toc' '*.xml' scripts .pkgmeta CHANGELOG.md .planning/ROADMAP.md CLAUDE.md | grep -c .)" -eq 0
test "$(git diff HEAD~1 HEAD -- $P | grep '^+' | grep -ciE '\bv1\b|for now|placeholder|future enhancement|simplified')" -eq 0
test "$(git tag --points-at HEAD | grep -c .)" -eq 0
echo "TASK 3 GATES PASS"
    </automated>
  </verify>
  <done>The "No refactors during cleanup phases" decision keeps its name and now states that it protects pre-existing code while `CLAUDE.md`'s mandate covers milestone-introduced duplication; the three `⏳ v0.3` outcome cells are qualified with what is actually known, the parity decision is recorded as superseded, and the widening has its own row; all three D-14 findings are recorded with their cited evidence including the `69893`-vs-`69913` hedge; Current State records v0.3 as functional on Forever but unshipped with both retail items named provisional; `Last shipped` still reads v0.2.6; the commit contains `PROJECT.md` alone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none crossed | Documentation-only plan. No code, no config, no script, no packaging file, no executable path. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-04 | Repudiation | Recording unverified retail results as verified would let a future reader ship on a false pass | mitigate | Gates assert the absence of "verified on retail"/"shipped" phrasing and the presence of "still open"/"provisional"; `Last shipped` asserted unchanged at v0.2.6 |
| T-30-05 | Tampering | A doc edit silently rewrites project history (Validated list, Key Decisions table) | mitigate | Gates assert zero deletions of `v0.2*` Validated lines and bound removed table rows at ≤4 |
| T-30-06 | Information disclosure | Recorded findings leak machine-local paths or credentials | accept | Findings are WoW API facts, build numbers and commit SHAs; no secrets exist in either document |
| T-30-SC | Tampering | npm/pip/cargo installs | accept | None performed. This plan runs no package manager and adds no dependency |
</threat_model>

<verification>
- `grep -c 'zero new features\|v0.3 adds no user-facing features' .planning/PROJECT.md` → `0`
- `grep -c '— v0.3 Phase ' .planning/PROJECT.md` → ≥ 6, with no Phase 29 entry
- `grep -ci 'still open' .planning/PROJECT.md` → ≥ 2; `grep -ci 'verified on retail'` → `0`
- `grep -c 'stylua.toml' CLAUDE.md` → ≥ 1; `grep -c '1.60.1.69893' CLAUDE.md` → `0`
- `grep -c '| ⏳ v0.3 |' .planning/PROJECT.md` → `0`
- `git log --oneline -3` → three doc commits, each single-file, each with the two required trailer lines
- `git status --porcelain -- '*.lua' '*.toc' '*.xml' scripts` → empty
- `git tag --points-at HEAD` → empty
</verification>

<success_criteria>
- The CLAUDE.md / PROJECT.md rule conflict is resolved **in both documents**, each naming the other, so it cannot recur at the next milestone cleanup (D-01, D-03)
- The stylua rule no longer describes the invocation that caused the LF drift (D-09)
- No document asserts a constraint the shipped code violates (D-12)
- v0.3's work is in Validated in the established format, with every unproven retail item marked unproven (D-13)
- The milestone's three substantive findings are recorded with citable evidence (D-14)
- Three atomic local doc commits, zero source files touched, no push, no tag
</success_criteria>

<output>
Create `.planning/phases/30-cleanup/30-02-SUMMARY.md` when done. It must contain: the before/after text of each amended rule (the two halves of the rule conflict quoted in full, so the resolution is auditable without a diff); the list of v0.3 Validated entries added and which carry an explicit "still open" retail caveat; the three findings as recorded, with their `FOREVER-TEST-PASS.md` sources; confirmation of planner findings 1–4 or a recorded contradiction; an explicit note that Task 1 edits 3 and 4 were planner discretion beyond `30-CONTEXT.md`'s named amendments; and a `<deferred_questions>` section (empty if none).
</output>
