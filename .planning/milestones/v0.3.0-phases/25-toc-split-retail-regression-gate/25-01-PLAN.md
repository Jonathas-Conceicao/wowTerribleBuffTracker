---
phase: 25-toc-split-retail-regression-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - TerribleBuffTracker.toc
  - TerribleBuffTracker_Mainline.toc
  - TerribleBuffTracker_Camelot.toc
autonomous: true
requirements: [TOC-01, TOC-02, TOC-03, TOC-04]

must_haves:
  truths:
    - "The repo contains exactly two TOC files, both flavor-suffixed; no unsuffixed TerribleBuffTracker.toc remains tracked"
    - "TerribleBuffTracker_Mainline.toc is byte-identical in content to the TOC that shipped v0.2.6 (rename only, zero content edits)"
    - "TerribleBuffTracker_Camelot.toc differs from _Mainline.toc on exactly two lines: ## Interface: and ## Notes:"
    - "git ls-files reports the Camelot filename with a capital C, proving the committed name survives a case-sensitive checkout"
    - "Both TOCs list the same 6 load entries, in the same order"
  artifacts:
    - path: "TerribleBuffTracker_Mainline.toc"
      provides: "Midnight retail TOC at Interface 120100"
      contains: "## Interface: 120100"
      min_lines: 18
    - path: "TerribleBuffTracker_Camelot.toc"
      provides: "WoW Forever TOC at Interface 16001"
      contains: "## Interface: 16001"
      min_lines: 18
  key_links:
    - from: "TerribleBuffTracker_Mainline.toc"
      to: "TerribleBuffTracker_Camelot.toc"
      via: "identical body outside the two allowlisted directive lines"
      pattern: "^(Core|BuffEngine|Providers|EditModeFrames|Display)\\.lua$|^CDMTab\\.xml$"
---

<objective>
Split the single `TerribleBuffTracker.toc` into the two flavor-suffixed TOC files the WoW client uses to pick a per-flavor TOC: `TerribleBuffTracker_Mainline.toc` (Interface 120100, Midnight retail) and `TerribleBuffTracker_Camelot.toc` (Interface 16001, WoW Forever). One shared Lua/XML file list, zero forked source.

Purpose: TOC-01/TOC-02/TOC-03/TOC-04. Everything downstream in v0.3 (install tooling, packaging split, Forever verification) is blocked until these two filenames exist with the exact capitalization git records.
Output: Two flavor TOCs, two isolated commits, and a machine-checked proof that their bodies have not drifted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-toc-split-retail-regression-gate/25-CONTEXT.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@TerribleBuffTracker.toc
</context>

<hard_constraints>
These bind every task in this plan. Violating any of them fails the plan.

- **No `git push`. No `git tag`. No `scripts/release.bat`.** Local commits only. The milestone branch stays local.
- **No in-game claims.** Nothing in this plan verifies that retail or Forever actually loads the addon. Do not write "VER-01 verified", "TOC-01 verified" or "TOC-02 verified" anywhere — the in-game halves of those requirements are gated by plan `25-03` and a human. This plan may only claim the filesystem facts it actually checked.
- **No questions.** If something is ambiguous, resolve it from `25-CONTEXT.md` (D-01…D-16 are locked) or record it under `<deferred_questions>` in the SUMMARY. Do not use AskUserQuestion.
- **Do not touch `scripts/install.bat`, `.pkgmeta`, `.github/workflows/release.yml`, or any `.lua`/`.xml` file.** `install.bat` still copies the now-nonexistent `TerribleBuffTracker.toc` and will therefore emit one "cannot find the file specified" line — that is expected and is Phase 26's INST-04 work. Record it, do not fix it.
- **Do not keep an unsuffixed `TerribleBuffTracker.toc` as a rollback net.** `PITFALLS.md` pitfall 1 step 3 recommends it; `REQUIREMENTS.md` Out of Scope forbids it and the user chose a clean two-TOC rename. The requirement wins.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Rename the TOC to the _Mainline flavor suffix (isolated commit)</name>
  <files>TerribleBuffTracker.toc, TerribleBuffTracker_Mainline.toc</files>
  <action>
Run `git mv TerribleBuffTracker.toc TerribleBuffTracker_Mainline.toc` from the repo root (per D-04 — `git mv`, not a copy-then-delete, so the file's history is preserved and `git status` records a rename rather than an add+delete).

Change nothing inside the file. Per D-01 and D-02 the `_Mainline` flavor diverges from today's shipping TOC on zero lines: `## Interface:` stays `120100`, `## Notes:` stays `Manual buff/cooldown timer tracking for WoW Midnight`, `## Category: Buffs & Debuffs, Combat` stays as-is (D-03), `## Version: @project-version@` must survive verbatim as a packager substitution keyword, and the 6-entry load list is untouched. Do not add `## AllowLoadGameType` (STACK.md "What NOT to Add"). Do not add `CDMTab.lua` to the load list — it loads via `<Script file="CDMTab.lua"/>` inside `CDMTab.xml`, and the TOC list is correct at 6 entries.

Note on line endings: the working tree file is CRLF, but `core.autocrlf=true` with no `.gitattributes` means the committed blob is LF. That asymmetry is pre-existing and correct — do not "fix" it, do not add a `.gitattributes`, and do not be alarmed that `git show :TerribleBuffTracker_Mainline.toc` prints LF.

Commit this rename **on its own**, with no other file in the commit (PITFALLS.md pitfall 1, step 4: a lone rename commit makes a revert or `git bisect` one clean action if a user ever reports the addon vanishing). Commit message: `refactor(25): rename TOC to TerribleBuffTracker_Mainline.toc (TOC-01, D-04)`. End the commit message with the session's attribution line.
  </action>
  <verify>
    <automated>
git ls-files | grep -qx 'TerribleBuffTracker_Mainline\.toc'
! ( git ls-files | grep -qx 'TerribleBuffTracker.toc' )
test ! -e TerribleBuffTracker.toc
test "$(grep -c $'\r' TerribleBuffTracker_Mainline.toc)" -eq 18
test "$(sed -n '1s/^## Interface: //p' TerribleBuffTracker_Mainline.toc | tr -d '\r')" = "120100"
grep -q 'WoW Midnight' TerribleBuffTracker_Mainline.toc
grep -q '@project-version@' TerribleBuffTracker_Mainline.toc
test "$(grep -cE '^(Core|BuffEngine|Providers|EditModeFrames|Display)\.lua|^CDMTab\.xml' TerribleBuffTracker_Mainline.toc)" -eq 6
git show --name-status --format= HEAD | grep -q '^R'
test "$(git show --name-only --format= HEAD | grep -c .)" -le 2
    </automated>
  </verify>
  <done>`TerribleBuffTracker_Mainline.toc` is tracked at exactly its old content (18 CRLF lines, Interface 120100, 6 load entries), the unsuffixed name is gone from both the index and the working tree, and HEAD is a rename-only commit touching nothing else.</done>
</task>

<task type="auto">
  <name>Task 2: Create TerribleBuffTracker_Camelot.toc by copy and a two-line edit</name>
  <files>TerribleBuffTracker_Camelot.toc</files>
  <action>
Create the Forever TOC by **copying** the Mainline file — never by hand-typing the name or retyping the contents (D-04; PITFALLS.md pitfall 3: a lowercase `_camelot.toc` looks identical in Explorer and to `dir` on case-insensitive NTFS, but the packager's suffix regex requires a capital `C` and the Linux CI runner checks out git's exact recorded bytes). Run `cp TerribleBuffTracker_Mainline.toc TerribleBuffTracker_Camelot.toc`.

Before editing, confirm line 1 starts with `## Interface:` and line 3 starts with `## Notes:` so the line-addressed edits below land on the right lines. Then apply exactly two substitutions that replace only the value substring, leaving each line's trailing CR intact (D-05 — the copy preserves CRLF for free; do not rewrite the whole file with a tool that normalizes line endings):
- `sed -i '1s/120100/16001/' TerribleBuffTracker_Camelot.toc` (D-01 allowlisted line 1: Interface 16001, the Forever beta value derived from build 1.60.1.69893)
- `sed -i '3s/WoW Midnight/WoW Forever/' TerribleBuffTracker_Camelot.toc` (D-02 allowlisted line 2: Notes becomes `Manual buff/cooldown timer tracking for WoW Forever`)

Nothing else may differ (D-01). Specifically leave identical: `## Title`, `## Author`, `## Version: @project-version@`, `## URL`, `## Category: Buffs & Debuffs, Combat` (D-03 — whether Forever accepts that taxonomy is an open research question; if it turns out not to, adding a third allowlisted divergence is the user's call, not this plan's), `## X-Curse-Project-ID`, `## X-Wago-ID`, `## IconTexture`, `## SavedVariables: TerribleBuffTrackerDB` (deliberately the same name — separate WTF folders per install, so no collision), the blank separator line, and all 6 load entries in the same order. Do not add `## AllowLoadGameType: camelot`; STACK.md rates it redundant under filename-suffix selection and adds an untested variable.

`git add TerribleBuffTracker_Camelot.toc`, then verify the capitalization through `git ls-files` — the index, never a Windows file browser (D-04, TOC-04). Commit: `feat(25): add TerribleBuffTracker_Camelot.toc for WoW Forever at Interface 16001 (TOC-02, TOC-03, TOC-04, D-01/D-02)`. End the commit message with the session's attribution line.

Record in the SUMMARY the exact `git ls-files` output line that proves the capital `C`, and the fact that the body diff outside the two allowlisted lines is empty.
  </action>
  <verify>
    <automated>
git ls-files | grep -qx 'TerribleBuffTracker_Camelot\.toc'
git ls-files | grep -qx 'TerribleBuffTracker_Mainline\.toc'
! ( git ls-files | grep -q 'TerribleBuffTracker_camelot.toc' )
test "$(sed -n '1s/^## Interface: //p' TerribleBuffTracker_Camelot.toc | tr -d '\r')" = "16001"
test "$(sed -n '1s/^## Interface: //p' TerribleBuffTracker_Mainline.toc | tr -d '\r')" = "120100"
grep -q 'timer tracking for WoW Forever' TerribleBuffTracker_Camelot.toc
grep -q 'Category: Buffs & Debuffs, Combat' TerribleBuffTracker_Camelot.toc
grep -q '@project-version@' TerribleBuffTracker_Camelot.toc
! grep -q 'AllowLoadGameType' TerribleBuffTracker_Camelot.toc
! grep -q 'CDMTab.lua' TerribleBuffTracker_Camelot.toc
test "$(grep -c $'\r' TerribleBuffTracker_Camelot.toc)" -eq 18
diff <(grep -vE '^## (Interface|Notes):' TerribleBuffTracker_Mainline.toc | tr -d '\r') <(grep -vE '^## (Interface|Notes):' TerribleBuffTracker_Camelot.toc | tr -d '\r')
test "$(diff <(tr -d '\r' < TerribleBuffTracker_Mainline.toc) <(tr -d '\r' < TerribleBuffTracker_Camelot.toc) | grep -c '^[<>]')" -eq 4
! ( git status --porcelain -- '*.toc' | grep . )
    </automated>
  </verify>
  <done>Both flavor TOCs are tracked with the exact capitalization `git ls-files` reports, `_Camelot.toc` declares Interface 16001 and the Forever Notes string, the full-file diff between the two files is exactly 4 changed lines (2 removed, 2 added — the Interface pair and the Notes pair), the body diff excluding those two directives is empty, and the working tree has no uncommitted TOC changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev workstation (NTFS, case-insensitive) → git index → Linux CI runner (case-sensitive) | A filename that is valid locally can be invalid downstream; the index is the only authority |
| repo → end users' retail installs | A wrong TOC filename or interface value silently removes the addon from every existing user's AddOns list, with no error dialog |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Tampering | `TerribleBuffTracker_Camelot.toc` filename case | mitigate | File is produced by `cp` from the Mainline file, never hand-typed (D-04); capitalization asserted via `git ls-files` exact-match grep, plus a negative grep for the lowercase spelling |
| T-25-02 | Denial of Service | existing retail installs | mitigate | `_Mainline.toc` is a content-free rename (`git mv`), interface pinned to 120100 by an automated assertion; the in-game proof is a separate blocking gate (plan 25-03) that no agent may sign off |
| T-25-03 | Tampering | shared Lua/XML source set | mitigate | Full-file diff asserted at exactly 4 changed lines and a body diff asserted empty, so no third line and no forked source entry can slip in |
| T-25-04 | Information Disclosure | TOC metadata | accept | TOC contains only already-public project IDs and URLs; no secrets |
| T-25-SC | Tampering | package-manager installs | accept | This plan installs nothing — no npm/pip/cargo step exists, so the supply-chain legitimacy gate has no surface here |
</threat_model>

<verification>
- `git ls-files | grep -i terriblebufftracker | grep '\.toc$'` returns exactly two lines: `TerribleBuffTracker_Camelot.toc` and `TerribleBuffTracker_Mainline.toc`.
- `git log --oneline -2` shows two separate commits: the rename, then the Camelot addition. No other file appears in either.
- `git status --porcelain` shows no modification to `.pkgmeta`, `.github/workflows/release.yml`, `scripts/`, or any `.lua`/`.xml` file.
</verification>

<success_criteria>
- TOC-02 (file half), TOC-03 and TOC-04 are satisfied on the filesystem and provable by command.
- TOC-01's filesystem half is satisfied; its in-game half remains open and is owned by plan 25-03.
- Two atomic, revertable commits exist locally. Nothing was pushed or tagged.
</success_criteria>

<output>
Create `.planning/phases/25-toc-split-retail-regression-gate/25-01-SUMMARY.md` when done.

Include in it:
- The verbatim `git ls-files` lines proving both TOC filenames and their capitalization.
- The 4-line full-file diff between the two TOCs.
- A `Deferred Questions` section carrying forward: (a) `scripts/install.bat` now fails its `.toc` copy line and cannot deploy either flavor until Phase 26 lands INST-04 — plan 25-03's checklist works around it with a manual copy; (b) D-03/research-question 1 (does Forever accept `## Category: Buffs & Debuffs, Combat`?) is still unanswered and is a user decision if it turns out negative.
</output>
