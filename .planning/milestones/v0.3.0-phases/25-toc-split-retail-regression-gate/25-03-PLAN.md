---
phase: 25-toc-split-retail-regression-gate
plan: 03
type: execute
wave: 3
depends_on: [25-01, 25-02]
files_modified:
  - .planning/phases/25-toc-split-retail-regression-gate/25-INGAME-VERIFICATION.md
autonomous: false
human_required: true
requirements: [TOC-01, TOC-02, VER-01]

must_haves:
  truths:
    - "A written checklist exists that a human can follow end-to-end without asking any follow-up questions"
    - "The checklist covers both gates of D-06: retail loads from _Mainline.toc, Forever loads from _Camelot.toc"
    - "The checklist names the stale-TOC false-pass trap and instructs deleting it before the retail test"
    - "The checklist states the D-09 non-failures and the D-11 failure protocol verbatim enough to follow under pressure"
    - "No agent-authored PASS mark exists anywhere in the artifact; every result cell starts empty"
  artifacts:
    - path: ".planning/phases/25-toc-split-retail-regression-gate/25-INGAME-VERIFICATION.md"
      provides: "Human-run checklist and result record for VER-01 and TOC-02"
      contains: "NOT RUN"
      min_lines: 80
  key_links:
    - from: "25-INGAME-VERIFICATION.md"
      to: "TerribleBuffTracker_Mainline.toc / TerribleBuffTracker_Camelot.toc"
      via: "manual deploy step listing every file to copy into each client"
      pattern: "TerribleBuffTracker_(Mainline|Camelot)\\.toc"
---

<objective>
Produce the human-run verification checklist for Phase 25's dual in-game gate (D-06), then stop. Both gates — a real retail client loading TBT from the renamed `_Mainline.toc` (VER-01), and a real Forever beta client loading it from `_Camelot.toc` (TOC-02) — require a running game client, a logged-in character, and a pair of human eyes. No agent can perform or certify them.

Purpose: VER-01 and TOC-02's in-game halves, plus TOC-01's in-game half. Phases 26, 27 and 29 are blocked until a human records a PASS here (D-06).
Output: `25-INGAME-VERIFICATION.md` — steps, expected results, non-failures, failure protocol, and an empty results table — followed by a blocking checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/25-toc-split-retail-regression-gate/25-CONTEXT.md
@.planning/phases/25-toc-split-retail-regression-gate/25-01-SUMMARY.md
@.planning/research/PITFALLS.md
@.planning/research/STACK.md
@scripts/install.bat
</context>

<hard_constraints>
The single most important constraint in this phase.

- **The agent must not claim, imply, or record that VER-01, TOC-01 or TOC-02 passed.** It cannot launch WoW, cannot log in, cannot read an AddOns list. Every result field in the artifact ships empty, the artifact's status ships as `NOT RUN`, and the SUMMARY must say the gate is open and blocking.
- **Do not mark the phase complete.** Plan 25-03 ends at a blocking checkpoint, by design.
- **No `git push`, no `git tag`, no `scripts/release.bat`.** Local commit of the artifact only.
- **No questions.** Everything needed is locked in `25-CONTEXT.md` (D-06…D-11, D-15). Unanswerable items go in `<deferred_questions>`.
- **Do not fix `scripts/install.bat`.** It still copies the deleted unsuffixed TOC and now fails that one line; Phase 26 owns INST-04. The checklist works around it with a manual copy, which D-10 explicitly permits.
</hard_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the in-game verification checklist artifact</name>
  <files>.planning/phases/25-toc-split-retail-regression-gate/25-INGAME-VERIFICATION.md</files>
  <action>
Write `25-INGAME-VERIFICATION.md` in the phase directory. Open it with a status block reading `Status: NOT RUN — awaiting human` plus the build identifiers under test (retail `wow` 12.1.0.69814 / Interface 120100; Forever `wow_classic_beta` 1.60.1.69893 / Interface 16001, both as recorded in STACK.md) and a one-line statement that no agent may fill in a result. Structure it as the sections below, in this order.

**Section 0 — Deploy (both clients).** `scripts/install.bat` is not yet flavor-aware: its `copy /Y "%SOURCE%TerribleBuffTracker.toc"` line now targets a file that no longer exists, so it prints one "The system cannot find the file specified" line and copies no TOC at all. Phase 26 fixes that (INST-04); D-10 explicitly permits a manual copy here rather than waiting. So: copy all ten files by hand into each client's `Interface\AddOns\TerribleBuffTracker` folder — `TerribleBuffTracker_Mainline.toc`, `TerribleBuffTracker_Camelot.toc`, `Core.lua`, `BuffEngine.lua`, `Providers.lua`, `EditModeFrames.lua`, `Display.lua`, `CDMTab.xml`, `CDMTab.lua`, `tbt_icon_64x64.blp`. Both TOCs go into **both** clients; the client picks, not the installer.

Retail path: `%PROGRAMFILES(x86)%\World of Warcraft\_retail_\Interface\AddOns\TerribleBuffTracker`. Forever path: `%PROGRAMFILES(x86)%\World of Warcraft\_classic_beta_\Interface\AddOns\TerribleBuffTracker`.

Flag the false-pass trap in bold, because it is the one mistake that makes the whole gate worthless: the retail folder still contains a stale `TerribleBuffTracker.toc` from the last `install.bat` run. If it is not deleted, retail will load that old unsuffixed TOC and the test will pass without proving anything about `_Mainline.toc`. **Delete `TerribleBuffTracker.toc` from both client AddOns folders before launching.** Have the reader confirm the folder afterwards contains exactly two `.toc` files, both suffixed.

Also note that the AddOns list will show the version as the literal `@project-version@` in a dev copy — that is the packager substitution keyword, not a defect.

**Section A — Gate 1: retail (VER-01, TOC-01, criteria per D-15).** Steps, each with its expected result: launch retail Midnight; at character select open the AddOns list and confirm `TerribleBuffTracker` is present and enabled, not greyed out and not "out of date"; log in and confirm the addon's normal load chat line prints; run `/tbt` and confirm the CDM settings window opens with the TBT tab present; glance at the tab's sections and confirm previously-tracked buffs are still listed. State explicitly that the last step is a confirmation glance, not a migration test — the rename touched only the TOC filename, never the addon folder name and never `## SavedVariables: TerribleBuffTrackerDB`, so the `WTF` path is structurally unchanged and no dedicated SavedVariables test is warranted (D-15).

**Section B — Gate 2: Forever (TOC-02, scope per D-08).** Run `/console scriptErrors 1` before reloading so silent Lua failures surface as a dialog. Launch the Forever beta client from `_classic_beta_`; confirm `TerribleBuffTracker` appears in the AddOns list; log in a character; confirm no Lua error at login. State the pass bar in one sentence and do not widen it: **appears in the AddOns list and loads with no Lua error at login.** This gate answers exactly one question — does the Forever client read `TerribleBuffTracker_Camelot.toc`?

**Section C — Explicitly NOT failures (D-09).** These get recorded for Phase 28 and must not fail Gate 2: the Cooldown Manager being unreachable or the TBT tab being absent on Forever; SavedVariables not persisting (a known defect on beta build 1.60.1.69893); empty Trinket, Pot or Lust tiles. Add the reason in one line — a Forever CDM problem is a Phase 28 finding, and letting it read as a TOC failure would send the next session chasing the wrong suffix.

**Section D — If Gate 2 fails (D-11).** If TBT never appears on Forever: **stop the phase and report to the user. Do not work around it.** Explicitly forbidden: auto-trying `_Vanilla.toc`, `_Forever.toc`, or any other suffix; falling back to a single unsuffixed TOC (also barred by REQUIREMENTS.md Out of Scope); adding `## AllowLoadGameType` speculatively. Collect and present these four diagnostics first, in this order:
1. The suffix Blizzard's own TOC files use inside the live client — list `_classic_beta_\Interface\AddOns\Blizzard_CooldownViewer\` and report the exact TOC filenames found. This is the strongest available ground truth, more current than any external doc.
2. The contents of `_classic_beta_\.flavor.info`.
3. The output of `/run print(C_AddOns.GetAddOnInfo("TerribleBuffTracker"))` — `nil` means the client has zero knowledge of the addon (suffix or case mismatch); a table with `reason = "INTERFACE_VERSION"` means the suffix resolved but the interface value was rejected, which is a different fix.
4. The AddOns list state — absent entirely, or present but greyed out.
Then ask the user which suffix to try next. Reproduce the symptom→cause→confirmation triage table from PITFALLS.md pitfall 3 here so the reader can tell the three failure modes apart without reopening the research.

**Section E — Observations to record while in there (free, and they close open research questions).** Cheap to capture during the same session: does Forever render `## Category: Buffs & Debuffs, Combat` sensibly or show the addon as uncategorised (research question 1, bears on D-03 — if it is rejected, adding a third allowlisted divergence line is a user decision, not a fix to apply on the spot); does the `## IconTexture` icon render on Forever (research question 2 — cosmetic, but knowing beforehand stops a missing icon being misread as a load failure); the exact Blizzard TOC filenames in the live client (research question 3, useful even on a PASS); and the version string in `_classic_beta_\.build.info` re-derived to an interface number via `major . zero-pad-2(minor) . zero-pad-2(patch)` (research question 4 — if it no longer computes to 16001, record it and report; do not bump the TOC unilaterally).

**Section F — Results.** A table with one row per check across both gates, columns `Check | Expected | Result | Notes`, every `Result` cell left empty. Below it, a sign-off block with empty fields for date, client builds actually tested, Gate 1 verdict, Gate 2 verdict, and observations. Close with a line stating that until both verdicts read PASS, Phases 26, 27 and 29 are blocked (D-06) and TOC-02 stays owned by Phase 25 rather than sliding into Phase 28 (D-07).

Commit as `docs(25): add in-game verification checklist for the retail + Forever load gate (VER-01, TOC-02, D-06/D-11)`.
  </action>
  <verify>
    <automated>
F=.planning/phases/25-toc-split-retail-regression-gate/25-INGAME-VERIFICATION.md
test -f "$F"
test "$(grep -c . "$F")" -ge 80
grep -q 'NOT RUN' "$F"
grep -q 'TerribleBuffTracker_Mainline\.toc' "$F"
grep -q 'TerribleBuffTracker_Camelot\.toc' "$F"
grep -q '_classic_beta_' "$F"
grep -q '_retail_' "$F"
grep -q 'scriptErrors 1' "$F"
grep -q 'GetAddOnInfo' "$F"
grep -q '\.flavor\.info' "$F"
grep -q 'tbt_icon_64x64\.blp' "$F"
grep -q 'CDMTab\.lua' "$F"
grep -qi 'do not' "$F" && grep -qi '_Vanilla' "$F"
# every result cell is still empty — no agent-authored verdict anywhere
! grep -qE '(Gate 1|Gate 2)[^|]{0,24}(PASS|FAIL)' "$F"
git diff --name-only HEAD~1 HEAD | grep -q '25-INGAME-VERIFICATION\.md'
    </automated>
  </verify>
  <done>The checklist exists, is committed, covers both gates with exact paths and commands, names the stale-TOC false-pass trap, carries the D-09 non-failures and the full D-11 failure protocol including all four diagnostics, and contains no verdict of any kind.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking" human_required="true">
  <name>Task 2: BLOCKING — human runs both in-game gates</name>
  <action>
Stop here. Hand the checklist to the user and wait. Do not run, simulate, infer, or partially credit either gate. Do not proceed to Phase 26, 27 or 29 — D-06 makes both gates blocking, and D-07 keeps TOC-02 owned by this phase rather than deferring it to Phase 28.

If the session ends before the user responds, that is the correct outcome: the phase stays open with the gate unresolved, and `25-INGAME-VERIFICATION.md` is the handoff.
  </action>
  <what-built>
Filesystem work only, all offline: `TerribleBuffTracker.toc` renamed to `TerribleBuffTracker_Mainline.toc` (content untouched, Interface 120100), `TerribleBuffTracker_Camelot.toc` created by copy with exactly two lines changed (Interface 16001, Notes → WoW Forever), `scripts/check-toc.ps1` drift guard wired into `scripts/release.bat` before the tag step, `CLAUDE.md` updated with the Forever reference and flavor facts, and this checklist. Nothing was pushed or tagged. Nothing has been loaded by any game client.
  </what-built>
  <how-to-verify>
Follow `.planning/phases/25-toc-split-retail-regression-gate/25-INGAME-VERIFICATION.md` end to end.

1. Section 0 — manually copy the ten files into both clients, and **delete the stale unsuffixed `TerribleBuffTracker.toc`** from each client's AddOns folder first. Skipping that deletion makes Gate 1 pass on the old TOC and proves nothing.
2. Section A — Gate 1, retail: addon present and enabled in the AddOns list, load chat line prints, `/tbt` opens the CDM window with the TBT tab, previously-tracked buffs still listed.
3. Section B — Gate 2, Forever: addon present in the AddOns list, loads with no Lua error at login. That is the entire bar (D-08).
4. Section C — a missing CDM tab, non-persisting SavedVariables, or empty Trinket/Pot/Lust tiles on Forever are Phase 28 findings, **not** Gate 2 failures (D-09).
5. If Gate 2 fails, follow Section D: collect the four diagnostics and report back. Do not try another suffix (D-11).
6. Fill in Section F and note anything from Section E worth keeping.
  </how-to-verify>
  <verify>
    <human-check>Both gate verdicts recorded in Section F of 25-INGAME-VERIFICATION.md by a human who ran the clients.</human-check>
  </verify>
  <resume-signal>
Reply with the two verdicts, e.g. `Gate 1 PASS, Gate 2 PASS` — or describe what happened. On `Gate 2 FAIL`, include the four Section D diagnostics and the next session will bring the suffix question back to you rather than guessing.
  </resume-signal>
  <done>A human has recorded both verdicts in Section F. No agent wrote either one.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent claim → requirement status | An agent-authored "verified" on an unrunnable in-game check silently converts an untested rename into a shipped regression for every existing user |
| dev machine AddOns folder → game client | A stale file left in the deploy target can satisfy the test instead of the artifact under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-09 | Spoofing | VER-01 / TOC-02 verification status | mitigate | Plan is non-autonomous and ends at a blocking `human_required` checkpoint; the artifact ships `NOT RUN` with empty result cells, and an automated gate greps for any `PASS`/`FAIL` in the table and fails the task if one exists |
| T-25-10 | Repudiation | test provenance | mitigate | Section F records the client builds actually tested alongside each verdict, so a PASS is attributable to a specific build rather than "it worked once" |
| T-25-11 | Tampering | stale deployed TOC | mitigate | Section 0 requires deleting the unsuffixed `TerribleBuffTracker.toc` from both client folders and confirming exactly two suffixed TOCs remain before launching |
| T-25-12 | Denial of Service | retail users after release | mitigate | Gate 1 is blocking and runs before any Forever-specific work; the phase cannot advance on a Gate 1 failure |
| T-25-SC | Tampering | package-manager installs | accept | No installs of any kind in this plan; a manual file copy into a local game folder is the only filesystem write outside the repo |
</threat_model>

<deferred_questions>
Not askable in this session; carry into the SUMMARY.

1. **D-03 / research question 1** — if Forever rejects `## Category: Buffs & Debuffs, Combat`, adding a third allowlisted divergence line is the user's decision. Section E captures the observation; no fix is applied on the spot.
2. **Research question 4** — if `_classic_beta_\.build.info` no longer derives to 16001, the TOC value is stale. The guard asserts the `16xxx` range, so a bump would not fail it, but the exact value is a user decision.
3. **Retail deploy is broken until Phase 26** — `install.bat` cannot copy either TOC (INST-04). The checklist works around it manually per D-10; confirm this is acceptable rather than pulling INST-04 forward.
4. **Forever deploy target at GA** — `_classic_beta_` is the beta piggyback path; `FTOOL-02` covers a dedicated `_forever_` folder if one appears.
</deferred_questions>

<verification>
- `25-INGAME-VERIFICATION.md` exists, is committed, and contains no `PASS` or `FAIL` marking.
- `git tag --list` is unchanged; `git log origin/..HEAD` shows the milestone commits are local only.
- The phase is left OPEN with the gate unresolved.
</verification>

<success_criteria>
- A human can execute the whole gate from the artifact alone, with no further agent input.
- VER-01, TOC-01's in-game half, and TOC-02's in-game half remain formally unverified and visibly blocking.
- Phases 26, 27 and 29 are documented as blocked until both verdicts read PASS (D-06).
</success_criteria>

<output>
Create `.planning/phases/25-toc-split-retail-regression-gate/25-03-SUMMARY.md` when done.

It must state plainly: the in-game gate is OPEN, VER-01 and TOC-02 are NOT verified, no client was launched, and Phases 26/27/29 are blocked pending the human run. Carry the `<deferred_questions>` list forward verbatim.
</output>
