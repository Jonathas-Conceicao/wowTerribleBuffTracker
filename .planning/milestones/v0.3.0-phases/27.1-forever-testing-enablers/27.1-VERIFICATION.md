---
phase: 27.1-forever-testing-enablers
verified: 2026-09-18T21:55:00Z
status: passed
score: 18/18 must-haves verified statically; all four deferred observable outcomes closed by the run-sheets
---

# Phase 27.1: Forever Testing Enablers Verification Report

**Phase Goal:** Forever spell IDs are discoverable in-game so cast detection can actually be tested, and the three meta tiles stop presenting themselves on a client where they can never carry a value.
**Verified:** 2026-09-18T21:55:00Z
**Status:** passed — superseded 2026-09-19, see Closure Note

## Closure Note

**Closed 2026-09-19 at milestone close.** All four deferred live observations were run by the user and recorded:

- `TOOL-01` tooltip line renders — confirmed on Forever, and widened mid-milestone at the user's request to label **both** spell and aura IDs. Worth recording precisely: the two numbers agree because `tooltipData.id` *is* the spell ID in both tooltip types — one field, not two that happen to coincide. Base/override lines are still unconfirmed because no override spell was encountered; low priority.
- Lust Suggested tile reads legibly (`PAR-02`) — confirmed.
- `META-01` in both directions — tiles hidden on Forever, all three present and draggable on retail.
- No duplicate `Spell ID:` line on TBT's own tiles — confirmed.

One follow-on defect in this phase's own code was found after the fact and fixed in `19c4ddc`: `RelatedID` compared a possibly-secret return value with no `issecretvalue()` guard, throwing when an aura tooltip was hovered in combat. The `pcall` did not cover it — the call succeeded and the comparison after it raised. Both the incoming `tooltipData.id` and every related-ID return are now screened before any comparison or concatenation, matching the rule already documented at `BuffEngine.lua:39`.

## Goal Achievement

### Observable Truths — Plan 27.1-01 (TOOL-01 + PAR-02)

Truths drawn from `27.1-01-PLAN.md`'s `must_haves.truths` frontmatter.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | A spell-tooltip post-call handler is registered once, guarded by an existence check on `TooltipDataProcessor.AddTooltipPostCall` (D-01, D-05) | VERIFIED | `Core.lua:142`: `if TooltipDataProcessor and TooltipDataProcessor.AddTooltipPostCall and Enum and Enum.TooltipDataType then` — single line, existence-shaped, not identity-shaped |
| 2 | The handler appends a single line reading `Spell ID: <n>` in grey 0.8, 0.8, 0.8 (D-03) | VERIFIED | `Core.lua:151`: `tooltip:AddLine("Spell ID: " .. spellID, 0.8, 0.8, 0.8)` — byte-identical colour to `Display.lua`'s existing format |
| 3 | The spell ID is read off `tooltipData` directly — no separate API round-trip (D-04) | VERIFIED | `Core.lua:147`: `local spellID = tooltipData.id`; zero matches for `GetDisplayedSpell\|C_TooltipInfo\|C_Spell.GetSpellName` in `Core.lua` |
| 4 | Only `Enum.TooltipDataType.Spell` is registered; no other tooltip type handler exists (D-02) | VERIFIED | Exactly one `Enum.TooltipDataType.Spell` in `Core.lua`; zero matches for Item/Unit/Aura/Currency/Mount/Quest/Achievement/Toy/EquipmentSet; zero `AllTypes`; zero pre-call/line-call registrations |
| 5 | `ns:ShowBuffTooltip` only takes `SetSpellByID` when `C_Spell.GetSpellInfo` confirms resolution; otherwise falls through to `SetText(proc.label)` (D-14) | VERIFIED | `Display.lua`: `if spellResolves then GameTooltip:SetSpellByID(spellID) else GameTooltip:SetText((proc and proc.label) or "Unknown", 1, 1, 1) end` — `spellResolves` is `spellID ~= nil and C_Spell.GetSpellInfo(spellID) ~= nil`, called exactly once |
| 6 | `LustProviderMixin:GetDisplayInfo` still returns a class-appropriate numeric spellID — `Providers.lua` untouched by this plan (D-15) | VERIFIED | `git diff HEAD~2 HEAD --name-only` (27.1-01's commit range) lists only `Core.lua`, `Display.lua` — `Providers.lua` absent |
| 7 | TBT's own tooltips show exactly one `Spell ID:` line, never two, now that the post-call also fires on `SetSpellByID` | VERIFIED | `opts.showSpellID and spellID and not spellResolves` gates TBT's own line; exactly one `"Spell ID: "` literal remains in `Display.lua`; spacer bookkeeping follows a new `addedIDLine` local instead of the old `opts.showSpellID` |
| 8 | No new Lua file; both TOCs stay identical; `check-toc.ps1` passes (D-07, D-19) | VERIFIED | `ls *.lua \| wc -l` = 6; `git status --porcelain -- '*.toc' '*.xml'` empty; `scripts/check-toc.ps1` exits 0 |
| 9 | No flavor-detection token exists in any Lua file (D-18) | VERIFIED | `grep -cE 'WOW_PROJECT_ID\|GetBuildInfo\|IsTestBuild'` over comment-stripped `*.lua` = 0 |

**Score:** 9/9 truths statically verified.

### Observable Truths — Plan 27.1-02 (META-01)

Truths drawn from `27.1-02-PLAN.md`'s `must_haves.truths` frontmatter.

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Each meta provider answers via `HasResolvableCatalog`, catalogs stay module-local (D-09) | VERIFIED | `Providers.lua`: exactly 4 `HasResolvableCatalog` definitions (base + 3 concrete), 1 call site; `TRINKET_SPELLS`/`POT_SPELLS`/`SHARED_LUST_BUFFS_LOCAL` remain module-local (`local`, not `ns.*`) |
| 2 | The test is catalog resolution, never equipped/bagged — retail tiles stay visible/draggable with nothing equipped (D-09) | VERIFIED | Zero matches for `GetInventoryItemID\|C_Item.GetItemCount\|INVSLOT_` inside `AnyCatalogSpellResolves`; inventory-API count elsewhere unchanged from baseline (3, all in `RefreshAtRest`) |
| 3 | Resolved once per key and memoised; catalogs never iterated per render (D-10) | VERIFIED | `ns:IsSuggestedKeyResolvable` hits `catalogResolvable[key]` first (`if cached ~= nil then return cached end`) before ever calling `HasResolvableCatalog()`; `CDMTab.lua`'s render loop calls the accessor once per key per render, never a catalog table |
| 4 | A tile whose catalog resolves nothing is simply not rendered — no `trackedBuffs` write (D-11) | VERIFIED | Unresolvable branch performs no `itemPool:Acquire()`, no `item:Hide()`, no `trackedBuffs` read/write; `git diff` added-lines contain zero `trackedBuffs`/`schemaVersion` mentions in either commit |
| 5 | Naming/comments describe hiding unresolvable tiles, not Forever (D-12) | VERIFIED | Comments in both files describe "does this client resolve" / "client-capability-shaped"; the word "Forever" appears only as an example case, never as the test condition itself |
| 6 | Remaining Suggested tiles keep gapless `layoutIndex`, Add square at 1, `suggestedIndex` still indexes `SUGGESTED_KEYS` (D-13) | VERIFIED | `local suggestedSlot = 1` + `item.layoutIndex = suggestedSlot` (incremented only for rendered tiles) replaces `i + 1`; `addSquare.layoutIndex = 1` unchanged; `item.suggestedIndex = i` unchanged |
| 7 | No spell API/catalog table referenced from `CDMTab.lua` (D-10) | VERIFIED | `grep -cE 'C_Spell\|TRINKET_SPELLS\|POT_SPELLS\|SHARED_LUST_BUFFS' CDMTab.lua` = 0, matching the pre-change baseline |
| 8 | Phase 27's PAR-01 invariants hold: no `FALLBACK_ORDER[1]`, `UnresolvedDisplayInfo` still sole unresolved-state return | VERIFIED | `FALLBACK_ORDER[1]` count in executable code = 0; `UnresolvedDisplayInfo(` call count = 3 (unchanged from Phase 27); both `*_FALLBACK_ORDER` arrays still declared |
| 9 | No flavor-detection token exists in any Lua file (D-08, D-18) | VERIFIED | `grep -cE 'WOW_PROJECT_ID\|GetBuildInfo\|IsTestBuild'` over comment-stripped `*.lua` = 0 |

**Score:** 9/9 truths statically verified.

**Combined score:** 18/18 code-level truths statically verified across both plans. **Not covered by either table:** whether the tooltip line, the lust tooltip, and the tile-hiding actually render correctly on a live client — this is ROADMAP Phase 27.1's observable half, owned by Phase 28, and cannot be checked from this environment. See "Human Verification Required" below.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Core.lua` | TOOL-01 registration, existence-guarded | VERIFIED — EXISTS + SUBSTANTIVE | 153 lines (≥ 130 min); contains `TooltipDataProcessor.AddTooltipPostCall`; `stylua --check` clean, CRLF preserved |
| `Display.lua` | PAR-02 resolve-before-SetSpellByID guard | VERIFIED — EXISTS + SUBSTANTIVE | 689 lines (≥ 670 min); contains `spellResolves`; `stylua --check` clean, CRLF preserved |
| `Providers.lua` | META-01 per-provider catalog test + memoised accessor | VERIFIED — EXISTS + SUBSTANTIVE | 743 lines (≥ 700 min); contains `function ns:IsSuggestedKeyResolvable`; `stylua --check` clean, CRLF preserved |
| `CDMTab.lua` | Suggested-section render filter, gapless layout | VERIFIED — EXISTS + SUBSTANTIVE | 1252 lines (≥ 1240 min); contains `ns:IsSuggestedKeyResolvable(suggestedKey)`; `stylua --check` clean, CRLF preserved (normalized from LF as expected) |

**Artifacts:** 4/4 verified.

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| `Core.lua` registration | `Enum.TooltipDataType.Spell` | `TooltipDataProcessor.AddTooltipPostCall` | WIRED | `AddTooltipPostCall(Enum.TooltipDataType.Spell` pattern present |
| post-call handler | hovered spell's ID | `tooltipData.id` read | WIRED | `tooltipData.id` pattern present, no API round-trip |
| `ns:ShowBuffTooltip` | `GameTooltip:SetSpellByID` | `spellResolves` conditional | WIRED | `if spellResolves then` pattern immediately precedes the call |
| `TrinketProviderMixin:HasResolvableCatalog` | `TRINKET_SPELLS` | shared `AnyCatalogSpellResolves` walk | WIRED | `AnyCatalogSpellResolves(TRINKET_SPELLS)` present |
| `PotProviderMixin:HasResolvableCatalog` | `POT_SPELLS` | shared `AnyCatalogSpellResolves` walk | WIRED | `AnyCatalogSpellResolves(POT_SPELLS)` present |
| `LustProviderMixin:HasResolvableCatalog` | `SHARED_LUST_BUFFS_LOCAL` | primary-plus-variant walk | WIRED | Direct `pairs`/`ipairs` walk over `SHARED_LUST_BUFFS_LOCAL`, no shared-helper reuse (table shape differs) |
| CDMTab Suggested render loop | `ns:IsSuggestedKeyResolvable` | memoised per-key boolean | WIRED | `if ns:IsSuggestedKeyResolvable(suggestedKey) then` gates tile acquisition |

**Wiring:** 7/7 connections verified statically. Runtime confirmation (does the tooltip actually render, do tiles actually hide/stay) is Phase 28's scope, not claimed here.

## Requirements Coverage

| Requirement | Status | Blocking Issue |
|-------------|--------|-----------------|
| TOOL-01: spell ID discoverable in every spell tooltip, shared implementation, no flavor branch | PARTIAL — code complete, statically verified; **live confirmation not performed** | Requires hovering a spell on a live Midnight retail client and a live Forever beta client to observe the tooltip line actually appearing. This environment cannot launch WoW. |
| META-01: Suggested meta tile hidden when its catalog resolves nothing; retail stays unaffected | PARTIAL — code complete, statically verified; **live confirmation not performed** | Requires a live retail character with nothing equipped/bagged (regression check) and a live Forever character (hiding check) to observe actual Suggested-section rendering. |
| PAR-02: Lust tile tooltip fix, narrow defensive read, no flavor branch | PARTIAL — code complete, statically verified; **live confirmation not performed** | Requires a live Forever character to observe the Lust tile's tooltip actually rendering legibly instead of empty. |

**Coverage:** 0/3 requirements fully satisfied end-to-end; 3/3 code-level implementations complete and gate-verified.

## Anti-Patterns Found

None. No `TODO`/`FIXME`/"coming soon" markers introduced. No stub data paths — `UnresolvedDisplayInfo`'s pre-existing `duration = 0` sentinel (Phase 27) is untouched and still the only placeholder shape in the codebase. `TRINKET_FALLBACK_ORDER`/`POT_FALLBACK_ORDER` remain declared-but-unreferenced by design (Phase 27 D-10, locked, handed to Phase 30). `item.suggestedIndex = i` in `CDMTab.lua` is a pre-existing Phase 23 D-13 artifact (read nowhere) left exactly as-is per the plan's explicit instruction — not a new stub introduced by this phase.

## Human Verification Required

### 1. TOOL-01 tooltip line renders in-game (ROADMAP Phase 27.1 criterion 1, observable half)
**Test:** Hover any spell (action bar, spellbook, tooltip anywhere in the game UI) on a Midnight retail client and on a Forever beta client. Observe whether a grey `Spell ID: <n>` line appears at the bottom of the tooltip.
**Expected:** The line appears on both flavors, with the ID matching the spell being hovered.
**Why human:** Requires a live WoW client. Not launchable from this execution environment.

### 2. TOOL-01 ID matches UNIT_SPELLCAST_SUCCEEDED delivery on Forever (VER-04, tracked in Phase 28)
**Test:** Cast a spell on Forever, note the ID shown by TOOL-01's tooltip, then confirm `UNIT_SPELLCAST_SUCCEEDED`'s delivered spellID matches when that same spell is added to TBT via the Add dialog.
**Expected:** IDs match, confirming Phase 28 step 4 / VER-04 can proceed using TOOL-01 as the discovery mechanism.
**Why human:** Requires a live Forever character and the actual cast-detection flow.

### 3. PAR-02 Lust tile tooltip reads legibly on Forever (ROADMAP Phase 27.1 criterion 3, observable half)
**Test:** On a Forever beta character, hover the Lust Suggested tile in the CDM settings grid.
**Expected:** A readable tooltip appears (the label fallback, or the ID line if `spellResolves` is false), never an empty tooltip frame.
**Why human:** Requires a live Forever client with the addon installed. Not launchable from this environment.

### 4. META-01 tiles hide on Forever and stay visible+draggable on retail (ROADMAP Phase 27.1 criterion 2, observable half — the single most important live observation this phase creates)
**Test:** On a Forever beta character, open CDM settings and observe whether all three meta tiles (Trinket, Pot, Lust) are absent from the Suggested section. On a retail character with nothing equipped and no potions bagged, confirm all three tiles are still present and can be dragged into a Bars/Buffs section.
**Expected:** Zero meta tiles on Forever; all three tiles present and draggable on retail — the exact split the catalog-resolution test (not an equipped/bagged test) was designed to produce.
**Why human:** Requires two live WoW clients in specific character states. A wrong catalog test would only surface here, per `27.1-02-PLAN.md`'s own framing.

## Gaps Summary

**No code-level gaps found.** All 18 `must_haves.truths` across both plans' frontmatter are statically verified: TOOL-01's registration is existence-guarded and spell-only, PAR-02's resolve-before-`SetSpellByID` guard is in place with the duplicate-line interaction resolved, and META-01's per-provider catalog test is memoised, never touches `trackedBuffs`, and keeps the render-time layout gapless.

**The four open items are not gaps in this phase's work — they are out of this phase's declared scope.** ROADMAP Phase 27.1's observable halves (does the tooltip line actually render, does the lust tooltip actually read legibly, do the tiles actually hide on Forever and stay draggable on retail) require a live WoW client and are explicitly assigned to Phase 28 by both `27.1-CONTEXT.md` and both plan files. This report's `human_needed` status reflects that unresolved observable half honestly — it is not a critical gap blocking Phase 27.1's own completion, but TOOL-01, META-01, and PAR-02 cannot be marked fully verified until Phase 28 confirms live behavior.

## Recommended Fix Plans

None. No code gaps exist. The remaining work is the live verification pass already scoped into Phase 28 — no new plan is needed inside Phase 27.1.

## Deferred Questions (carried from both plan SUMMARYs)

1. `PROJECT.md`'s "parity only" constraint is now inaccurate given TOOL-01 and META-01's deliberate scope-widening; should be amended at milestone close.
2. Both TOCs' `## Notes:` line still reads "Manual buff/cooldown timer tracking" — still accurate, noted only in case the user wants the new discoverability feature advertised.
3. A user who already holds a meta key (`"trinket"`/`"pot"`/`"lust"`) in `ns.db.trackedBuffs` will still render that bar/icon with question-mark art on a client where the catalog resolves nothing — D-11 deliberately leaves user data alone. Whether this residual case is worth addressing is a question for the user, not this phase.

## Verification Metadata

**Verification approach:** Goal-backward (derived from both plans' `must_haves` frontmatter and ROADMAP Phase 27.1's four success criteria)
**Must-haves source:** `27.1-01-PLAN.md` and `27.1-02-PLAN.md` frontmatter (`must_haves.truths`, `must_haves.artifacts`, `must_haves.key_links`)
**Automated checks:** 87 assertions across both plans' task-level `<verify>` blocks, all passed at commit time; final full-tree re-check (stylua --check on all 4 files, `git ls-files --eol` CRLF confirmation, `check-toc.ps1`, flavor-token grep) re-run clean after all four commits landed
**Human checks required:** 4 (all game-client-dependent; all explicitly owned by Phase 28)
**Total verification time:** ~45 min (shared with plan execution)

---
*Verified: 2026-09-18T21:55:00Z*
*Verifier: Claude (subagent)*
