---
phase: 49-forever-racial-catalogue
plan: 02
type: execute
wave: 2
depends_on: ["49-01"]
files_modified:
  - Providers.lua
autonomous: true
requirements: [RACE-07, RACE-08]

must_haves:
  truths:
    - "All ten Forever races collected in FOREVER-RACIALS.md have a RACIAL_SPELLS block"
    - "Gnome Eureka! reads cooldown 120, the value the beta actually reports"
    - "Orc's second racial is labelled Shatter Curse, not the Orc Racial placeholder"
    - "The two Skyborne races are separate blocks at raceID 95 and 96, sharing Walk on Air and differing on the second racial"
    - "A racial with no aura carries no duration key; a racial with no cooldown carries no cooldown key"
  artifacts:
    - path: "Providers.lua"
      provides: "RACIAL_SPELLS with ten raceID blocks and twenty-one racial rows"
      contains: "local RACIAL_SPELLS = {"
    - path: "Providers.lua"
      provides: "header comment documenting the full optional-field vocabulary the table now uses"
      contains: "cancelOnAuraLoss"
  key_links:
    - from: "Providers.lua RACIAL_SPELLS"
      to: ".planning/research/FOREVER-RACIALS.md"
      via: "every spellID, duration and cooldown copied from the collection log"
      pattern: "FOREVER-RACIALS"
    - from: "Providers.lua RACIAL_SPELLS"
      to: "ns:RacialDefsForPlayer"
      via: "raceID lookup built in 49-01"
      pattern: "RACIAL_SPELLS\\[raceID\\]"
---

<objective>
Fill `RACIAL_SPELLS` with the complete Forever roster: ten races, twenty-one racials, every spell
ID, duration and cooldown taken from `.planning/research/FOREVER-RACIALS.md`.

Purpose: this is **RACE-07** and **RACE-08** — the whole point of the phase. All data collection is
finished; this plan copies it into the table and does not gather, measure or infer anything. Two
corrections to already-shipped rows land here and must not be lost: gnome Eureka! moves from
`cooldown = 180` to `cooldown = 120` (the beta changed the ability; Phase 41's entry was correct
when written), and orc's `fallbackLabel = "Orc Racial"` placeholder becomes `"Shatter Curse"`.

Output: a ten-block table plus a rewritten header comment documenting the optional-field vocabulary
the rest of the phase consumes. **The new fields are inert after this plan.** `indefinite`,
`cancelOnAuraLoss`, `startFromAura`, `clearOnCombat` and `longDuration` are read by nothing until
49-04. That is deliberate and safe: 49-01's `if not def.duration then return nil end` guard means an
indefinite or cooldown-only row starts no buff proc and raises nothing in the interim.

Locked decisions implemented here:

| ID | Decision |
|----|----------|
| D-5 | Cultivation triggers on **20552 only**. 1312643 and 1312650 are documented but deliberately unwired — no `altTriggers`, no set matching |
| D-6 | Shadowmeld carries both an indefinite buff row **and** `cooldown = 10`; Find Treasure carries the buff row and **no** cooldown, because it genuinely has none |
| D-2 | Plainsrunning is in scope, as a tauren row with no cast and no cooldown |
| D-3 | Both Skyborne second racials carry the **short** duration plus a `longDuration` marker |
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/research/FOREVER-RACIALS.md
@.planning/phases/49-forever-racial-catalogue/49-CONTEXT.md
@.planning/phases/49-forever-racial-catalogue/49-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write all ten race blocks into RACIAL_SPELLS</name>
  <files>Providers.lua</files>
  <read_first>
    - `.planning/research/FOREVER-RACIALS.md` in full — **the single source for every number in this
      task.** Read the "Collected" section race by race. Do not take any value from this plan file
      without confirming it there; where the two disagree, FOREVER-RACIALS.md wins and the
      disagreement must be reported, not silently resolved.
    - `Providers.lua:772-806` — the `RACIAL_SPELLS` header comment and the current three-race body.
      Note the existing row style: one row per line, fields in the order
      `spellID, duration, cooldown, maxStacks, race, fallbackLabel`.
    - `Providers.lua` (post-49-01) `ns:RacialDefsForPlayer` — it copies a fixed field list onto each
      resolved def. Every field written in this task must be in that list or it will be silently
      dropped before any consumer sees it.
    - `.planning/phases/49-forever-racial-catalogue/49-CONTEXT.md` § Specific Ideas — the two
      corrections, the `fallbackLabel` rule ("always the **cast** name, never the aura's"), and the
      Skyborne 95/96 note.
  </read_first>
  <action>
    Replace the body of `local RACIAL_SPELLS` (`Providers.lua:794-806` as of 49-01) with ten blocks.
    Keep the existing style: `[raceID] = {` on its own line at one tab of indent, one `{ ... },` row
    per racial, fields in the order `spellID, duration, cooldown, maxStacks, auraID, indefinite,
    cancelOnAuraLoss, startFromAura, clearOnCombat, longDuration, race, fallbackLabel` — omitting any
    field the row does not have. `race` and `fallbackLabel` stay last so the rows read consistently.

    **Put NO comments inside the table body.** Every per-row note belongs in the header comment
    above the table (task 2). This is not style: `stylua` emits CRCRLF for a comment placed inside a
    multi-line expression, which makes git treat the file as binary, and rows carrying the optional
    booleans will exceed stylua's 120-column default and be exploded into multi-line tables.

    The exact rows, each traceable to a race section in FOREVER-RACIALS.md:

    **`[1]` Human** — `spellID = 20600, duration = 20, cooldown = 180, race = "Human", fallbackLabel = "Perception"`;
    and `spellID = 1259718, cooldown = 180, race = "Human", fallbackLabel = "Will to Survive"`. Will
    to Survive applies **no aura**, so it carries no `duration` key at all. Its 180 is user-supplied,
    not tool-read.

    **`[2]` Orc** — `spellID = 20572, duration = 15, cooldown = 120, race = "Orc", fallbackLabel = "Blood Fury"`;
    and `spellID = 1299026, duration = 8, cooldown = 180, race = "Orc", fallbackLabel = "Shatter Curse"`.
    The `"Orc Racial"` placeholder must be gone.

    **`[3]` Dwarf** — `spellID = 20594, duration = 8, cooldown = 180, race = "Dwarf", fallbackLabel = "Stoneform"`;
    and `spellID = 2481, auraID = 2481, indefinite = true, cancelOnAuraLoss = true, race = "Dwarf", fallbackLabel = "Find Treasure"`.
    Find Treasure has **no `duration` and no `cooldown`** — per D-6 its missing cooldown tile falls
    out of the data, not out of a rule.

    **`[4]` Night Elf** — `spellID = 20580, cooldown = 10, auraID = 20580, indefinite = true, cancelOnAuraLoss = true, clearOnCombat = true, race = "Night Elf", fallbackLabel = "Shadowmeld"`;
    and `spellID = 1259799, duration = 15, cooldown = 180, race = "Night Elf", fallbackLabel = "Elune's Light"`.
    Shadowmeld keeps its 10s cooldown — the user reversed an earlier decision to drop it (D-6).

    **`[5]` Undead** — `spellID = 20577, duration = 10, cooldown = 120, auraID = 20578, race = "Undead", fallbackLabel = "Cannibalize"`;
    and `spellID = 7744, cooldown = 120, race = "Undead", fallbackLabel = "Will of the Forsaken"`.
    Cannibalize is the cast/aura divergence (20577 casts, 20578 is the aura); it carries `auraID` for
    the record, but **no** `cancelOnAuraLoss` — only the three indefinite racials get aura-cancelled,
    and adding it here would change Cannibalize's shipped behaviour, which nothing asked for.

    **`[6]` Tauren** — three rows.
    `spellID = 20549, cooldown = 120, race = "Tauren", fallbackLabel = "War Stomp"`;
    `spellID = 20552, cooldown = 3600, race = "Tauren", fallbackLabel = "Cultivation"`;
    `spellID = 1299038, auraID = 1299038, indefinite = true, cancelOnAuraLoss = true, startFromAura = true, clearOnCombat = true, race = "Tauren", fallbackLabel = "Plainsrunning"`.
    Per **D-5**, Cultivation names 20552 and nothing else — do not add 1312643 or 1312650 in any
    form. Plainsrunning has no cast at all, so its aura ID 1299038 stands in as the row's identity
    (`spellID`); it has no cooldown.

    **`[7]` Gnome** — `spellID = 1259817, duration = 15, cooldown = 120, maxStacks = 3, race = "Gnome", fallbackLabel = "Eureka!"`;
    and `spellID = 20589, duration = 3, cooldown = 120, race = "Gnome", fallbackLabel = "Escape Artist"`.
    **The 120 on Eureka! is the correction** — the shipped table says 180.

    **`[8]` Troll** — `spellID = 20554, duration = 10, cooldown = 180, race = "Troll", fallbackLabel = "Berserking"`;
    and `spellID = 1260270, duration = 6, cooldown = 180, race = "Troll", fallbackLabel = "Rapid Regeneration"`.

    **`[95]` High Order Skyborne (Alliance)** — `spellID = 1259416, duration = 10, cooldown = 120, race = "Skyborne", fallbackLabel = "Walk on Air"`;
    and `spellID = 1259705, duration = 15, cooldown = 120, auraID = 1270842, longDuration = true, race = "Skyborne", fallbackLabel = "Read Ley Line"`.

    **`[96]` Windshaper Skyborne (Horde)** — `spellID = 1259416, duration = 10, cooldown = 120, race = "Skyborne", fallbackLabel = "Walk on Air"`;
    and `spellID = 1259686, duration = 30, cooldown = 120, auraID = 1259688, longDuration = true, race = "Skyborne", fallbackLabel = "Skysight"`.

    95 and 96 share Walk on Air's spellID exactly and differ entirely on the second racial. Write
    both blocks out in full; neither can be derived from the other, and copying one to the other
    would ship a wrong second racial to half the Skyborne population.

    On `fallbackLabel` for the three cast/aura divergences (Cannibalize, Read Ley Line, Skysight):
    the label is always the **cast** name, because that is what the player pressed and what the icon
    and tooltip resolve from — "Read Ley Line" not "Energized", "Skysight" not "Elemental Blessing".

    Order the blocks ascending by raceID: 1, 2, 3, 4, 5, 6, 7, 8, 95, 96.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(awk '/^\t\[[0-9]+\] = \{$/' Providers.lua | wc -l)" = "10" && B1=$(awk '/^\t\[1\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B1" && echo "$B1" | grep -q 'spellID = 20600, duration = 20, cooldown = 180' && echo "$B1" | grep -q 'spellID = 1259718' && test "$(echo "$B1" | grep -c 'Will to Survive')" = "1" && test "$(echo "$B1" | grep 'Will to Survive' | grep -c 'duration =')" = "0" && B2=$(awk '/^\t\[2\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B2" && echo "$B2" | grep -q 'Shatter Curse' && test "$(echo "$B2" | grep -c 'Orc Racial')" = "0" && echo "$B2" | grep -q 'spellID = 20572, duration = 15, cooldown = 120' && echo "$B2" | grep -q 'spellID = 1299026, duration = 8, cooldown = 180' && B3=$(awk '/^\t\[3\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B3" && echo "$B3" | grep -q 'spellID = 20594, duration = 8, cooldown = 180' && echo "$B3" | grep -q 'spellID = 2481' && test "$(echo "$B3" | grep -c 'indefinite = true')" = "1" && test "$(echo "$B3" | tr ',' '\n' | grep -c 'cooldown = ')" = "1" && B4=$(awk '/^\t\[4\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B4" && echo "$B4" | grep -q 'spellID = 20580' && echo "$B4" | grep -q 'cooldown = 10' && echo "$B4" | grep -q 'clearOnCombat = true' && echo "$B4" | grep -q 'spellID = 1259799, duration = 15, cooldown = 180' && B5=$(awk '/^\t\[5\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B5" && echo "$B5" | grep -q 'auraID = 20578' && echo "$B5" | grep -q 'spellID = 7744, cooldown = 120' && test "$(echo "$B5" | grep -c 'cancelOnAuraLoss')" = "0" && B6=$(awk '/^\t\[6\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B6" && echo "$B6" | grep -q 'spellID = 20549, cooldown = 120' && echo "$B6" | grep -q 'spellID = 20552, cooldown = 3600' && echo "$B6" | grep -q 'startFromAura = true' && echo "$B6" | grep -q 'spellID = 1299038' && test "$(echo "$B6" | grep -cE '1312643|1312650')" = "0" && B7=$(awk '/^\t\[7\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B7" && test "$(echo "$B7" | grep -c 'cooldown = 120')" = "2" && test "$(echo "$B7" | grep -c 'cooldown = 180')" = "0" && echo "$B7" | grep -q 'maxStacks = 3' && echo "$B7" | grep -q 'Escape Artist' && B8=$(awk '/^\t\[8\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B8" && echo "$B8" | grep -q 'spellID = 1260270, duration = 6, cooldown = 180' && B95=$(awk '/^\t\[95\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B95" && echo "$B95" | grep -q 'spellID = 1259416, duration = 10, cooldown = 120' && echo "$B95" | grep -q 'auraID = 1270842' && echo "$B95" | grep -q 'Read Ley Line' && B96=$(awk '/^\t\[96\] = \{$/,/^\t\},$/' Providers.lua) && test -n "$B96" && echo "$B96" | grep -q 'spellID = 1259416, duration = 10, cooldown = 120' && echo "$B96" | grep -q 'auraID = 1259688' && echo "$B96" | grep -q 'duration = 30' && echo "$B96" | grep -q 'Skysight' && test "$(grep -c 'longDuration = true' Providers.lua)" = "2" && test "$(grep -c 'startFromAura = true' Providers.lua)" = "1" && test "$(grep -c 'indefinite = true' Providers.lua)" = "3" && test "$(grep -c 'cancelOnAuraLoss = true' Providers.lua)" = "3" && test "$(grep -c 'clearOnCombat = true' Providers.lua)" = "2" && stylua . && stylua --check . && git ls-files --eol Providers.lua | grep -q 'i/lf' && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Structural assertion: `RACIAL_SPELLS` contains exactly ten `[raceID] = {` blocks, and each of
      the ten IDs 1, 2, 3, 4, 5, 6, 7, 8, 95, 96 extracts a non-empty block. Against current source
      this fails at 3 blocks.
    - Source assertion, human: `spellID = 20600, duration = 20, cooldown = 180` present; the
      Will to Survive row carries **no** `duration =` field.
    - Source assertion, orc: `Shatter Curse` present, `Orc Racial` absent (the placeholder correction).
    - Source assertion, dwarf: the block contains exactly one `cooldown = ` field across both rows —
      Stoneform's. Find Treasure has neither `duration` nor `cooldown` (D-6).
    - Source assertion, night elf: Shadowmeld's row carries `cooldown = 10` **and**
      `clearOnCombat = true` (D-6's reversal — the cooldown tile stays).
    - Source assertion, undead: `auraID = 20578` present on Cannibalize; `cancelOnAuraLoss` appears
      **zero** times in the undead block, so Cannibalize's shipped behaviour is unchanged.
    - Source assertion, tauren: three rows; `cooldown = 3600` on Cultivation; `startFromAura = true`
      on Plainsrunning; and **zero** occurrences of `1312643` or `1312650` anywhere in the block (D-5).
    - Source assertion, gnome: the block contains `cooldown = 120` twice and `cooldown = 180` zero
      times. This is the Eureka! drift correction and it fails against current source, which has
      `cooldown = 180` once.
    - Source assertion, Skyborne: blocks 95 and 96 both contain the identical Walk on Air row; 95
      carries `auraID = 1270842` and 96 carries `auraID = 1259688` and `duration = 30`.
    - Whole-file counts, which catch a field written on the wrong row: `longDuration = true` twice,
      `startFromAura = true` once, `indefinite = true` three times, `cancelOnAuraLoss = true` three
      times, `clearOnCombat = true` twice.
    - Structural assertion: `git ls-files --eol Providers.lua` reports `i/lf` — proving no comment
      landed inside the now multi-line table rows and produced CRCRLF.
  </acceptance_criteria>
  <done>All ten Forever races are in `RACIAL_SPELLS` with both corrections applied and no comment inside the table body.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the RACIAL_SPELLS header comment to document the field vocabulary</name>
  <files>Providers.lua</files>
  <read_first>
    - `Providers.lua:772-793` — the current header comment. Its paragraphs on numeric keying (a
      wrong-cased raceFile token would fail silently), on `maxStacks` being what makes a racial
      stack-driven, on Phase 44.1's list-per-race, and on `cooldown` feeding the Cooldowns tab are
      all still true and must survive. What must go is the "Absence from this table IS the 'not yet
      supported' state" framing and the "RACE-06 / RACE-07 add the remaining races in the next minor
      milestone" line.
    - `.planning/research/FOREVER-RACIALS.md` — the per-race notes each optional field exists to
      serve, and the warning at the top: "Every number here is a reading of the CURRENT BETA PATCH,
      and racials are being actively changed."
    - `.planning/phases/49-forever-racial-catalogue/49-CONTEXT.md` § Decisions — D-1, D-2, D-3, D-5,
      D-6, which are what the five new fields encode.
  </read_first>
  <action>
    Rewrite the header comment above `local RACIAL_SPELLS` so the table is self-documenting and no
    per-row comment is ever needed inside the body.

    Keep, unchanged in substance: the numeric-raceID rationale, the `maxStacks` paragraph, the
    list-per-race paragraph, and the `duration` vs `cooldown` paragraph.

    Replace the stale framing. "Absence from this table IS the 'not yet supported' state" is no
    longer the mechanism — under RACE-10 a race absent from the table contributes no suggestions at
    all, exactly as a character with no consumables contributes no item suggestions, and there is no
    tile and no message. Say that, and cite D-7 and the accepted consequence the user signed off on.
    Delete the "RACE-06 / RACE-07 add the remaining races" line; RACE-07 is done here and RACE-06
    (retail) is deferred.

    Add a field list covering every key the body now uses, each with the one reason it exists:

    - `spellID` (required) — the **cast** ID. For a racial with no cast at all (tauren Plainsrunning)
      the aura ID stands in as the row's identity, because the key namespace and every lookup are
      spellID-shaped.
    - `duration` (optional) — omitted for a racial that applies no aura, and for an indefinite one.
      `StartRacialProc` returns nil on a row without it, so the racial surfaces as its cooldown tile
      and nothing else.
    - `cooldown` (optional) — omitted for dwarf Find Treasure and tauren Plainsrunning, which
      genuinely have none. `ns:RacialCooldownKeys` emits a key only for a def that has one, so the
      missing tile falls out of the data rather than out of a rule (D-6).
    - `maxStacks` (optional) — gnome Eureka! only, the one known stacking racial.
    - `auraID` (optional) — set where the aura ID differs from the cast ID (undead Cannibalize
      20577/20578, Read Ley Line 1259705/1270842, Skysight 1259686/1259688) or where any aura read is
      needed at all.
    - `indefinite` (optional) — the buff has no natural end and renders with no countdown (D-6:
      Shadowmeld, Find Treasure, Plainsrunning). Consumed in 49-04.
    - `cancelOnAuraLoss` (optional) — the proc gets `aliveBuffs = { auraID }` so
      `ns:ScanActiveTimersForCancellation` ends it when a **readable** aura read positively reports
      the aura missing. Deliberately NOT set on Cannibalize: it has an `auraID` but a real duration,
      and aura-cancelling it would be a behaviour change nobody asked for.
    - `startFromAura` (optional) — Plainsrunning only. It has no cast, so its proc begins from
      `UNIT_AURA` rather than `UNIT_SPELLCAST_SUCCEEDED` (D-2).
    - `clearOnCombat` (optional) — the proc ends on `PLAYER_REGEN_DISABLED`. Shadowmeld, because aura
      reads are secret in combat for a tainted caller so the cancellation scan cannot see the drop
      there (D-1); Plainsrunning, because it is an out-of-combat passive that drops the instant
      combat begins (D-2).
    - `longDuration` (optional) — start on the short `duration` and correct **upward** out of combat
      when the aura's real duration reads longer (D-3). Both Skyborne second racials stretch to 15
      minutes on a condition; a racial that under-runs is recoverable, one hanging 14 minutes past
      its buff is not.
    - `race` and `fallbackLabel` (required) — `fallbackLabel` is always the **cast** name, never the
      aura's, where the two differ.

    Close with the staleness warning from FOREVER-RACIALS.md: every number is a reading of the
    current beta patch, racials are being actively changed, and a disagreement between this table and
    the client means "re-read the client", never "someone was careless" — gnome Eureka! moving from
    180 to 120 between Phase 41 and Phase 49 is the worked example. Name
    `.planning/research/FOREVER-RACIALS.md` as the source of record.

    Keep every comment line at the top level, above the `local RACIAL_SPELLS = {` statement. Nothing
    goes inside the table.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && H=$(awk 'f{print} /^-- RACIAL_SPELLS/{f=1} /^local RACIAL_SPELLS = \{$/{exit}' Providers.lua) && test -n "$H" && for t in auraID indefinite cancelOnAuraLoss startFromAura clearOnCombat longDuration maxStacks fallbackLabel; do echo "$H" | grep -q "$t" || exit 1; done && echo "$H" | grep -q 'FOREVER-RACIALS.md' && test "$(grep -c 'RACE-06 / RACE-07 add the remaining races' Providers.lua)" = "0" && test "$(grep -c 'there is no supported=false flag' Providers.lua)" = "0" && BODY=$(awk '/^local RACIAL_SPELLS = \{$/,/^\}$/' Providers.lua) && test "$(echo "$BODY" | grep -c -- '--')" = "0" && stylua . && stylua --check . && git ls-files --eol Providers.lua | grep -q 'i/lf' && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Structural assertion: the comment block between the `-- RACIAL_SPELLS` opening line and
      `local RACIAL_SPELLS = {` names every one of `auraID`, `indefinite`, `cancelOnAuraLoss`,
      `startFromAura`, `clearOnCombat`, `longDuration`, `maxStacks` and `fallbackLabel`. Against
      current source this fails — six of the eight are not mentioned anywhere.
    - Source assertion: the header cites `.planning/research/FOREVER-RACIALS.md` as the source of
      record.
    - Source assertion: the stale line "RACE-06 / RACE-07 add the remaining races in the next minor
      milestone" is gone, and the "there is no supported=false flag" framing is gone (D-7).
    - **Structural assertion, the CRCRLF guard:** the table body, from `local RACIAL_SPELLS = {` to
      its closing `}`, contains **zero** `--` comment markers. Every note lives in the header.
    - Structural assertion: `git ls-files --eol Providers.lua` reports `i/lf` and `w/crlf`.
  </acceptance_criteria>
  <done>The table documents its own field vocabulary, the D-7 framing is corrected, and no comment sits inside the body.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| research artifact → source table | hand-copied numeric data with no runtime validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-04 | Tampering (data integrity) | twenty-one hand-copied rows | mitigate | Per-row source assertions in the task gate, each naming the exact spellID/duration/cooldown triple; plus whole-file counts of the five optional booleans, which catch a field written onto the wrong row |
| T-49-05 | Repudiation | a stale value read as a bad entry | mitigate | The header comment records the beta-drift rule and names FOREVER-RACIALS.md as the source of record, with the Eureka! 180→120 case as the worked example |
| T-49-SC | Tampering | npm/pip/cargo installs | n/a | No packages installed in this phase |
</threat_model>

<verification>
1. `stylua . && stylua --check .` — clean.
2. `git ls-files --eol Providers.lua` reads `i/lf` and `w/crlf`.
3. Read `git diff Providers.lua` against `.planning/research/FOREVER-RACIALS.md` one race at a time.
   The gate proves the strings are present; only a human read proves they are the *right* strings
   for the *right* race.
4. `./scripts/install.bat`, then `/reload`: the addon loads with no Lua error on Forever, and on a
   troll `/tbt` opens with no error. Full verification is 49-05.
</verification>

<success_criteria>
- Ten raceID blocks: 1, 2, 3, 4, 5, 6, 7, 8, 95, 96.
- Twenty-one racial rows total, every field traceable to FOREVER-RACIALS.md.
- Gnome Eureka! at 120; orc's second racial labelled Shatter Curse.
- Cultivation names 20552 and nothing else.
- No comment anywhere inside the table body.
</success_criteria>

<output>
Create `.planning/phases/49-forever-racial-catalogue/49-02-SUMMARY.md` when done.
</output>
