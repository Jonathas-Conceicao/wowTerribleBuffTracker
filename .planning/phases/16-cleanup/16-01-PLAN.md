---
phase: 16-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BuffEngine.lua
  - CDMTab.lua
  - Display.lua
  - Core.lua
  - EditModeFrames.lua
  - CHANGELOG.md
  - TerribleBuffTracker.toc
  - .planning/PROJECT.md
autonomous: false
requirements: []

must_haves:
  truths:
    - "No uncommitted debug prints or commented-out code blocks remain from Phase 13/14 hotfixes"
    - "All Lua files pass stylua --check with zero changes required"
    - "CHANGELOG.md has a v0.2.3 section listing trinket meta-tracker, pot meta-tracker, and dynamic icon resolution"
    - "TerribleBuffTracker.toc Interface line covers 120000 and 120001 (already correct — no bump needed)"
    - "PROJECT.md Validated block includes all four v0.2.3 active features"
    - "release.bat no-arg invocation prints usage without attempting a tag push"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v0.2.3 release notes"
      contains: "## v0.2.3"
    - path: ".planning/PROJECT.md"
      provides: "Updated validated requirements"
      contains: "Trinket meta-tracker"
  key_links:
    - from: "CHANGELOG.md"
      to: "v0.2.3 features"
      via: "## v0.2.3 section header"
      pattern: "## v0\\.2\\.3"
    - from: ".planning/PROJECT.md"
      to: "Validated block"
      via: "### Validated"
      pattern: "Trinket meta-tracker.*v0\\.2\\.3"
---

<objective>
Release-prep cleanup for v0.2.3: dead-code scan, stylua pass, CHANGELOG entry, TOC/pkgmeta review, PROJECT.md update, and release.bat sanity check. No refactors (D-01 through D-05 — all tech debt deferred to next milestone).

Purpose: Ship a clean, correctly versioned v0.2.3 package with accurate release notes.
Output: Stylua-clean Lua files, v0.2.3 CHANGELOG entry, updated PROJECT.md, verified release.bat.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-cleanup/16-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dead-code scan and stylua pass (D-06, D-07)</name>
  <files>BuffEngine.lua, CDMTab.lua, Display.lua, Core.lua, EditModeFrames.lua</files>
  <action>
Scan all five Lua files for leftover debug bloat from Phase 13/14 hotfixes. Specifically:

1. Search for any bare `print("TBT Debug` or `print("TBT` calls that are NOT gated behind an `if ns.debugLogging then` block. The following are known-intentional and must be kept:
   - All prints in `ScanActiveTimersForCancellation`, `OnUnitAura`, `ClearAuraBlock`, and `StartLustTimer` that are already inside `if ns.debugLogging then` blocks — KEEP THESE.
   - User-facing prints (addon load message, AddTrackedBuff/RemoveTrackedBuff confirmations, schema migration print, CDM attach message, "Debug logging ON/OFF" toggle) — KEEP THESE.
   - If any ungated `print("TBT Debug` or bare debug dumps are found that are NOT in the list above — REMOVE THEM.

2. Search for commented-out code blocks (lines starting with `--` that contain what looks like disabled Lua code, not documentation). Remove any that are clearly Phase 13/14 artifacts (e.g., disabled event registrations, old SUGGESTED_BUFFS entries, old fan-out logic). Do NOT remove comment-documentation blocks (block comments explaining decisions, D-XX references, API warnings).

3. Run stylua on all five files:
   ```
   stylua BuffEngine.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua
   ```
   (stylua is at `~/.cargo/bin/stylua`)

4. Verify stylua produces no further changes:
   ```
   stylua --check BuffEngine.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua
   ```
   All files must exit 0.

DO NOT touch any of the following (D-01 through D-05):
- The duplicated icon resolution chain across CDMTab/Display/StartAllPreviewTimers
- TRINKET_FALLBACK_ORDER / POT_FALLBACK_ORDER arrays
- ns.metaIcons vs ns.metaAtRest dual cache
- metaIconsDirty flag logic
- StartAllPreviewTimers source-field filtering
  </action>
  <verify>
    <automated>cd /c/Users/jonat/Repositories/TerribleBuffTracker && ~/.cargo/bin/stylua --check BuffEngine.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua; echo "stylua exit: $?"</automated>
  </verify>
  <done>
All five Lua files pass `stylua --check` with exit code 0. No ungated debug prints remain. No commented-out Phase 13/14 code blocks remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG entry and release artifact review (D-08, D-11, D-12)</name>
  <files>CHANGELOG.md, TerribleBuffTracker.toc, .pkgmeta</files>
  <action>
**CHANGELOG.md** — Prepend a new `## v0.2.3` section above the existing `## v0.2.2` entry. Follow the established style (see v0.2.1 entry for reference). Content:

```markdown
## v0.2.3 — Trinket & Pot Meta-Trackers

### New Features
- Trinket meta-tracker slot in the Suggested section — drag to Bars or Buffs to activate; tracks all current-season on-use trinkets; only one trinket timer is ever active (newest cast wins)
- Damage pot meta-tracker slot in the Suggested section — tracks all current-season damage potions; same shared-slot behavior as trinket
- Dynamic icon resolution: trinket slot shows the icon of the first matching equipped trinket (slots 13/14); pot slot shows the icon of the first matching damage potion found in bags
- Icon resolution refreshes each time the CDM settings window opens (out of combat); falls back to the first CSV-ordered list entry if no matching gear/consumable is found
- While a timer is active, the icon switches to the specific cast spell icon; when the timer expires, the icon reverts to the resolved at-rest icon
- Tooltip for trinket/pot Suggested entries shows the buff spell name and duration of the resolved item
```

**TerribleBuffTracker.toc** — Review only. The file currently has:
```
## Interface: 120000, 120001
## Version: @project-version@
```
`@project-version@` is substituted by BigWigs Packager from the git tag — no static version string to change. Interface line already covers 120000 and 120001. No changes needed to the toc. Document this as confirmed-correct in the SUMMARY.

**pkgmeta** — Review only. Confirm `manual-changelog` points to `RELEASE_NOTES.md` (not CHANGELOG.md). This means the CurseForge/Wago release notes come from RELEASE_NOTES.md, not this CHANGELOG. No change needed to .pkgmeta. Note in the SUMMARY: CHANGELOG.md is the developer-facing changelog; RELEASE_NOTES.md is the packager-facing one. If RELEASE_NOTES.md does not exist or is empty, flag it for the user — but do NOT create it as part of this task.
  </action>
  <verify>
    <automated>cd /c/Users/jonat/Repositories/TerribleBuffTracker && grep -c "## v0.2.3" CHANGELOG.md</automated>
  </verify>
  <done>
CHANGELOG.md has exactly one `## v0.2.3` section above `## v0.2.2`. TOC and .pkgmeta reviewed and confirmed correct with rationale documented.
  </done>
</task>

<task type="auto">
  <name>Task 3: PROJECT.md update (D-10)</name>
  <files>.planning/PROJECT.md</files>
  <action>
Move the four items currently in the `### Active` block to the `### Validated` block, appending `— v0.2.3` to each. The four active items are:

```
- [ ] Trinket meta-tracker: shared slot tracking all season usable trinket buffs via cast detection, overwrite on re-proc
- [ ] Damage pot meta-tracker: shared slot tracking all damage potion buffs via cast detection, overwrite on re-proc
- [ ] Dynamic CDM icon resolution: trinket slot shows equipped trinket icon, pot slot shows bag consumable icon, refresh on CDM settings open (out of combat)
- [ ] Active timer icon switches to the specific cast spell; reverts to resolved icon when timer expires
```

Add them to `### Validated` as completed items (remove `[ ]`, append phase reference):

```
- Trinket meta-tracker: shared slot tracking all season on-use trinkets via cast detection, shared-slot overwrite (newest cast wins) — v0.2.3 Phases 12-13
- Damage pot meta-tracker: shared slot tracking all current-season damage potions via cast detection, shared-slot overwrite — v0.2.3 Phases 12-13
- Dynamic CDM icon resolution: trinket shows equipped trinket icon, pot shows bag consumable icon; refreshes on CDM settings open (out of combat); falls back to first CSV entry — v0.2.3 Phase 14
- Active timer icon switches to cast spell icon; reverts to at-rest resolved icon on expiry — v0.2.3 Phases 13-15
```

After moving, the `### Active` block should be empty (or contain only a placeholder comment). Update the `## Current Milestone` section to reflect that v0.2.3 is complete and ready for release. Update the `*Last updated:` footer to `2026-04-13 after Phase 16 cleanup`.
  </action>
  <verify>
    <automated>cd /c/Users/jonat/Repositories/TerribleBuffTracker && grep -c "Trinket meta-tracker.*v0\.2\.3" .planning/PROJECT.md</automated>
  </verify>
  <done>
All four v0.2.3 active features appear in the `### Validated` block with `— v0.2.3` annotations. The `### Active` block is empty. The milestone section reflects completion.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human review and release.bat smoke test (D-09)</name>
  <action>Human reviews CHANGELOG wording and PROJECT.md state; runs release.bat with no args to verify the usage message prints without creating a tag.</action>
  <what-built>
Dead-code scan complete, stylua clean on all 5 Lua files, CHANGELOG v0.2.3 entry written, TOC/pkgmeta confirmed, PROJECT.md updated with all four v0.2.3 features validated.
  </what-built>
  <how-to-verify>
1. Review CHANGELOG.md — confirm the v0.2.3 section reads well and captures all new features (trinket tracker, pot tracker, dynamic icon resolution, active icon switching).
2. Confirm `.planning/PROJECT.md` shows the four features in `### Validated` with `v0.2.3` annotations and an empty `### Active` block.
3. Run a smoke test of release.bat with no args to confirm it prints usage and exits cleanly (does NOT create a tag):
   ```
   scripts/release.bat
   ```
   Expected output: `Usage: release.bat <version>` and exits 1. No git tag created.
4. When satisfied, type "approved" to complete the phase. If any issues are found, describe them and the executor will fix before final commit.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
After human approval:

```bash
# stylua clean
cd /c/Users/jonat/Repositories/TerribleBuffTracker
~/.cargo/bin/stylua --check BuffEngine.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua

# CHANGELOG has v0.2.3 entry
grep "## v0.2.3" CHANGELOG.md

# PROJECT.md has validated features
grep "Trinket meta-tracker.*v0.2.3" .planning/PROJECT.md
grep "Damage pot meta-tracker.*v0.2.3" .planning/PROJECT.md
```
</verification>

<success_criteria>
- All five Lua files pass `stylua --check` with exit 0
- `CHANGELOG.md` has `## v0.2.3` section above `## v0.2.2` with new feature bullets
- `.planning/PROJECT.md` shows all four v0.2.3 active features moved to Validated
- `TerribleBuffTracker.toc` reviewed — Interface line confirmed correct, `@project-version@` confirmed as packager-managed
- `.pkgmeta` reviewed — `manual-changelog: RELEASE_NOTES.md` confirmed, no change needed
- `scripts/release.bat` smoke-tested: no-arg invocation prints usage without creating a tag
- User has approved the CHANGELOG wording and PROJECT.md state
</success_criteria>

<output>
After completion, create `.planning/phases/16-cleanup/16-01-SUMMARY.md` following the standard summary template.
</output>
