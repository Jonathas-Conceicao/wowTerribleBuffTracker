---
phase: 11-cleanup
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - CHANGELOG.md
  - .planning/REQUIREMENTS.md
  - BuffEngine.lua
  - CDMTab.lua
  - Display.lua
  - Core.lua
  - EditModeFrames.lua
autonomous: true
requirements: []

must_haves:
  truths:
    - "CHANGELOG.md has a v0.2.1 section describing aura cancellation and lust tracking"
    - "All Lua files pass stylua --check with exit 0"
    - "REQUIREMENTS.md shows LUST-02, LUST-03, LUST-04, ZONE-01 as complete"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "v0.2.1 release notes"
      contains: "v0.2.1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated requirement status"
      contains: "[x] **LUST-02**"
  key_links: []
---

<objective>
Final stylua pass on all Lua files, write v0.2.1 CHANGELOG entry, and mark completed requirements.

Purpose: Ensure codebase is formatted, documented, and requirement tracking is accurate before v0.2.1 release.
Output: Formatted Lua files, CHANGELOG.md with v0.2.1 entry, updated REQUIREMENTS.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-cleanup/11-CONTEXT.md
@.planning/phases/11-cleanup/11-RESEARCH.md
@.planning/phases/11-cleanup/11-01-SUMMARY.md
@CHANGELOG.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: stylua pass on all Lua files</name>
  <files>BuffEngine.lua, CDMTab.lua, Display.lua, Core.lua, EditModeFrames.lua</files>
  <read_first>BuffEngine.lua (first 5 lines to confirm file exists post plan-01)</read_first>
  <action>
Per D-03, run stylua on ALL Lua files in the addon root. This must happen AFTER plan 01's code changes.

```bash
~/.cargo/bin/stylua BuffEngine.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua
```

Then verify:
```bash
~/.cargo/bin/stylua --check BuffEngine.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua
```

If any file fails the check, investigate and fix the formatting issue.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" && ~/.cargo/bin/stylua --check BuffEngine.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `stylua --check BuffEngine.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua` exits with code 0
    - No formatting diff remains after the pass
  </acceptance_criteria>
  <done>All five Lua files pass stylua --check with exit 0</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG.md v0.2.1 entry and REQUIREMENTS.md status update</name>
  <files>CHANGELOG.md, .planning/REQUIREMENTS.md</files>
  <read_first>CHANGELOG.md, .planning/REQUIREMENTS.md</read_first>
  <action>
**CHANGELOG.md** — Per D-04, add a v0.2.1 section at the top (before v0.2.0). Use content from 11-RESEARCH.md "v0.2.1 Feature Summary" section:

```markdown
## v0.2.1 — Aura-Based Timer Cancellation

### New Features
- Timers for tracked buffs cancel automatically when the buff is no longer on the player
- Aura detection gated by secret value checks — automatically disabled in M+ and other restricted contexts
- Lust / Heroism timers start automatically when Sated, Exhaustion, Temporal Displacement, or Evoker Exhaustion debuffs are detected
- Class-aware lust icon: Mage sees Time Warp, Evoker sees Fury of the Aspects, others see Bloodlust
- Drag "Lust / Heroism" from the Suggested section to activate lust tracking
- Current-season drums (Drumsound) supported as a lust trigger

### Improvements
- Preview timers no longer destroy active buff countdowns when CDM settings window is opened
- Extracted shared spell resolution helper to reduce code duplication across display and config files
- Removed unused code (tbtTabActive flag)
```

**REQUIREMENTS.md** — Mark the following as complete (change `[ ]` to `[x]`):
- `LUST-02` (line 21) — Lust is represented as a single meta-buff in CDM tab
- `LUST-03` (line 22) — Meta-buff icon defaults to class-specific lust icon
- `LUST-04` (line 23) — CDM tab entry shows gray detail text
- `ZONE-01` (line 27) — Addon scans on zone transitions

Also update the Traceability table:
- ZONE-01: Status from "Pending" to "Complete"
- LUST-02: Status from "Pending" to "Complete"
- LUST-03: Status from "Pending" to "Complete"
- LUST-04: Status from "Pending" to "Complete"

**Release workflow constraint:** The v0.2.1-aura-cancellation branch MUST be squash-merged to main before `./scripts/release.bat v0.2.1` is called. This task does NOT run release.bat — it prepares the changelog and requirements so the branch is release-ready. The actual merge-and-release is a separate step after this plan completes.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" && grep -c "v0.2.1" CHANGELOG.md && grep "\[x\] \*\*LUST-02\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*LUST-03\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*LUST-04\*\*" .planning/REQUIREMENTS.md && grep "\[x\] \*\*ZONE-01\*\*" .planning/REQUIREMENTS.md && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "v0.2.1" CHANGELOG.md` finds at least 1 match
    - `grep "\[x\] \*\*LUST-02\*\*" .planning/REQUIREMENTS.md` finds 1 match
    - `grep "\[x\] \*\*LUST-03\*\*" .planning/REQUIREMENTS.md` finds 1 match
    - `grep "\[x\] \*\*LUST-04\*\*" .planning/REQUIREMENTS.md` finds 1 match
    - `grep "\[x\] \*\*ZONE-01\*\*" .planning/REQUIREMENTS.md` finds 1 match
    - CHANGELOG v0.2.1 section appears before v0.2.0 section
    - v0.2.1-aura-cancellation branch is squash-merged to main before release.bat v0.2.1 is called (this plan prepares the branch; merge+release happens after)
  </acceptance_criteria>
  <done>CHANGELOG.md contains v0.2.1 entry with aura cancellation and lust tracking features; REQUIREMENTS.md shows all v0.2.1 requirements as complete; branch is release-ready for squash-merge to main followed by release.bat v0.2.1</done>
</task>

</tasks>

<verification>
- `stylua --check BuffEngine.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua` exits 0
- `grep "v0.2.1" CHANGELOG.md` — entry exists
- `grep -c "\[ \]" .planning/REQUIREMENTS.md` under v0.2.1 section — should be 0 (all checked)
</verification>

<success_criteria>
- All Lua files pass stylua with no changes needed
- CHANGELOG.md has a complete v0.2.1 entry
- All v0.2.1 requirements marked complete in REQUIREMENTS.md traceability table
- Branch is ready for squash-merge to main, then release.bat v0.2.1
</success_criteria>

<output>
After completion, create `.planning/phases/11-cleanup/11-02-SUMMARY.md`
</output>
