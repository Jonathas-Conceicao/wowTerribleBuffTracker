---
phase: 46-item-catalogue-suggested-tiles
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - Providers.lua
  - Core.lua
  - CDMTab.lua
autonomous: true
requirements: [ITEM-01, ITEM-10]

must_haves:
  truths:
    - "Opening the Cooldown Manager rebuilds the consumables catalogue before Suggested is drawn"
    - "A bag change while the CDM is open rebuilds the catalogue at most once per frame"
    - "A bag change while the CDM is closed marks the catalogue dirty and does no scanning work"
    - "An item: key resolves to item display info and never reaches UserSpellProvider"
  artifacts:
    - path: "Providers.lua"
      provides: "ns:ItemDisplayInfo(itemID) and the item: branch in ns:GetDisplayInfoForKey"
      contains: "ItemDisplayInfo"
    - path: "Core.lua"
      provides: "BAG_UPDATE registration and dispatch branch calling ns:MarkItemCatalogueDirty"
      contains: "BAG_UPDATE"
    - path: "CDMTab.lua"
      provides: "StartPreview calls ns:RefreshItemCatalogue before ns:RefreshTBTSections"
      contains: "RefreshItemCatalogue"
  key_links:
    - from: "CDMTab.lua StartPreview"
      to: "ns:RefreshItemCatalogue"
      via: "direct call beside the existing ns:RefreshProvidersAtRest call"
      pattern: "RefreshItemCatalogue"
    - from: "Core.lua event dispatch"
      to: "ns:MarkItemCatalogueDirty"
      via: "BAG_UPDATE elseif branch"
      pattern: "MarkItemCatalogueDirty"
    - from: "Providers.lua ns:GetDisplayInfoForKey"
      to: "ns:ItemDisplayInfo"
      via: "ns:ItemKeyItemID branch placed before the CooldownKeySpellID reject"
      pattern: "ItemKeyItemID"
---

<objective>
Wire the catalogue built in Plan 01 to its three integration points: the CDM-open scan trigger, the
`BAG_UPDATE` dirty flag with per-frame coalescing, and `ns:GetDisplayInfoForKey`'s new `item:`
branch.

Purpose: ITEM-01's "**opening the Cooldown Manager** produces a catalogue" is the scan-trigger half,
owned here. ITEM-10's one-code-path constraint continues — `BAG_UPDATE` is registered through the
existing `TryRegisterEvent` capability helper, never assumed present.

Output: the catalogue is live and queryable. Still nothing renders — Plan 03 draws the tiles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-CONTEXT.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-RESEARCH.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-PATTERNS.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-01-SUMMARY.md
</context>

<interfaces>
Consumed from Plan 01 (do not redefine): `ns.ITEM_KEY_PREFIX`, `ns:ItemKeyItemID(key)`,
`ns:RefreshItemCatalogue()`, `ns:ItemCatalogue()`, `ns:ItemCatalogueIcon(itemID)`,
`ns:ItemCatalogueCount(itemID)`, `ns:IsItemCatalogueDirty()`, `ns:MarkItemCatalogueDirty()`.

Created here and consumed by Plan 03:
- `ns:ItemDisplayInfo(itemID)` — returns a pooled `{ icon, label, duration, spellID }` table, or nil
  when the itemID is not in the catalogue. `spellID` is always nil (an item is not a spell);
  `duration` is always 0 (cooldown reads are Phase 47).
- `ns:GetDisplayInfoForKey("item:<itemID>")` resolves through that helper.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Teach ns:GetDisplayInfoForKey the item: key</name>
  <files>Providers.lua</files>
  <read_first>
    - `Providers.lua:1101-1122` — `ns:GetDisplayInfoForKey` and its header comment. The comment is
      the literal bug record: a string key matching neither `keyToProvider` nor
      `ns:CooldownKeySpellID` returns nil, the caller falls back to `ns:GetSpellIcon(key)`, and a
      non-numeric key yields the 134400 question mark with no tooltip.
    - `Providers.lua:175-218` — `UserSpellProviderMixin:GetDisplayInfo`. Read it to see why an
      `item:` key must NOT reach it: it resolves any non-numeric key it accepts as a **spellID**.
      Note its use of `ns:AcquireDisplayInfo(key)` — that is the pooled-table idiom to copy.
    - `BuffEngine.lua:270-290` and `:340-349` — `ns:AcquireDisplayInfo` and the header comment
      explaining why `ns:GetDisplayInfoForKey` is on the render path and must not allocate.
    - `Display.lua:247-283` — `ns:ShowBuffTooltip`. Confirms a nil `proc.spellID` falls to
      `GameTooltip:SetText(proc.label)`, so an item tile's tooltip works with zero new tooltip code
      provided `label` is populated.
    - `46-RESEARCH.md` "Pitfall 4" and "Dispatch extension point".
  </read_first>
  <action>
    Add `ns:ItemDisplayInfo(itemID)` to `Providers.lua`, placed beside the catalogue block Plan 01
    added. It must:
    - Return nil when `ns:ItemCatalogueIcon(itemID)` and the itemID are both absent from the
      catalogue, so an `item:` key for an item the player no longer holds resolves to nil rather
      than a phantom tile.
    - Otherwise acquire a pooled table via `ns:AcquireDisplayInfo(ns.ITEM_KEY_PREFIX .. itemID)` —
      do NOT build a fresh table; this function is on the render and hover path.
    - Set `info.icon` to `ns:ItemCatalogueIcon(itemID) or 134400` (the existing question-mark
      default, per D-03's degrade-never-error rule).
    - Set `info.label` from `C_Item.GetItemNameByID(itemID)`, guarded `issecretvalue(name)` first
      then `type(name) ~= "string"`, falling back to `"Item " .. tostring(itemID)` — the same shape
      `UserSpellProvider`'s `"Spell " .. tostring(spellID)` fallback uses. A name can legitimately
      be absent before the item is cached client-side; that is a degrade, not an error.
    - Set `info.duration = 0` and `info.spellID = nil`. Phase 46 reads no item cooldowns — that is
      Phase 47 — and an itemID is not a spellID, so leaving `spellID` nil is what makes
      `ns:ShowBuffTooltip`'s `type(proc.spellID) == "number"` test correctly decline
      `SetSpellByID`.
    - Add a comment noting that `displayInfoPool` (`BuffEngine.lua:288`) gains one entry per distinct
      itemID ever catalogued — bounded by the player's distinct consumables, tens at most.

    Then extend `ns:GetDisplayInfoForKey` inside its `type(key) == "string"` block: after the
    `keyToProvider` lookup and **before** the `if not ns:CooldownKeySpellID(key) then return nil end`
    reject, add an `ns:ItemKeyItemID(key)` branch that returns `ns:ItemDisplayInfo(itemID)`.

    Placement is load-bearing (46-RESEARCH.md Pitfall 4): an `item:` key placed after the reject, or
    allowed to fall through, reaches `UserSpellProvider:GetDisplayInfo`, which would treat the itemID
    as a spellID and produce a wrong icon and a spell-shaped tooltip. Add a comment saying exactly
    that, citing the existing `cd:` comment immediately above it as the precedent.

    Do NOT add an entry to `keyToProvider` — that map is for fixed meta keys only; `item:` keys are
    dynamic per itemID. Do NOT add an `ItemProvider` mixin to `ns.providers`; Phase 47 owns that.
    Do NOT call `ns:IsSuggestedKeyResolvable` for `item:` keys anywhere: `keyToProvider` has no entry
    for them and its `not p then return true` fallback would make every item vacuously resolvable —
    harmless but misleading (46-PATTERNS.md section 3 flags this explicitly).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'function ns:ItemDisplayInfo' Providers.lua && test "$(grep -n 'ns:ItemKeyItemID(key)' Providers.lua | head -1 | cut -d: -f1)" -lt "$(grep -n 'if not ns:CooldownKeySpellID(key) then' Providers.lua | head -1 | cut -d: -f1)" && grep -c 'AcquireDisplayInfo(ns.ITEM_KEY_PREFIX' Providers.lua && test "$(grep -v '^\s*--' Providers.lua | grep -c 'keyToProvider\[ns.ITEM_KEY_PREFIX')" = "0" && stylua . && stylua --check . && git ls-files --eol Providers.lua</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: in `ns:GetDisplayInfoForKey`, the line number of the `ns:ItemKeyItemID(key)`
      call is strictly **less than** the line number of `if not ns:CooldownKeySpellID(key) then`.
      The verify command asserts this with `test ... -lt ...` over the two `grep -n` line numbers,
      so a misordering fails the gate rather than merely printing two numbers for a human to
      compare. Both patterns are anchored so they match only inside `ns:GetDisplayInfoForKey`:
      the reject side matches the full `if not ns:CooldownKeySpellID(key) then` line, because the
      bare substring `CooldownKeySpellID(key)` also occurs at `Providers.lua:185` in the unrelated,
      pre-existing `UserSpellProviderMixin:GetDisplayInfo`. A whole-file `head -1` would pick that
      line up instead and make the gate unsatisfiable by any correct implementation.
    - Source assertion: `ns:ItemDisplayInfo` calls `ns:AcquireDisplayInfo` and contains no `{` table
      constructor on any return path.
    - Source assertion: `ns:ItemDisplayInfo` sets `info.spellID` to nil and `info.duration` to 0.
      No `C_Item.GetItemCooldown` call appears anywhere in the file's new code — cooldown reads are
      Phase 47.
    - Source assertion (S4): `C_Item.GetItemNameByID`'s return is guarded `issecretvalue` before
      `type`.
    - Source assertion: `keyToProvider` gained no new entry, and `ns.providers` is byte-unchanged.
    - Source assertion: no `ns:IsSuggestedKeyResolvable` call takes an `item:` key.
    - Command: `stylua .` then `stylua --check .` exits 0; `git ls-files --eol Providers.lua` reports
      `w/crlf`.
    - Gates: S1, S2, S4. Ultimately answers G2 (the icon half).
  </acceptance_criteria>
  <done>`ns:GetDisplayInfoForKey("item:1234")` returns item-shaped display info from the catalogue cache, resolved before the `cd:` reject, allocating nothing, and never reaching `UserSpellProvider`.</done>
</task>

<task type="auto">
  <name>Task 2: Register BAG_UPDATE and dirty-flag the catalogue in Core.lua</name>
  <files>Core.lua</files>
  <read_first>
    - `Core.lua:812-841` — `TryRegisterEvent` and the `BAG_UPDATE_COOLDOWN` /
      `PLAYER_EQUIPMENT_CHANGED` / `BAG_UPDATE_DELAYED` registrations, with the Phase 38 (CD-04)
      comment explaining why every bag-family event goes through the `pcall`-wrapped helper: a
      capability check on the event, never a client-identity check.
    - `Core.lua:955-966` — the dispatch `elseif` chain. The
      `SPELL_UPDATE_COOLDOWN/.../BAG_UPDATE_COOLDOWN -> ns:MarkCooldownsDirty()` branch is the shape
      to copy: set a flag, do no work in the handler.
    - `46-PATTERNS.md` section 6 — states plainly what NOT to copy: the neighbouring
      `PLAYER_EQUIPMENT_CHANGED`/`BAG_UPDATE_DELAYED` branch calls `ns:RefreshProvidersAtRest()`
      **directly**, which is looser than this phase's locked cadence.
  </read_first>
  <action>
    Register `BAG_UPDATE` beside the existing bag-family registrations using
    `TryRegisterEvent(eventFrame, "BAG_UPDATE")` — the `pcall`-wrapped helper, NOT a bare
    `eventFrame:RegisterEvent`. `BAG_UPDATE` is in practice universal, but matching the file's
    existing treatment of every bag event keeps the capability-check discipline intact and costs
    nothing (D-03: capability checks, never client-identity checks).

    Add a dispatch branch `elseif event == "BAG_UPDATE" then ns:MarkItemCatalogueDirty()`. It must
    call nothing else. Do NOT call `ns:RefreshItemCatalogue()` from the event handler:
    `BAG_UPDATE` fires in bursts, and the locked cadence (D-01 scan cadence) is "rebuild only while
    the CDM is shown, coalesced to at most one rebuild per frame". The neighbouring branch's
    unconditional direct call is the pattern to deliberately NOT imitate; record that in the
    comment so a future reader does not "fix" the asymmetry.

    Place the new branch adjacent to the existing `BAG_UPDATE_COOLDOWN` branch so the bag-family
    handlers read together.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -c 'TryRegisterEvent(eventFrame, "BAG_UPDATE")' Core.lua && grep -c 'event == "BAG_UPDATE" then' Core.lua && test "$(grep -v '^\s*--' Core.lua | grep -c 'RefreshItemCatalogue')" = "0" && test "$(grep -v '^\s*--' Core.lua | grep -cE 'buildInterfaceVersion|GetBuildInfo|wow_classic|classicVersion')" = "1" && stylua . && stylua --check . && git ls-files --eol Core.lua</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: registration uses `TryRegisterEvent(eventFrame, "BAG_UPDATE")`. A bare
      `eventFrame:RegisterEvent("BAG_UPDATE")` is a fail.
    - Source assertion: the `BAG_UPDATE` dispatch branch body contains exactly one statement,
      `ns:MarkItemCatalogueDirty()`.
    - Command: `grep -v '^\s*--' Core.lua | grep -c 'RefreshItemCatalogue'` returns `0` — no rebuild
      is triggered from the event handler.
    - Command (S3, ITEM-10): `grep -v '^\s*--' Core.lua | grep -cE
      'buildInterfaceVersion|GetBuildInfo|wow_classic|classicVersion'` returns `1` — the count is
      **unchanged** from the pre-existing single licensed v0.4.0 check at `Core.lua:~530`. Any
      increase means this phase added a second flavour check, which is forbidden. If the baseline
      count differs from 1, record the pre-edit baseline in the SUMMARY and assert equality instead.
    - Command: `stylua .` then `stylua --check .` exits 0; `git ls-files --eol Core.lua` reports
      `w/crlf`.
    - Gates: S1, S2, S3. Ultimately answers G3.
  </acceptance_criteria>
  <done>`BAG_UPDATE` is registered through the capability helper and its handler sets the dirty flag and nothing else.</done>
</task>

<task type="auto">
  <name>Task 3: Scan on CDM open and coalesce the dirty rebuild in CDMTab.lua</name>
  <files>CDMTab.lua, Providers.lua</files>
  <read_first>
    - `CDMTab.lua:26-38` — `StartPreview` / `StopPreview`. `StartPreview` already calls
      `ns:RefreshProvidersAtRest()` then `ns:RefreshTBTSections()` then sets `ns.configOpen = true`.
      This is the real "CDM opened" hook.
    - `CDMTab.lua:1665-1697` — the `cdmWatcher` 0.5s `CooldownViewerSettings:IsVisible()` poll that
      drives `StartPreview`, with the TAINT SAFETY comment explaining why `HookScript` on
      `CooldownViewerSettings` is forbidden. Add no new poll and no new hook.
    - `CDMTab.lua:717-726` — the top of `ns:RefreshTBTSections`, to confirm this plan adds no bag
      call there.
    - `46-PATTERNS.md` section 6 closing note — `ns.configOpen` is the existing "CDM/config surface
      is open" flag; do not invent a second one.
  </read_first>
  <action>
    In `StartPreview`, call `ns:RefreshItemCatalogue()` immediately after the existing
    `ns:RefreshProvidersAtRest()` call and **before** `ns:RefreshTBTSections()`, so the cache is
    warm before Suggested is drawn. Rebuild unconditionally here rather than only when dirty — a CDM
    open is a rare, user-driven event, and an unconditional rebuild is what satisfies the locked
    cadence sentence "a bag update with the CDM closed marks the catalogue dirty; the next open
    rebuilds it" without needing the flag to be correct across a session.

    Add the coalescing consumer for the dirty flag. Declare a file-local boolean
    `itemCatalogueRebuildScheduled` and a local function that:
    - returns immediately unless `ns.configOpen` is true (the locked "only while the CDM is shown"
      half of the cadence);
    - returns immediately if `itemCatalogueRebuildScheduled` is already true;
    - otherwise sets it true and schedules `C_Timer.After(0, ...)`, whose body clears the flag and,
      if `ns:IsItemCatalogueDirty()` and `ns.configOpen` are both still true, calls
      `ns:RefreshItemCatalogue()` then `ns:RefreshTBTSections()`.

    The `C_Timer.After(0, ...)` next-frame deferral is what collapses a `BAG_UPDATE` burst into at
    most one rebuild per frame. The `itemCatalogueRebuildScheduled` guard is what makes a burst of
    twenty events schedule one timer, not twenty.

    Expose that local function on the namespace as `ns:RequestItemCatalogueRebuild()` and call it
    from `Providers.lua`'s `ns:MarkItemCatalogueDirty()` — guarded `if ns.RequestItemCatalogueRebuild
    then`, because `Providers.lua` loads before `CDMTab.lua` and the field is nil until then. This
    late-binding-through-`ns` idiom is already established in this codebase (`ns:RacialCooldownSeed`,
    `Providers.lua:880-889`, documents why: a file-local is an upvalue only to functions declared
    after it, and this project has produced that bug four times).

    Do NOT add any `C_Container` or `C_Item` call to `ns:RefreshTBTSections` or any of its eleven
    call sites. Do NOT add a `HookScript` to any CDM frame and do NOT add a second poll — the
    existing `cdmWatcher` is the only visibility detector (46-RESEARCH.md, T-46-04 in the threat
    model below).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -n 'RefreshItemCatalogue' CDMTab.lua && grep -c 'C_Timer.After(0' CDMTab.lua && grep -c 'function ns:RequestItemCatalogueRebuild\|ns.RequestItemCatalogueRebuild =' CDMTab.lua && test "$(grep -v '^\s*--' CDMTab.lua | grep -cE 'C_Container\.|GetItemInfoInstant|GetItemSpell')" = "0" && test "$(grep -v '^\s*--' CDMTab.lua | grep -c 'HookScript(CooldownViewerSettings')" = "0" && stylua . && stylua --check . && git ls-files --eol CDMTab.lua Providers.lua</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: inside `StartPreview`, `ns:RefreshItemCatalogue()` appears after
      `ns:RefreshProvidersAtRest()` and before `ns:RefreshTBTSections()`. Confirm by line order in
      the grep `-n` output.
    - Source assertion: the deferred rebuild body checks `ns.configOpen` **and**
      `ns:IsItemCatalogueDirty()` before rebuilding, and the scheduling function returns early when
      `itemCatalogueRebuildScheduled` is already true.
    - Command: `grep -v '^\s*--' CDMTab.lua | grep -cE 'C_Container\.|GetItemInfoInstant|GetItemSpell'`
      returns `0` — the render file makes no bag or item-classification call. This is the direct
      guard against 46-RESEARCH.md Pitfall 2 ("catalogue scan run from the render path").
    - Source assertion: `ns:RefreshTBTSections` gained no new call other than being invoked *by* the
      deferred rebuild. Its own body is unchanged by this task.
    - Source assertion: `Providers.lua`'s `ns:MarkItemCatalogueDirty` calls
      `ns:RequestItemCatalogueRebuild()` behind an `if ns.RequestItemCatalogueRebuild then` guard.
    - Source assertion: no new `HookScript` on a CDM frame, no new `OnUpdate` poll frame.
    - Command: `stylua .` then `stylua --check .` exits 0; `git ls-files --eol CDMTab.lua Providers.lua`
      reports `w/crlf` for both.
    - Gates: S1, S2. Ultimately answers G3 (count updates without reopening the CDM).
  </acceptance_criteria>
  <done>Opening the CDM rebuilds the catalogue before Suggested draws; a `BAG_UPDATE` burst while the CDM is open produces exactly one rebuild on the next frame; a burst while it is closed produces none.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WoW client API -> TBT Lua | `C_Item.GetItemNameByID`'s return crosses here in Task 1. |
| Blizzard secure CDM frame -> TBT frames | `CooldownViewerSettings` visibility is read, never hooked. Taint propagates across this boundary and is sticky until `/reload`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Tampering (taint propagation) | CDM-open detection | mitigate | Hook into the existing `StartPreview()`, driven by the existing 0.5s `cdmWatcher` `IsVisible()` poll. No new `HookScript` on `CooldownViewerSettings`, no new poll frame, no call into any Blizzard CDM mixin. Blocker recorded in STATE.md: five injection variants all tainted the CDM, and the taint survives combat and clears only on `/reload`. |
| T-46-05 | Tampering | `ns:GetDisplayInfoForKey` key routing | mitigate | The `ns:ItemKeyItemID` branch is placed before the `ns:CooldownKeySpellID` reject, so an `item:` key can never reach `UserSpellProvider` and be resolved as a spellID. Asserted numerically by comparing grep line numbers. |
| T-46-06 | Denial of Service (self-inflicted, frame time) | `BAG_UPDATE` burst | mitigate | Handler sets a flag only. Rebuild is deferred through one `C_Timer.After(0, ...)` guarded by `itemCatalogueRebuildScheduled`, and skipped entirely unless `ns.configOpen`. A twenty-event burst produces one rebuild. |
| T-46-07 | Tampering | `C_Item.GetItemNameByID` return | mitigate | `issecretvalue(name)` before `type(name)`, falling back to `"Item " .. itemID`. Degrades the label, never errors. |
| T-46-SC | Tampering | package installs | accept | Not applicable — no package manager is used by this project. |
</threat_model>

<verification>
Static only — no test runner exists for this project (46-VALIDATION.md "Test Infrastructure").

1. `stylua .` then `stylua --check .` exits 0 — S1.
2. `git ls-files --eol Core.lua Providers.lua CDMTab.lua` reports `w/crlf` for all three — S2.
3. Flavour-token count in each touched file is unchanged from its pre-edit baseline — S3.
4. Every new API read has `issecretvalue` textually before `type` — S4.
5. `TerribleBuffTracker.toc` unmodified; still `## Interface: 120100, 16001` — S5. No new file, so
   S6 is trivially held.
6. `AddSuggestedTracker` (`CDMTab.lua:101-122`) is byte-unchanged, so an `item:` key still no-ops
   through it — S7.
7. Line-order assertion: `ns:ItemKeyItemID` precedes `ns:CooldownKeySpellID`'s reject inside
   `ns:GetDisplayInfoForKey`.
8. `grep -v '^\s*--' CDMTab.lua | grep -cE 'C_Container\.|GetItemInfoInstant|GetItemSpell'` is `0`.
</verification>

<success_criteria>
- `StartPreview` rebuilds the catalogue before `ns:RefreshTBTSections` runs.
- `BAG_UPDATE` is registered through `TryRegisterEvent` and its handler only sets the dirty flag.
- A burst of `BAG_UPDATE` events while the CDM is open causes at most one rebuild per frame; while
  closed, zero.
- `ns:GetDisplayInfoForKey("item:<id>")` returns item display info, resolved before the `cd:` reject.
- `CDMTab.lua` makes no bag or item-classification call.
- No new CDM hook, no new poll frame, no second flavour check.
- `stylua --check .` exits 0 and all three touched files report `w/crlf`.
</success_criteria>

<output>
Create `.planning/phases/46-item-catalogue-suggested-tiles/46-02-SUMMARY.md` when done. Record the
pre-edit and post-edit flavour-token counts for `Core.lua` (S3's baseline), and the exact name and
location of the coalescing function so Plan 03 and Phase 47 can find it.
</output>
