---
phase: 46-item-catalogue-suggested-tiles
plan: 04
type: execute
wave: 4
depends_on: ["46-01", "46-02", "46-03"]
files_modified:
  - .planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md
autonomous: false
requirements: [ITEM-01, ITEM-03, ITEM-08, ITEM-10]

must_haves:
  truths:
    - "Every static assertion S1..S10 in 46-VALIDATION.md passes against the shipped source"
    - "stylua reports no drift and every touched file still reports w/crlf"
    - "A human has confirmed the catalogue and its tiles in-game on the Forever beta and on Midnight retail"
  artifacts:
    - path: ".planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md"
      provides: "Per-Task Verification Map filled in with real sweep results"
      contains: "Per-Task Verification Map"
  key_links:
    - from: "46-VALIDATION.md static assertion table"
      to: "shipped Core.lua / Providers.lua / CDMTab.lua"
      via: "each S1..S10 check run against the final source"
      pattern: "S10"
---

<objective>
Close the phase: run the full S1..S10 static assertion sweep against the shipped source, fill in
46-VALIDATION.md's Per-Task Verification Map, deploy, and take the in-game G1..G7 gate on both
clients.

Purpose: ITEM-01, ITEM-03, ITEM-08 and ITEM-10 are **only** provable in-game. 46-VALIDATION.md is
explicit that there is no test runner for this project and none is planned — the static half and the
human half do not substitute for each other. This plan runs both.

Output: a phase whose four requirements have real evidence behind them, on both flavours.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-CONTEXT.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-01-SUMMARY.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-02-SUMMARY.md
@.planning/phases/46-item-catalogue-suggested-tiles/46-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the S1..S10 static assertion sweep and the cleanup review</name>
  <files>Core.lua, Providers.lua, CDMTab.lua, TerribleBuffTracker.toc</files>
  <read_first>
    - `.planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md` "Static Assertions" — the
      S1..S10 table, which is the authoritative list. Each row names the check, the method, and what
      it proves.
    - `CLAUDE.md` "Workflow" — the `stylua` rule (bare, from repo root), the `.gitattributes` /
      `git ls-files --eol` rule, the `CHANGELOG.md` prohibition, and the post-commit
      performance-and-cleanup review mandate.
    - `46-01-SUMMARY.md`, `46-02-SUMMARY.md`, `46-03-SUMMARY.md` — for the recorded flavour-token
      baseline and the final function names, so the sweep asserts against what actually shipped.
  </read_first>
  <action>
    Run every static assertion S1..S10 against the final source and record each result — pass or
    fail, with the command output or the specific line read — for the SUMMARY. A bare "pass" with no
    evidence is not acceptable.

    S1. `stylua .` from the repo root, **bare, no flags** — the repo-root `stylua.toml` pins
    `line_endings = "Windows"`, so the bare invocation is correct. Then `stylua --check .` must
    exit 0.

    S2. `git ls-files --eol Core.lua Providers.lua CDMTab.lua` must report `w/crlf` for all three.
    Run it on `.planning/phases/46-item-catalogue-suggested-tiles/*.md` too. This is the only tool
    that can see a CRLF->LF reflow; `git diff` on a `text=auto` path cannot.

    S3. Flavour-token count per touched file, comments stripped:
    `grep -v '^\s*--' <file> | grep -cE 'buildInterfaceVersion|GetBuildInfo|wow_classic|classicVersion'`.
    `Providers.lua` and `CDMTab.lua` must be `0`. `Core.lua` must equal the pre-edit baseline
    recorded in `46-02-SUMMARY.md` — the single licensed v0.4.0 check at `Core.lua:~530` — not zero.
    Any increase anywhere is a hard fail: ITEM-10 and the locked "no second flavour check" rule.

    S4. Read every new `C_Item.*` / `C_Container.*` read added by Plans 01 and 02 and confirm
    `issecretvalue(v)` is evaluated textually before any `type(v)` or `==` on that value. This is a
    source read, not a grep — grep cannot prove ordering. List each guarded call by name in the
    SUMMARY: `GetContainerItemID` id, `GetItemInfoInstant` classID, subClassID and icon,
    `GetItemSpell` spellID, `GetItemCount` count, `GetItemNameByID` name.

    S5. `grep "^## Interface:" TerribleBuffTracker.toc` still reports `120100, 16001`, and
    `git diff --name-only` does not list `TerribleBuffTracker.toc`.

    S6. No new `.lua` file was added by this phase, so load order is trivially unchanged. Assert that
    the phase diff lists only `Core.lua`, `Providers.lua`, `CDMTab.lua` and `.planning/` paths.

    S7. Source read of `AddSuggestedTracker` (`CDMTab.lua:101-122`) — confirm it is byte-unchanged,
    and that an `item:<itemID>` key matches neither `ns:CooldownKeySpellID` nor `ns.SUGGESTED_KEYS`
    membership, so it returns without creating an entry.

    S8. Source read of the catalogue block in `Providers.lua` — confirm the stored tables are indexed
    by itemID and that `bag` and `slot` appear only inside the discovery walk.

    S9. Source read of the filter — confirm it tests only `classID` / `subClassID` from
    `C_Item.GetItemInfoInstant` plus the presence of a `C_Item.GetItemSpell` result, with no
    item-name matching anywhere.

    S10. Source read of `CDMTab.lua` — confirm the `section.itemPool` reset function clears and hides
    `chargeCount`, and that the item tile loop writes `SetText` + `Show` or `Hide` on every path.

    Then run the CLAUDE.md post-commit review over the phase diff:
    - hot-path allocations — the catalogue scan allocates nothing per scan, the render loop nothing
      per tile;
    - redundant per-frame work — no bag or item-classification call is reachable from
      `ns:RefreshTBTSections` or any of its eleven call sites;
    - dirty-check correctness — no path leaves `itemCatalogueRebuildScheduled` stuck true;
    - dead code — no unused local, no function defined but never called. `ns:ItemCatalogueIcon` is
      expected to have exactly one caller (`ns:ItemDisplayInfo`); `ns:IsItemCatalogueDirty` exactly
      one (the coalescing consumer in `CDMTab.lua`).

    Do NOT touch `CHANGELOG.md`. Not in this phase, not to add an entry, not for any reason. The
    user hand-edits it and a diff there is their deliberate edit, not a bug.

    Do NOT edit `.planning/PROJECT.md`. CONTEXT.md's Specifics note says it is stale on the
    Forever-only point, but the research verified directly (`PROJECT.md:21-32`) that the
    both-flavours correction already landed on 2026-09-24 carrying the ITEM-10 citation. Assert that
    it reads "both flavours" and record the assertion; make no edit. If it does NOT read that way,
    stop and surface the discrepancy to the user rather than editing.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && stylua . && stylua --check . && git ls-files --eol Core.lua Providers.lua CDMTab.lua && grep "^## Interface:" TerribleBuffTracker.toc && test -z "$(git diff --name-only -- TerribleBuffTracker.toc CHANGELOG.md .planning/PROJECT.md)" && test "$(grep -v '^\s*--' Providers.lua | grep -cE 'buildInterfaceVersion|GetBuildInfo|wow_classic|classicVersion')" = "0" && test "$(grep -v '^\s*--' CDMTab.lua | grep -cE 'buildInterfaceVersion|GetBuildInfo|wow_classic|classicVersion')" = "0" && test "$(grep -v '^\s*--' CDMTab.lua | grep -cE 'C_Container\.|GetItemInfoInstant|GetItemSpell|GetItemCount|GetItemIconByID')" = "0" && echo SWEEP_OK</automated>
  </verify>
  <acceptance_criteria>
    - Command: the verify command above prints `SWEEP_OK`.
    - Command: `stylua --check .` exits 0 after a bare `stylua .` run (S1).
    - Command: `git ls-files --eol Core.lua Providers.lua CDMTab.lua` reports `w/crlf` for all three,
      and the same for every `.md` in the phase directory (S2).
    - Command: `grep "^## Interface:" TerribleBuffTracker.toc` prints `## Interface: 120100, 16001`
      (S5).
    - Command: `git diff --name-only -- TerribleBuffTracker.toc CHANGELOG.md .planning/PROJECT.md`
      is empty. Any output at all is a hard fail.
    - Command: flavour-token count is `0` for `Providers.lua` and `CDMTab.lua`, and equal to the
      `46-02-SUMMARY.md` baseline for `Core.lua` (S3).
    - Source assertion: S4, S7, S8, S9 and S10 are each recorded in the SUMMARY naming the specific
      function or line they were checked against.
    - Source assertion: the cleanup review is recorded with its four findings (allocation, per-frame
      work, dirty-flag paths, dead code).
    - Gates: S1 through S10 — all ten.
  </acceptance_criteria>
  <done>All ten static assertions pass with recorded evidence, the cleanup review is complete, and `CHANGELOG.md`, `PROJECT.md` and the TOC are provably untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the Per-Task Verification Map and deploy to both clients</name>
  <files>.planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md</files>
  <read_first>
    - `.planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md:99-107` — the empty
      "Per-Task Verification Map" table with its `_(planner fills)_` placeholder row and its column
      headers: Task ID, Plan, Wave, Requirement, Static gate, In-game gate, Status.
    - `.planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md:1-8` — the front matter,
      including `status: draft`, `nyquist_compliant: false` and `wave_0_complete: false`.
    - `CLAUDE.md` "Workflow" — the `sed -i` / redirect prohibition on `.md` and `.planning/` paths.
    - `scripts/install.bat` — argument-free, deploys the shared file set to every WoW client folder
      present, skipping absent ones and failing when none is found.
  </read_first>
  <action>
    Replace the `_(planner fills)_` placeholder row with one row per task across Plans 01-04. Take
    Task ID, Plan, Wave and Requirement from each plan's front matter and task name, and the Static
    gate / In-game gate values from each task's `<acceptance_criteria>` "Gates:" line. Set Status
    from the actual Task 1 sweep result.

    Update the front matter: set `status: complete`. Resolve `nyquist_compliant` and
    `wave_0_complete` honestly — this project has no test runner, so "Nyquist compliance" here means
    the static assertion sweep plus the in-game gate, not automated test coverage. Add a one-line
    note under the table saying exactly that, rather than claiming a coverage property that does not
    exist.

    **Use the Edit tool.** Do NOT use `sed -i` and do NOT redirect over this file. A CRLF->LF reflow
    is invisible to `git diff` on a `.md` path (those still fall under the bare `* text=auto`),
    `sed -i` strips CR from every line including ones it never matched, and the incident is recorded
    in CLAUDE.md. After editing, assert with `git ls-files --eol` on the file.

    Then deploy for the human gate: run `./scripts/install.bat` from the repo root. No TOC changed
    this phase, so `/reload` is sufficient in-client — a full client restart is not required. Confirm
    the script reported a successful copy to at least one client folder before advancing to the
    checkpoint.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -c '_(planner fills)_' .planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md)" = "0" && test "$(grep -c '| 46-0' .planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md)" -ge "7" && git ls-files --eol .planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md && ./scripts/install.bat</automated>
  </verify>
  <acceptance_criteria>
    - Command: `grep -c '_(planner fills)_' 46-VALIDATION.md` returns `0` — the placeholder is gone.
    - Command: `grep -c '| 46-0' 46-VALIDATION.md` returns at least `7` — one row per task across
      the four plans.
    - Command: `git ls-files --eol .../46-VALIDATION.md` reports `w/crlf`. A `w/lf` here means a
      reflow happened and the edit must be redone with the Edit tool.
    - Source assertion: the file was changed with the Edit tool. No `sed`, no `>` redirect, no
      script-driven rewrite appears in the task's command history.
    - Source assertion: the front matter `nyquist_compliant` value is accompanied by the one-line
      note explaining what it means for a project with no test runner.
    - Command: `./scripts/install.bat` exits successfully and names at least one client folder it
      copied to.
  </acceptance_criteria>
  <done>The Per-Task Verification Map is filled with real results, the file still reports `w/crlf`, and the addon is deployed to every client present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: In-game gate G1..G7 on Forever, then on Midnight retail</name>
  <read_first>
    - `.planning/phases/46-item-catalogue-suggested-tiles/46-VALIDATION.md:78-95` — the G1..G7 table
      and the note explaining that G6's in-combat half is the one genuinely unmeasured area in this
      phase.
  </read_first>
  <action>
    Present the G1..G7 check list below to the user and stop. Do not attempt to verify any of it
    yourself — there is no test runner for this project and no automated substitute exists for
    opening a real Cooldown Manager with real bags. Record the user's per-gate answers verbatim,
    noting which client each came from, and do not mark any of ITEM-01, ITEM-03, ITEM-08 or ITEM-10
    as met until the user has answered.
  </action>
  <what-built>
    A bag-derived consumables catalogue keyed by itemID, rebuilt when the Cooldown Manager opens and
    coalesced to one rebuild per frame on `BAG_UPDATE` while it is open. Every catalogued item that
    is not already tracked renders as a tile in the Cooldowns tab's Suggested section, after the
    racial cooldown tiles, showing the item's own icon and the number currently held. Items are
    admitted by taxonomy only — `classID == 0`, a use-spell present, `subClassID ~= 7` — with no
    item-name matching anywhere.
  </what-built>
  <how-to-verify>
    Run on the **WoW Forever beta first** (every measured fact behind this design came from there),
    then repeat on Midnight retail.

    Setup: carry potions, a healthstone, a quest item, a recipe, a key, a trade good and a bandage.
    Split one potion stack across two different bags. `/reload`, then open the Cooldown Manager and
    select the **Cooldowns** tab.

    1. **G1 (ITEM-01):** the split potion appears **once**, not twice. Each distinct itemID produces
       exactly one tile regardless of how many slots hold it.
    2. **G2 (ITEM-03):** each item tile shows the item's own icon — not the 134400 question mark —
       and a count in the bottom-right matching the number in your bags. The count font and position
       should match a cooldown icon's charge count exactly.
    3. **G3 (ITEM-03):** with the CDM still open, drink or use one item down to a smaller stack. The
       count updates **without** closing and reopening the CDM.
    4. **G4 (ITEM-08):** the quest item, the recipe, the key, the trade good and the **bandage** are
       all absent.
    5. **G5 (ITEM-08):** the known `0/8` residue — glue, campfire kit, lute, crate, or whatever this
       character carries — is still **present**. This is accepted documented behaviour per ROADMAP
       999.6 section 1, not a bug. Its absence would mean the filter over-reached and is a **fail**.
    6. **G7:** drag an item tile into a container. Expected: the ghost clears, the success sound
       plays, and **nothing appears** in the target container — the tile stays in Suggested. This is
       the documented Phase 46 no-op. A tracker actually being created here is a defect.
    7. **G6 (ITEM-10):** repeat steps 1-6 on **Midnight retail**, out of combat first. Then, if you
       can reach combat, reopen the CDM in combat. Expected in combat: identical tiles, or a
       **degrade** — a tile with no count shown — never a Lua error.

    Report which of G1..G7 passed on each client. If anything errored, paste the Lua error text.
  </how-to-verify>
  <acceptance_criteria>
    - Behaviour assertion: G1, G2, G3, G4, G5 and G7 pass on the Forever beta.
    - Behaviour assertion: G6 passes on Midnight retail out of combat (tiles identical to Forever's
      shape), and in combat either renders identically or degrades to a hidden count. A Lua error in
      combat is a fail and must be reported, not worked around.
    - Behaviour assertion: G5's residue is **present**. Its absence is a fail.
    - Behaviour assertion: G7 creates no tracker. A tracker being created is a fail.
    - This checkpoint is the only evidence path for ITEM-01, ITEM-03, ITEM-08 and ITEM-10 — there is
      no test runner for this project and no automated substitute exists.
  </acceptance_criteria>
  <resume-signal>Type "approved" with the per-gate results, or describe what failed and on which client.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo working tree -> git index | A CRLF->LF reflow on a `.md` path is invisible to `git diff`; only `git ls-files --eol` can see it. |
| Planner/executor -> user-owned files | `CHANGELOG.md` and `PROJECT.md` are user-authored; a diff there is a deliberate edit, not a defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-12 | Tampering (silent, unlogged) | `.md` and `.planning/` edits | mitigate | Edit tool only — no `sed -i`, no redirect — followed by a `git ls-files --eol` assertion on every touched `.md`. Recorded incident in CLAUDE.md, confirmed on a controlled fixture 2026-09-23. |
| T-46-13 | Tampering (destruction of user work) | `CHANGELOG.md` | mitigate | Hard prohibition plus a `git diff --name-only` assertion that the file is untouched. Recorded incident: the whole v0.3.0 section was destroyed by a script on 2026-09-19. |
| T-46-14 | Repudiation (unevidenced pass) | static sweep results | mitigate | Each of S1..S10 must be recorded with the command output or the specific line read. A bare "pass" is rejected by the acceptance criteria. |
| T-46-SC | Tampering | package installs | accept | Not applicable — no package manager is used by this project. |
</threat_model>

<verification>
Two halves, neither substituting for the other (46-VALIDATION.md "Test Infrastructure"):

- **Static:** S1..S10, run in Task 1 with recorded evidence.
- **In-game:** G1..G7, run by the user in Task 3 on both clients.

There is **no test runner for this project and none is planned.** Do not invent a test command.
</verification>

<success_criteria>
- All ten static assertions pass with recorded evidence.
- The Per-Task Verification Map in 46-VALIDATION.md is filled from real results, and the file still
  reports `w/crlf`.
- `CHANGELOG.md`, `.planning/PROJECT.md` and `TerribleBuffTracker.toc` are untouched by this phase.
- The addon is deployed to every WoW client present on the machine.
- The user has confirmed G1..G5 and G7 on the Forever beta and G6 on Midnight retail.
</success_criteria>

<output>
Create `.planning/phases/46-item-catalogue-suggested-tiles/46-04-SUMMARY.md` when done. Record the
S1..S10 results with evidence, the cleanup-review findings, and the user's per-gate G1..G7 report
verbatim — including which client each result came from, since retail's in-combat behaviour is the
one genuinely unmeasured area and Phase 52's retail review pass will need this as its baseline.
</output>
