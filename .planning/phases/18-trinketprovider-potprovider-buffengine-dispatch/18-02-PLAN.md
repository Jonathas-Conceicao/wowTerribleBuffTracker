---
phase: 18-trinketprovider-potprovider-buffengine-dispatch
plan: 02
type: execute
wave: 2
depends_on:
  - "18-01"
files_modified:
  - BuffEngine.lua
  - Display.lua
autonomous: true
requirements:
  - PROV-02
  - LIFE-01

must_haves:
  truths:
    - "BuffEngine.OnSpellCastSucceeded contains no event-specific if/elseif chains — only the ns:DispatchEventToProviders call (PROV-02)"
    - "ns.activeTimers is the single authoritative timer table; trinket/pot procs land at ns.activeTimers[\"trinket\"] and ns.activeTimers[\"pot\"] via provider dispatch (LIFE-01)"
    - "Recasting the same trinket/pot overwrites the previous entry by key — the eviction loop is DELETED (D-03)"
    - "ScanActiveTimersForCancellation reads timer.castSpellID for source=\"cast\" procs with fallback to the table key when castSpellID absent (D-15, D-16) — no regression for user-spell, trinket, or pot cancellation"
    - "TRINKET_SPELLS, POT_SPELLS, TRINKET_ITEM_IDS, POT_ITEM_IDS, TRINKET_FALLBACK_ORDER, POT_FALLBACK_ORDER are no longer defined as locals in BuffEngine.lua; BuffEngine.RefreshMetaIcons reads them via ns.* exports from Providers.lua (D-08)"
    - "Display.lua no longer references timer.metaSlot — the dual-index fallback in bar and icon layout loops is removed (D-04)"
    - "FindSpellByItemID (BuffEngine.lua local helper) and ns.metaIcons / ns.metaAtRest caches and ns:RefreshMetaIcons and ns:GetAtRestMetaIcon and ns:GetAtRestMetaInfo REMAIN in BuffEngine.lua (D-09, PROV-F3 deferred)"
    - "OnUnitAura, StartLustTimer, StartAllPreviewTimers, ClearAllTimers, AddTrackedBuff, RemoveTrackedBuff, SetBuffSection, GetActiveTimers, GetSpellIcon, ResolveSuggestedSpellID, SUGGESTED_BUFFS, SATED_DEBUFF_TO_LUST, CLASS_LUST_SPELL are ALL unmodified (PITFALL-4 preview guard, Phase 19/20/21 scope preservation)"
  artifacts:
    - path: "BuffEngine.lua"
      provides: "Thin OnSpellCastSucceeded dispatcher; cancellation scan with castSpellID read; static data removed; meta-icon cache intact"
      contains: "ns:DispatchEventToProviders"
    - path: "Display.lua"
      provides: "Bar and icon layout loops that index activeTimers by slot.spellID only — no metaSlot branch"
      contains: "activeBarBySpell"
  key_links:
    - from: "BuffEngine.OnSpellCastSucceeded"
      to: "ns:DispatchEventToProviders"
      via: "single function call, zero branches"
      pattern: "ns:DispatchEventToProviders"
    - from: "ns:DispatchEventToProviders"
      to: "ns.activeTimers[\"trinket\"] / ns.activeTimers[\"pot\"]"
      via: "proc.key overwrite in Providers.lua line 163"
      pattern: "ns\\.activeTimers\\[proc\\.key\\]"
    - from: "ScanActiveTimersForCancellation"
      to: "C_UnitAuras.GetPlayerAuraBySpellID"
      via: "timer.castSpellID or fallback to table key"
      pattern: "GetPlayerAuraBySpellID"
    - from: "Display.lua activeBarBySpell / activeBySpell"
      to: "slot.spellID lookup"
      via: "direct key read (metaSlot dual-index removed)"
      pattern: "activeBarBySpell\\[slot\\.spellID\\]"
---

<objective>
Finalize the Phase 18 dispatch migration: remove the trinket (branch 1) and pot (branch 2) fan-out blocks from BuffEngine.OnSpellCastSucceeded so it becomes a zero-branch dispatcher (PROV-02, D-18), relocate-consumer-side the trinket/pot static data locals out of BuffEngine.lua (they now live in Providers.lua per Plan 18-01), update ScanActiveTimersForCancellation to read timer.castSpellID with fallback (D-15, D-16), and migrate Display.lua bar/icon layout loops off the metaSlot dual-index (D-04) since the key IS the slot now.

Purpose: Closes PROV-02 and LIFE-01 for this phase. After this plan, casting a trinket or pot spell writes a proc at ns.activeTimers["trinket"] / ns.activeTimers["pot"] via the provider dispatch, reproc overwrites cleanly by key, and cancellation still works for all three cast-triggered buff types.

Output: BuffEngine.lua thinned to a single-line OnSpellCastSucceeded; static data definitions removed (but ns.* consumer sites in RefreshMetaIcons updated to read from Providers.lua exports); cancellation scan migrated to castSpellID pattern. Display.lua bar/icon loops no longer branch on metaSlot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-trinketprovider-potprovider-buffengine-dispatch/18-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/18-trinketprovider-potprovider-buffengine-dispatch/18-01-PLAN.md
@Providers.lua
@BuffEngine.lua
@Display.lua
@CLAUDE.md

<interfaces>
<!-- Contracts the executor must preserve. -->

After Plan 18-01 the following are available on ns from Providers.lua:
- ns.TRINKET_SPELLS[spellID] -> { duration, itemID }
- ns.POT_SPELLS[spellID]     -> { duration, itemID }
- ns.TRINKET_ITEM_IDS[itemID] -> true/nil
- ns.POT_ITEM_IDS[itemID]    -> true/nil
- ns.TRINKET_FALLBACK_ORDER  -> array of itemIDs in CSV order
- ns.POT_FALLBACK_ORDER      -> array of itemIDs in CSV order
- ns.providers = { TrinketProvider, PotProvider, UserSpellProvider }

BuffEngine.lua MUST continue to provide:
- ns.metaIcons = { trinket, pot }            -- texture cache (D-09)
- ns.metaAtRest = { trinket = {...}, pot = {...} }   -- full at-rest info (D-09)
- ns:RefreshMetaIcons()                       -- combat-gated inventory scan (D-09)
- ns:GetAtRestMetaIcon(key)                   -- public reader (D-09)
- ns:GetAtRestMetaInfo(key)                   -- public reader (D-09)
- ns.SATED_DEBUFF_TO_LUST                     -- lust dispatch table (Phase 19 scope)
- ns.CLASS_LUST_SPELL                         -- lust dispatch table (Phase 19 scope)
- ns.SUGGESTED_BUFFS                          -- CDM tab suggested section (Phase 19+ scope)
- ns:InitBuffEngine(), ns:GetSpellIcon, ns:ResolveSuggestedSpellID
- ns:OnSpellCastSucceeded (thin wrapper around dispatch)
- ns:GetActiveTimers, ns:AddTrackedBuff, ns:RemoveTrackedBuff, ns:SetBuffSection
- ns:StartAllPreviewTimers  (PITFALL-4 — DO NOT MODIFY; still iterates ns.db.trackedBuffs directly)
- ns:ClearAllTimers
- ns:ScanActiveTimersForCancellation (modified in this plan — castSpellID read with fallback)
- ns:StartLustTimer  (Phase 19 scope — DO NOT MODIFY)
- ns:OnUnitAura  (Phase 19 scope — DO NOT MODIFY; LUST-01 pre-gate must stay intact)
- ns:ClearAuraBlock

Display.lua MUST continue to:
- Render bars and icons keyed by slot.spellID (the DB key for user spells = numeric; for trinket/pot/lust = string)
- Reach the timer via activeBarBySpell[slot.spellID] / activeBySpell[slot.spellID]
- Preserve the wipe(activeBarBySpell) / wipe(activeBySpell) accumulator pattern (PITFALL-7 GC)

Dispatcher install site (Providers.lua line 163):
    ns.activeTimers[proc.key] = proc
For trinket/pot this writes to ns.activeTimers["trinket"] / ns.activeTimers["pot"]. For user spells it writes to ns.activeTimers[<numericSpellID>]. Display.lua already has slot.spellID equal to the string for trinket/pot entries in ns.db.trackedBuffs (confirmed by reading the DB migration comments). Therefore activeBarBySpell[slot.spellID] with slot.spellID="trinket" finds the trinket timer correctly — this is why the metaSlot dual-index can be DELETED.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove static data locals from BuffEngine.lua; update RefreshMetaIcons / FindSpellByItemID to read from ns.*</name>

  <read_first>
    - BuffEngine.lua (FULL)
    - Providers.lua (after Plan 18-01 — verify ns.TRINKET_SPELLS, ns.POT_SPELLS, ns.TRINKET_ITEM_IDS, ns.POT_ITEM_IDS, ns.TRINKET_FALLBACK_ORDER, ns.POT_FALLBACK_ORDER exports exist)
    - .planning/phases/18-trinketprovider-potprovider-buffengine-dispatch/18-CONTEXT.md (D-08, D-09, D-10, D-11 — hybrid data split)
    - .planning/research/PITFALLS.md PITFALL-5 (combat lockdown — GetInventoryItemID / C_Item.GetItemCount stay in BuffEngine.RefreshMetaIcons, which stays in BuffEngine this phase)
    - CLAUDE.md (stylua requirement)
  </read_first>

  <files>BuffEngine.lua</files>

  <action>
    STEP 1 — Delete the static data local definitions and the `ns.* = <local>` export lines. Remove BuffEngine.lua lines 49-107 (from `-- Static lookup: spellID -> { duration, itemID } for tracked on-use trinkets (D-01).` through `local POT_FALLBACK_ORDER = { 241308, 241288, 241292, 241302 }` INCLUSIVE of the ns.* exports on lines 98-101 — those live in Providers.lua now per D-11).

    The block to delete encompasses:
    - Comment block describing TRINKET_SPELLS
    - `local TRINKET_SPELLS = { ... }` (9 entries)
    - Comment block describing POT_SPELLS
    - `local POT_SPELLS = { ... }` (4 entries)
    - `local TRINKET_ITEM_IDS = {}` + populate loop
    - `local POT_ITEM_IDS = {}` + populate loop
    - `ns.TRINKET_SPELLS = TRINKET_SPELLS` / `ns.POT_SPELLS = POT_SPELLS` / `ns.TRINKET_ITEM_IDS = TRINKET_ITEM_IDS` / `ns.POT_ITEM_IDS = POT_ITEM_IDS` (these ns.* exports now come from Providers.lua)
    - Comment block describing FALLBACK_ORDER
    - `local TRINKET_FALLBACK_ORDER = { ... }`
    - `local POT_FALLBACK_ORDER = { ... }`

    Preserve the comment block starting `-- D-01: Eager-cached at-rest resolution for meta-slots.` and everything from `ns.metaIcons = { trinket = nil, pot = nil }` onward — those are the CACHES (D-09) and must remain.

    STEP 2 — Update FindSpellByItemID (currently takes a spellTable parameter like `TRINKET_SPELLS` as a direct local reference). No signature change needed — the function already accepts the table as a parameter. But its CALL SITES inside RefreshMetaIcons read `TRINKET_SPELLS` / `POT_SPELLS` as locals. Update those call sites.

    STEP 3 — In ns:RefreshMetaIcons (currently starts at BuffEngine.lua ~line 136), replace every local-reference read of the relocated tables with the ns.* export read:
    - `TRINKET_ITEM_IDS[equipped]` → `ns.TRINKET_ITEM_IDS[equipped]`
    - `ipairs(TRINKET_FALLBACK_ORDER)` → `ipairs(ns.TRINKET_FALLBACK_ORDER)`
    - `FindSpellByItemID(TRINKET_SPELLS, trinketItemID)` → `FindSpellByItemID(ns.TRINKET_SPELLS, trinketItemID)`
    - `ipairs(POT_FALLBACK_ORDER)` → `ipairs(ns.POT_FALLBACK_ORDER)`
    - `POT_FALLBACK_ORDER[1]` → `ns.POT_FALLBACK_ORDER[1]`
    - `FindSpellByItemID(POT_SPELLS, potItemID)` → `FindSpellByItemID(ns.POT_SPELLS, potItemID)`

    NOTE: `INVSLOT_TRINKET1` and `INVSLOT_TRINKET2` are Blizzard globals — leave them alone.

    Do NOT relocate FindSpellByItemID itself — it's a local helper in BuffEngine and D-09 keeps it here (caller is only RefreshMetaIcons, also in BuffEngine).

    STEP 4 — Add a one-line comment ABOVE the `-- D-01: Eager-cached at-rest resolution for meta-slots.` comment referencing Providers.lua for navigation aid (Claude's Discretion in CONTEXT.md allows this):

        `-- NOTE: TRINKET_SPELLS, POT_SPELLS, *_ITEM_IDS, *_FALLBACK_ORDER relocated to Providers.lua in Phase 18 (D-08). Exposed on ns.* for use below.`

    STEP 5 — Do NOT touch OnSpellCastSucceeded in this task (Task 2 handles it). Do NOT touch ScanActiveTimersForCancellation (Task 2 handles it). Do NOT touch StartAllPreviewTimers (PITFALL-4 — out of scope until Phase 21). Do NOT touch OnUnitAura or StartLustTimer (Phase 19 scope).

    STEP 6 — Run stylua:
        `stylua BuffEngine.lua`
  </action>

  <verify>
    <automated>stylua --check BuffEngine.lua && [ "$(grep -c "^local TRINKET_SPELLS = {" BuffEngine.lua)" = "0" ] && [ "$(grep -c "^local POT_SPELLS = {" BuffEngine.lua)" = "0" ] && [ "$(grep -c "^local TRINKET_ITEM_IDS" BuffEngine.lua)" = "0" ] && [ "$(grep -c "^local POT_ITEM_IDS" BuffEngine.lua)" = "0" ] && [ "$(grep -c "^local TRINKET_FALLBACK_ORDER" BuffEngine.lua)" = "0" ] && [ "$(grep -c "^local POT_FALLBACK_ORDER" BuffEngine.lua)" = "0" ] && grep -qE "ns\.TRINKET_ITEM_IDS\[equipped\]" BuffEngine.lua && grep -qE "ns\.TRINKET_FALLBACK_ORDER" BuffEngine.lua && grep -qE "ns\.POT_FALLBACK_ORDER" BuffEngine.lua && grep -qE "FindSpellByItemID\(ns\.TRINKET_SPELLS" BuffEngine.lua && grep -qE "FindSpellByItemID\(ns\.POT_SPELLS" BuffEngine.lua && grep -qE "function ns:RefreshMetaIcons" BuffEngine.lua && grep -qE "ns\.metaIcons" BuffEngine.lua && grep -qE "function ns:GetAtRestMetaIcon" BuffEngine.lua</automated>
  </verify>

  <acceptance_criteria>
    - `stylua --check BuffEngine.lua` exits 0
    - `grep -c "^local TRINKET_SPELLS = {" BuffEngine.lua` returns 0
    - `grep -c "^local POT_SPELLS = {" BuffEngine.lua` returns 0
    - `grep -c "^local TRINKET_ITEM_IDS" BuffEngine.lua` returns 0
    - `grep -c "^local POT_ITEM_IDS" BuffEngine.lua` returns 0
    - `grep -c "^local TRINKET_FALLBACK_ORDER" BuffEngine.lua` returns 0
    - `grep -c "^local POT_FALLBACK_ORDER" BuffEngine.lua` returns 0
    - `grep -c "ns.TRINKET_SPELLS = TRINKET_SPELLS" BuffEngine.lua` returns 0 (export was deleted; Providers.lua owns this now)
    - `grep -c "ns.POT_SPELLS = POT_SPELLS" BuffEngine.lua` returns 0
    - `grep -c "ns.TRINKET_ITEM_IDS\[equipped\]" BuffEngine.lua` returns 1 (RefreshMetaIcons now reads via ns)
    - `grep -c "ns.TRINKET_FALLBACK_ORDER" BuffEngine.lua` returns at least 1
    - `grep -c "ns.POT_FALLBACK_ORDER" BuffEngine.lua` returns at least 2 (once in ipairs, once in fallback index)
    - `grep -c "FindSpellByItemID(ns.TRINKET_SPELLS" BuffEngine.lua` returns 1
    - `grep -c "FindSpellByItemID(ns.POT_SPELLS" BuffEngine.lua` returns 1
    - `grep -c "function ns:RefreshMetaIcons" BuffEngine.lua` returns 1 (preserved per D-09)
    - `grep -c "function ns:GetAtRestMetaIcon" BuffEngine.lua` returns 1 (preserved per D-09)
    - `grep -c "function ns:GetAtRestMetaInfo" BuffEngine.lua` returns 1 (preserved per D-09)
    - `grep -c "local function FindSpellByItemID" BuffEngine.lua` returns 1 (preserved per D-09)
    - `grep -c "ns.metaIcons = { trinket = nil, pot = nil }" BuffEngine.lua` returns 1 (cache preserved per D-09)
    - `grep -c "ns.metaAtRest = {" BuffEngine.lua` returns 1 (cache preserved)
    - `grep -c "function ns:StartAllPreviewTimers" BuffEngine.lua` returns 1 (PITFALL-4 guard — function still present)
    - `grep -c "function ns:OnUnitAura" BuffEngine.lua` returns 1 (Phase 19 scope — untouched)
    - `grep -c "function ns:StartLustTimer" BuffEngine.lua` returns 1 (Phase 19 scope — untouched)
    - `grep -c "SATED_DEBUFF_TO_LUST" BuffEngine.lua` returns at least 2 (assignment + OnUnitAura read)
    - `grep -c "CURRENT_SCHEMA_VERSION = 3" BuffEngine.lua` returns 1 (PITFALL-1 — no schema bump)
  </acceptance_criteria>

  <done>
    BuffEngine.lua no longer defines trinket/pot static data as locals. RefreshMetaIcons reads them via ns.* exports from Providers.lua. Meta-icon cache, GetAtRestMetaIcon, GetAtRestMetaInfo, FindSpellByItemID, OnUnitAura, StartLustTimer, StartAllPreviewTimers all preserved intact. stylua passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove OnSpellCastSucceeded branches 1 and 2; update ScanActiveTimersForCancellation to read timer.castSpellID with fallback</name>

  <read_first>
    - BuffEngine.lua (FULL — after Task 1; confirm static data block is gone and OnSpellCastSucceeded still has branches 1/2 intact at this point)
    - Providers.lua (after Plan 18-01 — verify TrinketProvider and PotProvider produce procs with `key="trinket"/"pot"` and `castSpellID=<numericSpellID>`)
    - .planning/phases/18-trinketprovider-potprovider-buffengine-dispatch/18-CONTEXT.md (D-03 trivial overwrite replaces eviction loop; D-15/D-16/D-17 cancellation scan castSpellID pattern; D-18/D-19 zero branches)
    - .planning/research/PITFALLS.md PITFALL-2 (timer identity — cancellation MUST read castSpellID, not the table key, because the table key for trinket/pot is now a string)
    - CLAUDE.md (stylua requirement)
  </read_first>

  <files>BuffEngine.lua</files>

  <action>
    STEP 1 — Delete OnSpellCastSucceeded branches 1 (trinket fan-out) and 2 (pot fan-out). The function currently spans approximately lines 319-404 (numbering shifted after Task 1 — use function signature `function ns:OnSpellCastSucceeded(spellID)` as anchor).

    Delete the entire block from `-- D-02: Meta-slot fan-out happens FIRST` (comment opener of the fan-out section) through the end of the pot branch `return` (just before the comment `-- User-spell path: routed through SpellProvider dispatch`).

    Concretely, delete:
    - Comment block D-02 describing meta-slot fan-out
    - Comment `-- Trinket fan-out (D-01, D-03, D-04, D-06, D-08)` and the entire `if trinketDef then ... return end` block (including the eviction `for existingID, existingTimer in pairs(ns.activeTimers) do ... end` loop — D-03 replaces this with trivial overwrite inside the provider dispatcher)
    - Comment `-- Pot fan-out (same shape as trinket, metaSlot = "pot")` and the entire `if potDef then ... return end` block (including its eviction loop)

    Preserve:
    - The function signature line `function ns:OnSpellCastSucceeded(spellID)`
    - The comment block above the dispatch call: `-- User-spell path: routed through SpellProvider dispatch (Plan 17-02, req PROV-03).` — UPDATE this comment to reflect Phase 18 state (see Step 2)
    - The dispatch call itself: `ns:DispatchEventToProviders("UNIT_SPELLCAST_SUCCEEDED", "player", nil, spellID)`
    - The closing `end`

    STEP 2 — Replace the entire comment block above the dispatch call with a single short comment matching D-18/D-19:

        `-- PROV-02, D-18/D-19: OnSpellCastSucceeded has zero branches. All cast-triggered buff`
        `-- detection (trinket, pot, user-spell) is dispatched to providers via GetEventInterests.`
        `-- See Providers.lua for TrinketProvider, PotProvider, UserSpellProvider definitions.`

    The final function body must be exactly:
    ```lua
    function ns:OnSpellCastSucceeded(spellID)
        -- PROV-02, D-18/D-19: OnSpellCastSucceeded has zero branches. All cast-triggered buff
        -- detection (trinket, pot, user-spell) is dispatched to providers via GetEventInterests.
        -- See Providers.lua for TrinketProvider, PotProvider, UserSpellProvider definitions.
        ns:DispatchEventToProviders("UNIT_SPELLCAST_SUCCEEDED", "player", nil, spellID)
    end
    ```

    STEP 3 — Update ns:ScanActiveTimersForCancellation (currently around line 603-664 of the ORIGINAL file; will have shifted after branches 1/2 removed). Locate the `if timer.source == "cast" then` branch inside the `for spellID, timer in pairs(ns.activeTimers) do` loop.

    Current body of that branch:
    ```lua
    local aura = C_UnitAuras.GetPlayerAuraBySpellID(spellID)
    if aura == nil then
        shouldCancel = true
    end
    ```

    Replace with (D-15, D-16 — read castSpellID with fallback to the table key for backwards compat):
    ```lua
    -- D-15/D-16: read castSpellID from proc (set by all cast providers per D-17). Fallback to
    -- the table key for backwards compat with any in-flight procs that predate this plan.
    local lookupID = timer.castSpellID or spellID
    if type(lookupID) == "number" then
        local aura = C_UnitAuras.GetPlayerAuraBySpellID(lookupID)
        if aura == nil then
            shouldCancel = true
        end
    end
    -- If lookupID is not numeric (should not happen post-Phase 18 since all cast procs carry
    -- numeric castSpellID), leave shouldCancel=false — aura scan cannot dispatch for non-numeric IDs.
    ```

    The type guard is important: the table key for trinket/pot procs is now the STRING "trinket" / "pot". Without castSpellID fallback, `GetPlayerAuraBySpellID("trinket")` would be called — which errors or returns nil unpredictably. The guard protects against the absent-castSpellID edge case.

    Do NOT modify the `elseif timer.source == "debuff"` branch — that's lust and stays for Phase 19.
    Do NOT modify the `if shouldCancel then ns.activeTimers[spellID] = nil` removal — removal by the table key is correct regardless of what the key type is.
    Do NOT modify the debug logging or the final UpdateDisplay call.

    STEP 4 — Run stylua:
        `stylua BuffEngine.lua`
  </action>

  <verify>
    <automated>stylua --check BuffEngine.lua && [ "$(grep -c "metaSlot" BuffEngine.lua)" = "0" ] && [ "$(grep -c "ns.TRINKET_SPELLS\[spellID\]" BuffEngine.lua)" = "0" ] && [ "$(grep -c "ns.POT_SPELLS\[spellID\]" BuffEngine.lua)" = "0" ] && grep -qE "local lookupID = timer\.castSpellID or spellID" BuffEngine.lua && grep -qE "GetPlayerAuraBySpellID\(lookupID\)" BuffEngine.lua && grep -qE "PROV-02, D-18/D-19" BuffEngine.lua && [ "$(grep -cE "if trinketDef then" BuffEngine.lua)" = "0" ] && [ "$(grep -cE "if potDef then" BuffEngine.lua)" = "0" ]</automated>
  </verify>

  <acceptance_criteria>
    - `stylua --check BuffEngine.lua` exits 0
    - `grep -c "metaSlot" BuffEngine.lua` returns 0 (D-04: metaSlot field and all references deleted)
    - `grep -c "ns.TRINKET_SPELLS\[spellID\]" BuffEngine.lua` returns 0 (branch 1 deleted)
    - `grep -c "ns.POT_SPELLS\[spellID\]" BuffEngine.lua` returns 0 (branch 2 deleted)
    - `grep -c "if trinketDef then" BuffEngine.lua` returns 0
    - `grep -c "if potDef then" BuffEngine.lua` returns 0
    - `grep -c "local lookupID = timer.castSpellID or spellID" BuffEngine.lua` returns 1
    - `grep -c "GetPlayerAuraBySpellID(lookupID)" BuffEngine.lua` returns 1
    - `grep -c "GetPlayerAuraBySpellID(spellID)" BuffEngine.lua` returns 0 (old read site replaced — BUT note lust branch uses `GetPlayerAuraBySpellID(buffID)` and `GetPlayerAuraBySpellID(timer.lustBuffID)` which are different and must be preserved)
    - `grep -c "PROV-02, D-18/D-19" BuffEngine.lua` returns 1 (new comment present)
    - `grep -c "ns:DispatchEventToProviders" BuffEngine.lua` returns 1 (single dispatch call)
    - `grep -cE "^function ns:OnSpellCastSucceeded\(spellID\)" BuffEngine.lua` returns 1
    - Line count of OnSpellCastSucceeded function body is ≤ 6 lines (function signature + 3 comment lines + 1 dispatch call + `end`) — verify by running `awk '/^function ns:OnSpellCastSucceeded/,/^end/' BuffEngine.lua | wc -l` returns ≤ 6
    - `grep -c "elseif timer.source == \"debuff\"" BuffEngine.lua` returns 1 (lust branch preserved)
    - `grep -c "for _, buffID in ipairs(buffsToCheck) do" BuffEngine.lua` returns 1 (SHARED_LUST_BUFFS iteration preserved)
    - `grep -c "function ns:StartLustTimer" BuffEngine.lua` returns 1 (Phase 19 scope preserved)
    - `grep -c "function ns:OnUnitAura" BuffEngine.lua` returns 1 (Phase 19 scope preserved)
    - `grep -c "function ns:StartAllPreviewTimers" BuffEngine.lua` returns 1 (PITFALL-4 — still present)
    - `grep -c "CURRENT_SCHEMA_VERSION = 3" BuffEngine.lua` returns 1 (PITFALL-1 — no schema bump)
    - `grep -c "LUST-01" BuffEngine.lua` returns at least 1 (pre-gate comment preserved in OnUnitAura)
  </acceptance_criteria>

  <done>
    BuffEngine.OnSpellCastSucceeded contains only the dispatcher call plus explanatory comment. ScanActiveTimersForCancellation reads timer.castSpellID with fallback and type-guards against non-numeric IDs. No metaSlot references remain. Lust, preview, and all other preserved functions untouched. stylua passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Migrate Display.lua metaSlot dual-index reads — slot key IS the lookup key</name>

  <read_first>
    - Display.lua (FULL — especially lines 415-475 for bar layout and 585-645 for icon layout)
    - .planning/phases/18-trinketprovider-potprovider-buffengine-dispatch/18-CONTEXT.md (D-04 metaSlot removal)
    - Providers.lua (after Plan 18-01 — confirm procs land at ns.activeTimers["trinket"] / ns.activeTimers["pot"] via dispatcher writing ns.activeTimers[proc.key])
    - BuffEngine.lua (after Task 2 — confirm no metaSlot produced anywhere)
    - CLAUDE.md (stylua requirement)
  </read_first>

  <files>Display.lua</files>

  <action>
    Display.lua has TWO metaSlot dual-index sites, both simple 3-line blocks that follow the same pattern. After Plan 18-02 Task 2, no proc carries metaSlot anymore — the dual-index can be REMOVED because `slot.spellID` from the DB equals the proc key directly (user spells: numeric; trinket/pot/lust: string matching the DB key).

    SITE 1 — Bar layout loop (Display.lua lines ~425-435):

    Current code:
    ```lua
    wipe(activeBarBySpell)
    for _, timer in ipairs(barTimers) do
        -- Meta-slot timers (metaSlot="trinket"/"pot") are stored in ns.activeTimers
        -- keyed by the numeric cast spellID but DB slots use the meta string key.
        -- When hideWhenInactive=true and CDM is closed, the slot IS the timer so
        -- slot.spellID is numeric — index by BOTH keys so lookup works either way.
        activeBarBySpell[timer.spellID] = timer
        if timer.metaSlot then
            activeBarBySpell[timer.metaSlot] = timer
        end
    end
    ```

    Replace with:
    ```lua
    wipe(activeBarBySpell)
    for _, timer in ipairs(barTimers) do
        -- Phase 18 (D-04): proc.key is now the DB slot key directly ("trinket"/"pot"/"lust"
        -- or numeric spellID). timer.spellID is preserved by all providers to match this
        -- key shape exactly, so a single-index read is sufficient. metaSlot dual-index
        -- removed — the dispatcher writes ns.activeTimers[proc.key] in Providers.lua.
        activeBarBySpell[timer.spellID] = timer
    end
    ```

    NOTE: `timer.spellID` is preserved by all three providers in Providers.lua — UserSpellProvider sets `spellID = <numericSpellID>` (== key), TrinketProvider sets `spellID = <numericCastSpellID>` (mismatches key="trinket"), PotProvider likewise sets `spellID = <numericCastSpellID>` (mismatches key="pot"). Wait — this is a problem. For trinket/pot, `timer.spellID` is the numeric cast spellID, NOT "trinket"/"pot". The lookup `activeBarBySpell[timer.spellID]` would only index by the numeric cast ID.

    The correct migration is to index by the TIMER's KEY (from the dispatcher's ns.activeTimers table), not by `timer.spellID`. Read barTimers — it's produced by ns:GetActiveTimers() which returns an ARRAY of timers (not keyed). We need the key. Since the dispatcher stores `ns.activeTimers[proc.key] = proc` and proc has `key = <the DB slot key>`, we can read `timer.key` directly (providers already set this).

    CORRECTED REPLACEMENT:
    ```lua
    wipe(activeBarBySpell)
    for _, timer in ipairs(barTimers) do
        -- Phase 18 (D-04): index by the stable provider key. For user spells this is a
        -- numeric spellID equal to timer.spellID; for trinket/pot/lust it's the string
        -- DB slot key. ns.activeTimers[proc.key] is the canonical write site in the
        -- dispatcher (Providers.lua). metaSlot dual-index removed.
        activeBarBySpell[timer.key or timer.spellID] = timer
    end
    ```

    The `or timer.spellID` fallback covers any proc that predates Phase 17 wiring (lust/preview procs in BuffEngine.lua do NOT set `key` currently — they set `spellID` only — see StartLustTimer line 683 sets `spellID = "lust"`). So for lust, `timer.spellID` == "lust" and the fallback works. For preview timers in StartAllPreviewTimers line 562, `spellID = spellID` where spellID is the DB key — same fallback. Both covered.

    SITE 2 — Icon layout loop (Display.lua lines ~598-605):

    Current code:
    ```lua
    wipe(activeBySpell)
    for _, timer in ipairs(iconTimers) do
        -- Index by both numeric spellID and metaSlot string key — see bar path above.
        activeBySpell[timer.spellID] = timer
        if timer.metaSlot then
            activeBySpell[timer.metaSlot] = timer
        end
    end
    ```

    Replace with (mirroring Site 1):
    ```lua
    wipe(activeBySpell)
    for _, timer in ipairs(iconTimers) do
        -- Phase 18 (D-04): index by the stable provider key. See bar path above for rationale.
        activeBySpell[timer.key or timer.spellID] = timer
    end
    ```

    STEP 3 — No other Display.lua changes needed. Do NOT modify the `for i, slot in ipairs(barSlots) do ... local timer = activeBarBySpell[slot.spellID]` lookup site (lines ~462-464) — `slot.spellID` comes from the DB iteration (line 442: `entry.spellID = spellID`), which uses the DB key verbatim. That key matches `timer.key` via the new index, so the lookup still works. Same for icon layout lookup at line 616.

    STEP 4 — Verify the wipe() accumulator pattern is preserved (PITFALL-7 GC guard): both `wipe(activeBarBySpell)` and `wipe(activeBySpell)` calls MUST remain.

    STEP 5 — Run stylua:
        `stylua Display.lua`
  </action>

  <verify>
    <automated>stylua --check Display.lua && [ "$(grep -c "metaSlot" Display.lua)" = "0" ] && grep -qE "activeBarBySpell\[timer\.key or timer\.spellID\] = timer" Display.lua && grep -qE "activeBySpell\[timer\.key or timer\.spellID\] = timer" Display.lua && grep -qE "wipe\(activeBarBySpell\)" Display.lua && grep -qE "wipe\(activeBySpell\)" Display.lua</automated>
  </verify>

  <acceptance_criteria>
    - `stylua --check Display.lua` exits 0
    - `grep -c "metaSlot" Display.lua` returns 0 (D-04 — all metaSlot references deleted)
    - `grep -c "activeBarBySpell\[timer.key or timer.spellID\] = timer" Display.lua` returns 1
    - `grep -c "activeBySpell\[timer.key or timer.spellID\] = timer" Display.lua` returns 1
    - `grep -c "wipe(activeBarBySpell)" Display.lua` returns 1 (PITFALL-7 — accumulator preserved)
    - `grep -c "wipe(activeBySpell)" Display.lua` returns 1 (PITFALL-7 — accumulator preserved)
    - `grep -c "activeBarBySpell\[slot.spellID\]" Display.lua` returns at least 1 (layout loop read site unchanged)
    - `grep -c "activeBySpell\[entry.spellID\]" Display.lua` returns at least 1 (icon layout loop read site unchanged)
    - ALL-FILE-SWEEP: `grep -rn "metaSlot" --include="*.lua" .` returns NO MATCHES in any .lua file (BuffEngine already cleaned in Task 2; Display cleaned here; Providers was never polluted; CDMTab had no references)
  </acceptance_criteria>

  <done>
    Display.lua bar and icon layout loops index by timer.key (with fallback to timer.spellID). No metaSlot references anywhere in the codebase. wipe() accumulator pattern preserved. stylua passes.
  </done>
</task>

</tasks>

<verification>
- `stylua --check BuffEngine.lua Display.lua` passes
- `grep -rn "metaSlot" --include="*.lua" .` returns zero matches across all Lua files
- `grep -rn "if trinketDef then\|if potDef then" --include="*.lua" .` returns zero matches
- BuffEngine.OnSpellCastSucceeded has a single function body line (the DispatchEventToProviders call) plus comments
- ScanActiveTimersForCancellation reads timer.castSpellID with fallback and type-guards
- ns.metaIcons, ns.metaAtRest, ns:RefreshMetaIcons, ns:GetAtRestMetaIcon, ns:GetAtRestMetaInfo, ns:StartAllPreviewTimers, ns:OnUnitAura, ns:StartLustTimer, ns.SATED_DEBUFF_TO_LUST, ns.CLASS_LUST_SPELL, ns.SUGGESTED_BUFFS all preserved in BuffEngine.lua
- CURRENT_SCHEMA_VERSION = 3 unchanged (PITFALL-1 guard)
- LUST-01 pre-gate ordering in OnUnitAura intact (Phase 19 scope)
- All tests pass end-to-end in Plan 18-03 human-verify checkpoint
</verification>

<success_criteria>
- PROV-02 satisfied: BuffEngine.OnSpellCastSucceeded contains no event-specific if/elseif chains
- LIFE-01 satisfied: ns.activeTimers is the single authoritative table; trinket/pot procs overwrite by string key
- Cancellation scan works for all three cast-triggered providers (user, trinket, pot)
- Preview mode unaffected (PITFALL-4 guard — StartAllPreviewTimers untouched)
- Lust detection and cancellation unaffected (Phase 19 scope preservation)
- Meta-icon cache and CDMTab integration unaffected (D-09 hybrid split)
</success_criteria>

<output>
After completion, create `.planning/phases/18-trinketprovider-potprovider-buffengine-dispatch/18-02-SUMMARY.md` following the standard SUMMARY template.
</output>
