---
phase: 05.1-edit-mode-settings-popup
verified: 2026-03-30T03:04:09Z
status: passed
score: 11/11 must-haves verified
re_verification: false
human_verification:
  - test: "Yellow NineSlice selection highlight appears on click"
    expected: "Clicking a TBT container in Edit Mode shows a yellow border around it; blue highlight remains on the unselected container"
    why_human: "NineSlice atlas rendering cannot be verified programmatically — requires in-game visual inspection"
  - test: "Click-vs-drag discrimination"
    expected: "A true drag (hold and move) repositions the container without opening the popup; a short click opens the popup"
    why_human: "DRAG_THRESHOLD=4 pixel check is correct in code but the actual feel of the threshold can only be assessed in-game"
  - test: "Real-time setting preview"
    expected: "Adjusting a slider (e.g., Scale) in the popup immediately resizes bars/icons without reloading"
    why_human: "Live rendering feedback requires an active game session"
  - test: "Settings persist across /reload"
    expected: "After changing a setting and reloading, re-opening the popup shows the saved value"
    why_human: "SavedVariables round-trip requires a running WoW client"
  - test: "Fresh-install defaults match CDM visuals"
    expected: "Deleting SavedVariables and reloading produces bar/icon layout identical to CDM defaults"
    why_human: "Visual comparison requires in-game inspection"
---

# Phase 05.1: Edit Mode Settings Popup — Verification Report

**Phase Goal:** Clicking TBT containers in Edit Mode selects them (yellow highlight) and opens a settings popup for orientation, growth direction, scale, padding, visibility, bar width.
**Verified:** 2026-03-30T03:04:09Z
**Status:** passed
**Re-verification:** No — initial verification (user approved in-game prior to this report)

---

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Fresh install creates `ns.db.containerSettings` with CDM-matching defaults | VERIFIED | `Core.lua` lines 32-58: `if not ns.db.containerSettings` guard initializes `bars` and `buffs` sub-tables with orientation, growthDirection, scale, padding, barWidth, opacity, visibility, hideWhenInactive, showTimer, showTooltips |
| 2 | Display.lua reads bar/icon settings from `ns.db.containerSettings` instead of hardcoded tables | VERIFIED | `Display.lua` lines 19-62: `cachedBarSettings`/`cachedIconSettings` are empty declarations; `RefreshContainerSettings()` reads all values from `ns.db.containerSettings.bars`/`.buffs` |
| 3 | Existing behavior is identical — no visual regression | VERIFIED | Default values in `Core.lua` (`scale=100`, `padding=5`, `barWidth=100`, `visibility=0`, `hideWhenInactive=true`) map via `RefreshContainerSettings` to the same effective values used by `ShouldShow` and `ApplyBarStyle`; backfill block (lines 59-76) handles existing DBs missing new fields |
| 4 | Clicking a TBT container in Edit Mode shows a yellow selected NineSlice border | VERIFIED | `SELECTED_NINESLICE_LAYOUT` defined (lines 76-96) using `editmode-actionbar-selected` atlas; `CreateSelectedOverlay` (lines 124-139) creates NineSlice frame at `frameLevel+11`; `ns.barSelectedOverlay`/`ns.iconSelectedOverlay` created in `InitEditModeFrames` (lines 766-767); `SelectContainer` calls `overlay:Show()` (line 163) |
| 5 | Only one container has the selected border at a time | VERIFIED | `ns:SelectContainer` (lines 149-166) deselects the other container first (`ns:DeselectContainer(selectedContainer)` when `selectedContainer ~= which`); module-level `selectedContainer` tracks current state |
| 6 | Click-and-drag moves the container without selecting | VERIFIED | `WireSelectableDrag` (lines 527-552) records `dragStartX/Y` on `OnMouseDown`, checks pixel delta against `DRAG_THRESHOLD=4` on `OnMouseUp` — selection only fires when `not moved` |
| 7 | A settings popup appears anchored to the selected container | VERIFIED | `CreateSettingsPopup` anchors at `BOTTOMRIGHT UIParent -250,200` (line 208); popup created lazily in `ns:ShowSettingsPopup` (line 314-315); `SelectContainer` calls `ns:ShowSettingsPopup(which)` (line 165) |
| 8 | Popup has orientation, growth direction, scale, padding, visibility controls (plus bar width for bars) | VERIFIED | Bars branch (lines 407-418): Icon Size, Icon Padding, Bar Width, Opacity, Visibility, Hide When Inactive, Show Timer, Show Tooltips. Buffs branch (lines 419-430): Orientation, Icon Direction, Icon Size, Icon Padding, Opacity, Visibility, Hide When Inactive, Show Timer, Show Tooltips |
| 9 | Popup uses Blizzard EditMode setting templates (dropdowns and sliders) | VERIFIED | `popup.pools:CreatePool("FRAME", settings, "EditModeSettingDropdownTemplate")` and `"EditModeSettingSliderTemplate"` and `"EditModeSettingCheckboxTemplate"` (lines 235-238); `AddDropdown` acquires from dropdown pool, `AddSlider` from slider pool |
| 10 | Popup hides when Edit Mode exits | VERIFIED | `ns:OnEditModeExit` (lines 720-742) calls `ns:ClearSelection()` (line 728) which calls `ns:HideSettingsPopup()` (line 178); `HideSettingsPopup` hides the popup and releases all pools (lines 445-451) |
| 11 | Changing a setting updates the display in real-time | VERIFIED | Every `AddDropdown` radio callback and `AddSlider` OnValueChanged handler calls `ns.RefreshContainerSettings()` then `ns:UpdateDisplay()` (lines 341-343, 373-375, 391-393); `ns.RefreshContainerSettings` is exposed on namespace (Display.lua line 62) and calls `RefreshContainerSettings()` which repopulates cached tables |

**Score:** 11/11 truths verified

---

### Required Artifacts

#### Plan 01 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Core.lua` | `containerSettings` DB init with defaults | VERIFIED | Lines 32-76: init guard + backfill for new fields |
| `Display.lua` | DB-backed settings reads replacing hardcoded tables | VERIFIED | `cachedBarSettings`/`cachedIconSettings` declared empty; `RefreshContainerSettings()` populates from DB; called in `InitDisplay()` (line 338) |

#### Plan 02 Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `EditModeFrames.lua` (SelectContainer) | Selection system | VERIFIED | `ns:SelectContainer`, `ns:DeselectContainer`, `ns:ClearSelection` all present and wired |
| `EditModeFrames.lua` (TBTSettingsPopup) | Settings popup singleton | VERIFIED | `CreateSettingsPopup()` creates `"TBTSettingsPopup"` frame; stored in module-level `tbtSettingsPopup` |
| `EditModeFrames.lua` (SELECTED_NINESLICE_LAYOUT) | Yellow NineSlice layout | VERIFIED | Defined lines 76-96 with `editmode-actionbar-selected` atlas family |

---

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `Core.lua` (ADDON_LOADED) | `ns.db.containerSettings` | `if not ns.db.containerSettings` guard | VERIFIED | Lines 32-58 in Core.lua |
| `Display.lua` (RefreshContainerSettings) | `ns.db.containerSettings` | Direct field reads from `ns.db.containerSettings.bars`/`.buffs` | VERIFIED | Lines 35-59 in Display.lua |
| `Display.lua` (InitDisplay) | `RefreshContainerSettings()` | Called once at end of InitDisplay | VERIFIED | Line 338 in Display.lua |
| `ns.RefreshContainerSettings` (namespace) | `RefreshContainerSettings` (local fn) | `ns.RefreshContainerSettings = RefreshContainerSettings` | VERIFIED | Line 62 in Display.lua |
| `EditModeFrames.lua` (popup OnValueChanged) | `ns.db.containerSettings` + `ns.RefreshContainerSettings()` | Direct DB write + refresh in AddDropdown/AddSlider/AddCheckbox callbacks | VERIFIED | Lines 341-343, 373-375, 391-393 in EditModeFrames.lua |
| `EditModeFrames.lua` (OnMouseUp) | `ns:SelectContainer` | `WireSelectableDrag` DRAG_THRESHOLD check | VERIFIED | Lines 527-552 in EditModeFrames.lua |
| `EditModeFrames.lua` (OnEditModeExit) | `ns:ClearSelection` | Called before save/cleanup | VERIFIED | Line 728 in EditModeFrames.lua |
| `GLOBAL_MOUSE_DOWN` | `ns:ClearSelection` | `clickDetector` event frame, `IsMouseOverTBTFrame()` guard | VERIFIED | Lines 667-690 in EditModeFrames.lua |
| `pcall(ClearSelectedSystem)` | Blizzard selection cleared | `SelectContainer` calls pcall to avoid taint | VERIFIED | Lines 156-159 in EditModeFrames.lua |
| `ShowUIPanel(CooldownViewerSettings)` | ESC-closable CDM settings | TBT Settings button uses ShowUIPanel | VERIFIED | Lines 291-293 in EditModeFrames.lua |

---

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|---------------|--------|--------------------|--------|
| `Display.lua` (UpdateDisplay — bars) | `cachedBarSettings` | `RefreshContainerSettings()` reads `ns.db.containerSettings.bars` | Yes — reads live DB values | FLOWING |
| `Display.lua` (UpdateDisplay — icons) | `cachedIconSettings` | `RefreshContainerSettings()` reads `ns.db.containerSettings.buffs` | Yes — reads live DB values | FLOWING |
| `EditModeFrames.lua` (popup controls) | `ns.db.containerSettings[containerKey][settingKey]` | Direct DB read at `AddSlider`/`AddDropdown` call time; writes back in callbacks | Yes — reads and writes live DB | FLOWING |

---

### Behavioral Spot-Checks

Step 7b: SKIPPED (no runnable entry points — WoW addon, requires live game client). User performed in-game verification and approved.

---

### Requirements Coverage

Requirements for this phase are defined by CONTEXT.md decisions D-01 through D-11 (no REQUIREMENTS.md IDs assigned — inserted phase).

| Decision | Description | Status | Evidence |
|----------|-------------|--------|---------|
| D-01 | Yellow NineSlice on click using `editmode-actionbar-selected` atlas | SATISFIED | `SELECTED_NINESLICE_LAYOUT` uses `editmode-actionbar-selected` atlas; `CreateSelectedOverlay` applies it |
| D-02 | Only one container selected at a time | SATISFIED | `SelectContainer` deselects previous before selecting new |
| D-03 | Click-and-hold starts drag without selecting; click selects | SATISFIED | `WireSelectableDrag` with `DRAG_THRESHOLD=4` |
| D-04 | Full CDM-like controls per container type (bars: +Bar Width; buffs: +Orientation, Growth Direction) | SATISFIED | Bars popup: Icon Size, Icon Padding, Bar Width, Opacity, Visibility, Hide When Inactive, Show Timer, Show Tooltips. Buffs popup: Orientation, Icon Direction, Icon Size, Icon Padding, Opacity, Visibility, Hide When Inactive, Show Timer, Show Tooltips |
| D-05 | Blizzard UI templates — dropdowns, sliders, checkboxes | SATISFIED | `EditModeSettingDropdownTemplate`, `EditModeSettingSliderTemplate`, `EditModeSettingCheckboxTemplate` pools |
| D-06 | Match CDM Edit Mode settings popup style | SATISFIED | `ResizeLayoutFrame` + `DialogBorderTranslucentTemplate` + `VerticalLayoutFrame` matching CDM pattern |
| D-07 | Popup anchored to selected container | SATISFIED | Anchored at `BOTTOMRIGHT UIParent -250,200` (fixed CDM-matching position); user verified position in-game |
| D-08 | Popup hides when Edit Mode exits or container is deselected | SATISFIED | `OnEditModeExit` → `ClearSelection` → `HideSettingsPopup`; close button calls `ClearSelection` |
| D-09 | Per-container settings in `ns.db.containerSettings.bars` / `.buffs` | SATISFIED | Core.lua initializes both sub-tables; all popup writes target `ns.db.containerSettings[key]` |
| D-10 | Display.lua reads from `ns.db.containerSettings` instead of hardcoded values | SATISFIED | `RefreshContainerSettings()` replaces all hardcoded table literals |
| D-11 | Default values match current hardcoded CDM-matching behavior | SATISFIED | Defaults: `scale=100`, `padding=5`, `barWidth=100`, `hideWhenInactive=true`; backfill block handles existing DBs |

---

### Anti-Patterns Found

No blockers. Notable observations:

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `EditModeFrames.lua` | 433-438 | `C_Timer.After(0, ...)` deferred layout call guards `pendingShow` flag | Info | Layout deferral is intentional to let ResizeLayoutFrame measure children; not a stub |
| `Core.lua` | 39 | `barWidth=100` default (percentage) differs from Plan 01's documented default of `barWidth=220` (absolute px) | Info | Plan 01 SUMMARY documented `barWidth=220` but actual implementation uses percentage — consistent with the "Bar Width as percentage (50-200%)" decision in Plan 02 SUMMARY; `Display.lua:39` correctly scales: `BAR_WIDTH * (bs.barWidth or 100) / 100` |
| `EditModeFrames.lua` | 627-639 | `OnUpdate` polling at 0.25s interval to track `EditModeManagerFrame` position | Warning | Necessary for the floating panel to follow the dialog, but runs while Edit Mode is active; acceptable cost for a non-hot-path frame |

---

### Human Verification Required

All human verification was completed in-game prior to this report. The user approved all checks. Items documented here for record:

**1. Yellow NineSlice visual**
**Test:** Enter Edit Mode, click TBT Bars container.
**Expected:** Yellow border appears around bars container; unselected container keeps blue highlight.
**Why human:** Atlas rendering requires live game client.

**2. Click-vs-drag feel**
**Test:** Click-and-hold-drag a container, then release and do a quick click.
**Expected:** Drag moves without popup; click opens popup.
**Why human:** DRAG_THRESHOLD=4 pixel check is correct in code but subjective feel requires in-game assessment.

**3. Real-time preview**
**Test:** Adjust Scale slider in the popup.
**Expected:** Bars/icons grow or shrink immediately.
**Why human:** Live rendering feedback requires active game session.

**4. Settings persistence**
**Test:** Change a setting, `/reload`, re-enter Edit Mode, click container, verify popup shows saved value.
**Why human:** SavedVariables round-trip requires a running WoW client.

**5. Fresh-install defaults**
**Test:** Delete SavedVariables file, reload, verify bar and icon layout matches CDM defaults.
**Why human:** Visual comparison requires in-game inspection.

---

### Gaps Summary

No gaps. All 11 observable truths are verified against the codebase. All decisions D-01 through D-11 are satisfied. Key implementation details confirmed present:

- Yellow `editmode-actionbar-selected` NineSlice with `OnSizeChanged` re-apply tracking
- Settings popup at `BOTTOMRIGHT UIParent -250,200` with Blizzard templates
- Slider Init guard (`tbtInitializing` flag) prevents value corruption from recycled pool frames
- Copy Blizzard CDM Config button reads live CDM settings via `GetSettingValue`
- Visibility options: Always Visible / In Combat / Hidden (3-value, not 4)
- Bar Width as percentage (50-200%) with `BAR_WIDTH * value / 100` scaling
- `GLOBAL_MOUSE_DOWN` for outside-click deselection with `IsMouseOverTBTFrame()` guard
- `pcall(ClearSelectedSystem)` clears Blizzard selection without taint risk
- `ShowUIPanel(CooldownViewerSettings)` for ESC-closable CDM settings panel
- `ns.RefreshContainerSettings` exposed on namespace for cross-module use

---

_Verified: 2026-03-30T03:04:09Z_
_Verifier: Claude (gsd-verifier)_
