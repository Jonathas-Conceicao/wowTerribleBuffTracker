---
phase: 22-display-lua-unification
plan: 03
type: execute
wave: 3
depends_on: ["22-01", "22-02"]
files_modified:
  - CDMTab.lua
autonomous: false
requirements: [DISP-01]

must_haves:
  truths:
    - "CDMTab.lua no longer writes ns.metaIconsDirty — the orphaned setter is deleted"
    - "StartPreview body otherwise unchanged — RefreshMetaIcons + RefreshTBTSections + StartAllPreviewTimers sequence preserved"
    - "In-game: trinket bar renders correctly (icon from cast spellID, tooltip shows trinket spell)"
    - "In-game: pot bar renders correctly"
    - "In-game: lust bar renders correctly (icon class-aware, tooltip shows class lust spell)"
    - "In-game: user-spell bar/icon renders correctly"
    - "In-game: cancellation works for all 4 buff types (including lust group check via aliveBuffs)"
    - "In-game: mid-CDM cast during preview persists after CDM close (Phase 21 behavior preserved)"
    - "In-game: tooltips show correct spell name for both active timers and at-rest placeholders"
    - "In-game: icon cache refreshes when equipped trinket swaps (close + reopen CDM)"
  artifacts:
    - path: "CDMTab.lua"
      provides: "StartPreview body with metaIconsDirty setter removed (line 20)"
      contains: "ns:StartAllPreviewTimers"
  key_links:
    - from: "CDMTab.lua StartPreview"
      to: "ns:StartAllPreviewTimers"
      via: "direct call"
      pattern: "ns:StartAllPreviewTimers"
    - from: "CDMTab.lua StartPreview"
      to: "ns:RefreshMetaIcons"
      via: "direct call (preserved)"
      pattern: "ns:RefreshMetaIcons"
---

<objective>
Delete the single orphaned `ns.metaIconsDirty = true` setter in CDMTab.lua:20 (the flag's readers and clearer were removed in Plan 22-02) and verify in-game that the complete Phase 22 refactor (Plans 22-01 + 22-02 + this 1-line fix) produces zero user-visible behavior changes across all four buff types.

Per D-25 (CDMTab writer deletion). Per ROADMAP success criteria: "All active timer bars and icons render correctly for all four buff types with icons matching pre-refactor behavior."

Purpose: Complete the metaIconsDirty flag removal started in Plan 22-02 and validate the entire Phase 22 refactor against live gameplay. This is the phase-closing human-verification gate.

Output: CDMTab.lua with line 20 deleted (1-line edit); human confirmation that all four buff types render identically to Phase 21.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-display-lua-unification/22-CONTEXT.md
@.planning/phases/22-display-lua-unification/22-01-PLAN.md
@.planning/phases/22-display-lua-unification/22-02-PLAN.md
@CLAUDE.md
@CDMTab.lua

<interfaces>
<!-- CDMTab.lua StartPreview current body (lines 13-22): -->

local function StartPreview()
    -- D-14 / ICON-05: Refresh at-rest meta-icons (trinket/pot) before preview begins.
    -- Combat-gated inside RefreshMetaIcons (D-07) — safe to call unconditionally.
    ns:RefreshMetaIcons()
    ns:RefreshTBTSections()
    ns.configOpen = true
    -- Signal placeholder icons need a refresh (stale from prior CDM session or live swap).
    ns.metaIconsDirty = true       -- LINE 20 — DELETE THIS LINE (D-25)
    ns:StartAllPreviewTimers()
end

<!-- After the deletion, StartPreview becomes: -->

local function StartPreview()
    -- D-14 / ICON-05: Refresh at-rest meta-icons (trinket/pot) before preview begins.
    -- Combat-gated inside RefreshMetaIcons (D-07) — safe to call unconditionally.
    ns:RefreshMetaIcons()
    ns:RefreshTBTSections()
    ns.configOpen = true
    ns:StartAllPreviewTimers()
end

<!-- CDMTab still depends on (UNTOUCHED in Phase 22): -->
-- ns:RefreshMetaIcons (BuffEngine.lua wrapper that iterates ns.providers)
-- ns:RefreshTBTSections (CDMTab-internal)
-- ns.configOpen (shared flag with Display.lua)
-- ns:StartAllPreviewTimers (BuffEngine.lua — now writes icon-free preview procs per Plan 22-01)
-- ns:ClearAllTimers (BuffEngine.lua — wipes ns.previewTimers only)
-- ns:GetAtRestMetaInfo / ns:ResolveSuggestedSpellID / ns:GetAtRestMetaIcon (shims; used by CDMTab tooltip + icon resolution at lines 86-98, 435-440, 790-795 — stay until Phase 23 migration; DO NOT touch in this plan)
-- ns.CLASS_LUST_SPELL / ns.GetHunterLustSpell (still used by CDMTab Suggested section; stay until Phase 23)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete ns.metaIconsDirty setter line in CDMTab.lua:20 (and the preceding comment)</name>
  <files>CDMTab.lua</files>

  <read_first>
    - CDMTab.lua (lines 1-30 — StartPreview function)
    - .planning/phases/22-display-lua-unification/22-CONTEXT.md (D-25)
    - .planning/phases/22-display-lua-unification/22-02-PLAN.md (confirms Display-side readers + clearer already removed)
    - CLAUDE.md (stylua)
  </read_first>

  <action>
    Per D-25 final writer deletion. Surgical 2-line edit.

    **Edit: Delete CDMTab.lua line 20 and its preceding comment line 19**

    Currently in CDMTab.lua StartPreview function (approximately lines 13-22):

    ```lua
    local function StartPreview()
        -- D-14 / ICON-05: Refresh at-rest meta-icons (trinket/pot) before preview begins.
        -- Combat-gated inside RefreshMetaIcons (D-07) — safe to call unconditionally.
        ns:RefreshMetaIcons()
        ns:RefreshTBTSections()
        ns.configOpen = true
        -- Signal placeholder icons need a refresh (stale from prior CDM session or live swap).
        ns.metaIconsDirty = true
        ns:StartAllPreviewTimers()
    end
    ```

    Delete these two lines:
    ```lua
        -- Signal placeholder icons need a refresh (stale from prior CDM session or live swap).
        ns.metaIconsDirty = true
    ```

    Resulting StartPreview body:

    ```lua
    local function StartPreview()
        -- D-14 / ICON-05: Refresh at-rest meta-icons (trinket/pot) before preview begins.
        -- Combat-gated inside RefreshMetaIcons (D-07) — safe to call unconditionally.
        ns:RefreshMetaIcons()
        ns:RefreshTBTSections()
        ns.configOpen = true
        ns:StartAllPreviewTimers()
    end
    ```

    Rationale (D-26): With numeric `proc.spellID` now always populated by providers (Plan 22-01) and per-widget icon cache keyed by spellID (Plan 22-02), cache invalidation happens naturally via spellID-change detection. When equipped trinket swaps A→B, the next RefreshMetaIcons updates TrinketProvider.atRest.spellID, which propagates through ns:GetDisplayInfoForKey into StartAllPreviewTimers' preview procs, which propagates into Display's per-widget cache on next render — `bar.cachedSpellID ~= info.spellID` fires and `bar.cachedIcon` refreshes. Flag is redundant.

    **Preservation checklist (DO NOT TOUCH):**
    - StopPreview body (lines 24-27) — UNCHANGED
    - All OTHER references to ns:GetAtRestMetaInfo (CDMTab.lua:86), ns:ResolveSuggestedSpellID (CDMTab.lua:94, 439, 794), ns:GetAtRestMetaIcon (CDMTab.lua:435, 790) — LEFT IN PLACE (Phase 23 migrates these)
    - All GameTooltip code paths in CDMTab (lines 76-147, 971-977, 1022-1027) — UNCHANGED (Phase 23 will migrate CDMTab tooltips to use ns:ShowBuffTooltip exported in Plan 22-02)
    - CDMTab drag-and-drop, section framework, add/delete dialogs — UNCHANGED
    - ICON_PATH constant, SECTION_DEFS, VALID_DROP_SECTIONS, tbtDragState — UNCHANGED
    - CreateIconFrame, RefreshTBTSections, BeginDrag, EndDrag — UNCHANGED

    **Run `stylua CDMTab.lua` after edit.**
  </action>

  <verify>
    <automated>
      cd C:/Users/jonat/Repositories/TerribleBuffTracker &amp;&amp;
      stylua --check CDMTab.lua &amp;&amp;
      ! grep -q "metaIconsDirty" CDMTab.lua &amp;&amp;
      grep -q "ns:RefreshMetaIcons()" CDMTab.lua &amp;&amp;
      grep -q "ns:RefreshTBTSections()" CDMTab.lua &amp;&amp;
      grep -q "ns:StartAllPreviewTimers()" CDMTab.lua &amp;&amp;
      grep -q "ns.configOpen = true" CDMTab.lua &amp;&amp;
      grep -q "local function StopPreview" CDMTab.lua &amp;&amp;
      ! grep -rq "metaIconsDirty" Providers.lua BuffEngine.lua Display.lua Core.lua CDMTab.lua EditModeFrames.lua
    </automated>
  </verify>

  <acceptance_criteria>
    - CDMTab.lua passes stylua --check
    - Zero `metaIconsDirty` references in CDMTab.lua
    - Zero `metaIconsDirty` references in the ENTIRE addon (Providers.lua, BuffEngine.lua, Display.lua, Core.lua, CDMTab.lua, EditModeFrames.lua all grep-clean)
    - StartPreview still calls ns:RefreshMetaIcons, ns:RefreshTBTSections, sets ns.configOpen, calls ns:StartAllPreviewTimers (in that order — D-25 preservation)
    - StopPreview unchanged
    - CDMTab.lua:86 ns:GetAtRestMetaInfo reference still present (Phase 23 territory)
    - CDMTab.lua ns:ResolveSuggestedSpellID references still present
    - CDMTab.lua ns:GetAtRestMetaIcon references still present
    - No changes to Core.lua, Providers.lua, BuffEngine.lua, Display.lua, EditModeFrames.lua in this plan
  </acceptance_criteria>

  <done>
    CDMTab.lua no longer writes ns.metaIconsDirty; flag is fully removed from the addon. StartPreview sequence preserved. Ready for in-game verification.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Deploy to WoW via install.bat</name>
  <files>(no file edits — runs install script)</files>

  <read_first>
    - CLAUDE.md (install.bat workflow)
    - scripts/install.bat (path verification)
  </read_first>

  <action>
    Run the install script to copy the addon to WoW's AddOns folder so the human verifier can test in-game:

    ```bash
    ./scripts/install.bat
    ```

    The script copies all .lua, .xml, .toc, and .blp files to `%PROGRAMFILES(x86)%\World of Warcraft\_retail_\Interface\AddOns\TerribleBuffTracker`.

    If WoW is currently running, the verifier will need to `/reload` in-game (or fully exit and relaunch) for the new files to load.
  </action>

  <verify>
    <automated>
      cd C:/Users/jonat/Repositories/TerribleBuffTracker &amp;&amp;
      ls "/c/Program Files (x86)/World of Warcraft/_retail_/Interface/AddOns/TerribleBuffTracker/Providers.lua" 2>/dev/null &amp;&amp;
      ls "/c/Program Files (x86)/World of Warcraft/_retail_/Interface/AddOns/TerribleBuffTracker/BuffEngine.lua" 2>/dev/null &amp;&amp;
      ls "/c/Program Files (x86)/World of Warcraft/_retail_/Interface/AddOns/TerribleBuffTracker/Display.lua" 2>/dev/null &amp;&amp;
      ls "/c/Program Files (x86)/World of Warcraft/_retail_/Interface/AddOns/TerribleBuffTracker/CDMTab.lua" 2>/dev/null
    </automated>
  </verify>

  <acceptance_criteria>
    - install.bat exits with 0 status
    - Providers.lua, BuffEngine.lua, Display.lua, CDMTab.lua present in the WoW AddOns install folder
    - No file copy errors in script output
  </acceptance_criteria>

  <done>
    Addon deployed to WoW AddOns folder; human verifier can launch WoW and /reload to test Phase 22 behavior.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — all four buff types render identically to Phase 21 (zero visual regression)</name>
  <files></files>

  <read_first>
    - .planning/phases/22-display-lua-unification/22-CONTEXT.md (behavior expected section)
    - .planning/phases/21-preview-mode-migration/21-VERIFICATION.md (template for verification outcome format)
    - .planning/research/PITFALLS.md (PITFALL-4 cancellation regression signals)
  </read_first>

  <what-built>
    **Phase 22 complete refactor — the three plans together:**

    - **Plan 22-01:** Proc shape collapsed to 9 fields across all providers; cancellation scan unified to single-branch aliveBuffs iteration; SHARED_LUST_BUFFS demoted to Providers-local; preview procs no longer carry icon field.
    - **Plan 22-02:** Display.lua consumes uniform proc shape; GetSuggestedAtRestIcon helper deleted; type-discriminating branches deleted; ns:ShowBuffTooltip shared handler exported and consumed by both bar/icon OnEnter; per-widget icon cache via frame.cachedSpellID / frame.cachedIcon; wipe() accumulators + frame pools preserved.
    - **Plan 22-03:** metaIconsDirty flag writer deleted from CDMTab.lua (last remaining reference across the addon).

    **Expected outcome: ZERO user-visible behavior changes.** Phase 22 is a refactor — every buff type, bar, icon, tooltip, cancellation, preview, and mid-CDM cast interaction must behave identically to Phase 21 (last known-good state from commit 8b585c8).
  </what-built>

  <action>
    The executor does not write code in this task. The executor's job is to:

    1. Confirm Task 1 committed and Task 2 install.bat completed successfully.
    2. Present the `<how-to-verify>` 12-step matrix below to the user verbatim.
    3. WAIT for the user's response. Do not make assumptions. Do not attempt to launch WoW from the command line (client is user-driven).
    4. On user reply, record the outcome in the SUMMARY for this plan (Task 3's resume-signal response).

    Before presenting the matrix, run a final grep sweep and report results to the user so they know code-level checks passed before in-game verification starts:

    ```bash
    echo "=== metaIconsDirty sweep (expect zero hits) ==="
    grep -rn "metaIconsDirty" Providers.lua BuffEngine.lua Display.lua Core.lua CDMTab.lua EditModeFrames.lua || echo "(clean)"
    echo "=== type-discriminator sweep (expect zero hits) ==="
    grep -rn 'type([^)]*) == "string"' Display.lua || echo "(clean)"
    echo "=== ShowBuffTooltip definition + call sites ==="
    grep -n "ShowBuffTooltip" Display.lua
    echo "=== aliveBuffs surface ==="
    grep -n "aliveBuffs" Providers.lua BuffEngine.lua
    ```
  </action>

  <how-to-verify>
    **PRE-CHECK (Claude automates before checkpoint fires):**
    - Confirm no Lua errors on `/reload` (check /console scriptErrors or look for red error frames)
    - Confirm `/tbt` or `/terriblebufftracker` opens CDM with the TBT tab visible

    **Step 1: Trinket cast — active bar and tooltip**
    1. Equip a tracked trinket (any item from trinket_info.csv — e.g. Algeth'ar Puzzle Box 193701 or Light Company Guidon 249344)
    2. Use the trinket (on-use activate) OUT of combat
    3. EXPECTED: Trinket bar appears in the bar container with the trinket's buff icon (NOT a question mark)
    4. EXPECTED: Bar label shows the trinket's spell name
    5. EXPECTED: Bar countdown matches the trinket's expected duration (e.g. 20s for Algeth'ar)
    6. Hover the bar
    7. EXPECTED: Tooltip shows the trinket's spell (SetSpellByID via ns:ShowBuffTooltip)

    **Step 2: Pot cast — active bar and tooltip**
    1. Have a tracked damage pot in bags (e.g. Light's Potential 241308)
    2. Use the pot OUT of combat
    3. EXPECTED: Pot bar appears in the bar container with the pot's buff icon
    4. EXPECTED: Countdown matches pot duration (30s for Light's Potential)
    5. Hover the bar — tooltip shows pot spell

    **Step 3: User-created spell — active bar/icon and tooltip**
    1. Ensure you have a user-tracked spell (e.g. a cooldown you added via CDM)
    2. Cast the spell
    3. EXPECTED: Bar or icon appears (depending on which section the entry is in) with correct icon, label, countdown
    4. Hover — tooltip shows the spell

    **Step 4: Lust tracking — active bar, group check, tooltip**
    1. Join a group that will use Bloodlust / Heroism / Time Warp (or solo via a Drum of Fury 1243972 if possible)
    2. Get the Sated-family debuff
    3. EXPECTED: Lust bar appears with class-aware lust icon (Bloodlust for Shaman, Time Warp for Mage, etc.)
    4. EXPECTED: Countdown is 40s
    5. Hover — tooltip shows the class-specific lust spell

    **Step 5: Cancellation works (buff dispelled / dropped early)**
    1. While ANY tracked bar is active, manually cancel the buff (right-click to cancel, or have it dispelled)
    2. EXPECTED: Bar disappears within ~1 UNIT_AURA cycle (< 1 second)
    3. With `/tbt debug` toggled on, EXPECTED: "Cancelled N timer(s): <label>" message prints
    4. This tests the NEW aliveBuffs-based scan (Plan 22-01 D-09)

    **Step 6: Lust group cancellation (regression guard)**
    1. If possible, test in a group where Heroism and Drums might overlap (or simulate: get Bloodlust, have it drop; confirm bar disappears)
    2. EXPECTED: Lust bar disappears ONLY when ALL buffs in the aliveBuffs list are absent (e.g., Heroism + Drums both gone)
    3. This tests the multi-buff aliveBuffs path specifically — LustProvider's `aliveBuffs = SHARED_LUST_BUFFS_LOCAL[lustSpellID]`

    **Step 7: Preview mode — all 4 buff types render correctly at rest**
    1. Open CDM via /tbt or from the minimap/CDM UI
    2. EXPECTED: Preview mode activates; placeholder bars/icons show for all non-hidden tracked buffs
    3. EXPECTED: Trinket placeholder shows equipped trinket's icon (NOT question mark, NOT CSV-fallback if an equipped trinket matches)
    4. EXPECTED: Pot placeholder shows first-in-bag pot icon
    5. EXPECTED: Lust placeholder shows class-aware lust icon
    6. EXPECTED: User-spell placeholders show correct icons
    7. EXPECTED: Duration bars show proper remaining-time gradient (blue → yellow → red) — trinket/pot show non-zero durations (LIFE-03 Phase 21 preserved)

    **Step 8: Mid-CDM real cast persists (Phase 21 architectural behavior)**
    1. With CDM open (preview active), cast a trinket or user-spell
    2. EXPECTED: Real cast bar appears, overlaying the preview for that slot
    3. Close CDM
    4. EXPECTED: Real cast bar CONTINUES to run (ns.previewTimers cleared, ns.activeTimers preserved per Phase 21 design)

    **Step 9: Tooltip on placeholders (at-rest hover)**
    1. With CDM preview open (or with hideWhenInactive = false), hover a placeholder bar or icon
    2. EXPECTED: Tooltip shows the resolved at-rest spell (e.g. equipped trinket's spell, class lust spell, user spell)
    3. This tests the Plan 22-02 synthetic-proc path — `bar.proc = { spellID = info.spellID, label = info.label, key = slot.spellID }` fallback

    **Step 10: Icon cache refresh on trinket swap**
    1. With CDM open, swap equipped trinket A → B (must be out of combat)
    2. Close CDM, reopen CDM
    3. EXPECTED: Trinket placeholder icon updates to the new trinket's icon (RefreshMetaIcons on CDM open → TrinketProvider.atRest.spellID updates → ns:GetDisplayInfoForKey returns new spellID → per-widget cache `bar.cachedSpellID ~= info.spellID` fires → new icon rendered)

    **Step 11: No Lua errors**
    1. Check `/console scriptErrors 1` or look at error frames across all above steps
    2. EXPECTED: Zero Lua errors from TBT during any of the above interactions

    **Step 12: Edit Mode regression check**
    1. Enter Blizzard Edit Mode
    2. EXPECTED: TBT bar and buff containers still selectable/draggable
    3. EXPECTED: Bars/icons still visible in Edit Mode (per editModeActive gating in Display.lua)

    **What to report:**
    - **APPROVED** if all 12 steps behave as expected.
    - **REGRESSION** with exact step number, what was expected, what was observed, and any Lua error text.
  </how-to-verify>

  <verify>
    <automated>MISSING — checkpoint tasks are human-gated; verification automation is the grep sweep run in the action block plus the user's in-game approval. Gap closure via `/gsd:plan-phase --gaps` if any step fails.</automated>
  </verify>

  <acceptance_criteria>
    - User has executed all 12 verification steps and reported either APPROVED or specific regressions
    - Regression reports include: step number, expected behavior, observed behavior, any Lua error text
    - Executor captures the user's response text verbatim for use in the plan SUMMARY
    - If Step 1-4 show wrong icons / question marks, that is a BLOCKING DISP-01 regression (icon cache or GetDisplayInfoForKey bug) — must be documented
    - If Step 5-6 show cancellation failures, that is a BLOCKING aliveBuffs population bug in a provider — must be documented
    - If Step 7-8 show LIFE-03 or Phase 21 regressions, that is a BLOCKING side-effect from the preview-proc shape change — must be documented
    - If Step 9 shows "Unknown" tooltip on placeholders, that is a BLOCKING synthetic-proc bug in Plan 22-02 — must be documented
  </acceptance_criteria>

  <done>
    User has responded with either "approved" or a regression report. The response is captured and ready to write into 22-03-SUMMARY.md.
  </done>

  <resume-signal>
    Type "approved" if all 12 verification steps pass with zero visual regression vs Phase 21 behavior.

    Type "ISSUE: &lt;description&gt;" if ANY step fails. Specify:
    - Which step # failed
    - What you observed vs what was expected
    - Screenshot if helpful
    - Lua error text if any

    Common failure modes to flag:
    - Question-mark icon instead of correct spell icon → icon-cache bug (Plan 22-02 frame.cachedSpellID / frame.cachedIcon)
    - Tooltip shows wrong spell or "Unknown" → ns:ShowBuffTooltip proc argument issue or bar.proc/icon.proc assignment issue
    - Bar doesn't disappear when buff is cancelled → aliveBuffs population bug in provider OnTrigger (Plan 22-01) or iteration bug in ScanActiveTimersForCancellation (Plan 22-01 Task 2)
    - Lust bar starts but then immediately cancels → aliveBuffs = SHARED_LUST_BUFFS_LOCAL[lustSpellID] returning nil (no fallback to { lustSpellID })
    - Preview mode shows 0-second bars → regression of LIFE-03 (Phase 21 behavior not preserved)
    - Mid-CDM real cast disappears on CDM close → regression of Phase 21 architectural fix
  </resume-signal>
</task>

</tasks>

<verification>
  Phase 22 complete when all 12 checkpoint steps pass. Code-level verification:

  - Grep sweep on final state (after all 3 plans land):
    - `grep -rn "metaIconsDirty" .` returns zero hits in all .lua files
    - `grep -rn "metaSlot" *.lua` returns zero hits
    - `grep -rn "GetSuggestedAtRestIcon" *.lua` returns zero hits
    - `grep -rn "ns.SHARED_LUST_BUFFS" *.lua` returns zero hits (demoted to local)
    - `grep -rn "timer\.source\|timer\.castSpellID\|timer\.lustBuffID" *.lua` returns zero hits
  - Display.lua: zero `type(...) == "string"` branches related to spellID
  - Display.lua: ns:ShowBuffTooltip called by both bar and icon OnEnter
  - BuffEngine.lua: ScanActiveTimersForCancellation is single-branch aliveBuffs iteration
  - All 4 providers: OnTrigger returns 9-field shape with numeric spellID + aliveBuffs
  - Core.lua: unchanged (git diff confirms)
  - Schema version 3: unchanged
  - stylua --check: passes on all .lua files
  - In-game: zero behavior regression vs Phase 21
</verification>

<success_criteria>
All 4 ROADMAP Phase 22 success criteria satisfied:

1. Display.lua contains zero references to metaSlot, ResolveSuggestedSpellID, or any type-discriminating branch — verified by code inspection AND grep sweep
2. All active timer bars and icons render correctly for all four buff types with icons matching pre-refactor behavior — verified by human verification Steps 1-4
3. A single tooltip handler function is used by both timer bars and buff icons — no duplicated OnEnter tooltip logic — verified by grep showing 1 definition of ns:ShowBuffTooltip + 2+ call sites
4. The wipe() accumulator pattern in the display update loop is preserved — no per-frame table allocation introduced — verified by grep showing wipe() calls preserved on all module-level reusable tables (barTimers, iconTimers, activeBarBySpell, barSlots, buffSlots, activeBySpell)

Additionally:
- DISP-01 requirement satisfied (Display.lua zero type-specific branches)
- DISP-03 requirement satisfied (shared tooltip handler)
- Phase 21 architectural behavior preserved (ns.previewTimers / ns.activeTimers separation, mid-CDM cast persistence)
- No Core.lua changes (untouched through all 3 plans)
- No SavedVariables schema change (CURRENT_SCHEMA_VERSION = 3)
- Human verifier approves all 12 verification steps
</success_criteria>

<output>
After completion, create `.planning/phases/22-display-lua-unification/22-03-SUMMARY.md` documenting:
- CDMTab.lua diff (2 lines removed: metaIconsDirty setter + comment)
- Human verification outcome: approved / issues encountered + resolution
- Final phase grep-sweep results confirming DISP-01 + DISP-03 compliance
- Link to phase SUMMARY (to be created by `/gsd:complete-phase` after this plan closes)

Leave CDMTab's ns:GetAtRestMetaInfo / ns:ResolveSuggestedSpellID / ns:GetAtRestMetaIcon call sites IN PLACE — Phase 23 migrates them. Leave ns.CLASS_LUST_SPELL / ns.GetHunterLustSpell exports IN PLACE — Phase 23 demotes them after CDMTab migration. ns:RefreshMetaIcons wrapper stays until Phase 24 cleanup. This is the minimum viable Phase 22 scope.
</output>
