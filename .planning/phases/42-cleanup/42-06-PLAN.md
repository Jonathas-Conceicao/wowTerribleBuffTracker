---
phase: 42-cleanup
plan: 06
type: execute
wave: 4
depends_on: [42-01, 42-02, 42-03, 42-04, 42-05]
files_modified:
  - .planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "Every hot-path finding carries an explicit written verdict: removed, or justified"
    - "Every duplication candidate from 42-CONTEXT.md carries a verdict, including the ones that turned out not to exist"
    - "stylua runs clean from the repo root with no flags and git diff shows no line-ending-only churn"
    - "The addon is deployed to every WoW client present, ready for Phase 43"
  artifacts:
    - path: ".planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md"
      provides: "The phase's written verdict record for success criteria 1 and 2"
      contains: "Verdict"
  key_links:
    - from: "42-HOT-PATH-AUDIT.md"
      to: "ns:UpdateDisplay and the Merge Mode event path"
      via: "one row per finding, each naming the file and function it was measured in"
      pattern: "UpdateDisplay"
---

<objective>
Close the phase with the two things it owes in writing rather than in code: the hot-path audit
(42-CONTEXT.md **D6** — a written verdict per finding, not just edits) and the duplication verdicts
for every candidate 42-CONTEXT.md listed, **including the ones that turned out not to exist**.

This plan runs last because the audit must describe the code as it ships, after plans 42-01 through
42-05 have landed. It changes no source file.

Purpose: ROADMAP success criteria 1, 2 and 4.

Output: `.planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md`, a clean `stylua`, a clean `git diff`,
and a deployed addon ready for Phase 43.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/42-cleanup/42-CONTEXT.md
@.planning/STATE.md
@Display.lua
@MergeMode.lua
@Core.lua
</context>

<constraints>
Binding for every task in this plan:

- **NO SOURCE CHANGES.** This plan writes one document and runs verification commands. If the audit
  turns up something worth fixing, record it as a finding with a verdict of "carry to a todo" and
  write the todo file — do **not** open a source file and fix it here. A late edit to the render
  path with no plan and no review is exactly what this phase exists to avoid.
- **NEVER touch `CHANGELOG.md`.**
- **Do not create, rename or switch a git branch.**
- **A verdict must be a verdict.** "Looks fine" is not one. Each row says either *removed* (naming
  the commit that removed it) or *justified* (naming the reason it must stay), and nothing else.

## The three structures that must not be "simplified" (42-CONTEXT.md D5)

The audit reads `MergeMode.lua` and `Display.lua` closely, which is exactly the condition under
which these look like waste. They are not:

1. **One aura container per merged entry per unit** (`MergeMode.lua`). An aura FRAME carries
   `DenyTaintedAccessWhenAurasAreSecret` and cannot be moved in combat; the CONTAINER carries no
   such restriction. TBT moves containers, never frames. Collapsing back to one container per unit
   breaks Centered growth outright. The per-entry container count is a cost the audit **records and
   justifies**, never a finding to remove.
2. **`ns:GridSlotPlacement` as the single source of grid arithmetic** (`Display.lua`), used by both
   Display and MergeMode. The two drifted three separate times when they each derived it. Inlining
   it "to save a call" is a regression, not an optimisation.
3. **`entry.cdmShown`, stamped from Blizzard's own CDM item frames.** It looks like it duplicates an
   aura lookup. It does not — the aura APIs are secret in combat and the engine's own visible count
   is unreadable; the CDM's `IsShown()` is the one plain boolean available.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Write the hot-path and duplication audit</name>
  <files>.planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md</files>
  <action>
Write `.planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md` with three sections.

### Section 1 — Hot-path findings

Scope: `ns:UpdateDisplay` and everything it reaches, plus the Merge Mode event path. Read the code
as it stands after plans 42-01..42-05. One row per finding:

| # | Site | What it costs | Verdict | Reason |

Cover at minimum the four D6 items, each of which was flagged during planning and each of which
needs confirming against the shipped code rather than copying from here:

1. **`ns:PlaceMergeAura` (`MergeMode.lua`)** — four separate stamp comparisons
   (`placedAnchor`/`placedX`/`placedY`/`placedScale`) per merged entry per tick, deliberately not a
   packed string key. Confirm the four-table shape is still what is there and say so. The expected
   verdict is *justified*: a packed key would allocate a string per merged entry per tick at 20 Hz,
   which is the cost the four comparisons exist to avoid. State it in those terms.
2. **`ApplyUserCooldown` (`Display.lua`)** — runs every tick by necessity. Nothing fires an event
   when a TBT-owned cooldown ends, so the grey has to come off by the clock. Confirm the steady-path
   cost is still two number comparisons and two stamp tests with no API call and no allocation.
3. **`ns:GetContainerCategory(def)`** — called per container per tick from `RenderIconContainer` to
   re-derive whether Centered applies. Confirm the function body is still
   `(def and def.category) or "buffs"` — one field read and a fallback. Record why it is re-derived
   rather than trusted from the database: a value left behind by a container that changed category
   must not produce a layout its dropdown never offered.
4. **The centred pre-pass (`RenderIconContainer`)** — walks `#slots` a second time calling
   `SlotDraws` before the layout loop, to learn `drawnCount`. Confirm it still runs **only** when
   `centered` is true, so a non-centred container pays nothing, and that `SlotDraws` itself
   allocates nothing.

Then extend past D6's list. At minimum also give a verdict on:

- `ns:UpdateDisplay`'s per-tick `wipe(timersByContainer[def.key])` loop over `ns.CONTAINERS`, now
  that Phase 36 made the container count unbounded.
- `RefreshCooldownSlotCounts` — confirm it is still gated on `ns.trackerGeneration` and is a single
  integer compare and return on the unchanged path.
- The four helpers plan 42-05 introduced — confirm each is called once per container per tick and
  not once per widget, and that `MergedSlotsFor` returns two values rather than a table.
- The `_placeholderProc` reuse plan 42-04 introduced — confirm the placeholder path now allocates
  nothing on the second and subsequent ticks.
- `ns.mergeShownSlots` / `ns.mergeItemFrames` — confirm both are built event-driven and only
  indexed from the render path, never rebuilt in it.
- The Merge Mode event path: `ns:QueueMergeShownSlots` and `ns:QueueMergeMirror`. Record their
  `C_Timer.After(0)` deferrals and their single-flight guards as **justified and load-bearing** —
  they defer because they read state Blizzard is mid-computation on, and `CallbackRegistryMixin`
  fires in registration order, so TBT's callback runs before the CDM viewers have rebuilt their
  item pools. Making either inline is a coin flip. This is a structure to protect, not a cost to
  remove.
- `ns:RefreshMergeAuraGroups` / `SyncEntryContainers` — the `{ includeSpellIDs = includeSet }` table
  constructor allocated per entry per refresh. Give it a verdict. It is event-driven (a
  configuration change), not per-tick, which is the whole argument; say so explicitly so the next
  reader does not mistake it for the per-tick allocation that D2 fixed.

### Section 2 — Duplication verdicts

One row per candidate from 42-CONTEXT.md's `<code_context>`, with a verdict. Three of the four were
checked during planning and each needs confirming, not copying:

| Candidate | Verdict |
|-----------|---------|
| Mutually-exclusive checkbox-pair idiom in `CDMTab.lua` | **Unified** in plan 42-03. Record the correction: the idiom appears **twice**, not the "at least three times" D4 estimated — the third instance was the add dialog's Buff/Cooldown pair, removed when `ADD-01`/`ADD-02` were dropped as controls on 2026-09-22. |
| Pooled-widget reset blocks in `RenderIconContainer` | **Unified** in plan 42-04 as `ClearCooldownStamps`. Record that there were **two** copies, not three, and that the placeholder copy's extra `_mergedExpiry` clear stayed out of the helper on purpose. |
| `MergeMode.lua`'s filter-building loop "written twice (create path and re-filter path) inside `SyncEntryContainers`" | **Does not exist.** Confirm by reading: the loop that fills `includeSet` from `entry.spellID` and `entry.linkedSpellIDs` appears **once**, above the `AURA_UNITS` loop, and both the create path and the re-filter path hand the *same* `includeSet` table to the engine. There is nothing to unify. Record this as a candidate that did not survive contact with the code, not as work skipped. |
| `ns.EnsureContainerSettings` defaults vs the Edit Mode settings popup's control list | **Real duplication, deliberately not unified.** Confirm and record that the same container settings are actually stated in **three** places with **different** fallbacks: `ns.EnsureContainerSettings` (`Core.lua`) seeds `padding = 5`, `scale = 100`, `opacity = 100`, `itemsPerRow = 12`; `RefreshContainerSettings` (`Display.lua`) re-states them as `or` fallbacks on read; and the popup's `AddSlider` falls back to `or minVal`, which for padding is `0`, not `5`. Because the three disagree, collapsing them **changes behaviour** for a partially-populated settings table — and no such table can be produced or observed before Phase 43. That is a reason to leave it, and to say why in writing, not a reason to pretend it is not duplication. |

### Section 3 — Out-of-scope findings, carried not fixed

Record what the sweep found that the scope fence protects, so Phase 43 and the next milestone
inherit it rather than rediscovering it:

- Nine unreferenced `ns.*` exports in `Providers.lua` — `ns.SpellProviderBaseMixin`,
  `ns.TrinketProviderMixin`, `ns.PotProviderMixin`, `ns.LustProviderMixin`,
  `ns.UserSpellProviderMixin`, `ns.TRINKET_SPELLS`, `ns.POT_SPELLS`, `ns.TRINKET_ITEM_IDS`,
  `ns.POT_ITEM_IDS`. Each has exactly one writer and no readers. All nine predate v0.4.0 (Phases
  17-22), so `PROJECT.md`'s "No refactors during cleanup phases" protects them. Confirm the counts
  and record them; do not delete them.
- The dead-function sweep found **zero** functions with no callers, local or namespaced, across all
  seven Lua files. Record the sweep that proves it so criterion 3 has evidence rather than an
  assertion.
- **Unused local VARIABLES, swept separately from functions.** ROADMAP criterion 3 names three
  categories — "unused variables, dead functions and stale comments" — and `CLAUDE.md`'s GSD
  Workflow repeats "unused variables and definitions". A dead-function sweep does not answer it: a
  `local x = ...` that is declared and never read is a different shape and a different grep. Sweep
  every `local` declaration added since Phase 31 and record the result as FIXED or CLEAN with the
  evidence, exactly as the dead-function sweep does. Expect it to come back clean; the point is
  that criterion 3 then rests on evidence rather than on the absence of a check. A practical
  approach, since there is no Lua linter in this repo: for each file, list `local <name>`
  declarations, then `grep -c` each name in that file — a count of 1 means declared and never
  used. Report per-file counts, not a bare "clean".
- The `"spells"` category key still means "Cooldowns" in the UI. Renaming it is explicitly deferred
  (42-CONTEXT.md Deferred Ideas) because it is persisted in `ns.db.userContainers` and would need a
  schema migration. Note it so the mismatch is not read as an oversight.

### Section 4 — Decisions and criteria closed without a code change

Two of this phase's obligations are already satisfied and would otherwise leave no written trace.
A phase whose record accounts for five of six decisions looks like one that missed the sixth.

- **42-CONTEXT.md D3 — delete `.luarc.json` and `foo.md` — is already done.** Both were removed
  when the phase context was committed. Record it as satisfied WITH the evidence rather than
  assuming it:

      git ls-files | grep -iE 'luarc|foo\.md'    # must print nothing
      ls -a | grep -iE 'luarc|foo\.md'           # must print nothing

  Note in one line what each was, so the record explains the decision rather than just logging it:
  `.luarc.json` was an editor-only Lua Language Server config carrying a machine-specific absolute
  path, and `foo.md` was a draft announcement post whose content is already published on
  CurseForge, Wago and GitHub.

- **ROADMAP criterion 4's "read end to end" needs an artifact.** The release tooling was read during
  planning, which is why 42-01 tells the executor not to re-read it — but a read that leaves no
  record is indistinguishable from one that did not happen. Write one line each for
  `scripts/install.bat`, `scripts/install.ps1`, `scripts/release.bat` and
  `.github/workflows/release.yml` stating what was confirmed correct. What that read established,
  to be restated rather than rediscovered: `install.ps1` derives the file set from the TOC,
  follows XML `file=` references, resolves the `IconTexture` extension, substitutes a dev version
  into the DEPLOYED TOC only so the packager keyword survives in the repo, and prunes stale files —
  with SavedVariables living under `WTF\\` so pruning cannot touch player data; `release.bat`
  carries the REL-01 branch guard with a deliberate `TBT_ALLOW_BRANCH=1` override; `release.yml`
  triggers on any tag, extracts the first `##` section of `CHANGELOG.md` into
  `RELEASE_NOTES.md`, and runs the BigWigs packager with CurseForge and Wago keys still commented
  out, so it publishes to GitHub only. The ONE defect that read found is fixed by 42-01: `.pkgmeta`
  omitted `.planning` from its ignore list, shipping 334 tracked planning files in every release.

Write in prose and tables, not bullets-of-bullets. This document is read by whoever runs Phase 43
and by whoever opens the render path next; it is the phase's only durable output.
  </action>
  <verify>
    <automated>test -f .planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md && test "$(grep -c 'Verdict\|verdict' .planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md)" -ge 3 && test "$(grep -c 'PlaceMergeAura' .planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md)" -ge 1 && test "$(grep -c 'ApplyUserCooldown' .planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md)" -ge 1 && test "$(grep -c 'GetContainerCategory' .planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md)" -ge 1 && test "$(grep -c 'QueueMergeMirror' .planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md)" -ge 1</automated>
All four D6 items and the Merge Mode deferral path are present.

Then confirm the source tree really is untouched by this plan:

    git status --porcelain -- '*.lua' '*.xml' '*.toc' .pkgmeta

must print nothing. A source file modified by this task is a plan violation.

Finally, confirm each claim the audit makes about the code by grepping it rather than trusting this
plan's summary of it. In particular:

    grep -c 'placedAnchor\|placedX\|placedY\|placedScale' MergeMode.lua   # four stamp tables still separate
    grep -c 'includeSet\[' MergeMode.lua                                   # one filter-fill site
    grep -c 'or minVal' EditModeFrames.lua                                 # the popup's differing fallback
  </verify>
  <done>`42-HOT-PATH-AUDIT.md` exists with all three sections; every hot-path finding and every duplication candidate carries an explicit *removed* or *justified* verdict; the three "did not survive contact with the code" / "deliberately not unified" / "out of scope by fence" cases are each recorded with their grep evidence; no source file changed.</done>
</task>

<task type="auto">
  <name>Task 2: Mark ROADMAP success criterion 1 PARTIAL</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Criterion 1 reads: *"Duplication introduced by this milestone has one implementation each —
container settings, tracker rendering and the add-panel surfaces are not copy-pasted per container
type or per tracker type."*

After this phase, tracker rendering and the add-panel surfaces are unified (42-05, 42-03). The
**container settings** clause is not, and deliberately so: the defaults are stated in three places
with three DIFFERENT fallbacks — `ns.EnsureContainerSettings` seeds `padding = 5`,
`ns.RefreshContainerSettings` re-states `src.padding or 5`, and the Edit Mode popup's
`AddSlider` falls back to `or minVal`, which for padding is `0`. Because the three disagree,
collapsing them changes behaviour for a partially-populated settings table — a state that cannot be
produced or tested before Phase 43, which makes it exactly the kind of change this phase's
"no behaviour changes" constraint forbids.

So annotate the criterion rather than quietly leaving it false. Follow the style Phase 40 already
used for STEAL-04/STEAL-05: append a short **PARTIAL** note naming what is unified, what is not,
and the one-line reason, with a pointer to the audit document for the full argument.

Do not reword the criterion itself — the bar it sets is still the right bar, and a future milestone
should be able to close it once the three fallbacks can be reconciled behind a test.
  </action>
  <verify>
    <automated>grep -c 'PARTIAL' .planning/ROADMAP.md</automated>
The annotation exists. Then read the Phase 42 success-criteria block and confirm the note names
container settings specifically, points at `42-HOT-PATH-AUDIT.md`, and leaves the original
criterion text intact.
  </verify>
  <done>ROADMAP Phase 42 criterion 1 carries a PARTIAL annotation explaining the container-settings
exception, in the same style Phase 40 used, with the criterion's own wording unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Phase closeout — stylua, diff hygiene, deploy</name>
  <files>(no files modified — verification only)</files>
  <action>
Run the phase's closing checks. This task edits nothing; it proves the phase left the addon in a
state worth testing, which is the bar 42-CONTEXT.md sets for a cleanup phase that runs *before* its
verification pass rather than after.

1. Run `stylua` from the repo root with **no flags**. The repo-root `stylua.toml` pins
   `line_endings = "Windows"`, so the bare invocation is the correct one — do not pass
   `--line-endings`, and do not pass a path list.
2. Run `git diff` (and `git diff` against the phase's first commit) and read it for line-ending-only
   hunks. This matters more than it looks: `core.autocrlf=true` with no `.gitattributes` makes the
   index `i/lf`, so an LF reflow produces an **empty** `git diff` and leaves no trace. The check is
   therefore that the diff is *non-empty where changes were made and empty everywhere else* — if a
   file you know was edited shows no diff, that is the failure mode, not a pass.
3. Confirm `CHANGELOG.md` was not touched anywhere in the phase:
   `git diff --name-only <phase-base>..HEAD | grep -c CHANGELOG` must print `0`. If it does not,
   stop and report to the user — do not "restore" the file.
4. Deploy with `./scripts/install.bat`. It takes no arguments and installs to every WoW client
   folder present on the machine, skipping absent ones. Confirm it reports at least one client and
   does not fail.
5. Do **not** launch the game and do **not** attempt any in-game check. Nothing in this phase is
   verifiable before Phase 43, and an improvised half-test is worse than none because it would be
   recorded as evidence.

Record the results in the plan summary: the `stylua` exit status, the list of files the phase
changed, and the `install.bat` client count.
  </action>
  <verify>
    <automated>stylua . && stylua --check . && test "$(git diff --name-only | grep -c CHANGELOG)" -eq 0</automated>
`stylua` must reformat nothing (the second invocation exiting 0 proves the first was a no-op or
converged), and `CHANGELOG.md` must not appear in the working-tree diff.

Then the line-ending check, which cannot be automated for the reason in the action above — read
`git diff --stat` for the whole phase and confirm every file you expect to have changed shows a
non-zero line count. A file with a known edit and a zero-line diff means an LF reflow silently
landed in the index.

Then `./scripts/install.bat` and read its output for the deployed client list.
  </verify>
  <done>`stylua` clean from the repo root with no flags; no line-ending-only churn and no silently-empty diff on an edited file; `CHANGELOG.md` untouched across the whole phase; `install.bat` deployed to at least one client without failing; no in-game check attempted.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → deployed AddOns folder | `install.bat` copies the shared file set and the TOC into every WoW client present. It is the last point at which a bad file becomes something the client loads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-13 | Tampering | `stylua` reformat | mitigate | Task 2 runs the bare invocation so the repo-root `stylua.toml` governs line endings, and explicitly checks for the silent-empty-diff failure mode that hid two earlier LF reflows. |
| T-42-14 | Repudiation | audit without evidence | mitigate | Task 1 requires a grep or a code read behind every verdict, including the negative ones, so a later reader can re-run the checks rather than trusting this pass. |
| T-42-15 | Denial of Service | deploying an untested render path | accept | Accepted by the phase swap decision of 2026-09-22: cleanup runs before the Forever end-to-end pass, so a regression introduced here is caught by Phase 43 rather than slipping past one. The mitigation is the reading discipline in plans 42-04 and 42-05, plus the explicit ban on improvised in-game checks here. |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No package manager runs in this phase. |
</threat_model>

<verification>
1. `.planning/phases/42-cleanup/42-HOT-PATH-AUDIT.md` exists and every row has a verdict.
2. `stylua --check .` exits 0 from the repo root with no flags.
3. `git status --porcelain -- '*.lua' '*.xml' '*.toc' .pkgmeta` is empty after Task 1.
4. `CHANGELOG.md` appears nowhere in the phase's diff.
5. `./scripts/install.bat` succeeds and names at least one client folder.
</verification>

<success_criteria>
- All four D6 hot-path items have an explicit written verdict, plus the seven extras named in Task 1.
- All four duplication candidates have a verdict, including the one that does not exist and the one
  deliberately left alone.
- Out-of-scope findings are recorded rather than silently dropped.
- `stylua` clean, no line-ending churn, addon deployed.
- Two atomic commits, one per task.
</success_criteria>

<commits>
One commit per task, imperative subject under ~72 chars, body in prose explaining WHY.

Task 1 subject: `docs(42): hot-path audit and duplication verdicts`
Body: say what the audit covers and why a written verdict beats a silent edit here — the phase runs
before its own verification pass, so the record of what was examined and left alone is what Phase
43 and the next reader of the render path actually inherit. Mention that one candidate turned out
not to exist and one was left unified-on-purpose, because "not done" and "found not to apply" are
different facts and the roadmap criterion needs both.

Task 2 subject: `docs(42): mark criterion 1 partial — container settings stay triplicated`
Task 3 subject: `chore(42): close the cleanup phase — stylua, diff check, deploy`
Body: record the `stylua` result, the phase's changed-file list and the deploy, and note the
silent-empty-diff trap that the line-ending check exists to catch.

Both commits end with:

    Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
    Claude-Session: https://claude.ai/code/session_01UBnJrcKyhWYjBUQ8LxffBd
</commits>

<output>
Create `.planning/phases/42-cleanup/42-06-SUMMARY.md` when done.
</output>
