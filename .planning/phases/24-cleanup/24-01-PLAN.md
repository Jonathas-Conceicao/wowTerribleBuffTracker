---
phase: 24-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - BuffEngine.lua
  - Providers.lua
  - CDMTab.lua
  - pots_info.csv
  - trinket_info.csv
autonomous: true
requirements:
  - DISP-04

must_haves:
  truths:
    - "ns:GetAtRestMetaIcon is not defined or called anywhere in the Lua codebase"
    - "ns:GetAtRestMetaInfo is not defined or called anywhere in the Lua codebase"
    - "ns:ResolveSuggestedSpellID is not defined or called anywhere in the Lua codebase"
    - "ns.TRINKET_FALLBACK_ORDER and ns.POT_FALLBACK_ORDER are not assigned on the namespace"
    - "ns:RefreshMetaIcons is replaced by ns:RefreshProvidersAtRest (definition + sole call site in CDMTab.lua)"
    - "pots_info.csv and trinket_info.csv do not exist in the repository root"
    - "Providers.lua contains no 'Source: trinket_info.csv' or 'Source: pots_info.csv' comment lines"
    - "Stale Phase-N comments on soon-deleted shims and other orphaned phase-lineage comments are removed"
  artifacts:
    - path: "BuffEngine.lua"
      provides: "Post-cleanup file: renamed RefreshProvidersAtRest, no shim trio, no stale Phase-N comments"
      contains: "function ns:RefreshProvidersAtRest"
    - path: "Providers.lua"
      provides: "Post-cleanup file: no FALLBACK_ORDER namespace exports, no 'Source: X.csv' comments, trimmed Phase-N refs"
      contains: "local TRINKET_FALLBACK_ORDER"
    - path: "CDMTab.lua"
      provides: "Updated call site: ns:RefreshProvidersAtRest()"
      contains: "ns:RefreshProvidersAtRest()"
  key_links:
    - from: "CDMTab.lua:StartPreview"
      to: "BuffEngine.lua:RefreshProvidersAtRest"
      via: "ns:RefreshProvidersAtRest()"
      pattern: "ns:RefreshProvidersAtRest\\(\\)"
    - from: "Providers.lua"
      to: "BuffEngine.lua (no longer exports FALLBACK_ORDER)"
      via: "module-local arrays only"
      pattern: "local TRINKET_FALLBACK_ORDER"
---

<objective>
Grep-gated dead-code sweep: delete three backwards-compat shims, delete two namespace exports, rename the one remaining public API that still advertised the old "meta icons" vocabulary, delete the two now-unused CSV source files plus their dangling "Source: X.csv" comments, and scrub stale "Phase N will remove this shim" / "Relocated from X in Phase Y" comments left over from the provider migration. This is the atomic DISP-04 closure commit. No behavior changes.

Purpose: Close DISP-04 — the final outstanding v0.2.4 requirement. Every deletion is verified by grep (zero external readers) before the diff, per D-01 of CONTEXT.md.
Output: BuffEngine.lua shorter by ~40 lines (three shims gone, rename applied, stale comments scrubbed); Providers.lua shorter by ~6 lines (two ns.* exports gone, two "Source:" lines gone, assorted Phase-N refs trimmed); CDMTab.lua with one updated call site; two CSV files deleted at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-cleanup/24-CONTEXT.md
@BuffEngine.lua
@Providers.lua
@CDMTab.lua
</context>

<interfaces>
<!-- Current shim signatures being deleted (from BuffEngine.lua). Executor should confirm each grep gate BEFORE deleting. -->

From BuffEngine.lua (to be deleted):
```lua
-- Line 26: function ns:RefreshMetaIcons() — RENAME to ns:RefreshProvidersAtRest (D-02)
-- Line 39: function ns:GetAtRestMetaIcon(key)   — DELETE shim (D-01)
-- Line 48: function ns:GetAtRestMetaInfo(key)   — DELETE shim (D-01)
-- Line 133: function ns:ResolveSuggestedSpellID(key) — DELETE shim (D-01)
```

From Providers.lua (to be deleted / trimmed):
```lua
-- Line 171: ns.TRINKET_FALLBACK_ORDER = TRINKET_FALLBACK_ORDER  — DELETE export (D-01)
-- Line 172: ns.POT_FALLBACK_ORDER = POT_FALLBACK_ORDER          — DELETE export (D-01)
-- Line 117: "-- Relocated from BuffEngine.lua in Phase 18. Source: trinket_info.csv."  — DELETE "Source:" phrase (D-09)
-- Line 140: "-- Source: pots_info.csv."  — DELETE whole line (D-09)
```

From CDMTab.lua (to be updated):
```lua
-- Line 24: ns:RefreshMetaIcons()  — UPDATE to ns:RefreshProvidersAtRest() (D-02, sole external call site)
```

PRESERVED (do NOT touch — used internally in Providers.lua):
```lua
-- Providers.lua:163: local TRINKET_FALLBACK_ORDER = { ... }  (module-local; read at lines 308, 322)
-- Providers.lua:164: local POT_FALLBACK_ORDER = { ... }      (module-local; read at lines 396, 403, 415)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Grep-gate and delete three backwards-compat shims (D-01)</name>
  <files>BuffEngine.lua</files>
  <read_first>
    - BuffEngine.lua (current state — confirm line numbers against live file before editing)
    - Providers.lua (confirm ns:GetDisplayInfoForKey is the public API all callers now use)
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-01 grep-gate rule)
  </read_first>
  <action>
For each of the three shims, first run the grep gate; ONLY if the gate reports zero external callers, delete the function AND its preceding block comment(s).

**Shim 1: ns:GetAtRestMetaIcon (BuffEngine.lua:39)**
1. Grep gate — must return 0 call sites outside its own definition:
   ```
   grep -rn "GetAtRestMetaIcon" --include="*.lua" .
   ```
   Expected: matches ONLY inside BuffEngine.lua (the definition at line 39 + any preceding comment references on lines 35-38 + one tail-reference in a comment at line 280). Zero matches in CDMTab.lua / Display.lua / Core.lua / Providers.lua / EditModeFrames.lua.
2. Delete the function body at BuffEngine.lua:39-42 (`function ns:GetAtRestMetaIcon(key) ... end`).
3. Delete the preceding block comment at BuffEngine.lua:35-38 (the four-line "D-24: Backwards-compat shim..." comment ending with "Phase 24 will remove this shim.").

**Shim 2: ns:GetAtRestMetaInfo (BuffEngine.lua:48)**
1. Grep gate — must return 0 external call sites:
   ```
   grep -rn "GetAtRestMetaInfo" --include="*.lua" .
   ```
   Expected: matches ONLY inside BuffEngine.lua (definition + preceding comment). Zero elsewhere.
2. Delete the function body at BuffEngine.lua:48-54.
3. Delete the preceding block comment at BuffEngine.lua:44-47 (the four-line "D-24: Backwards-compat shim..." comment ending with "Phase 24 will remove this shim.").

**Shim 3: ns:ResolveSuggestedSpellID (BuffEngine.lua:133)**
1. Grep gate — must return 0 external call sites:
   ```
   grep -rn "ResolveSuggestedSpellID" --include="*.lua" .
   ```
   Expected: matches ONLY inside BuffEngine.lua (definition at line 133 + preceding comment block lines 127-132 + tail-reference in a comment at line 279). Zero elsewhere.
2. Delete the function body at BuffEngine.lua:133-139.
3. Delete the preceding block comment at BuffEngine.lua:127-132 (the six-line "D-24: Backwards-compat shim..." comment ending with "Phase 24 will remove this shim.").

**Scrub tail-reference comments (D-03):**
4. In `ns:StartAllPreviewTimers` at BuffEngine.lua:279-280, update the comment "No direct reads of entry.label / ns:ResolveSuggestedSpellID / ns:GetAtRestMetaIcon from the caller." — replace those shim references with "No direct reads of entry.label / old shim helpers from the caller." OR remove the two-line explanatory comment entirely if it no longer adds value. Keep the preceding D-08 reference line.

Do NOT run stylua — Plan 24-03 runs the single stylua pass across all files after all cleanup is done.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; echo "=== GetAtRestMetaIcon (must be 0) ===" &amp;&amp; grep -c "GetAtRestMetaIcon" BuffEngine.lua CDMTab.lua Display.lua Core.lua Providers.lua EditModeFrames.lua 2&gt;/dev/null ; echo "=== GetAtRestMetaInfo (must be 0) ===" &amp;&amp; grep -c "GetAtRestMetaInfo" BuffEngine.lua CDMTab.lua Display.lua Core.lua Providers.lua EditModeFrames.lua 2&gt;/dev/null ; echo "=== ResolveSuggestedSpellID (must be 0) ===" &amp;&amp; grep -c "ResolveSuggestedSpellID" BuffEngine.lua CDMTab.lua Display.lua Core.lua Providers.lua EditModeFrames.lua 2&gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "function ns:GetAtRestMetaIcon" BuffEngine.lua` returns 0
    - `grep -c "function ns:GetAtRestMetaInfo" BuffEngine.lua` returns 0
    - `grep -c "function ns:ResolveSuggestedSpellID" BuffEngine.lua` returns 0
    - `grep -c "GetAtRestMetaIcon" BuffEngine.lua CDMTab.lua Display.lua Core.lua Providers.lua EditModeFrames.lua` returns 0 across all files
    - `grep -c "GetAtRestMetaInfo" BuffEngine.lua CDMTab.lua Display.lua Core.lua Providers.lua EditModeFrames.lua` returns 0 across all files
    - `grep -c "ResolveSuggestedSpellID" BuffEngine.lua CDMTab.lua Display.lua Core.lua Providers.lua EditModeFrames.lua` returns 0 across all files
    - `grep -c "Phase 24 will remove this shim" BuffEngine.lua` returns 0 (stale Phase-N comments gone)
    - `grep -c "function ns:GetSpellIcon" BuffEngine.lua` still returns 1 (sanity: we didn't accidentally delete GetSpellIcon)
    - `grep -c "function ns:GetActiveTimers" BuffEngine.lua` still returns 1 (sanity)
  </acceptance_criteria>
  <done>Three shim functions plus their "D-24 Backwards-compat shim" preceding comment blocks are deleted from BuffEngine.lua; tail-reference comment at line 279-280 scrubbed; no external .lua file in the addon still references any of the three shim names.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ns.*_FALLBACK_ORDER namespace exports + rename RefreshMetaIcons (D-01, D-02)</name>
  <files>Providers.lua, BuffEngine.lua, CDMTab.lua</files>
  <read_first>
    - Providers.lua (confirm module-local TRINKET_FALLBACK_ORDER / POT_FALLBACK_ORDER stay intact at lines 163-164 and are still read by the Trinket/Pot RefreshAtRest / GetDisplayInfo methods at lines 308, 322, 396, 403, 415)
    - BuffEngine.lua (current RefreshMetaIcons definition and its preceding comment block, lines 20-33)
    - CDMTab.lua (sole call site ns:RefreshMetaIcons() at line 24 inside StartPreview)
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-01 grep rule, D-02 rename rule)
  </read_first>
  <action>
**Part A — Delete ns.*_FALLBACK_ORDER namespace exports (D-01):**

1. Grep gate — must return 0 external readers of the `ns.TRINKET_FALLBACK_ORDER` / `ns.POT_FALLBACK_ORDER` namespace forms:
   ```
   grep -rn "ns\.TRINKET_FALLBACK_ORDER\|ns\.POT_FALLBACK_ORDER" --include="*.lua" .
   ```
   Expected: matches ONLY on Providers.lua:171 (the `ns.TRINKET_FALLBACK_ORDER = TRINKET_FALLBACK_ORDER` assignment) and Providers.lua:172 (the `ns.POT_FALLBACK_ORDER = POT_FALLBACK_ORDER` assignment). Zero reads elsewhere.
2. Delete Providers.lua:171 (`ns.TRINKET_FALLBACK_ORDER = TRINKET_FALLBACK_ORDER`).
3. Delete Providers.lua:172 (`ns.POT_FALLBACK_ORDER = POT_FALLBACK_ORDER`).
4. Update the preceding comment at Providers.lua:166 — the "D-11: Namespace exports preserved for BuffEngine.RefreshMetaIcons (stays in BuffEngine this phase)." line. Since the ns.* exports being referenced ARE gone after steps 2-3, either (a) rewrite the comment above lines 167-170 (the remaining `ns.TRINKET_SPELLS` / `ns.POT_SPELLS` / `ns.TRINKET_ITEM_IDS` / `ns.POT_ITEM_IDS` exports) to something like "-- Namespace exports for the data tables (consumed by tests and future provider extensions)." — or (b) delete the line entirely if the four remaining ns.* assignments are self-explanatory. Pick (a) for clarity.
5. Also update the comment at Providers.lua:162 ("Ordered fallback iteration in CSV order (consumed by BuffEngine.RefreshMetaIcons).") — replace "consumed by BuffEngine.RefreshMetaIcons" with "consumed by TrinketProvider:RefreshAtRest / PotProvider:RefreshAtRest below" since the RefreshMetaIcons-era consumer is gone.

**Part B — Rename ns:RefreshMetaIcons → ns:RefreshProvidersAtRest (D-02):**

6. In BuffEngine.lua at line 26, rename the function definition:
   ```
   FROM: function ns:RefreshMetaIcons()
   TO:   function ns:RefreshProvidersAtRest()
   ```
7. Update the preceding comment block at BuffEngine.lua:20-25. The current text mentions "meta-icons" and carries Phase-N rename-deferral language. Rewrite the block to something like:
   ```
   -- Thin wrapper. Combat-gates once at entry (PITFALL-5), then iterates all registered
   -- providers calling each one's RefreshAtRest method. Meta-providers (Trinket/Pot)
   -- perform the actual inventory scan inside their own RefreshAtRest; Lust/UserSpell
   -- providers inherit the base no-op from SpellProviderBaseMixin.
   ```
   Remove the two lines referencing "Name retained for Phase 20 backwards compatibility..." and "Rename deferred to Phase 24 cleanup." — they are now obsolete.
8. Update the sole external call site in CDMTab.lua at line 24:
   ```
   FROM: ns:RefreshMetaIcons()
   TO:   ns:RefreshProvidersAtRest()
   ```
9. Update the preceding comment at CDMTab.lua:22-23. Replace "Refresh at-rest meta-icons (trinket/pot) before preview begins. / Combat-gated inside RefreshMetaIcons (D-07) — safe to call unconditionally." with something like:
   ```
   -- D-14 / ICON-05: Refresh provider at-rest state (trinket/pot cache) before preview begins.
   -- Combat-gated inside RefreshProvidersAtRest (D-07) — safe to call unconditionally.
   ```
10. Also scrub the stale mention of "RefreshMetaIcons" in the Providers.lua comment at line 41 (`D-17: Caller (ns:RefreshMetaIcons wrapper) combat-gates before calling.`) — replace `ns:RefreshMetaIcons` with `ns:RefreshProvidersAtRest`.
11. Also scrub the stale mention on Providers.lua:292 (`D-17/D-18: ns:RefreshMetaIcons wrapper combat-gates; defensive double-gate here...`) — same rename.

**Part C — Confirm no stragglers:**

12. After all edits, run:
    ```
    grep -rn "RefreshMetaIcons" --include="*.lua" .
    ```
    Must return 0 matches across all Lua files.

Do NOT run stylua — Plan 24-03 runs the single pass.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; echo "=== RefreshMetaIcons (must be 0 across all files) ===" &amp;&amp; grep -c "RefreshMetaIcons" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2&gt;/dev/null ; echo "=== ns.TRINKET_FALLBACK_ORDER (must be 0) ===" &amp;&amp; grep -c "ns\.TRINKET_FALLBACK_ORDER" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2&gt;/dev/null ; echo "=== ns.POT_FALLBACK_ORDER (must be 0) ===" &amp;&amp; grep -c "ns\.POT_FALLBACK_ORDER" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2&gt;/dev/null ; echo "=== RefreshProvidersAtRest (must be exactly 1 def + 1 call = 2) ===" &amp;&amp; grep -c "RefreshProvidersAtRest" BuffEngine.lua CDMTab.lua ; echo "=== module-local TRINKET_FALLBACK_ORDER preserved (must still exist) ===" &amp;&amp; grep -c "local TRINKET_FALLBACK_ORDER" Providers.lua ; echo "=== module-local POT_FALLBACK_ORDER preserved (must still exist) ===" &amp;&amp; grep -c "local POT_FALLBACK_ORDER" Providers.lua</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "RefreshMetaIcons" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua` returns 0 for every file
    - `grep -c "ns\.TRINKET_FALLBACK_ORDER" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua` returns 0 for every file
    - `grep -c "ns\.POT_FALLBACK_ORDER" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua` returns 0 for every file
    - `grep -c "function ns:RefreshProvidersAtRest" BuffEngine.lua` returns 1
    - `grep -c "ns:RefreshProvidersAtRest()" CDMTab.lua` returns 1
    - `grep -c "local TRINKET_FALLBACK_ORDER" Providers.lua` returns 1 (module-local preserved)
    - `grep -c "local POT_FALLBACK_ORDER" Providers.lua` returns 1 (module-local preserved)
    - `grep -c "TRINKET_FALLBACK_ORDER\[1\]" Providers.lua` returns at least 2 (internal reads at lines ~308 and ~322 still present — sanity that we didn't break trinket at-rest)
    - `grep -c "for _, itemID in ipairs(POT_FALLBACK_ORDER)" Providers.lua` returns 1 (pot scan loop preserved)
  </acceptance_criteria>
  <done>ns.TRINKET_FALLBACK_ORDER and ns.POT_FALLBACK_ORDER removed from Providers.lua (module-locals preserved and still read internally); ns:RefreshMetaIcons renamed to ns:RefreshProvidersAtRest (definition in BuffEngine.lua, sole call site in CDMTab.lua); all stale "RefreshMetaIcons" references in surrounding comments updated to the new name; no file contains the old name.</done>
</task>

<task type="auto">
  <name>Task 3: Delete CSV source files and their "Source: X.csv" comment lines (D-09)</name>
  <files>Providers.lua, pots_info.csv, trinket_info.csv</files>
  <read_first>
    - Providers.lua (lines 117 and 140 where the two "Source: X.csv" comments live; also survey remaining Phase-N references at lines 41, 162, 166, 192, 225, 292, 559 mentioned in CONTEXT.md)
    - pots_info.csv and trinket_info.csv at repo root (confirm they are standalone data files — no build step reads them; they were source-of-truth for the lua-array copy-paste, now obsolete)
    - .planning/phases/24-cleanup/24-CONTEXT.md (D-09 decision, D-03 comment scrub policy)
  </read_first>
  <action>
**Part A — Delete the two "Source: X.csv" comment references (D-09):**

1. In Providers.lua at line 117, the current comment reads:
   ```
   -- Relocated from BuffEngine.lua in Phase 18. Source: trinket_info.csv.
   ```
   Delete the entire "Source: trinket_info.csv." phrase AND the "Relocated from BuffEngine.lua in Phase 18." phrase per D-03 (stale Phase-N comment). The preceding line 116 (`-- Static lookup: spellID -> { duration, itemID } for tracked on-use trinkets (D-08).`) is sufficient documentation. Outcome: line 117 is deleted entirely, leaving only the D-08 one-liner as the table header comment.

2. In Providers.lua at line 140, the current comment reads:
   ```
   -- Source: pots_info.csv.
   ```
   Delete the entire line. The preceding line 139 (`-- Static lookup: spellID -> { duration, itemID } for tracked damage potions.`) is sufficient documentation.

**Part B — Delete the CSV files at repo root (D-09):**

3. Delete `pots_info.csv` at the repository root:
   ```
   rm "C:/Users/jonat/Repositories/TerribleBuffTracker/pots_info.csv"
   ```
4. Delete `trinket_info.csv` at the repository root:
   ```
   rm "C:/Users/jonat/Repositories/TerribleBuffTracker/trinket_info.csv"
   ```
5. Confirm neither file is referenced in `.pkgmeta` (CI ignore list check) or `scripts/` — grep-gate:
   ```
   grep -rn "pots_info.csv\|trinket_info.csv" . --exclude-dir=.git --exclude-dir=.planning
   ```
   Expected: 0 matches. (The `.planning/` hierarchy may mention them historically in SUMMARY / CONTEXT — those are allowed; we're only checking the live-code roots.)

**Part C — Scrub remaining orphaned Phase-N comments per D-03 (surgical, not blanket):**

The CONTEXT identified these lines as carrying Phase-N lineage comments: Providers.lua:41, 162, 166, 192, 225, 292, 559. Tasks 1 and 2 already addressed line 41 (RefreshMetaIcons rename) and line 162/166 (fallback-order comments rewritten) and line 292 (RefreshMetaIcons rename). The remaining three to address here:

6. Providers.lua:192 — currently reads something like "Relocated from BuffEngine.lua. Namespace exports preserved so BuffEngine's ns.SUGGESTED_BUFFS closures and ScanActiveTimersForCancellation can read them." This describes a migration that is DONE and mentions ns.SUGGESTED_BUFFS (deleted in Phase 23). Rewrite to: `-- Lust data tables. Consumed by LustProvider:OnTrigger (below) and ScanActiveTimersForCancellation (via proc.aliveBuffs).` Remove the "Relocated from..." and "Namespace exports preserved..." language.

7. Providers.lua:225 — currently reads "Provider-local (D-19 Phase 23) — previously ns.* exported for BuffEngine SUGGESTED_BUFFS closure, which was deleted in Plan 23-01." Rewrite to: `-- Hunter uses Primal Rage by default; MM Hunter (spec ID 254) uses Harrier's Cry.` (Keep the behavioral documentation above, drop the migration history.) The existing behavioral comment already exists above this line; just delete the two-line Phase-23 history comment.

8. Providers.lua:559 — currently reads "Callers that don't need all fields (e.g. old ns:GetAtRestMetaIcon shim wants only .icon) / read the subset they need — the full shape is returned unconditionally." Since the GetAtRestMetaIcon shim is deleted in Task 1, this comment now references a non-existent symbol. Rewrite to: `-- Callers read the subset of fields they need; the full shape is returned unconditionally.` Remove the "(e.g. old ns:GetAtRestMetaIcon shim wants only .icon)" parenthetical.

9. Final grep to confirm no dangling Phase-23 / Phase-24 / "will remove this shim" references remain:
   ```
   grep -rn "will remove this shim\|will remove in Phase\|Relocated from BuffEngine.lua in Phase" Providers.lua BuffEngine.lua
   ```
   Must return 0 matches. (Individual `(D-NN)` decision-ID references are preserved — they document current invariants, not migration history.)

Do NOT run stylua — Plan 24-03 handles the single pass.
  </action>
  <verify>
    <automated>cd "C:/Users/jonat/Repositories/TerribleBuffTracker" &amp;&amp; echo "=== CSV files deleted (must be 0 both) ===" &amp;&amp; test ! -f pots_info.csv &amp;&amp; echo "pots_info.csv: deleted" || echo "pots_info.csv: STILL PRESENT" ; test ! -f trinket_info.csv &amp;&amp; echo "trinket_info.csv: deleted" || echo "trinket_info.csv: STILL PRESENT" ; echo "=== No 'Source: X.csv' comments (must be 0) ===" &amp;&amp; grep -c "Source: trinket_info.csv\|Source: pots_info.csv" Providers.lua ; echo "=== No stale 'will remove this shim' / 'Relocated from BuffEngine.lua in Phase' comments ===" &amp;&amp; grep -c "will remove this shim\|Relocated from BuffEngine.lua in Phase" BuffEngine.lua Providers.lua 2&gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f pots_info.csv` exits 0 (file removed)
    - `test ! -f trinket_info.csv` exits 0 (file removed)
    - `grep -c "Source: trinket_info.csv" Providers.lua` returns 0
    - `grep -c "Source: pots_info.csv" Providers.lua` returns 0
    - `grep -c "will remove this shim" BuffEngine.lua Providers.lua` returns 0 across both files
    - `grep -c "Relocated from BuffEngine.lua in Phase" Providers.lua BuffEngine.lua` returns 0 across both files
    - `grep -c "ns.SUGGESTED_BUFFS" Providers.lua BuffEngine.lua CDMTab.lua Display.lua Core.lua` returns 0 across all files (stragglers check — ns.SUGGESTED_BUFFS was deleted Phase 23, verifies no comment still mentions it as live)
    - `grep -c "local SHARED_LUST_BUFFS_LOCAL" Providers.lua` returns 1 (sanity: lust-data table preserved)
    - `grep -c "local TRINKET_SPELLS" Providers.lua` returns 1 (sanity: trinket data table preserved)
    - `grep -c "local POT_SPELLS" Providers.lua` returns 1 (sanity: pot data table preserved)
  </acceptance_criteria>
  <done>pots_info.csv and trinket_info.csv deleted from repo root; Providers.lua:117 and :140 "Source: X.csv" comments removed; remaining stale Phase-N/migration-history comments on lines 192, 225, 559 rewritten to document current invariants without migration history; all data tables and function logic preserved intact.</done>
</task>

</tasks>

<verification>
After all three tasks complete, run the full DISP-04 closure grep suite:

```bash
cd "C:/Users/jonat/Repositories/TerribleBuffTracker"

# D-01 dead-symbol sweep — every count must be 0
for sym in "GetAtRestMetaIcon" "GetAtRestMetaInfo" "ResolveSuggestedSpellID" "RefreshMetaIcons" "ns\.TRINKET_FALLBACK_ORDER" "ns\.POT_FALLBACK_ORDER"; do
    echo "$sym:"
    grep -c "$sym" BuffEngine.lua Providers.lua CDMTab.lua Display.lua Core.lua EditModeFrames.lua 2>/dev/null
done

# D-02 rename — def (1) + call (1) = 2 total across BuffEngine.lua + CDMTab.lua
grep -c "RefreshProvidersAtRest" BuffEngine.lua CDMTab.lua

# D-09 CSV deletion
test ! -f pots_info.csv && test ! -f trinket_info.csv && echo "CSVs deleted"

# Provider module-locals preserved
grep -c "local TRINKET_FALLBACK_ORDER" Providers.lua  # must be 1
grep -c "local POT_FALLBACK_ORDER" Providers.lua     # must be 1
```

All dead-symbol counts must be 0 across all files. RefreshProvidersAtRest appears exactly once in BuffEngine.lua (def) and exactly once in CDMTab.lua (call). Module-locals remain.
</verification>

<success_criteria>
- Three shim functions (GetAtRestMetaIcon, GetAtRestMetaInfo, ResolveSuggestedSpellID) deleted from BuffEngine.lua
- Two namespace exports (ns.TRINKET_FALLBACK_ORDER, ns.POT_FALLBACK_ORDER) deleted from Providers.lua
- ns:RefreshMetaIcons renamed to ns:RefreshProvidersAtRest (def + sole call site in CDMTab.lua)
- pots_info.csv and trinket_info.csv deleted from repo root
- "Source: trinket_info.csv" and "Source: pots_info.csv" comment lines deleted from Providers.lua
- Stale "Phase N will remove" and "Relocated from X in Phase Y" comments scrubbed per D-03 (not a blanket comment purge — only the orphaned migration-history lines)
- Module-local TRINKET_FALLBACK_ORDER / POT_FALLBACK_ORDER arrays preserved and still read internally
- No stylua pass yet — deferred to Plan 24-03
</success_criteria>

<output>
After completion, create `.planning/phases/24-cleanup/24-01-SUMMARY.md` following the standard summary template. Include a one-section "DISP-04 grep-gate results" block showing the zero-count grep results as post-delete evidence.
</output>
