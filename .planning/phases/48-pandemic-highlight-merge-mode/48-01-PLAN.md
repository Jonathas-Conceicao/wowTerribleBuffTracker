---
phase: 48-pandemic-highlight-merge-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MergeMode.lua
autonomous: true
requirements: [PAND-03, PAND-04, PAND-05]

must_haves:
  truths:
    - "The pandemic state of a merged entry is read only through plain table-field reads on a cached CDM item frame"
    - "No Blizzard CDM mixin method is called, no field is written on a CDM frame, and viewer.pandemicIconPool is never named"
    - "A raise anywhere in the pandemic read cannot empty the merged containers"
    - "Both timestamps are guarded issecretvalue() before type(), and an unreadable pair degrades to the boolean signal"
    - "Readable numbers decide the answer; the boolean is the fallback, never the reverse"
    - "A render-time resolver answers 'is this entry in its pandemic window right now' from plain numbers and TBT's own GetTime()"
    - "With /tbt debug on, every pandemic state change prints one line carrying enough to answer PANDEMIC.md's unknowns 1, 2 and 5"
    - "With /tbt debug on, /tbt merge prints a per-entry pandemic dump carrying the answer to unknowns 3 and 4"
    - "Core.lua is byte-unchanged"
  artifacts:
    - path: "MergeMode.lua"
      provides: "ReadPandemicState — the pcall'd per-entry pandemic stamp read off ns.mergeItemFrames"
      contains: "local function ReadPandemicState"
    - path: "MergeMode.lua"
      provides: "ns:IsMergedEntryInPandemic(entry, now) — the render-time resolver Display consumes"
      contains: "function ns:IsMergedEntryInPandemic"
    - path: "MergeMode.lua"
      provides: "ns.debugLogging-gated pandemic state-change log and the /tbt merge per-entry dump"
      contains: "PANDEMIC"
  key_links:
    - from: "MergeMode.lua ns:RefreshMergeShownSlots"
      to: "ReadPandemicState"
      via: "pcall, sibling of the entry.cdmShown stamp"
      pattern: "pcall\\(ReadPandemicState, entry\\)"
    - from: "MergeMode.lua ReadPandemicState"
      to: "ns.mergeItemFrames"
      via: "cached CDM item frame keyed by entry.cooldownID"
      pattern: "ns\\.mergeItemFrames\\[entry\\.cooldownID\\]"
    - from: "MergeMode.lua ns:IsMergedEntryInPandemic"
      to: "entry.pandemicStart / entry.pandemicFinish / entry.pandemicActive"
      via: "plain-number window test against the caller's now, boolean otherwise"
      pattern: "entry\\.pandemicActive"
---

<objective>
Add the **read half** of the pandemic feature to `MergeMode.lua`: a pcall-wrapped per-entry stamp
inside the existing `ns:RefreshMergeShownSlots` pass, a render-time resolver Display will consume in
Plan 02, and two `ns.debugLogging`-gated dumps that answer `PANDEMIC.md`'s five unknowns in game
instead of guessing them.

Purpose: this is the only plan that touches a CDM-owned object, so **every** no-taint argument in
the phase lives here and nowhere else. Plan 02 renders from the three plain fields this plan stamps
onto TBT's own `entry` tables and never looks at a CDM frame at all.

Output: one module-local function, one exported resolver, two debug prints. **Nothing renders yet.**
After this plan the addon must look and behave exactly as it does today — a player with Merge Mode
on sees no visual change whatsoever.

**Locked decisions used throughout this phase** (from `48-CONTEXT.md`, front-loaded by the user
2026-09-24):

| ID | Decision |
|----|----------|
| D-01 | Build **both** signals. Primary `itemFrame.PandemicIcon ~= nil`; refinement the two guarded timestamps vs TBT's own `GetTime()`. **Precedence is fixed: readable numbers win, boolean otherwise — never inverted** |
| D-02 | Recomputing the window is **CLOSED** — it needs `auraInstanceID`, flagged `DisallowTaintedAccess`. Read Blizzard's answer, never rebuild it |
| D-03 | A `/tbt debug` dump answering PANDEMIC.md's five unknowns, reusing the existing `ns.debugLogging` gate from a **NEW call site** |
| D-04 | `Core.lua`'s debug cast/item log is **PROTECTED** — `pendingItemCasts`, `LogPlayerCast`, `LogItemUse`. Do not touch, move or generalise it |
| D-05 | **Merged entries only.** TBT's native trackers computing their own pandemic window is out of scope |
| D-06 | The no-taint rule (`MergeMode.lua:5-25`): no mixin method on a CDM frame, no field write on one, no reparent into one, no `C_CooldownViewer.SetLayoutData`. `viewer.pandemicIconPool` is **not** admitted |
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-pandemic-highlight-merge-mode/48-CONTEXT.md
@.planning/phases/48-pandemic-highlight-merge-mode/48-PATTERNS.md
@.planning/phases/48-pandemic-highlight-merge-mode/48-VALIDATION.md
@.planning/research/PANDEMIC.md
</context>

<interfaces>
Contracts this plan creates. Plan 02 consumes these exact names and shapes — do not rename, do not
change a return type, and do not move the read into `Display.lua`.

Stamped onto TBT's own merged `entry` table (never onto a CDM frame):

- `entry.pandemicActive` — `true` while `itemFrame.PandemicIcon` is non-nil, i.e. while Blizzard is
  currently drawing its own highlight for that entry. `false` otherwise. Never `nil` once an entry
  has been through one pass with a cached item frame.
- `entry.pandemicStart` / `entry.pandemicFinish` — plain numbers on the `GetTime()` clock, or
  **both** `nil`. Never one without the other. `nil` means "the timestamps were secret or absent on
  this pass" and is the normal in-combat case per D-01.

Exported:

- `ns:IsMergedEntryInPandemic(entry, now)` — returns a plain boolean. `now` is the caller's
  `GetTime()` value (`Display.lua` already has one per tick and must pass it, not call `GetTime()`
  again). Reads only the three stamped fields; makes no API call, touches no frame, allocates
  nothing, and is safe to call once per merged slot per tick.

Module-local, never exported:

- `ReadPandemicState(entry)` — the stamp. Called **only** through `pcall` from
  `ns:RefreshMergeShownSlots`.
</interfaces>

<source_audit>
Multi-source coverage audit for the whole phase. Every item is COVERED; there are no gaps and no
unplanned items.

| Source | Item | Covered by |
|--------|------|-----------|
| GOAL | A merged entry in its pandemic window carries Blizzard's highlight onto TBT's mirrored icon | 48-01 Task 1 (signal) + 48-02 Tasks 1-2 (render) |
| GOAL | ...and onto its mirrored bar | 48-01 Task 1 + 48-02 Tasks 1-2 |
| GOAL | ...read without tainting a CDM frame | 48-01 Task 1, gated S4/S5/S6/S11, proven in game by 48-03 G4 |
| REQ | PAND-01 — highlight on the mirrored icon | 48-02 Tasks 1-2, gate G1 |
| REQ | PAND-02 — highlight on the mirrored bar, border in front of the fill | 48-02 Tasks 1-2 (S13), gate G2 |
| REQ | PAND-03 — clears at window end without an unrelated aura event | 48-01 Task 1 (numeric route, resolved per tick) + 48-02 Task 2, gate G3 |
| REQ | PAND-04 — never calls a CDM mixin method, no frame tainted | 48-01 Task 1, 48-03 Task 1 sweep, gate G4 |
| REQ | PAND-05 — unobservable state renders exactly as today | 48-01 Task 1 (degrade to boolean, then to nothing) + 48-02 Task 1 (`pcall` around both templates), gate G5 |
| RESEARCH | `itemFrame.PandemicIcon` is the primary signal, reachable off the existing cache | 48-01 Task 1 |
| RESEARCH | The two timestamps are plain field reads, almost certainly secret in combat | 48-01 Task 1 |
| RESEARCH | Recompute is closed (`auraInstanceID` / `DisallowTaintedAccess`) | no task — correctly absent |
| RESEARCH | Item-backed entries structurally never carry the signal; no filtering needed | 48-01 Task 1 (no category filter), gate G5 |
| RESEARCH | Icon anchor `-6/+6`; bar anchor `-9/+10` plus a frame-level bump | 48-02 Task 1 |
| RESEARCH | Both templates are virtual, instantiable by name, with no existence probe | 48-02 Task 1 |
| RESEARCH | Forever ships identical code under `AllowLoadGameType: standard, camelot` — no flavour gate | every task; asserted by S3 in each gate |
| RESEARCH | Five unknowns must be answered in game | 48-01 Task 2 (the dumps) + 48-03 Task 2 (G0) |
| CONTEXT | D-01 both signals, numbers win | 48-01 Task 1 |
| CONTEXT | D-02 recompute closed | no task — correctly absent |
| CONTEXT | D-03 `/tbt debug` dump from a NEW call site | 48-01 Task 2 |
| CONTEXT | D-04 `Core.lua` debug block protected | gated in every task (`Core.lua` byte-unchanged) |
| CONTEXT | D-05 merged entries only | 48-01 Task 1 and 48-02 Task 2 both key off `entry.isMerged` / `entry.cooldownID` |
| CONTEXT | D-06 the no-taint rule | 48-01 Task 1, gates S4/S5/S6/S11 |
| CONTEXT | Discretion: one boolean plus guarded numbers | 48-01 Task 1 — resolved as one boolean + a number pair that is all-or-nothing |
| CONTEXT | Discretion: follow `Core.lua`'s debug log style | 48-01 Task 2 |
| CONTEXT | Discretion: icon and bar highlight pooling | 48-02 Task 1 — resolved as the `Display.lua` lazy 1:1 idiom, two fields, no `CreateObjectPool` |

Deferred Ideas from `48-CONTEXT.md` — native-tracker pandemic windows, recomputing from aura data,
reading via CDM mixin methods, and a second phase for the numeric fallback — appear in **no** task.
That is correct, not a gap.
</source_audit>

<tasks>

<task type="auto">
  <name>Task 1: Stamp the pandemic state in ns:RefreshMergeShownSlots and export the resolver</name>
  <files>MergeMode.lua</files>
  <read_first>
    - `MergeMode.lua:1-30` — the file header and the four-clause no-taint rule. **This is the
      constraint the whole task is written against.** Clause 1 forbids any mixin method on a CDM
      frame; the admitted exception list is "plain C widget getters, plain table-field reads, and
      exactly one generic-container call, `viewer.itemFramePool:EnumerateActive`".
    - `MergeMode.lua:478-500` — `CollectShownCooldownIDs`, the admitted `EnumerateActive` walk. Line
      497, `ns.mergeItemFrames[cooldownID] = itemFrame`, is the cache this task reads from. **Do not
      modify this function.** It is why no new CDM surface is needed.
    - `MergeMode.lua:694-806` — `ns:RefreshMergeShownSlots` in full. Specifically: the
      `wipe(ns.mergeItemFrames)` at `:701`; the `ns.db.mergeMode ~= true` early return at `:703-705`
      (which is what makes the cache safe to read inside the loop and nowhere else); the
      `pcall(CollectShownCooldownIDs, viewer)` at `:745-749`; the `entry.cdmShown` stamp at `:786`;
      and the `pcall(ResolveMergedAuraTiming, entry)` at `:793-799` **with its comment** — that
      comment is the structural argument this task inherits verbatim.
    - `MergeMode.lua:640-670` — the `issecretvalue()`-then-`type()` guard on
      `aura.expirationTime` / `aura.duration`. The worked example to copy field for field.
    - `MergeMode.lua:363-376` — the mirror-build-time stamps (`hideAura`, `hasCharges`,
      `selfAura`). **Read to confirm this is the WRONG site** and not to copy: those come off a
      static info struct rebuilt only on a spec/talent/config change, and the pandemic window is
      time-varying per cast.
    - `.planning/research/PANDEMIC.md` § "Reading The State Without Tainting" and § "Recompute
      Route" — the authority for why `PandemicIcon` is primary and why the recompute is closed.
    - `48-PATTERNS.md` sections 1 and 3 — the stamp site and the guard shape, with the explicit
      instruction that the pandemic read must **not** go inside `pcall(ResolveMergedAuraTiming, ...)`
      but must get a `pcall` of its own.
  </read_first>
  <action>
    Add one module-local function `ReadPandemicState(entry)` immediately above
    `ns:RefreshMergeShownSlots`, and one exported resolver `ns:IsMergedEntryInPandemic(entry, now)`
    beside it.

    `ReadPandemicState(entry)` resolves the CDM item frame from the existing cache —
    `ns.mergeItemFrames[entry.cooldownID]` — and does nothing else to reach it. No
    `EnumerateActive`, no viewer lookup, no `C_CooldownViewer` call. When there is no cached frame
    for that cooldownID, clear all three stamps (`entry.pandemicActive = false`,
    `entry.pandemicStart = nil`, `entry.pandemicFinish = nil`) and return: a merged entry whose
    frame is not in the cache has no observable pandemic state, and a stale `true` from a previous
    pass is exactly the "stuck highlight" PAND-05 forbids.

    The primary signal is `itemFrame.PandemicIcon ~= nil` (D-01). Comment why this specific read is
    legal and why it carries no `issecretvalue` guard: it is a **plain table-field read**, permitted
    by clause 1 of the header rule, and the value is a **frame reference** — only numeric and string
    data fields can be secret-tainted, so there is nothing for `issecretvalue` to catch. Add the
    second half of that argument too: the whole call is `pcall`'d at its one call site, so even if a
    future client did make this read raise, the feature degrades instead of taking the pass down.

    The refinement is the two timestamps, `itemFrame.pandemicStartTime` and
    `itemFrame.pandemicEndTime`, guarded exactly as `MergeMode.lua:650-656` guards
    `aura.expirationTime` — `issecretvalue(v)` **first**, then `type(v) ~= "number"`, per field,
    never the reverse. Stamp the pair all-or-nothing: if either is unreadable, **both**
    `entry.pandemicStart` and `entry.pandemicFinish` are set to `nil`, so no consumer can ever see a
    half window. Do not add a `duration <= 0`-style sanity test — that one belongs to aura duration
    semantics and has no equivalent here.

    Do **not** gate the stamp on category, `hideAura`, `engineOwns`, `isAuraCategory`, `equipSlot` or
    `spellCategoryID`. Blizzard short-circuits `IsItem()` before it ever computes a pandemic window,
    so item-backed entries structurally never carry the signal and need no filter on TBT's side
    (PANDEMIC.md, "no filtering needed"). Record that in the comment so a later reader does not add
    a filter back.

    Call it from exactly one place: inside the per-entry `for _, entry in ipairs(slots)` loop of
    `ns:RefreshMergeShownSlots`, on the line after the `entry.cdmShown` stamp, as
    `pcall(ReadPandemicState, entry)` — **unconditionally, for every entry, published or not**,
    exactly as `entry.cdmShown` is stamped. Place it before the `if engineOwns or seen == 0 or ...`
    publish branch, not inside it.

    The `pcall` is mandatory and its comment must state the reason, which is structural and not a
    prediction: this pass wipes the shown-slot arrays before refilling them, so **any** raise partway
    through leaves every merged container empty until the next aura event — the exact failure a
    `GetAuraDataByIndex` raise produced on 2026-09-22. Cite that precedent and point at the existing
    comment above `pcall(ResolveMergedAuraTiming, entry)`. Do **not** put the pandemic read inside
    that existing pcall: its scope is aura-timing resolution, and widening it would make a raise in
    either half indistinguishable.

    `ns:IsMergedEntryInPandemic(entry, now)` is the render-time half and must touch no frame and no
    API. Return `false` for a nil entry. Then apply D-01's precedence, which must not be inverted:
    when **both** `entry.pandemicStart` and `entry.pandemicFinish` are present, the answer is
    `now >= start and now <= finish` and the boolean is ignored; otherwise the answer is
    `entry.pandemicActive == true`. Comment why the numbers win: they give the exact window bounds,
    so the highlight can clear at the true end of the window on the render tick (PAND-03), whereas
    the boolean can only ever mirror whatever Blizzard is currently drawing and inherits Blizzard's
    own `OnUpdate`-deregistration lag (PANDEMIC.md unknown 1). Both stamped numbers are already
    proven plain by the stamp, and `now` is the caller's own `GetTime()`, so no guard is needed here
    — say so, so nobody adds one and makes the hot path pay for it twice.

    Allocate nothing: no table constructor, no closure, no string concatenation on this path.
    `pcall(fn, entry)` with a hoisted function reference is the existing idiom and allocates no
    argument table.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -c '^local function ReadPandemicState(entry)' MergeMode.lua)" = "1" && test "$(grep -c '^function ns:IsMergedEntryInPandemic(entry, now)' MergeMode.lua)" = "1" && test "$(sed -n '/^function ns:RefreshMergeShownSlots/,/^end$/p' MergeMode.lua | grep -c 'pcall(ReadPandemicState, entry)')" = "1" && test "$(sed -n '/^function ns:RefreshMergeShownSlots/,/^end$/p' MergeMode.lua | grep -c 'entry.cdmShown =')" = "1" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -c 'ns.mergeItemFrames\[entry.cooldownID\]')" = "1" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -c 'itemFrame.PandemicIcon')" -ge "1" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -c 'itemFrame.pandemicStartTime')" = "1" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -c 'itemFrame.pandemicEndTime')" = "1" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -c 'issecretvalue')" -ge "2" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -c 'itemFrame:')" = "0" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -cE 'itemFrame\.[A-Za-z]+ *=[^=]')" = "0" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -cE 'C_CooldownViewer|EnumerateActive|itemFramePool')" = "0" && test "$(sed -n '/^function ns:IsMergedEntryInPandemic/,/^end$/p' MergeMode.lua | grep -c 'entry.pandemicActive')" = "1" && test "$(sed -n '/^function ns:IsMergedEntryInPandemic/,/^end$/p' MergeMode.lua | grep -cE 'itemFrame|GetTime\(\)|C_')" = "0" && test "$(grep -c 'pandemicIconPool' MergeMode.lua)" = "0" && test "$(grep -v '^\s*--' MergeMode.lua | grep -cE 'IsInPandemicTime|ShowPandemicStateFrame|HidePandemicStateFrame|CheckSetPandemicAlertTriggerTime|SetPandemicAlertTriggerTime|SetLayoutData')" = "0" && test "$(grep -cE 'GetBuildInfo|buildInterfaceVersion|wow_classic|WOW_PROJECT' MergeMode.lua)" = "0" && test "$(wc -l < Core.lua)" = "1367" && stylua . && stylua --check . && stylua . && stylua --check . && git ls-files --eol MergeMode.lua | grep -q 'w/crlf' && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: exactly one `local function ReadPandemicState(entry)` and exactly one
      `function ns:IsMergedEntryInPandemic(entry, now)` exist in `MergeMode.lua`.
    - Source assertion: `ns:RefreshMergeShownSlots` contains exactly one
      `pcall(ReadPandemicState, entry)` and still contains its single `entry.cdmShown` stamp — the
      stamp site was extended, not rewritten (**S9**, and the `pcall` reason is stated in the
      comment).
    - Source assertion: `ReadPandemicState` reads the frame only from
      `ns.mergeItemFrames[entry.cooldownID]` and contains zero occurrences of `C_CooldownViewer`,
      `EnumerateActive` or `itemFramePool` — no new CDM surface.
    - Source assertion (**S4**): `ReadPandemicState` contains zero `itemFrame:` occurrences — every
      CDM access in it is a plain table-field read, no mixin method. File-wide, `MergeMode.lua`
      contains zero occurrences of `IsInPandemicTime`, `ShowPandemicStateFrame`,
      `HidePandemicStateFrame`, `CheckSetPandemicAlertTriggerTime`, `SetPandemicAlertTriggerTime` or
      `SetLayoutData`.
    - Source assertion (**S5**): `ReadPandemicState` contains zero matches for
      `itemFrame\.[A-Za-z]+ *=[^=]` — nothing is assigned onto the CDM frame, and no CDM frame is
      passed as a `CreateFrame` parent anywhere in this plan (this plan creates no frame).
    - Source assertion (**S6**): `pandemicIconPool` appears nowhere in `MergeMode.lua`.
    - Source assertion (**S7**): both `itemFrame.pandemicStartTime` and `itemFrame.pandemicEndTime`
      are read, and `issecretvalue` appears at least twice inside `ReadPandemicState`, before the
      `type()` test on each field.
    - Source assertion (**S8**): `ns:IsMergedEntryInPandemic` references `entry.pandemicActive`
      exactly once, on the fallback path, after the two-number branch — readable numbers win.
    - Source assertion (**S14**): `ns:IsMergedEntryInPandemic` contains no `itemFrame`, no
      `GetTime()` call of its own and no `C_` API call; it is a pure read of three stamped fields.
    - Source assertion (**S3**): `MergeMode.lua` contains zero flavour tokens (`GetBuildInfo`,
      `buildInterfaceVersion`, `wow_classic`, `WOW_PROJECT`). The file has zero today, so this gate
      is diff-equivalent — `Core.lua:565-576`'s sanctioned one-off lives in another file and is
      never matched.
    - Source assertion (**S12**, **D-04**): `wc -l < Core.lua` is still `1367`. `Core.lua` being
      byte-unchanged is the mechanical proof that the protected `pendingItemCasts` / `LogPlayerCast`
      / `LogItemUse` block was not touched.
    - Command (**S1**): `stylua .` then `stylua --check .`, **run twice**, exits 0 both times — the
      second pair is the idempotency check a Phase 47 comment placement broke.
    - Command (**S2**): `git ls-files --eol MergeMode.lua` reports `w/crlf`.
    - Gates satisfied: S1, S2, S3, S4, S5, S6, S7, S8, S9, S12, S14. Feeds in-game gates G0, G3,
      G4, G5.
    - **There is no test runner in this project and none is planned.** Every gate above is a source
      assertion or a shell command. Behaviour is proven only by the in-game checks in Plan 48-03.
  </acceptance_criteria>
  <done>`ReadPandemicState` stamps `entry.pandemicActive` plus an all-or-nothing `pandemicStart`/`pandemicFinish` pair from plain field reads on the cached CDM item frame, is called once per entry under its own `pcall` beside the `cdmShown` stamp, `ns:IsMergedEntryInPandemic` answers from those three fields with numbers winning, and nothing renders differently yet.</done>
</task>

<task type="auto">
  <name>Task 2: Add the two ns.debugLogging-gated pandemic dumps that answer PANDEMIC.md's five unknowns</name>
  <files>MergeMode.lua</files>
  <read_first>
    - `Core.lua:1053-1060` — the `/tbt debug` slash branch and the `ns.debugLogging` toggle. **Read
      only.** This task adds a NEW call site for that existing flag and must not edit `Core.lua` at
      all (D-03, D-04).
    - `Core.lua:1172-1303` — the PROTECTED debug cast/item log: `pendingItemCasts`, `SafeNumber`,
      `SafeString`, `LogItemUse`, `LogPlayerCast`, and the print at `:1273-1277` whose colour idiom
      (`|cff00ccffTBT Debug|r:` prefix, a second-colour category tag, plain `..` concatenation, no
      `string.format`) this task copies. **Read only — D-04 forbids touching, moving, extending,
      wrapping or generalising any of it.**
    - `MergeMode.lua:2097-2225` — `ns:PrintMergeDiagnostics`, the existing `/tbt merge` diagnostic.
      Its per-entry `bits` loop is where the new pandemic rows go. Note it already prints
      `id=`/`spell=`/`equipSlot=` in the same shape.
    - `MergeMode.lua:640-670` — the guard order, again, for anything bound for a print.
    - `.planning/research/PANDEMIC.md` § "Unknowns Requiring In-Game Verification" — the five
      questions this task must make answerable. Unknown 3 in particular names
      `C_CooldownViewer.GetValidAlertTypes(cooldownID)` as the data source behind Blizzard's
      `CanTriggerAlertType`.
    - `48-PATTERNS.md` section 7 — the gate idiom (`if not ns.debugLogging then return end` as a
      guard clause, never a whole-body `if`) and the PROTECTED marking on `Core.lua`'s block.
  </read_first>
  <action>
    Add two prints, both in `MergeMode.lua`, both gated on the existing `ns.debugLogging` flag, both
    new call sites. **Edit no other file.** Neither print may call, wrap, extend or relocate
    `LogPlayerCast` or `LogItemUse` (D-04).

    **Print 1 — the state-change log (answers unknowns 1, 2 and 5).** Inside `ReadPandemicState`,
    capture `entry.pandemicActive`'s previous value before overwriting it, and when the new value
    differs from the old one, emit one line. Use the previous value already on the entry rather than
    adding a fourth stamped field. Gate it with a guard-clause `if not ns.debugLogging then` early
    return inside a small dedicated logging function so the hot path costs one boolean test when
    debug is off and allocates nothing.

    The line must carry, at minimum: the category tag `|cffff8040PANDEMIC|r`, the entry's `label`
    and `cooldownID`, the new state (`ON` / `OFF`), `GetTime()` at the moment of the change, and —
    for each of the two timestamps — whether it was secret, its `type()`, and its value when
    readable. Screen every value through the same `issecretvalue()`-then-`type()` discipline before
    it reaches the concatenation; `issecretvalue(v)` and `type(v)` are themselves always safe to
    print, because they are the introspection functions used to detect a secret and never return
    one.

    This shape is what makes the answers fall out: the `OFF` line's `GetTime()` compared against the
    last `finish` value answers unknown 1 (does `PandemicIcon` clear promptly or linger);
    the secret/type columns in and out of combat answer unknown 2; and running it while a
    target-debuff entry is up answers unknown 5.

    **Print 2 — the on-demand dump (answers unknowns 3 and 4).** Extend `ns:PrintMergeDiagnostics`
    with a per-entry pandemic block that prints **only when `ns.debugLogging` is on**, so ordinary
    `/tbt merge` output is byte-identical for a user who has not enabled debug. Document in a
    comment that the operator procedure is `/tbt debug` then `/tbt merge`, and that this is
    deliberately not a new slash command because `Core.lua` is protected this phase (D-03, D-04).

    For each shown merged entry, print its `cooldownID`, `label`, the live
    `ns.mergeItemFrames[...] ~= nil` cache hit (the backwards-trace check: a highlight that never
    appears with `frame=no` on every row means `CollectShownCooldownIDs` has not run, not that the
    signal is broken), `PandemicIcon` presence, both timestamps with their secret/type columns, the
    resolved answer from `ns:IsMergedEntryInPandemic(entry, GetTime())`, and **which route decided
    it** (`numbers` or `boolean`).

    Add one more column for unknown 3: whether `Enum.CooldownViewerAlertEventType.PandemicTime`
    appears in `C_CooldownViewer.GetValidAlertTypes(entry.cooldownID)`. This is a plain
    `C_CooldownViewer` namespace call — the same class of API the whole file already uses, **not** a
    mixin method on a frame, so it is permitted — but treat it defensively anyway: short-circuit on
    `Enum.CooldownViewerAlertEventType` existing (the capability-check idiom at `MergeMode.lua:43-54`),
    `pcall` the call itself, verify the result with the existing `ns:CanReadTable` before iterating,
    and guard each element with `issecretvalue` before comparing it. Print `?` when any of that
    degrades. A run on Forever where no entry ever reports the PandemicTime capability is the answer
    to unknown 4.

    Both prints are diagnostics: no behaviour anywhere may depend on them, and neither may write any
    field an execution path reads.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -c 'ns.debugLogging' MergeMode.lua)" -ge "2" && test "$(grep -c 'PANDEMIC' MergeMode.lua)" -ge "1" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -cE 'Log|debugLogging')" -ge "1" && test "$(sed -n '/^function ns:PrintMergeDiagnostics/,/^end$/p' MergeMode.lua | grep -c 'ns.debugLogging')" -ge "1" && test "$(sed -n '/^function ns:PrintMergeDiagnostics/,/^end$/p' MergeMode.lua | grep -c 'GetValidAlertTypes')" = "1" && test "$(grep -c 'CooldownViewerAlertEventType' MergeMode.lua)" -ge "1" && test "$(grep -c 'pcall(C_CooldownViewer.GetValidAlertTypes' MergeMode.lua)" = "1" && test "$(grep -cE 'LogItemUse|LogPlayerCast|pendingItemCasts' MergeMode.lua)" = "0" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -c 'itemFrame:')" = "0" && test "$(grep -c 'pandemicIconPool' MergeMode.lua)" = "0" && test "$(grep -cE 'GetBuildInfo|buildInterfaceVersion|wow_classic|WOW_PROJECT' MergeMode.lua)" = "0" && test "$(wc -l < Core.lua)" = "1367" && test -z "$(git status --porcelain -- Core.lua)" && stylua . && stylua --check . && stylua . && stylua --check . && git ls-files --eol MergeMode.lua | grep -q 'w/crlf' && echo GATES_OK</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion (**D-03**): `ns.debugLogging` is referenced at least twice in
      `MergeMode.lua` — the state-change log and the `/tbt merge` block — and both are new call
      sites of the existing flag, not a new flag.
    - Source assertion: `ns:PrintMergeDiagnostics` contains an `ns.debugLogging` gate, so
      `/tbt merge` output is unchanged for a user who has not run `/tbt debug`.
    - Source assertion (unknown 3): exactly one `pcall(C_CooldownViewer.GetValidAlertTypes` call
      exists, inside `ns:PrintMergeDiagnostics`, and `Enum.CooldownViewerAlertEventType` is
      capability-checked before use.
    - Source assertion (**S12**, **D-04**): `MergeMode.lua` contains zero occurrences of
      `LogItemUse`, `LogPlayerCast` or `pendingItemCasts`; `wc -l < Core.lua` is still `1367`; and
      `git status --porcelain -- Core.lua` is empty. `Core.lua` is byte-unchanged.
    - Source assertion (**S4**, **S6**): `ReadPandemicState` still contains zero `itemFrame:`
      occurrences after the logging was added, and `pandemicIconPool` still appears nowhere.
    - Source assertion (**S3**): still zero flavour tokens in `MergeMode.lua`.
    - Command (**S1**): `stylua .` then `stylua --check .`, run twice, exits 0 both times.
    - Command (**S2**): `git ls-files --eol MergeMode.lua` reports `w/crlf`.
    - Behavioural criterion, provable only in game (G0): with `/tbt debug` on, a tracked DoT recast
      inside its refresh window produces an `ON` line and later an `OFF` line, and `/tbt merge`
      prints one pandemic row per shown merged entry. This is Plan 48-03's Task 2, not this task's.
    - Gates satisfied: S1, S2, S3, S4, S6, S12. Feeds in-game gate G0 directly — this task is the
      instrument G0 reads.
    - No test runner exists; every gate above is a source assertion or a shell command.
  </acceptance_criteria>
  <done>A pandemic state change prints one debug line carrying its timestamps' secrecy, type and value; `/tbt debug` followed by `/tbt merge` prints a per-entry dump including the PandemicTime capability; and `Core.lua` is byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBT (tainted addon code) → Blizzard CDM frames | The only boundary in this phase. Everything TBT reads across it is attacker-free but taint-relevant: a wrong-shaped touch permanently taints the player's own Cooldown Manager |
| Game API → TBT | Values crossing may be Secret Values; a comparison or concatenation against one raises |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-01 | Tampering (taint propagation) | `ReadPandemicState`'s access to a cached CDM item frame | mitigate | Plain table-field reads only; zero `itemFrame:` method calls; zero assignments onto the frame; `pandemicIconPool` never named. Gated S4/S5/S6, proven in game by G4 |
| T-48-02 | Tampering (of TBT's own logic) | Secret-value type confusion on `pandemicStartTime`/`pandemicEndTime` | mitigate | `issecretvalue()` before `type()` per field; the pair is stamped all-or-nothing so no consumer sees a half window (S7) |
| T-48-03 | Denial of service (self-inflicted) | A raise inside the wipe-then-refill pass emptying every merged container | mitigate | The stamp runs under its own `pcall`, for the structural reason recorded at `MergeMode.lua:793-799` (S9) |
| T-48-04 | Information disclosure | Debug dump printing game values | accept | Single-player client addon, no network surface. Output is gated behind `ns.debugLogging`, off by default, and every value is screened before concatenation |
| T-48-SC | Tampering | package installs | n/a | This phase installs no package. No npm/pip/cargo surface exists in this repo |
</threat_model>

<verification>
- `stylua .` then `stylua --check .` from the repo root, bare, run **twice** — clean and idempotent
  both times (S1).
- `git ls-files --eol MergeMode.lua` reports `w/crlf` (S2). Never `sed -i` and never redirect over a
  source or `.planning/` file.
- `Core.lua` byte-unchanged: `wc -l < Core.lua` is `1367` and `git status --porcelain -- Core.lua` is
  empty (S12, D-04).
- The no-taint sweep S4/S5/S6 passes against `MergeMode.lua` as a whole and against
  `ReadPandemicState` specifically.
- Deploy is **not** part of this plan. Nothing renders yet; the visual result is Plan 02's.
</verification>

<success_criteria>
- `MergeMode.lua` stamps `entry.pandemicActive` and an all-or-nothing
  `entry.pandemicStart`/`entry.pandemicFinish` pair for every merged entry, once per shown-slot
  refresh, under its own `pcall`.
- `ns:IsMergedEntryInPandemic(entry, now)` exists, is pure, and applies D-01's precedence with
  numbers winning.
- Two `ns.debugLogging`-gated prints exist, both new call sites in `MergeMode.lua`, together
  covering all five of PANDEMIC.md's unknowns.
- No CDM mixin method is called, no CDM frame field is written, `pandemicIconPool` appears nowhere,
  and `Core.lua` is byte-unchanged.
- The addon renders exactly as it did before this plan.
</success_criteria>

<output>
Create `.planning/phases/48-pandemic-highlight-merge-mode/48-01-SUMMARY.md` when done.
</output>
