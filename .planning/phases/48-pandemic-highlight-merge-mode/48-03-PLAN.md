---
phase: 48-pandemic-highlight-merge-mode
plan: 03
type: execute
wave: 3
depends_on: ["48-02"]
files_modified: []
autonomous: false
requirements: [PAND-01, PAND-02, PAND-03, PAND-04, PAND-05]

must_haves:
  truths:
    - "Every static gate S1 through S14 is run once, together, across both touched files and its result recorded"
    - "The built phase is deployed to every installed WoW client before the human is asked to look at it"
    - "The five unknowns in PANDEMIC.md are answered from a real game session, not inferred"
    - "No requirement is reported closed on static evidence alone"
    - "A CDM that misbehaves after the highlight ships is reported as a taint failure and stops the phase"
  artifacts:
    - path: ".planning/phases/48-pandemic-highlight-merge-mode/48-03-SUMMARY.md"
      provides: "The consolidated static sweep result and the verbatim in-game gate outcomes"
      contains: "G0"
  key_links:
    - from: "48-VALIDATION.md S1..S14"
      to: "48-03 Task 1"
      via: "one consolidated sweep across MergeMode.lua and Display.lua"
      pattern: "S14"
    - from: "48-VALIDATION.md G0..G6"
      to: "48-03 Task 2"
      via: "blocking human checkpoint, Forever first then retail"
      pattern: "G4"
---

<objective>
Gate the phase. Task 1 runs every static assertion in `48-VALIDATION.md` once, as one sweep, across
both files the phase touched, and deploys the result. Task 2 hands the in-game checks to the user
and stops.

Purpose: **this project has no test runner and none is planned.** The static sweep proves the code
is *shaped* correctly — that no mixin method is called, that the guards are ordered right, that the
anchors match Blizzard's. It cannot prove that a highlight renders in a real Cooldown Manager, that
it clears when the window ends, or — the one that matters most — that no CDM frame was tainted.
Taint does not announce itself; it surfaces as the player's own Cooldown Manager quietly breaking
later in the session.

Output: a SUMMARY carrying the sweep result and the user's verbatim gate outcomes. **No PAND
requirement closes until Task 2's report comes back.** An unrun check is `human_needed`, not a pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/48-pandemic-highlight-merge-mode/48-VALIDATION.md
@.planning/phases/48-pandemic-highlight-merge-mode/48-CONTEXT.md
@.planning/phases/48-pandemic-highlight-merge-mode/48-01-SUMMARY.md
@.planning/phases/48-pandemic-highlight-merge-mode/48-02-SUMMARY.md
@.planning/research/PANDEMIC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the consolidated S1–S14 static sweep and deploy</name>
  <files>none — verification and deploy only; this task modifies no source file</files>
  <read_first>
    - `.planning/phases/48-pandemic-highlight-merge-mode/48-VALIDATION.md` § Static Assertions —
      the authoritative S1..S14 table. Where this task and that table disagree, **the table wins**.
      Note its three warnings: scope gates to the diff and not the file; `grep -c` exits 1 on a zero
      count so zero-match assertions must be wrapped; and `stylua` must be run **twice** because a
      Phase 47 comment placement made it non-idempotent.
    - `48-01-SUMMARY.md` and `48-02-SUMMARY.md` — the identifiers that actually shipped, so a failed
      assertion is attributed to the right plan instead of guessed at.
    - `MergeMode.lua:1-30` — the four-clause no-taint rule, re-read here deliberately. S4, S5, S6
      and S11 together are PAND-04, and they are the highest-value gates in the phase.
    - `CLAUDE.md` § Workflow — `stylua` bare from the repo root, twice; `git ls-files --eol` is the
      only thing that can see a CRLF reflow; deploy with `./scripts/install.bat`.
  </read_first>
  <action>
    Write no code. Run the sweep below as one command chain and record its output verbatim in the
    SUMMARY, including which gates are prohibition gates (zero-match) and which are positive
    assertions.

    Then re-read, by eye and not by grep, every line the phase added that mentions `itemFrame`, and
    confirm each is a plain table-field read or a plain C widget getter. S4 and S5 are stated in
    `48-VALIDATION.md` as **source reads** rather than commands for a reason: a grep can prove the
    absence of the five named mixin methods, but only a read can prove that a sixth was not
    invented. Record that confirmation explicitly in the SUMMARY, naming each line.

    Do the backwards trace `48-VALIDATION.md` § Verification Guidance demands, and write it down:
    start from `ApplyPandemicIcon` / `ApplyPandemicBar`, go back through
    `ns:IsMergedEntryInPandemic` to the three stamped fields, to `ReadPandemicState`, to
    `ns.mergeItemFrames`, and state what guarantees that table is populated at the moment the stamp
    is read — specifically whether `CollectShownCooldownIDs` has necessarily run, and what the stamp
    reads when it has not. Phase 47's blocker was a correctly-wired chain fed by a table nothing
    populated outside a narrow condition, and it read as sound in the forward direction to both a
    plan checker and a verifier.

    Finally deploy with `./scripts/install.bat` — argument-free, it covers every WoW client folder
    present on the machine. This phase changes no TOC, so `/reload` is sufficient in game and no
    full client restart is needed. Say so in the handoff.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && test "$(grep -c '^local function ReadPandemicState(entry)' MergeMode.lua)" = "1" && test "$(grep -c '^function ns:IsMergedEntryInPandemic(entry, now)' MergeMode.lua)" = "1" && test "$(sed -n '/^function ns:RefreshMergeShownSlots/,/^end$/p' MergeMode.lua | grep -c 'pcall(ReadPandemicState, entry)')" = "1" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -c 'issecretvalue')" -ge "2" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -c 'itemFrame:')" = "0" && test "$(sed -n '/^local function ReadPandemicState/,/^end$/p' MergeMode.lua | grep -cE 'itemFrame\.[A-Za-z]+ *=[^=]')" = "0" && test "$(cat MergeMode.lua Display.lua | grep -v '^\s*--' | grep -cE 'pandemicIconPool|SetupPandemicStateFrameForItem|IsInPandemicTime|ShowPandemicStateFrame|HidePandemicStateFrame|CheckSetPandemicAlertTriggerTime|SetLayoutData')" = "0" && test "$(grep -c 'CreateFrame.*CooldownPandemic' Display.lua)" = "2" && test "$(grep 'CreateFrame' Display.lua | grep 'CooldownPandemic' | grep -vc 'pcall')" = "0" && test "$(sed -n '/^local function EnsurePandemicBarFX/,/^end$/p' Display.lua | grep -c 'bar.statusBar:GetFrameLevel() + 1')" = "1" && test "$(sed -n '/^local function RenderIconContainer/,/^end$/p' Display.lua | grep -c 'ApplyPandemicIcon(')" = "2" && test "$(sed -n '/^local function RenderBarContainer/,/^end$/p' Display.lua | grep -c 'ApplyPandemicBar(')" = "2" && test "$(grep -cE 'GetBuildInfo|buildInterfaceVersion|wow_classic|WOW_PROJECT' MergeMode.lua)" = "0" && test "$(grep -cE 'GetBuildInfo|buildInterfaceVersion|wow_classic|WOW_PROJECT' Display.lua)" = "0" && test "$(grep -cE 'LogItemUse|LogPlayerCast|pendingItemCasts' MergeMode.lua)" = "0" && test "$(grep -cE 'LogItemUse|LogPlayerCast|pendingItemCasts' Display.lua)" = "0" && test "$(wc -l < Core.lua)" = "1367" && stylua . && stylua --check . && stylua . && stylua --check . && git ls-files --eol MergeMode.lua | grep -q 'w/crlf' && git ls-files --eol Display.lua | grep -q 'w/crlf' && ./scripts/install.bat && echo SWEEP_OK</automated>
  </verify>
  <acceptance_criteria>
    - Command: the chain above exits 0 and prints `SWEEP_OK`. It covers S1 (stylua twice), S2 (both
      files `w/crlf`), S3 (zero flavour tokens in either touched file), S4 and S6 (the seven
      forbidden CDM tokens absent from both files), S5 (no assignment onto an `itemFrame`), S7
      (`issecretvalue` present at least twice in the stamp), S9 (the stamp's `pcall`), S10 (two
      `CooldownPandemic*` `CreateFrame` lines, zero without `pcall`), S12 (`Core.lua` byte-unchanged
      at 1367 lines, and neither touched file names the protected block's symbols) and S13 (the bar
      frame-level bump).
    - Source read, recorded in the SUMMARY line by line (**S4**, **S5**, **S11**): every added line
      mentioning `itemFrame` is a plain table-field read or a plain C widget getter; no mixin method
      was invented beyond the five the grep covers; every highlight frame is TBT-created and
      parented to a TBT widget.
    - Source read, recorded in the SUMMARY (**S8**): the precedence in `ns:IsMergedEntryInPandemic`
      reads readable-numbers-first, boolean-fallback — not inverted.
    - Source read, recorded in the SUMMARY (**S14**): neither the stamp site nor either render call
      site allocates a table, a closure or a string per pass, and neither render function calls
      `GetTime()`.
    - Written backwards trace, recorded in the SUMMARY: from the rendered highlight back to
      `ns.mergeItemFrames`, naming what guarantees population and what the stamp reads when
      `CollectShownCooldownIDs` has not run.
    - Command: `./scripts/install.bat` completes, deploying to every installed client.
    - Gates satisfied: S1–S14 in full.
    - **This task closes no requirement.** It proves shape and nothing else; PAND-01..05 close only
      on Task 2's report.
  </acceptance_criteria>
  <done>Every static gate S1–S14 has been run and recorded, the no-taint source read and the backwards trace are written down, and the phase is deployed to every installed client.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: In-game phase gate — run G0 through G6, Forever first then retail</name>
  <files>none — verification only; this task modifies no file</files>
  <read_first>
    - `.planning/phases/48-pandemic-highlight-merge-mode/48-VALIDATION.md` § In-Game Human Checks —
      the authoritative G0..G6 table. The checks below restate it; where the two disagree, the table
      wins. Note that **G0 must run first** and that **G4 cannot be skipped**.
    - `.planning/research/PANDEMIC.md` § "Unknowns Requiring In-Game Verification" — the five
      questions G0 exists to answer, with the reasoning behind each.
    - `48-01-SUMMARY.md` and `48-02-SUMMARY.md` — what shipped, so a FAIL is attributed to the right
      plan.
  </read_first>
  <action>
    Write no code. Confirm Task 1's deploy ran, then hand the checks below to the user and stop.
    Record their results verbatim in the SUMMARY, including any check they did not run — an unrun
    check is `human_needed`, not a pass.

    If G4 fails, **stop the phase and report immediately**. Do not attempt a fix in this plan: a
    taint failure means a read in `ReadPandemicState` is the wrong shape, and that is Plan 01's
    surface, not this one's.
  </action>
  <what-built>
    The whole of Phase 48, across two implementation plans:

    - `MergeMode.lua` — `ReadPandemicState`, a `pcall`-wrapped per-entry stamp that reads
      `itemFrame.PandemicIcon` and the two guarded timestamps off the item frame already cached by
      `CollectShownCooldownIDs`, writing three plain fields onto TBT's own entry;
      `ns:IsMergedEntryInPandemic`, the render-time resolver where readable numbers win and the
      boolean is the fallback; and two `ns.debugLogging`-gated dumps — a state-change line, and a
      per-entry block printed by `/tbt merge` while debug is on.
    - `Display.lua` — TBT-owned instances of Blizzard's `CooldownPandemicFXTemplate` and
      `CooldownPandemicBarFXTemplate`, both `pcall`-guarded at creation, anchored with Blizzard's own
      `-6/+6` (icon) and `-9/+10` (bar, plus a frame level one above `bar.statusBar`) offsets, and
      toggled by one unconditional call per slot in each render function.

    **`Core.lua` is byte-unchanged** — the protected debug cast/item log was not touched.

    Every static gate S1–S14 passed. **That proves the code is shaped correctly and nothing more.**
    This project has no test runner and none is planned; the only runtime is the game client. None
    of PAND-01..05 is closed until the checks below come back. No TOC changed, so `/reload` is
    enough — no full client restart needed.
  </what-built>
  <how-to-verify>
    Run on the **WoW Forever beta (build 1.60.1) first**, then repeat on Midnight retail. You need
    Merge Mode on, a merged CDM entry for a **refreshable DoT or HoT** (not a trinket or a potion —
    Blizzard excludes item-backed entries from pandemic at the source), and that entry visible both
    as an icon and as a bar if your CDM configuration allows it.

    **G0 — the unknowns. Run this first; it decides whether the rest is even observable.**
    Type `/tbt debug` to turn logging on. Track a refreshable DoT/HoT, cast it, then recast it
    *inside* its refresh window. Watch the chat log, then type `/tbt merge` while the entry is up.
    Report back, verbatim, the `PANDEMIC` lines and the `/tbt merge` pandemic rows. Specifically:
    - the `GetTime()` on the `OFF` line versus the last `finish` value printed — that is unknown 1,
      whether `PandemicIcon` clears promptly or lingers;
    - the `secret=` / `type=` columns for both timestamps, **once out of combat and once in
      combat** — that is unknown 2, and it is the one that decides whether the numeric route is
      load-bearing or a nicety;
    - the PandemicTime capability column in `/tbt merge` — unknown 3, and if no entry ever reports
      it, unknown 4 (Forever simply does not fire the mechanic);
    - one run with a **debuff on a target** rather than a self-buff — unknown 5.
    Then `/tbt debug` again to turn logging off before the visual checks, so the chat is quiet.

    **G1 — PAND-01.** With that merged entry inside its pandemic window, look at TBT's mirrored
    **icon**.
    PASS when: the highlight shows — Blizzard's animated pandemic border, the same one drawn on the
    CDM's own icon. Compare the two side by side if the CDM is visible. A highlight that appears on
    Blizzard's icon and not on TBT's is a FAIL; say which container it was in (Tracked Buffs,
    Essential or Utility), because the Tracked Buffs path is the one where TBT's own icon is hidden
    and the engine draws the aura instead.

    **G2 — PAND-02.** Same entry, TBT's mirrored **bar**.
    PASS when: the highlight shows **and its border sits in front of the bar fill, not behind it**.
    Look at where the border crosses the filled part of the bar. A border disappearing under the
    fill means the frame-level bump did not take.

    **G3 — PAND-03.** Let the window end on its own, without casting anything else and without any
    other aura coming or going.
    PASS when: the highlight clears. If it lingers, **record roughly how long** — that is
    PANDEMIC.md's unknown 1 showing up in the visual path, and it decides whether the numeric route
    needs to become load-bearing in a follow-up phase. A lingering highlight is a recorded
    measurement, not necessarily a FAIL; a highlight that never clears at all is a FAIL.

    **G4 — PAND-04. This is the one that cannot be skipped.** After G1–G3, use Blizzard's own
    Cooldown Manager normally: open it, change a setting, drag an entry between containers, close
    it. Then keep playing for a few minutes.
    PASS when: it behaves normally throughout. **Any Lua error, or a Cooldown Manager that stops
    responding until `/reload`, means a frame was tainted — stop immediately and report the exact
    error text.** Taint is sticky: it survives combat and clears only on `/reload`, so a CDM that
    misbehaves "a while later" is still this phase's fault.

    **G5 — PAND-05.** Find an entry whose pandemic state cannot be observed — an item-backed merged
    entry is the natural case (a trinket, a potion or a healthstone), because Blizzard's own code
    short-circuits on `IsItem()` before ever computing a window for it.
    PASS when: it renders exactly as it did before this phase — no highlight, no Lua error, and no
    highlight stuck on from some earlier entry that used the same pooled slot. Watch one specifically
    after a container has shrunk (let some entries drop off) — a highlight floating over an empty
    cell is a FAIL.

    **G6 — repeat G1 through G5 on Midnight retail.** Forever ships identical pandemic code, which
    is HIGH confidence; Forever *behaving* the same is only MEDIUM, and retail's secret-value
    behaviour on the two timestamps has never been measured at all. Report any difference between
    the two clients, especially in G0's `secret=` columns.
  </how-to-verify>
  <acceptance_criteria>
    - Every one of G0..G6 is reported as PASS, FAIL or not-run, in the user's own words, recorded
      verbatim in the SUMMARY.
    - G0's answers to all five unknowns are recorded as data (the actual printed values), not as a
      summary judgement.
    - PAND-01 closes on G1, PAND-02 on G2, PAND-03 on G3, PAND-04 on G4, PAND-05 on G5 — each only
      on the Forever **and** retail runs, per G6.
    - A FAIL on G4 stops the phase and is reported with the exact error text; no fix is attempted in
      this plan.
    - This checkpoint is the **only** evidence path for all five PAND requirements. There is no test
      runner and no automated substitute; a static sweep cannot see a rendered frame or a tainted
      one.
  </acceptance_criteria>
  <done>G0 through G6 have been run on Forever and on retail and their results recorded verbatim, including any check the user did not run, which is recorded as human_needed rather than as a pass. No automated substitute exists: this project has no test runner.</done>
  <resume-signal>Type "approved" with the G0 data and the G1–G6 results, or describe what failed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TBT → the player's live Cooldown Manager | The only boundary. This plan writes no code; it exists to prove the boundary was respected, by sweep and by play |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-08 | Tampering (taint propagation) | A CDM frame tainted by Phase 48's read | mitigate | S4/S5/S6/S11 statically in Task 1, then G4 in play — the only check that can actually observe it. Sticky for the session, clearing only on `/reload` |
| T-48-09 | Repudiation (of evidence) | A requirement reported closed on static evidence | mitigate | Task 1 closes nothing by construction; an unrun G-check is recorded as `human_needed`, never as a pass |
| T-48-SC | Tampering | package installs | n/a | No package installs in this repo |
</threat_model>

<verification>
- Task 1's chain exits 0 with `SWEEP_OK`, covering S1–S14.
- `./scripts/install.bat` has deployed before the checkpoint is presented.
- Task 2's results are recorded verbatim, per gate, with G0's raw printed values included.
</verification>

<success_criteria>
- S1 through S14 all run and all recorded, including the three that are source reads rather than
  commands.
- The backwards trace from the rendered highlight to `ns.mergeItemFrames` is written down.
- G0 through G6 are run on Forever and on retail, and their results — including any not-run check —
  are recorded verbatim.
- PAND-01..05 are marked satisfied only where the corresponding in-game check passed on both
  clients.
</success_criteria>

<output>
Create `.planning/phases/48-pandemic-highlight-merge-mode/48-03-SUMMARY.md` when done.
</output>
