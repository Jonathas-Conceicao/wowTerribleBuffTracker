---
phase: 35.1-config-panel
plan: 02
subsystem: ui
tags: [wow-addon, cdm, saved-variables, config-panel]

# Dependency graph
requires:
  - phase: 35.1-config-panel
    plan: 01
    provides: ns.tbtConfigPanel, panel.Content, ns:ShowTBTConfigPage/ns:ShowTBTTrackerList
provides:
  - ns.db.stealMode — addon-wide, persisted, off by default, inert until Phase 40
  - TBTStealModeCheckbox — labelled checkbox on the config page, re-synced from ns.db on every show
  - ns.tbtConfigContainerSection — named, hidden, empty seam frame for Phase 36 (CONT-04/CONT-05)
affects: [phase-36-containers, phase-40-steal-mode]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Addon-wide boolean flags live as siblings of ns.db.tbtVisible (seeded via the `if ns.db.X == nil then` idiom in Core.lua's ADDON_LOADED), not inside ns.db.containerSettings, when the setting applies across all containers rather than to one"
    - "Checkbox state is re-read from ns.db on every page show (not just at build time), mirroring EditModeFrames.lua's ShowEditModePanel/ns.tbtSidebarCheckbox precedent, so a flag flipped elsewhere is always reflected"
    - "A future phase's UI seam is a named, hidden, empty frame anchored last in the content stack with a grep-able comment marker (`-- SEAM: Phase N`), so growing it later cannot displace sibling controls"

key-files:
  created: []
  modified:
    - Core.lua
    - CDMTab.lua

key-decisions:
  - "ns.db.stealMode written only by the checkbox's OnClick; zero readers anywhere in the codebase in this plan — Phase 40 owns every reader, per CFG-03/STEAL-06's addon-wide-not-per-category shape"
  - "No CURRENT_SCHEMA_VERSION bump and BuffEngine.lua untouched — seeding a missing key with a default is idempotent and belongs at the read site (ADDON_LOADED), not behind a version gate"
  - "ns.tbtConfigContainerSection is the last element in CreateConfigPanel, anchored below the steal-mode description and Hide()-den so the panel shows no blank section until Phase 36 fills it"

requirements-completed: [CFG-03]

# Metrics
duration: ~15min
completed: 2026-09-21
---

# Phase 35.1 Plan 02: Steal-Mode Toggle and the Phase 36 Seam Summary

**`ns.db.stealMode` persists an addon-wide CDM steal-mode flag (off by default, sibling of `tbtVisible`) via a new labelled checkbox on the config page, plus a named hidden `ns.tbtConfigContainerSection` seam frame for Phase 36 — the flag has zero readers anywhere in the codebase this plan touches.**

## Performance

- **Duration:** ~15 min
- **Completed:** 2026-09-21T05:24:29Z
- **Tasks:** 3 (2 code tasks + 1 format/verify/deploy task)
- **Files modified:** 2 (`Core.lua`, `CDMTab.lua`)

## Accomplishments
- `Core.lua`'s `ADDON_LOADED` handler now seeds `ns.db.stealMode = false` immediately after the `tbtVisible` default and before the `containerSettings` registry pass, with a comment naming CFG-03, the addon-wide-not-per-container rationale, and Phase 40 as the only future reader
- `CDMTab.lua`'s `CreateConfigPanel` gained `TBTStealModeCheckbox` (`UICheckButtonTemplate`, `GameFontHighlightMedium` label "CDM Steal Mode", `GameFontHighlightSmall` description), whose `OnClick` writes `ns.db.stealMode` (coerced to a real boolean) and plays the standard checkbox sound pair, and calls nothing else
- `ns:ShowTBTConfigPage` re-syncs `ns.tbtStealCheckbox:SetChecked(ns.db.stealMode == true)` on every show, matching the `ShowEditModePanel`/`ns.tbtSidebarCheckbox` precedent
- `ns.tbtConfigContainerSection` added as the last element of `CreateConfigPanel`, anchored below the steal-mode description, sized to the content column, hidden, and marked with a `-- SEAM: Phase 36 (CONT-04/CONT-05)` comment block for that phase's plan to grep for

## Task Commits

Each task was committed atomically:

1. **Task 1: `Core.lua` — default the flag** - `62c4899` (feat)
2. **Task 2: `CDMTab.lua` — the toggle and the Phase 36 seam** - `6e25a4c` (feat)
3. **Task 3: format, deploy, verify** - no separate commit; `stylua` was run (no reflow needed) before each commit above so both commits are already formatted (`stylua --check .` exits 0 on HEAD); `./scripts/install.bat` deployed successfully to all four detected WoW client folders

**Plan metadata:** SUMMARY.md intentionally not committed per the execution objective (orchestrator owns STATE.md/ROADMAP.md updates).

## Files Created/Modified
- `Core.lua` - `ns.db.stealMode` default block (8 lines) added between the `tbtVisible` default and the `containerSettings` registry pass
- `CDMTab.lua` - `TBTStealModeCheckbox` + label + description fontstrings + `OnClick` handler, `ns.tbtConfigContainerSection` seam frame, and the `SetChecked` re-sync line in `ns:ShowTBTConfigPage`

## Decisions Made
See `key-decisions` in frontmatter above — all decisions were the plan's own explicit design (sibling-of-tbtVisible placement, no schema bump, hidden-seam-as-last-element). No decisions deviated from the plan.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Explanatory comment used "steal-mode"/"steal mode" instead of the literal `stealMode` identifier, undershooting the acceptance grep count**
- **Found during:** Task 1 self-verification (`grep -c 'stealMode' Core.lua` returned 2, not the required 3)
- **Issue:** The plan's acceptance criterion requires the literal substring `stealMode` to appear exactly 3 times in `Core.lua` (comment + nil check + assignment); the first draft of the comment used the hyphenated/spaced prose form "steal-mode flag" / "steal mode is one switch", which doesn't contain the substring.
- **Fix:** Reworded the comment's opening clause to read "ns.db.stealMode is an addon-wide flag", giving one literal comment-level hit alongside the nil-check and assignment lines, with no change to meaning.
- **Files modified:** `Core.lua`
- **Verification:** Re-ran `grep -c 'stealMode' Core.lua` — returns 3.
- **Committed in:** `62c4899` (caught and fixed before the commit was made, so no separate fix commit was needed)

---

**Total deviations:** 1 auto-fixed (Rule 1 - comment wording to satisfy a literal-string acceptance grep, not a functional bug)
**Impact on plan:** No functional or behavioural impact — comment text only. No scope creep.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- `ns.tbtConfigContainerSection` is the named, hidden seam Phase 36 parents its container create/delete controls into and calls `:Show()` on — grep target `SEAM: Phase 36`.
- `ns.db.stealMode` is persisted and fully inert; Phase 40 (`STEAL-01`...`STEAL-08`) is expected to be the first and only reader.
- The six in-game observations in the plan's Verification section were not performed — this run was autonomous, per the plan's own instruction to fold them into Phase 42's Forever end-to-end pass and mark this phase code complete rather than blocking.
- `luac` is not on this machine's PATH; `stylua --check .` (exit 0) was used as the syntax/format check per the plan's testing-reality guidance.
- Scope fence held: no container create/delete, no debug toggle, no reset-positions button, no global scale/opacity override, no `cdmViewerGlobal`/`CooldownViewerCategory` reference were added.

---
*Phase: 35.1-config-panel*
*Completed: 2026-09-21*
## Self-Check: PASSED
