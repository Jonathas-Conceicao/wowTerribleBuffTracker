---
phase: 50-cleanup-release-prep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "REQUIREMENTS.md's RACE rows state what actually shipped, and every RACE checkbox agrees with its traceability row"
    - "A ready-to-paste v0.4.1 CHANGELOG entry exists as its own artifact for the user to edit"
    - "CHANGELOG.md and README.md are byte-identical to how this phase found them"
    - "install.bat, install.ps1, release.bat, .pkgmeta and the TOC are reviewed against what this milestone actually changed, with the findings on the record"
    - "The leftover executor worktree is gone from git worktree list, and it was proven to hold nothing unmerged before it went"
  artifacts:
    - path: ".planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md"
      provides: "The drafted v0.4.1 entry, in CHANGELOG.md's own format, for the user to paste"
      contains: "## v0.4.1"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Corrected RACE traceability"
      contains: "- [x] **RACE-10**"
  key_links:
    - from: ".planning/REQUIREMENTS.md RACE checkboxes"
      to: ".planning/REQUIREMENTS.md Traceability table"
      via: "each ticked requirement has a row stating its in-game verdict"
      pattern: "RACE-(07|08|10) \\| Phase 49 \\|"
---

<objective>
The paperwork half of the phase: correct the two bookkeeping errors CONTEXT.md names, review the
release mechanics against what this milestone actually changed, and hand the user a drafted CHANGELOG
entry they can paste and edit themselves.

**The single hardest constraint in this plan is a prohibition.** `CLAUDE.md`: never overwrite or
rewrite `CHANGELOG.md`. The user hand-edits it, it is append-only and only when asked, and a diff
against expectation is their deliberate edit rather than a bug. It was destroyed once, on 2026-09-19,
by an agent that read a mismatch as a failed edit. CONTEXT.md D-08 therefore has this phase DRAFT the
entry as a separate file. CONTEXT.md D-07 removes README and the store copy from scope entirely.

Purpose: close the milestone's records honestly without touching the user's own copy.
Output: a corrected `REQUIREMENTS.md`, a `50-CHANGELOG-DRAFT.md` artifact, a release-mechanics review
recorded in the SUMMARY, and one fewer stale git worktree.

**Do not write to `CHANGELOG.md`. Do not write to `README.md`. Do not open either for editing.**
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/50-cleanup-release-prep/50-CONTEXT.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct REQUIREMENTS.md's stale RACE traceability</name>

  <read_first>
    - `.planning/REQUIREMENTS.md` — the whole file, but in particular the `### RACE` section
      (RACE-07, RACE-08, RACE-09, RACE-10) and the `## Traceability` table with the italic footnote
      under it.
    - `.planning/STATE.md` § Current Position — the Phase 49 close: G1-G10 run in game across all ten
      races, G4 (the migration) passed on a real logout/login, G8 waived for this release.
    - `CLAUDE.md` § Workflow — the `sed -i` prohibition and the `git ls-files --eol` assertion.
  </read_first>

  <files>.planning/REQUIREMENTS.md</files>

  <action>
Bring the RACE bookkeeping up to what Phase 49 actually closed. **Use the Edit tool for every change.
Never `sed -i`, never a shell redirect over this file** — a CRLF→LF reflow on a `text=auto` file is
invisible to `git diff` and was confirmed on a controlled fixture on 2026-09-23.

1. **Tick three checkboxes.** `- [ ] **RACE-07**`, `- [ ] **RACE-08**` and `- [ ] **RACE-10**` become
   `- [x]`. Leave `RACE-09` exactly as it is — it is struck through and closed as obsolete by D-7
   rather than implemented, and its unticked box is correct.

2. **Replace two stale traceability rows.** The RACE-07 row currently reads "Data collected for all 10
   races; implementation not started" and the RACE-10 row reads "Not started — per-racial race-gated
   trackers (replaces the two-slot model)". Both are a full phase out of date. Rewrite each Status cell
   to state the in-game verdict, in the same voice the ITEM and DISP rows use — complete, with what was
   observed and where. Source the verdicts from STATE.md: all 21 racials across all ten races collected
   and tracked, G1-G10 run by the user, G4 (the `racial`/`racial2` → `racial:<spellID>` migration)
   passed on a real logout/login, and G8 (the Skyborne upward duration correction) waived for this
   release so both Skyborne second racials ship on their minimum duration. Name the G8 waiver in the
   RACE-07 row — a waiver is not a pass and the table must not read as though it were.

3. **Update the RACE-08 row for consistency.** It currently opens "Data collected; reshaped by
   RACE-10", which reads as not-implemented beside a ticked box. Keep its "reshaped by RACE-10 — every
   race is served by its own entries" substance and give it a completed verdict.

4. **Fix the footnote under the table.** It reads "*Phases 49 (Cleanup & Release Prep), 51 (Forever
   Full Review) and 52 (Retail Full Review) map to no requirement*". Phase 49 is the Forever Racial
   Catalogue; **50** is Cleanup & Release Prep. Correct the number and keep the rest of the sentence.

Change nothing else in the file. Do not touch the ITEM, PAND or DISP sections, the Future Requirements
list, or the Out of Scope list.
  </action>

  <acceptance_criteria>
    - `grep -c '^- \[x\] \*\*RACE-\(07\|08\|10\)' .planning/REQUIREMENTS.md` prints `3`. It prints `0`
      before this task.
    - `grep -c '^- \[ \] \*\*RACE-\(07\|08\|10\)' .planning/REQUIREMENTS.md` prints `0` (grep exits 1;
      expected). It prints `3` before this task.
    - `grep -ci 'not started' .planning/REQUIREMENTS.md` prints `0` (grep exits 1; expected). It prints
      `2` before this task.
    - `grep -c 'Phases 50 (Cleanup & Release Prep)' .planning/REQUIREMENTS.md` prints `1`, and
      `grep -c 'Phases 49 (Cleanup & Release Prep)' .planning/REQUIREMENTS.md` prints `0`.
    - `grep -c '^- \[ \] ~~\*\*RACE-09' .planning/REQUIREMENTS.md` prints `1` — RACE-09 is untouched.
    - `grep -c 'G8' .planning/REQUIREMENTS.md` is at least `1` — the waiver is named in the table.
    - `git ls-files --eol .planning/REQUIREMENTS.md` reads `i/lf    w/crlf` after the edit. A `w/lf`
      or a `-text` reading means the file was reflowed and the change must be redone with the Edit
      tool.
    - `git diff --stat -- .planning/REQUIREMENTS.md` reports fewer than 20 changed lines — a whole-file
      rewrite is a failure, not a pass.
  </acceptance_criteria>

  <verify>
    <automated>grep -c '^- \[x\] \*\*RACE-\(07\|08\|10\)' .planning/REQUIREMENTS.md; grep -ci 'not started' .planning/REQUIREMENTS.md; grep -c 'Phases 50 (Cleanup & Release Prep)' .planning/REQUIREMENTS.md; git ls-files --eol .planning/REQUIREMENTS.md; git diff --stat -- .planning/REQUIREMENTS.md</automated>
  </verify>

  <done>RACE-07, RACE-08 and RACE-10 are ticked with traceability rows stating their real in-game
verdicts including the G8 waiver, the phase-number footnote is correct, RACE-09 is untouched, the file
is still `i/lf w/crlf`, and the diff is small and surgical.</done>
</task>

<task type="auto">
  <name>Task 2: Draft the v0.4.1 CHANGELOG entry as its own artifact</name>

  <read_first>
    - `CHANGELOG.md` — **READ ONLY, for its format and the user's own voice.** Read the `## v0.4.0`,
      `## v0.3.0` and `## v0.2.6` entries: `## vX.Y.Z — Title`, a short intro paragraph, then
      `### New Features` / `### Fixes` / `### Known Issues` with plain user-facing bullets, hard-wrapped
      at roughly 72 columns. The `## v0.3.0` entry's Known Issues bullet about Forever SavedVariables is
      the precedent for how this project writes up a limitation. **Do not edit this file. Do not append
      to it. Do not stage it.**
    - `.planning/REQUIREMENTS.md` as left by Task 1 — ITEM-01..10, PAND-01..05, DISP-01..04, RACE-07/08/10
      with their in-game verdicts. This is the source of truth for what actually shipped.
    - `.planning/STATE.md` § Current Position — the nine post-gate fixes and the two waivers.
    - `.planning/research/FOREVER-RACIALS.md` — the catalogue's shape: 10 races, 21 racials.
  </read_first>

  <files>.planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md</files>

  <action>
Write a NEW file, `.planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md`, containing a
proposed `## v0.4.1` entry for the user to paste into `CHANGELOG.md` above the `## v0.4.0` entry
themselves.

Structure the artifact as: a short header (four lines at most) saying this is a draft, that no agent
appends it, and where it goes; then the entry itself inside a fenced ` ```markdown ` block so it can be
copied cleanly; then a short "Notes for the user" section carrying the two recommendations below.

**The entry** uses `CHANGELOG.md`'s own format and covers four things in the order the milestone built
them:

1. **Generic item tracking** (ITEM-01..10, both flavours) — the usable consumables in your bags are
   catalogued and offered in Suggested with their icon and the number you are holding; drag one in to
   track it; using an item refreshes every tracked item's cooldown, so potions that share a cooldown
   all show it; counts go down only when a use actually lands and are reconciled against your bags
   when you leave combat; a tracked item keeps its tile and its running cooldown after the stack runs
   out.
2. **Pandemic highlight** (PAND-01..05) — a merged Cooldown Manager entry inside its pandemic window
   shows the highlight on TBT's mirrored icon and bar, and clears when the window ends. Retail only in
   practice; Forever has no pandemic and simply shows nothing.
3. **Dispel-type border** (DISP-01..04) — a merged entry the Cooldown Manager borders by dispel type
   shows the same border on TBT's icon and bar. TBT draws it only where Blizzard draws it.
4. **Forever racial catalogue** (RACE-07/08/10) — all ten Forever races supported, every racial its own
   tracker, visible only to its own race, replacing the old two generic racial slots. Existing racial
   placements are migrated, so nothing you had placed is lost.

Write it for a player, not for a developer: no REQ-IDs, no file names, no phase numbers, no API names
in the entry itself.

**The `### Fixes` section is conditional and must be earned.** Include a fix ONLY if it repairs
behaviour that shipped in v0.4.0 or earlier. The test: `CHANGELOG.md`'s v0.4.0 entry shipped racial
tracking for **gnome, troll and orc** only, so a fix to one of those three races' racials is a fix to
released behaviour and belongs here; a fix to a night elf, tauren, draenei, dwarf, human, undead or
Skyborne racial is part of a feature that never shipped and belongs under New Features, not Fixes.
Check STATE.md's nine post-gate fixes against that test one by one. If nothing clears it, omit the
`### Fixes` section entirely and say so in the Notes section — an empty heading is worse than no
heading.

**The "Notes for the user" section** must carry these two recommendations, each as a recommendation
with its reasoning, not as a silent decision:

- **G8 — the Skyborne upward duration correction (waived).** Both Skyborne second racials ship on
  their minimum duration, so a Skyborne player can see a shorter timer than the buff actually has.
  This is user-visible, which is the same reason the Forever SavedVariables bug earned a Known Issues
  bullet. Recommend INCLUDING it as a `### Known Issues` bullet, and supply the drafted bullet text so
  the user only has to keep or cut it.
- **F-2's priest branch — a gnome priest's Eureka! spending a stack on a heal, untested.** Recommend
  LEAVING IT OUT: it is an untested path rather than a known defect, and a changelog that lists
  untested paths tells a player nothing they can act on. It stays on the record in STATE.md and
  REQUIREMENTS.md. State the recommendation and the reasoning so the user can overrule it.

Propose the entry title in the header line — `## v0.4.1 — Generic Item Tracking and Forever Racials`
is the milestone's own name; offer one shorter alternative in the Notes section and let the user pick.
  </action>

  <acceptance_criteria>
    - `test -f .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md` succeeds.
    - `grep -c '^## v0.4.1' .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md` prints
      at least `1`.
    - `grep -c '^### New Features' .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md`
      prints `1`.
    - The draft's New Features section contains one bullet per feature area — item tracking, pandemic
      highlight, dispel-type border, racials — in that order.
    - `grep -ci 'G8\|Skyborne' .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md` is at
      least `1` and the Notes section states a recommendation for it.
    - `grep -ci 'priest' .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md` is at least
      `1` and the Notes section states the leave-it-out recommendation with its reasoning.
    - The entry contains no REQ-ID: `grep -c 'ITEM-0\|PAND-0\|DISP-0\|RACE-0' .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md`
      may be non-zero only in the Notes/header prose, never inside the fenced entry block.
    - **`git status --porcelain -- CHANGELOG.md README.md` prints nothing.** This is the hardest
      criterion in the phase; a single changed byte in either file is a failed task.
    - `git diff --stat HEAD -- CHANGELOG.md README.md` prints nothing.
    - `git ls-files --eol .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md` (after
      staging) reads `i/lf` — any `-text` reading means the file went in binary and must be rewritten.
  </acceptance_criteria>

  <verify>
    <automated>test -f .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md; grep -c '^## v0.4.1' .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md; git status --porcelain -- CHANGELOG.md README.md</automated>
  </verify>

  <done>A ready-to-paste v0.4.1 entry exists as its own artifact, covering the four features in build
order, with the G8 and priest-branch recommendations stated for the user to decide. `CHANGELOG.md` and
`README.md` are untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Review the release mechanics and sweep the leftover executor worktree</name>

  <read_first>
    - `TerribleBuffTracker.toc` — the `## Interface: 120100, 16001` line, `## Version: @project-version@`,
      and the load list.
    - `scripts/install.bat` and `scripts/install.ps1` — in particular how `install.ps1` derives the file
      set from the TOC, follows XML `file="..."` references, and resolves `## IconTexture:` (INST-09).
    - `scripts/release.bat` — the REL-01 branch guard and the `TBT_ALLOW_BRANCH` override.
    - `.pkgmeta` and `.github/workflows/release.yml` — the ignore list and the packaging job.
    - `.planning/phases/50-cleanup-release-prep/50-CONTEXT.md` § Bookkeeping to correct in this phase —
      the leftover worktree.
  </read_first>

  <files>(none — review and a git-level sweep; findings recorded in the SUMMARY)</files>

  <action>
**Part A — review the release mechanics against what this milestone actually changed. Do not run a
release.** `release.bat` is REVIEWED, never executed: Phases 51 and 52 come after this one, and the
milestone is squash-merged to `main` before any tag exists.

Start from the actual change set: `git diff --name-status main...HEAD -- ':!.planning'`. Answer these
five questions in a SUMMARY table, each with the command or file:line that proves the answer:

1. Did this milestone ADD or REMOVE any runtime source file? (An `A` or `D` entry outside `.planning`
   and `tools/` would mean the TOC load list, and therefore install and packaging coverage, need a
   change.)
2. Does `install.ps1` still deploy every file the addon loads — the TOC load list, the `.lua` that
   `CDMTab.xml` pulls in via `file="..."`, and the `## IconTexture:` `.blp`?
3. Does `.pkgmeta`'s ignore list still keep everything non-shipping out of the zip — in particular
   `tools` (which holds `tools/TBTProbe/`, modified this milestone and deliberately not shipped),
   `.planning`, `scripts` and `.gitattributes`?
4. Does `release.bat` still refuse to tag from a non-`main` branch unless `TBT_ALLOW_BRANCH=1`? This is
   the guard behind the standing rule: squash-merge the milestone branch to `main` FIRST, then release
   from `main`; never tag from the milestone branch.
5. Does the TOC still declare both target clients (`120100` retail, `16001` Forever) in one
   `## Interface:` line, with no second TOC anywhere in the repo?

Where the answer is "no change needed", say so and show the evidence. Where something IS needed,
record it as a finding — do not fix release tooling in this plan; the phase ships no release.

**Part B — sweep the leftover executor worktree.** A worktree sits at
`.claude/worktrees/agent-ad696cb1` on branch `worktree-agent-ad696cb1` at `a3b1aa3`, still registered
in `git worktree list`. Prove it holds nothing unmerged, then remove it. In this order:

1. `pwd` — confirm you are NOT inside `.claude/worktrees/agent-ad696cb1`. If you are, stop and record
   it; never remove the worktree you are running in.
2. `git -C .claude/worktrees/agent-ad696cb1 status --porcelain` — must print nothing. Any output means
   uncommitted work; stop and record it instead of removing.
3. `git diff --name-status main..worktree-agent-ad696cb1 | awk '{print $1}' | sort | uniq -c` — there
   must be **zero `A` entries**. An `A` would mean the branch holds a file `main` does not have, which
   is the definition of unmerged content here. Record the full status counts in the SUMMARY.
4. Record the tip SHA (`git rev-parse worktree-agent-ad696cb1`) in the SUMMARY BEFORE deleting
   anything, so the commits stay findable through the reflog.
5. Only if 1-3 all pass: `git worktree remove .claude/worktrees/agent-ad696cb1` (add `--force` only if
   git refuses for a reason you have recorded), then `git worktree prune`, then
   `git branch -D worktree-agent-ad696cb1`.
6. The sibling directory `.claude/worktrees/agent-aee141ff7b90895e2` is an empty orphan that
   `git worktree list` does not know about. Confirm it is empty (`ls -A` prints nothing) and remove the
   directory. If it is NOT empty, leave it and record what is in it.

`.claude/` is gitignored, so none of Part B produces a commit — it is a repo-state change that takes
effect immediately. Record every command and its output in the SUMMARY.
  </action>

  <acceptance_criteria>
    - The SUMMARY contains a five-row release-review table, one row per question in Part A, each with a
      verdict and the command or `file:line` that proves it.
    - `git diff --name-status main...HEAD -- ':!.planning' | grep -c '^[AD]'` prints `0` — the evidence
      behind question 1 (grep exits 1 on zero; expected).
    - `grep -c '^  - tools$' .pkgmeta` prints `1` and `grep -c '^  - .planning$' .pkgmeta` prints `1`.
    - `grep -c 'TBT_ALLOW_BRANCH' scripts/release.bat` prints at least `2` — the guard and its override
      are both still present.
    - `ls *.toc | wc -l` prints `1`, and `grep -c '^## Interface: 120100, 16001$' TerribleBuffTracker.toc`
      prints `1`.
    - `git status --porcelain -- scripts .pkgmeta .github TerribleBuffTracker.toc` prints nothing — this
      task reviews, it does not edit.
    - `git worktree list | grep -c agent-ad696cb1` prints `0` (grep exits 1; expected). It prints `1`
      before this task.
    - `git branch --list worktree-agent-ad696cb1` prints nothing.
    - `ls .claude/worktrees/` does not list `agent-ad696cb1` or `agent-aee141ff7b90895e2`.
    - The SUMMARY records the tip SHA that was deleted and the `A`-entry count that justified deleting
      it.
  </acceptance_criteria>

  <verify>
    <automated>git diff --name-status main...HEAD -- ':!.planning' | grep -c '^[AD]'; grep -c 'TBT_ALLOW_BRANCH' scripts/release.bat; ls *.toc | wc -l; git worktree list | grep -c agent-ad696cb1; git status --porcelain -- scripts .pkgmeta .github TerribleBuffTracker.toc</automated>
  </verify>

  <done>The five release-mechanics questions are answered with evidence in the SUMMARY, no release
file was edited and no release was run, and the leftover worktree and its branch are gone with the
proof that they held nothing unmerged recorded first.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent → user-authored files | `CHANGELOG.md` and `README.md` are hand-maintained by the user. An agent write to either is the failure mode this phase is shaped around. |
| Agent → git history | Part B deletes a branch and a worktree; a wrong call loses commits that exist nowhere else. |
| Agent → `.planning/` records | These files are the project's memory; a silent CRLF reflow corrupts them invisibly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-50-10 | Tampering / data loss | `CHANGELOG.md`, `README.md` | mitigate | Neither file is opened for editing; the entry is written to a separate artifact (D-07, D-08). `git status --porcelain -- CHANGELOG.md README.md` printing nothing is a hard acceptance criterion on Task 2. Destroyed once on 2026-09-19 by exactly this class of action. |
| T-50-11 | Tampering (invisible) | `.planning/REQUIREMENTS.md` | mitigate | Edit tool only — no `sed -i`, no shell redirect. `git ls-files --eol` must read `i/lf w/crlf` afterwards, because a CRLF→LF reflow on a `text=auto` file shows nothing in `git diff`. A diffstat ceiling catches a whole-file rewrite. |
| T-50-12 | Repudiation / data loss | `worktree-agent-ad696cb1` | mitigate | Deletion is gated on a clean `status --porcelain` and zero `A` entries against `main`, and the tip SHA is recorded in the SUMMARY before the branch is deleted so the commits stay reachable through the reflog. |
| T-50-13 | Elevation / premature release | `scripts/release.bat` | mitigate | The script is reviewed, never executed; the acceptance criteria assert the `TBT_ALLOW_BRANCH` branch guard still exists rather than exercising it. No tag is created and nothing is pushed. |
| T-50-14 | Information disclosure | `.pkgmeta` ignore list | mitigate | Question 3 of the review asserts `tools` and `.planning` stay out of the shipped zip — `tools/TBTProbe/` was modified this milestone and must not reach users. |
| T-50-15 | Tampering | `TerribleBuffTrackerDB` | accept | No code runs and no addon file changes in this plan; the user's account-wide database is not reachable from it. |
| T-50-SC | Tampering | package installs | accept | No package manager is invoked; no dependency is added, removed or upgraded. |
</threat_model>

<verification>
Run from the repo root:

1. `grep -c '^- \[x\] \*\*RACE-\(07\|08\|10\)' .planning/REQUIREMENTS.md` → `3`
2. `grep -ci 'not started' .planning/REQUIREMENTS.md` → `0`
3. `grep -c 'Phases 50 (Cleanup & Release Prep)' .planning/REQUIREMENTS.md` → `1`
4. `git ls-files --eol .planning/REQUIREMENTS.md` → `i/lf    w/crlf`
5. `test -f .planning/phases/50-cleanup-release-prep/50-CHANGELOG-DRAFT.md` → succeeds
6. `git status --porcelain -- CHANGELOG.md README.md` → **no output** (the phase's hardest gate)
7. `git status --porcelain -- scripts .pkgmeta .github TerribleBuffTracker.toc *.lua` → no output
8. `git worktree list | grep -c agent-ad696cb1` → `0`

**Human gate — NOT an executor criterion.** The CHANGELOG draft is a proposal. The user reads it,
edits it and pastes it into `CHANGELOG.md` themselves, and decides the two open recommendations (the
G8 Known Issues bullet, and whether the untested priest branch is mentioned at all). No agent appends
it.
</verification>

<success_criteria>
- REQUIREMENTS.md's RACE traceability matches what shipped, with the G8 waiver named (CONTEXT.md
  bookkeeping item 1).
- A drafted, paste-ready v0.4.1 CHANGELOG entry exists as its own artifact (ROADMAP SC5 as amended by
  D-07/D-08).
- `CHANGELOG.md` and `README.md` are byte-identical to how the phase found them.
- `install.bat`, `install.ps1`, `release.bat`, `.pkgmeta` and the TOC are reviewed against the
  milestone's real change set, with findings recorded and nothing run (ROADMAP SC4, second half).
- The leftover executor worktree and its branch are swept, with the proof recorded (CONTEXT.md
  bookkeeping item 2).
</success_criteria>

<output>
Create `.planning/phases/50-cleanup-release-prep/50-03-SUMMARY.md` when done.
</output>
