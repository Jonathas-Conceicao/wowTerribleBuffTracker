---
phase: 21-preview-mode-migration
plan: 02
type: execute
wave: 2
depends_on: ["21-01"]
files_modified: []
autonomous: false
requirements: [LIFE-03]
must_haves:
  truths:
    - "Zero Lua errors in chat on /reload and during gameplay"
    - "Opening CDM produces preview bars for trinket AND pot with their REAL durations (>0 seconds) — the v0.2.3 0-second sentinel is gone (LIFE-03 fix)"
    - "Lust preview bar shows correct class-aware icon + 40s duration during preview"
    - "User-spell preview bars continue to show correctly (no regression vs Phase 20)"
    - "Casting a trinket / pot / user spell while CDM is OPEN — the real proc bar displays, overriding the preview bar for the same key (real-priority merge)"
    - "Closing CDM after a mid-CDM real cast — the real proc bar persists and continues counting down to completion (mid-CDM real-cast-loss bug from Phase 20 verification is FIXED)"
    - "Closing CDM without a mid-CDM cast — all preview bars disappear cleanly, no ghost bars, no leaked timers, no Lua errors"
    - "Section rebuilds during CDM (e.g., drag a buff to a different section) do NOT wipe real procs — any in-flight real timer persists"
    - "Lust detection + no-restart guard + cancellation (cast/debuff branches) still work correctly — ScanActiveTimersForCancellation is unaffected (D-19)"
    - "M+ secret-gate path still works — OnUnitAura's ShouldAurasBeSecret log-gate still fires correctly; dispatcher ran before the gate so LustProvider still detects Sated"
  artifacts:
    - path: ".planning/phases/21-preview-mode-migration/21-VERIFICATION.md"
      provides: "In-game verification record of Phase 21 LIFE-03 outcome (approved) or diagnosed gaps"
      min_lines: 25
  key_links:
    - from: "StartPreview -> ns:StartAllPreviewTimers"
      to: "ns.previewTimers populated via ns:GetDisplayInfoForKey for all non-hidden tracked keys"
      via: "unchanged call site; new internal additive behavior"
      pattern: "StartAllPreviewTimers"
    - from: "Mid-CDM cast -> provider OnTrigger -> ns.activeTimers[key] = proc"
      to: "Display sees real proc immediately (merge in GetActiveTimers)"
      via: "real-priority merge overrides preview for same key"
      pattern: "ns\\.activeTimers"
    - from: "StopPreview -> ns:ClearAllTimers"
      to: "only ns.previewTimers wiped; ns.activeTimers untouched"
      via: "preview-scoped wipe — mid-CDM real cast survives CDM close"
      pattern: "wipe\\(ns\\.previewTimers\\)"
---

<objective>
Deploy the Phase 21 code via `scripts/install.bat`, launch WoW, and human-verify the three LIFE-03 success criteria PLUS the mid-CDM real-cast-persistence fix that falls out of the separate-tables architecture.

Purpose: Close LIFE-03 with on-client confirmation. The provider layer was fixed in Phase 20 (GetDisplayInfo returns real durations for trinket/pot). Phase 21 migrated the consumer (StartAllPreviewTimers) to use that provider layer, and restructured the storage (separate ns.previewTimers + merge in GetActiveTimers) so mid-CDM real casts don't get wiped at CDM-close. This plan proves both end-to-end on the running client.

Output: `.planning/phases/21-preview-mode-migration/21-VERIFICATION.md` recording verification outcome (APPROVED) or diagnosed gaps (for `/gsd:plan-phase 21 --gaps`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-preview-mode-migration/21-CONTEXT.md
@.planning/phases/21-preview-mode-migration/21-01-PLAN.md
@.planning/research/PITFALLS.md
@CLAUDE.md

<interfaces>
<!-- Expected state after Plan 21-01 completes: -->

**BuffEngine.lua:**
- `ns.previewTimers = {}` declared at module scope
- `ns.previewActive` deleted entirely (declaration + 3 body references + OnUnitAura guard)
- `local savedPreviewTimers` deleted entirely (declaration + 4 body references)
- `ns:StartAllPreviewTimers` rewritten: additive; writes ns.previewTimers; sources from ns:GetDisplayInfoForKey
- `ns:ClearAllTimers` rewritten: wipes only ns.previewTimers; ns.activeTimers untouched
- `ns:GetActiveTimers` rewritten: merges both tables with real-priority; returns sorted list
- `ns:OnUnitAura`: preview guard deleted; all other logic intact
- `ns:ScanActiveTimersForCancellation`: UNCHANGED

**CDMTab.lua:**
- Lines 183, 574, 813: `ns.previewActive or ns.configOpen` collapsed to `ns.configOpen`
- StartPreview / StopPreview / section rebuild handlers otherwise UNTOUCHED

**Providers.lua, Core.lua, Display.lua, TerribleBuffTracker.toc:**
- UNTOUCHED
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Deploy Phase 21 changes to WoW via install.bat and verify zero load errors</name>
  <files></files>
  <read_first>
    - CLAUDE.md (install workflow — `./scripts/install.bat`)
    - .planning/phases/21-preview-mode-migration/21-01-PLAN.md (Task 1-6 acceptance criteria — these are the preflight checks)
  </read_first>
  <action>
    **Step 1 — Preflight: confirm all Plan 21-01 acceptance checks pass.**

    Run the aggregated preflight on the two modified files:

    ```bash
    ~/.cargo/bin/stylua --check BuffEngine.lua CDMTab.lua
    ```

    Run the key structural greps. All MUST match expected values. If any fails, STOP and return to the failing plan:

    ```bash
    # BuffEngine.lua — Plan 21-01 Tasks 1-5 acceptance
    grep -c "ns\.previewTimers = {}" BuffEngine.lua            # expect 1
    grep -c "local savedPreviewTimers" BuffEngine.lua           # expect 0
    grep -c "ns\.previewActive" BuffEngine.lua                  # expect 0
    grep -c "savedPreviewTimers" BuffEngine.lua                 # expect 0
    grep -c "wipe(ns\.previewTimers)" BuffEngine.lua            # expect 2
    grep -c "function ns:StartAllPreviewTimers" BuffEngine.lua  # expect 1
    grep -c "function ns:ClearAllTimers" BuffEngine.lua         # expect 1
    grep -c "function ns:GetActiveTimers" BuffEngine.lua        # expect 1
    grep -c "function ns:OnUnitAura" BuffEngine.lua             # expect 1
    grep -c "if ns\.previewActive then" BuffEngine.lua          # expect 0
    grep -c "CURRENT_SCHEMA_VERSION = 3" BuffEngine.lua         # expect 1

    # CDMTab.lua — Plan 21-01 Task 6 acceptance
    grep -c "ns\.previewActive" CDMTab.lua                      # expect 0
    grep -c "if ns\.configOpen then" CDMTab.lua                 # expect >=3
    grep -c "ns:StartAllPreviewTimers()" CDMTab.lua             # expect 5 (unchanged baseline)
    grep -c "ns\.configOpen = true" CDMTab.lua                  # expect 1
    grep -c "ns\.configOpen = false" CDMTab.lua                 # expect 1

    # Untouched files (git status check)
    [ -z "$(git status --porcelain Providers.lua)" ] && echo "Providers.lua clean"
    [ -z "$(git status --porcelain Core.lua)" ] && echo "Core.lua clean"
    [ -z "$(git status --porcelain Display.lua)" ] && echo "Display.lua clean"
    [ -z "$(git status --porcelain TerribleBuffTracker.toc)" ] && echo "TOC clean"
    ```

    **Step 2 — Deploy.**

    Run the install script (Windows shell path — forward slashes OK under bash on Windows):

    ```bash
    ./scripts/install.bat
    ```

    Confirm the script reports the addon was copied into `_retail_\Interface\AddOns\TerribleBuffTracker`.

    **Step 3 — Hand off to user for in-game launch.**

    User launches WoW (or `/reload`s if already running). Report any immediate Lua errors before proceeding to Task 2's detailed verification matrix. If errors appear, copy the full error text into the verification doc and STOP — the error diagnosis informs gap-closure planning.
  </action>
  <acceptance_criteria>
    - stylua --check passes on both BuffEngine.lua and CDMTab.lua
    - All structural grep counts above match expected values
    - git status shows Providers.lua, Core.lua, Display.lua, and TerribleBuffTracker.toc as UNMODIFIED
    - `./scripts/install.bat` runs without error and reports successful copy
    - User confirms zero Lua errors on /reload / login
    - /tbt opens CDM with the TBT tab visible and some preview bars appear (detailed verification in Task 2)
  </acceptance_criteria>
  <verify>
    <automated>~/.cargo/bin/stylua --check BuffEngine.lua CDMTab.lua &amp;&amp; grep -c "ns\.previewTimers = {}" BuffEngine.lua | grep -q "^1$" &amp;&amp; grep -c "ns\.previewActive" BuffEngine.lua | grep -q "^0$" &amp;&amp; grep -c "savedPreviewTimers" BuffEngine.lua | grep -q "^0$" &amp;&amp; grep -c "wipe(ns\.previewTimers)" BuffEngine.lua | grep -q "^2$" &amp;&amp; grep -c "ns\.previewActive" CDMTab.lua | grep -q "^0$" &amp;&amp; [ -z "$(git status --porcelain Providers.lua)" ] &amp;&amp; [ -z "$(git status --porcelain Core.lua)" ] &amp;&amp; [ -z "$(git status --porcelain Display.lua)" ]</automated>
  </verify>
  <done>
    Code is deployed; addon loads without Lua errors on /reload; CDM tab opens; all structural code checks pass. Ready for Task 2's detailed behavior matrix.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — LIFE-03 fix + mid-CDM real-cast persistence across all four buff types</name>
  <files></files>
  <read_first>
    - .planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md (template for verification write-up)
    - .planning/phases/21-preview-mode-migration/21-CONTEXT.md (D-21 through D-23 — scenario walkthrough the user is testing)
    - .planning/research/PITFALLS.md (PITFALL-4 — the 0-second trinket/pot preview bug this phase fixes)
  </read_first>
  <what-built>
    **Phase 21 additive-preview refactor.**

    The code now:
    - Stores preview procs in a separate `ns.previewTimers` table (was: wipe+restore hack in `ns.activeTimers`)
    - `StartAllPreviewTimers` sources icon/label/duration/spellID from `ns:GetDisplayInfoForKey(key)` — the provider-owned helper from Phase 20 that resolves REAL durations (not the v0.2.3 0-second sentinel)
    - `StartAllPreviewTimers` SKIPS any key that already has a live real proc (D-05 priority at insertion time)
    - `ClearAllTimers` wipes ONLY `ns.previewTimers` — real procs in `ns.activeTimers` are never touched
    - `GetActiveTimers` merges both tables, real wins per key, sorted ascending by expiresAt
    - `ns.previewActive` flag deleted; `OnUnitAura` preview guard deleted; `ScanActiveTimersForCancellation` unchanged
    - CDMTab.lua 3 sites collapsed from `ns.previewActive or ns.configOpen` to `ns.configOpen`

    **Critical expectation:** The 0-second trinket/pot preview bug is GONE. Mid-CDM real casts persist after CDM close. No ghost bars after CDM close. No regression on user spells, lust, or cancellation.
  </what-built>
  <action>
    The executor does not write code in this task. The executor's job is to:

    1. Confirm Task 1 completed successfully (install.bat ran, zero load errors).
    2. Present the `<how-to-verify>` 9-test matrix below to the user verbatim.
    3. WAIT for the user's response. Do not make assumptions. Do not attempt to launch WoW from the command line (client is user-driven).
    4. On user reply, proceed to Task 3 to write the verification file.
  </action>
  <how-to-verify>
    **Environment:** Log in with the character that typically tests TBT. Ensure you have:
    - At least one equipped on-use trinket from `trinket_info.csv`
    - At least one damage pot from `pots_info.csv` in your bags
    - At least one user-tracked spell (e.g., a buff already in the Tracked Bars section)
    - Sections "bars", "buffs", and "suggested" all populated with at least one entry between them (the lust/trinket/pot suggested entries should be draggable into bars/buffs for preview)

    **Step 1 — Reload to baseline:**
    1. `/reload`
    2. Observe chat. Expected: ZERO Lua errors. If errors appear, COPY them verbatim and report.

    **Step 2 — Open CDM and verify LIFE-03 fix (trinket + pot preview with REAL durations):**
    1. Press `Esc` -> `Options` -> `Edit Mode` -> click the Cooldown Manager -> click the `TBT` tab. (Alternatively: `/tbt`.)
    2. Preview timer bars should appear for all non-hidden tracked buffs.
    3. **CRITICAL — LIFE-03 check:**
       - The **trinket preview bar** shows a REAL duration (e.g., 15s / 20s / 30s depending on the equipped trinket's buff duration from TRINKET_SPELLS). It is NOT a 0-second bar.
       - The **pot preview bar** shows a REAL duration (e.g., 25s / 30s for most damage pots). It is NOT a 0-second bar.
       - The **lust preview bar** shows 40s.
       - **User-spell preview bars** show their stored duration (unchanged from Phase 20).
    4. If any of the trinket/pot/lust bars are 0 seconds, THIS IS A REGRESSION — report step 2 + which bar.

    **Step 3 — Close CDM cleanly (no in-flight casts):**
    1. Close the Edit Mode / CDM window.
    2. Expected: all preview bars disappear. No ghost bars remain. No Lua errors.

    **Step 4 — Trinket cast (exercises TrinketProvider:OnTrigger — unchanged by Phase 21):**
    1. With CDM closed, cast your on-use trinket.
    2. Confirm a bar appears in the TBT bars container with the correct icon, correct duration, counting down.
    3. This verifies Phase 18/19/20 trinket path still works end-to-end.

    **Step 5 — Mid-CDM real cast: the fix (MOST IMPORTANT TEST):**
    1. Cast your on-use trinket (so you have a live real trinket proc with, say, 15s remaining).
    2. IMMEDIATELY open CDM (`/tbt`) while the real trinket bar is still ticking.
    3. Expected: the TBT timer bar area should show the REAL trinket timer (continuing to tick) — NOT a preview 20-second bar that overrides it.
       Rationale: D-05 real-priority — if a real proc exists at insertion time of StartAllPreviewTimers, the preview for that key is SKIPPED. Then if StartAllPreviewTimers does get called via a section rebuild mid-CDM, the merge in GetActiveTimers still has real winning.
    4. Close CDM (`Esc` or click away).
    5. **CRITICAL — mid-CDM real-cast persistence check:** The trinket bar must STILL BE THERE after CDM close, continuing to count down from whatever time is remaining. It must NOT vanish / reset / get wiped.
    6. If the trinket bar disappears when CDM closes, THIS IS A REGRESSION (the fix didn't apply). Report step 5 with observed behavior.

    **Step 6 — Mid-CDM cast OF A PREVIEW-ONLY TYPE (stronger test):**
    1. Open CDM (preview bars now show for trinket + pot + lust + user-spells at 0..N seconds).
    2. Cast a **POT** mid-CDM.
    3. Expected: the pot preview bar is REPLACED by the real pot bar (same key "pot"), which begins counting down from the real duration. No two bars for "pot" — only one.
    4. Close CDM.
    5. **CRITICAL:** the real pot bar persists and continues counting down.
    6. Report PASS if the pot bar persists; REGRESSION if it disappears.

    **Step 7 — Section rebuilds during CDM don't wipe real procs:**
    1. Cast a trinket (real trinket bar starts).
    2. Open CDM (real trinket continues; other preview bars appear).
    3. Drag a user-spell from one section to another (this triggers RefreshTBTSections -> StartAllPreviewTimers).
    4. Expected: the real trinket bar is UNAFFECTED by the rebuild — it continues to count down. Only preview procs rebuild (because StartAllPreviewTimers wipes only ns.previewTimers and skips keys with live real procs, per D-23).
    5. Close CDM — trinket bar persists.

    **Step 8 — User-spell + lust regression:**
    1. With CDM closed, cast any tracked user buff. Confirm bar appears with correct icon + duration (no regression vs Phase 20).
    2. Trigger lust detection (receive lust from a party member or cast Bloodlust/Heroism). Confirm a 40s bar appears with class-appropriate label. Confirm no-restart guard: a second Sated doesn't reset the first.

    **Step 9 — Cancellation still works:**
    1. Cast a tracked user buff whose buff can be dispelled or manually removed.
    2. Cancel the buff (right-click, dispel, or wait for M+ purge).
    3. Confirm the TBT bar disappears (ScanActiveTimersForCancellation still runs in OnUnitAura because the preview guard was removed — D-19 says the scan naturally ignores previews by iterating only ns.activeTimers).

    **What to report:**
    - **APPROVED** if all 9 steps behave as expected.
    - **REGRESSION** with exact step number, what was expected, what was observed, and any Lua error text.

    **Critical success signals:**
    - Trinket / pot preview bars show non-zero durations (Step 2) — this is the LIFE-03 fix.
    - Mid-CDM real cast persists after CDM close (Steps 5-6-7) — this is the Phase 20 verification bug fix.
  </how-to-verify>
  <acceptance_criteria>
    - User has executed all 9 verification steps and reported either APPROVED or specific regressions
    - Regression reports include: step number, expected behavior, observed behavior, any Lua error text
    - Executor captures the user's response text verbatim for use in Task 3
    - If Step 2 trinket/pot shows 0 seconds, that is a BLOCKING LIFE-03 regression — must be marked diagnosed in Task 3
    - If Step 5 or 6 trinket/pot bar disappears on CDM close, that is a BLOCKING regression on the side-effect fix — must be marked diagnosed
  </acceptance_criteria>
  <verify>
    <automated>MISSING — checkpoint tasks are human-gated; verification automation is Task 3's verification file write</automated>
  </verify>
  <done>
    User has responded with either "approved" or a regression report. The response is captured and ready to write into 21-VERIFICATION.md in Task 3.
  </done>
  <resume-signal>Type "approved" or describe observed regressions with step number, expected vs observed behavior, and any Lua error text.</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Write 21-VERIFICATION.md recording the verification outcome</name>
  <files>.planning/phases/21-preview-mode-migration/21-VERIFICATION.md</files>
  <read_first>
    - User's approval or regression report from Task 2
    - .planning/phases/20-getpreviewinfo-dispatch-helper/20-VERIFICATION.md (for format template)
    - .planning/phases/21-preview-mode-migration/21-CONTEXT.md (success criteria and scenario walkthroughs)
  </read_first>
  <action>
    Create `.planning/phases/21-preview-mode-migration/21-VERIFICATION.md` recording Phase 21 verification outcome.

    **If APPROVED**, use this template:

    ```markdown
    # Phase 21 Verification: Preview Mode Migration

    **Verified:** {today's date}
    **Status:** APPROVED

    ## Scope Verified

    - Additive-preview storage (ns.previewTimers separate from ns.activeTimers)
    - StartAllPreviewTimers uses ns:GetDisplayInfoForKey as sole data source
    - ClearAllTimers wipes only ns.previewTimers (real procs untouched)
    - GetActiveTimers merges both tables with real-priority
    - ns.previewActive flag fully deleted (BuffEngine + CDMTab 3 sites)
    - OnUnitAura preview guard deleted; ScanActiveTimersForCancellation unchanged

    ## Verification Steps Executed

    1. `/reload` — zero Lua errors observed
    2. CDM open — trinket/pot preview bars show REAL durations (NOT 0 seconds) — LIFE-03 fix confirmed
    3. CDM close (no in-flight cast) — all preview bars cleared, no ghosts, no errors
    4. Trinket cast (CDM closed) — bar appears, correct icon/duration, reproc overwrites
    5. Mid-CDM real trinket cast — real proc wins over preview; persists after CDM close
    6. Mid-CDM real pot cast — same; preview replaced by real; persists after CDM close
    7. Section rebuild mid-CDM — real procs survive the rebuild (D-23)
    8. User-spell cast + lust detection — no regression vs Phase 20
    9. Cancellation (dispel/manual remove) — scan still cancels correctly (D-19 preserved)

    ## Success Criteria Met

    - [x] SC-1 (LIFE-03): StartAllPreviewTimers produces at least one preview proc per registered provider — trinket, pot, lust bars appear with correct durations
    - [x] SC-2: Preview proc count equals or exceeds the pre-refactor count — no regression on user-created buffs
    - [x] SC-3: Exiting preview clears all preview procs correctly — no ghost bars remain
    - [x] SC-4 (side-effect): Mid-CDM real casts persist after CDM close — pre-existing bug from Phase 20 verification is fixed

    ## Requirements Closed

    - LIFE-03 — complete

    ## Pitfalls Retired

    - PITFALL-4 (preview regression / 0-second trinket/pot bars) — RESOLVED

    ## Next Phase

    Phase 22: Display.lua Unification (DISP-01, DISP-03) — zero type-specific branches; single shared tooltip handler; wipe() accumulator pattern preserved.
    ```

    **If REGRESSION**, use this template (status: diagnosed — so `/gsd:plan-phase 21 --gaps` can pick it up):

    ```markdown
    # Phase 21 Verification: Preview Mode Migration

    **Verified:** {today's date}
    **Status:** diagnosed
    **Blocking:** yes

    ## Gaps Detected

    ### Gap 1: {title}

    **Truth that failed:** {which success criterion or must_have}
    **Reason:** {root cause from user report / error text}
    **Artifacts involved:** {file paths and specific functions, e.g., BuffEngine.lua ns:StartAllPreviewTimers}
    **Missing / To Fix:**
    - {specific thing that must be added or changed}
    - {if more than one, list each}

    ### Gap N: (repeat for each distinct regression)

    ## Steps That Passed

    - {list the steps that did succeed, so gap-closure plan knows what NOT to redo}

    ## Next

    Run `/gsd:plan-phase 21 --gaps` to generate a targeted fix plan.
    ```

    Keep it concise — 25-50 lines is enough. Each gap MUST have truth / reason / artifacts / missing fields so gap-closure can consume it.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/21-preview-mode-migration/21-VERIFICATION.md` exists
    - File is at least 25 lines (min_lines in must_haves)
    - Status is either `APPROVED` or `diagnosed` — not a vague placeholder
    - File lists which of the 4 phase success criteria were met (if approved) or which gap fires (if diagnosed)
    - If diagnosed, each gap has truth + reason + artifacts + missing fields so `/gsd:plan-phase 21 --gaps` can consume it
    - If APPROVED, PITFALL-4 is explicitly marked RESOLVED
  </acceptance_criteria>
  <verify>
    <automated>test -f ".planning/phases/21-preview-mode-migration/21-VERIFICATION.md" &amp;&amp; [ "$(wc -l < .planning/phases/21-preview-mode-migration/21-VERIFICATION.md)" -ge 25 ] &amp;&amp; grep -qE "Status:\s+(APPROVED|diagnosed)" .planning/phases/21-preview-mode-migration/21-VERIFICATION.md</automated>
  </verify>
  <done>
    21-VERIFICATION.md is written reflecting user's actual verification outcome. Either LIFE-03 is marked complete (APPROVED path) or diagnosed gaps are documented with enough detail for `/gsd:plan-phase 21 --gaps` to generate a targeted fix plan.
  </done>
</task>

</tasks>

<verification>
- install.bat completes successfully and copies the addon into the WoW AddOns folder
- Zero Lua errors on /reload (user confirmation in Task 2)
- User confirms all 9 verification steps (or reports regressions)
- 21-VERIFICATION.md exists with either APPROVED or diagnosed status
- If APPROVED: all 4 Phase 21 success criteria marked complete; LIFE-03 closed; PITFALL-4 retired
- If diagnosed: gaps have enough detail (truth / reason / artifacts / missing) for gap-closure planning
</verification>

<success_criteria>
- Addon deploys via install.bat with zero errors
- User confirms CDM opens with trinket/pot/lust preview bars showing REAL durations (LIFE-03 fix)
- User confirms mid-CDM real casts (trinket, pot) persist after CDM close (side-effect fix)
- User confirms section rebuilds don't wipe real procs (D-23)
- No regression on user-spell, lust detection, no-restart guard, cancellation (D-19)
- 21-VERIFICATION.md written with appropriate status
- LIFE-03 closed if approved, or gap-closure path cleanly available if diagnosed
</success_criteria>

<output>
After completion, create `.planning/phases/21-preview-mode-migration/21-02-SUMMARY.md`.
</output>
